# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AppEngineEvent(PulumiAutoModelEncoder):
    """
    :var sequence: int - declared
    :var timestamp: int - declared
    :var cancel_event: AppCancelEvent - declared
    :var stdout_event: AppStdoutEngineEvent - declared
    :var diagnostic_event: AppDiagnosticEvent - declared
    :var prelude_event: AppPreludeEvent - declared
    :var summary_event: AppSummaryEvent - declared
    :var resource_pre_event: AppResourcePreEvent - declared
    :var res_outputs_event: AppResOutputsEvent - declared
    :var res_op_failed_event: AppResOpFailedEvent - declared
    :var policy_event: AppPolicyEvent - declared
    :var policy_remediation_event: AppPolicyRemediationEvent - declared
    :var policy_load_event: AppPolicyLoadEvent - declared
    :var policy_analyze_summary_event: AppPolicyAnalyzeSummaryEvent - declared
    :var policy_remediate_summary_event: AppPolicyRemediateSummaryEvent - declared
    :var policy_analyze_stack_summary_event: AppPolicyAnalyzeStackSummaryEvent - declared
    :var start_debugging_event: AppStartDebuggingEvent - declared
    :var progress_event: AppProgressEvent - declared
    :var error_event: AppErrorEvent - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'sequence': 'int',
        'timestamp': 'int',
        'cancel_event': 'AppCancelEvent',
        'stdout_event': 'AppStdoutEngineEvent',
        'diagnostic_event': 'AppDiagnosticEvent',
        'prelude_event': 'AppPreludeEvent',
        'summary_event': 'AppSummaryEvent',
        'resource_pre_event': 'AppResourcePreEvent',
        'res_outputs_event': 'AppResOutputsEvent',
        'res_op_failed_event': 'AppResOpFailedEvent',
        'policy_event': 'AppPolicyEvent',
        'policy_remediation_event': 'AppPolicyRemediationEvent',
        'policy_load_event': 'AppPolicyLoadEvent',
        'policy_analyze_summary_event': 'AppPolicyAnalyzeSummaryEvent',
        'policy_remediate_summary_event': 'AppPolicyRemediateSummaryEvent',
        'policy_analyze_stack_summary_event': 'AppPolicyAnalyzeStackSummaryEvent',
        'start_debugging_event': 'AppStartDebuggingEvent',
        'progress_event': 'AppProgressEvent',
        'error_event': 'AppErrorEvent',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'sequence': 'sequence',
        'timestamp': 'timestamp',
        'cancel_event': 'cancelEvent',
        'stdout_event': 'stdoutEvent',
        'diagnostic_event': 'diagnosticEvent',
        'prelude_event': 'preludeEvent',
        'summary_event': 'summaryEvent',
        'resource_pre_event': 'resourcePreEvent',
        'res_outputs_event': 'resOutputsEvent',
        'res_op_failed_event': 'resOpFailedEvent',
        'policy_event': 'policyEvent',
        'policy_remediation_event': 'policyRemediationEvent',
        'policy_load_event': 'policyLoadEvent',
        'policy_analyze_summary_event': 'policyAnalyzeSummaryEvent',
        'policy_remediate_summary_event': 'policyRemediateSummaryEvent',
        'policy_analyze_stack_summary_event': 'policyAnalyzeStackSummaryEvent',
        'start_debugging_event': 'startDebuggingEvent',
        'progress_event': 'progressEvent',
        'error_event': 'errorEvent',
    }

    FIELDS_sequence = 'sequence'
    FIELDS_timestamp = 'timestamp'
    FIELDS_cancel_event = 'cancelEvent'
    FIELDS_stdout_event = 'stdoutEvent'
    FIELDS_diagnostic_event = 'diagnosticEvent'
    FIELDS_prelude_event = 'preludeEvent'
    FIELDS_summary_event = 'summaryEvent'
    FIELDS_resource_pre_event = 'resourcePreEvent'
    FIELDS_res_outputs_event = 'resOutputsEvent'
    FIELDS_res_op_failed_event = 'resOpFailedEvent'
    FIELDS_policy_event = 'policyEvent'
    FIELDS_policy_remediation_event = 'policyRemediationEvent'
    FIELDS_policy_load_event = 'policyLoadEvent'
    FIELDS_policy_analyze_summary_event = 'policyAnalyzeSummaryEvent'
    FIELDS_policy_remediate_summary_event = 'policyRemediateSummaryEvent'
    FIELDS_policy_analyze_stack_summary_event = 'policyAnalyzeStackSummaryEvent'
    FIELDS_start_debugging_event = 'startDebuggingEvent'
    FIELDS_progress_event = 'progressEvent'
    FIELDS_error_event = 'errorEvent'

    _sequence: 'int'
    _timestamp: 'int'
    _cancel_event: 'AppCancelEvent'
    _stdout_event: 'AppStdoutEngineEvent'
    _diagnostic_event: 'AppDiagnosticEvent'
    _prelude_event: 'AppPreludeEvent'
    _summary_event: 'AppSummaryEvent'
    _resource_pre_event: 'AppResourcePreEvent'
    _res_outputs_event: 'AppResOutputsEvent'
    _res_op_failed_event: 'AppResOpFailedEvent'
    _policy_event: 'AppPolicyEvent'
    _policy_remediation_event: 'AppPolicyRemediationEvent'
    _policy_load_event: 'AppPolicyLoadEvent'
    _policy_analyze_summary_event: 'AppPolicyAnalyzeSummaryEvent'
    _policy_remediate_summary_event: 'AppPolicyRemediateSummaryEvent'
    _policy_analyze_stack_summary_event: 'AppPolicyAnalyzeStackSummaryEvent'
    _start_debugging_event: 'AppStartDebuggingEvent'
    _progress_event: 'AppProgressEvent'
    _error_event: 'AppErrorEvent'

    def __init__(
        self,
        sequence: 'int',
        timestamp: 'int',
        cancel_event: 'AppCancelEvent' = None,
        stdout_event: 'AppStdoutEngineEvent' = None,
        diagnostic_event: 'AppDiagnosticEvent' = None,
        prelude_event: 'AppPreludeEvent' = None,
        summary_event: 'AppSummaryEvent' = None,
        resource_pre_event: 'AppResourcePreEvent' = None,
        res_outputs_event: 'AppResOutputsEvent' = None,
        res_op_failed_event: 'AppResOpFailedEvent' = None,
        policy_event: 'AppPolicyEvent' = None,
        policy_remediation_event: 'AppPolicyRemediationEvent' = None,
        policy_load_event: 'AppPolicyLoadEvent' = None,
        policy_analyze_summary_event: 'AppPolicyAnalyzeSummaryEvent' = None,
        policy_remediate_summary_event: 'AppPolicyRemediateSummaryEvent' = None,
        policy_analyze_stack_summary_event: 'AppPolicyAnalyzeStackSummaryEvent' = None,
        start_debugging_event: 'AppStartDebuggingEvent' = None,
        progress_event: 'AppProgressEvent' = None,
        error_event: 'AppErrorEvent' = None,
    ) -> None:
        super().__init__()

        self.sequence = sequence
        self.timestamp = timestamp
        self.cancel_event = cancel_event
        self.stdout_event = stdout_event
        self.diagnostic_event = diagnostic_event
        self.prelude_event = prelude_event
        self.summary_event = summary_event
        self.resource_pre_event = resource_pre_event
        self.res_outputs_event = res_outputs_event
        self.res_op_failed_event = res_op_failed_event
        self.policy_event = policy_event
        self.policy_remediation_event = policy_remediation_event
        self.policy_load_event = policy_load_event
        self.policy_analyze_summary_event = policy_analyze_summary_event
        self.policy_remediate_summary_event = policy_remediate_summary_event
        self.policy_analyze_stack_summary_event = policy_analyze_stack_summary_event
        self.start_debugging_event = start_debugging_event
        self.progress_event = progress_event
        self.error_event = error_event

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AppEngineEvent):
            self._sequence = source._sequence
            self._timestamp = source._timestamp
            self._cancel_event = source._cancel_event
            self._stdout_event = source._stdout_event
            self._diagnostic_event = source._diagnostic_event
            self._prelude_event = source._prelude_event
            self._summary_event = source._summary_event
            self._resource_pre_event = source._resource_pre_event
            self._res_outputs_event = source._res_outputs_event
            self._res_op_failed_event = source._res_op_failed_event
            self._policy_event = source._policy_event
            self._policy_remediation_event = source._policy_remediation_event
            self._policy_load_event = source._policy_load_event
            self._policy_analyze_summary_event = source._policy_analyze_summary_event
            self._policy_remediate_summary_event = source._policy_remediate_summary_event
            self._policy_analyze_stack_summary_event = source._policy_analyze_stack_summary_event
            self._start_debugging_event = source._start_debugging_event
            self._progress_event = source._progress_event
            self._error_event = source._error_event

    @property
    def sequence(self) -> 'int':
        return self._sequence

    @sequence.setter
    def sequence(self, sequence: 'int'):
        if sequence is None:
            raise ValueError("Invalid value for `sequence`, must not be `None`")

        self._sequence = sequence

    @property
    def timestamp(self) -> 'int':
        return self._timestamp

    @timestamp.setter
    def timestamp(self, timestamp: 'int'):
        if timestamp is None:
            raise ValueError("Invalid value for `timestamp`, must not be `None`")

        self._timestamp = timestamp

    @property
    def cancel_event(self) -> 'AppCancelEvent':
        return self._cancel_event

    @cancel_event.setter
    def cancel_event(self, cancel_event: 'AppCancelEvent'):
        self._cancel_event = cancel_event

    @property
    def cancel_event__autoinit(self) -> 'AppCancelEvent':
        """Return ``cancel_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._cancel_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``cancel_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._cancel_event is None:
            self._cancel_event = self.cancel_event__safederef

        return self._cancel_event

    @property
    def cancel_event__safederef(self) -> 'AppCancelEvent':
        """Return ``cancel_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``cancel_event`` stays unset and each
        call returns a new default. Use ``cancel_event__autoinit`` to persist the default on
        first access.
        """
        if self._cancel_event is None:
            from .. import models
            return models.AppCancelEvent._new_empty()

        return self._cancel_event

    @property
    def stdout_event(self) -> 'AppStdoutEngineEvent':
        return self._stdout_event

    @stdout_event.setter
    def stdout_event(self, stdout_event: 'AppStdoutEngineEvent'):
        self._stdout_event = stdout_event

    @property
    def stdout_event__autoinit(self) -> 'AppStdoutEngineEvent':
        """Return ``stdout_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._stdout_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``stdout_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._stdout_event is None:
            self._stdout_event = self.stdout_event__safederef

        return self._stdout_event

    @property
    def stdout_event__safederef(self) -> 'AppStdoutEngineEvent':
        """Return ``stdout_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``stdout_event`` stays unset and each
        call returns a new default. Use ``stdout_event__autoinit`` to persist the default on
        first access.
        """
        if self._stdout_event is None:
            from .. import models
            return models.AppStdoutEngineEvent._new_empty()

        return self._stdout_event

    @property
    def diagnostic_event(self) -> 'AppDiagnosticEvent':
        return self._diagnostic_event

    @diagnostic_event.setter
    def diagnostic_event(self, diagnostic_event: 'AppDiagnosticEvent'):
        self._diagnostic_event = diagnostic_event

    @property
    def diagnostic_event__autoinit(self) -> 'AppDiagnosticEvent':
        """Return ``diagnostic_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._diagnostic_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``diagnostic_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._diagnostic_event is None:
            self._diagnostic_event = self.diagnostic_event__safederef

        return self._diagnostic_event

    @property
    def diagnostic_event__safederef(self) -> 'AppDiagnosticEvent':
        """Return ``diagnostic_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``diagnostic_event`` stays unset and each
        call returns a new default. Use ``diagnostic_event__autoinit`` to persist the default on
        first access.
        """
        if self._diagnostic_event is None:
            from .. import models
            return models.AppDiagnosticEvent._new_empty()

        return self._diagnostic_event

    @property
    def prelude_event(self) -> 'AppPreludeEvent':
        return self._prelude_event

    @prelude_event.setter
    def prelude_event(self, prelude_event: 'AppPreludeEvent'):
        self._prelude_event = prelude_event

    @property
    def prelude_event__autoinit(self) -> 'AppPreludeEvent':
        """Return ``prelude_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._prelude_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``prelude_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._prelude_event is None:
            self._prelude_event = self.prelude_event__safederef

        return self._prelude_event

    @property
    def prelude_event__safederef(self) -> 'AppPreludeEvent':
        """Return ``prelude_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``prelude_event`` stays unset and each
        call returns a new default. Use ``prelude_event__autoinit`` to persist the default on
        first access.
        """
        if self._prelude_event is None:
            from .. import models
            return models.AppPreludeEvent._new_empty()

        return self._prelude_event

    @property
    def summary_event(self) -> 'AppSummaryEvent':
        return self._summary_event

    @summary_event.setter
    def summary_event(self, summary_event: 'AppSummaryEvent'):
        self._summary_event = summary_event

    @property
    def summary_event__autoinit(self) -> 'AppSummaryEvent':
        """Return ``summary_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._summary_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``summary_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._summary_event is None:
            self._summary_event = self.summary_event__safederef

        return self._summary_event

    @property
    def summary_event__safederef(self) -> 'AppSummaryEvent':
        """Return ``summary_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``summary_event`` stays unset and each
        call returns a new default. Use ``summary_event__autoinit`` to persist the default on
        first access.
        """
        if self._summary_event is None:
            from .. import models
            return models.AppSummaryEvent._new_empty()

        return self._summary_event

    @property
    def resource_pre_event(self) -> 'AppResourcePreEvent':
        return self._resource_pre_event

    @resource_pre_event.setter
    def resource_pre_event(self, resource_pre_event: 'AppResourcePreEvent'):
        self._resource_pre_event = resource_pre_event

    @property
    def resource_pre_event__autoinit(self) -> 'AppResourcePreEvent':
        """Return ``resource_pre_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._resource_pre_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``resource_pre_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._resource_pre_event is None:
            self._resource_pre_event = self.resource_pre_event__safederef

        return self._resource_pre_event

    @property
    def resource_pre_event__safederef(self) -> 'AppResourcePreEvent':
        """Return ``resource_pre_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``resource_pre_event`` stays unset and each
        call returns a new default. Use ``resource_pre_event__autoinit`` to persist the default on
        first access.
        """
        if self._resource_pre_event is None:
            from .. import models
            return models.AppResourcePreEvent._new_empty()

        return self._resource_pre_event

    @property
    def res_outputs_event(self) -> 'AppResOutputsEvent':
        return self._res_outputs_event

    @res_outputs_event.setter
    def res_outputs_event(self, res_outputs_event: 'AppResOutputsEvent'):
        self._res_outputs_event = res_outputs_event

    @property
    def res_outputs_event__autoinit(self) -> 'AppResOutputsEvent':
        """Return ``res_outputs_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._res_outputs_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``res_outputs_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._res_outputs_event is None:
            self._res_outputs_event = self.res_outputs_event__safederef

        return self._res_outputs_event

    @property
    def res_outputs_event__safederef(self) -> 'AppResOutputsEvent':
        """Return ``res_outputs_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``res_outputs_event`` stays unset and each
        call returns a new default. Use ``res_outputs_event__autoinit`` to persist the default on
        first access.
        """
        if self._res_outputs_event is None:
            from .. import models
            return models.AppResOutputsEvent._new_empty()

        return self._res_outputs_event

    @property
    def res_op_failed_event(self) -> 'AppResOpFailedEvent':
        return self._res_op_failed_event

    @res_op_failed_event.setter
    def res_op_failed_event(self, res_op_failed_event: 'AppResOpFailedEvent'):
        self._res_op_failed_event = res_op_failed_event

    @property
    def res_op_failed_event__autoinit(self) -> 'AppResOpFailedEvent':
        """Return ``res_op_failed_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._res_op_failed_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``res_op_failed_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._res_op_failed_event is None:
            self._res_op_failed_event = self.res_op_failed_event__safederef

        return self._res_op_failed_event

    @property
    def res_op_failed_event__safederef(self) -> 'AppResOpFailedEvent':
        """Return ``res_op_failed_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``res_op_failed_event`` stays unset and each
        call returns a new default. Use ``res_op_failed_event__autoinit`` to persist the default on
        first access.
        """
        if self._res_op_failed_event is None:
            from .. import models
            return models.AppResOpFailedEvent._new_empty()

        return self._res_op_failed_event

    @property
    def policy_event(self) -> 'AppPolicyEvent':
        return self._policy_event

    @policy_event.setter
    def policy_event(self, policy_event: 'AppPolicyEvent'):
        self._policy_event = policy_event

    @property
    def policy_event__autoinit(self) -> 'AppPolicyEvent':
        """Return ``policy_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._policy_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``policy_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._policy_event is None:
            self._policy_event = self.policy_event__safederef

        return self._policy_event

    @property
    def policy_event__safederef(self) -> 'AppPolicyEvent':
        """Return ``policy_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``policy_event`` stays unset and each
        call returns a new default. Use ``policy_event__autoinit`` to persist the default on
        first access.
        """
        if self._policy_event is None:
            from .. import models
            return models.AppPolicyEvent._new_empty()

        return self._policy_event

    @property
    def policy_remediation_event(self) -> 'AppPolicyRemediationEvent':
        return self._policy_remediation_event

    @policy_remediation_event.setter
    def policy_remediation_event(self, policy_remediation_event: 'AppPolicyRemediationEvent'):
        self._policy_remediation_event = policy_remediation_event

    @property
    def policy_remediation_event__autoinit(self) -> 'AppPolicyRemediationEvent':
        """Return ``policy_remediation_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._policy_remediation_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``policy_remediation_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._policy_remediation_event is None:
            self._policy_remediation_event = self.policy_remediation_event__safederef

        return self._policy_remediation_event

    @property
    def policy_remediation_event__safederef(self) -> 'AppPolicyRemediationEvent':
        """Return ``policy_remediation_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``policy_remediation_event`` stays unset and each
        call returns a new default. Use ``policy_remediation_event__autoinit`` to persist the default on
        first access.
        """
        if self._policy_remediation_event is None:
            from .. import models
            return models.AppPolicyRemediationEvent._new_empty()

        return self._policy_remediation_event

    @property
    def policy_load_event(self) -> 'AppPolicyLoadEvent':
        return self._policy_load_event

    @policy_load_event.setter
    def policy_load_event(self, policy_load_event: 'AppPolicyLoadEvent'):
        self._policy_load_event = policy_load_event

    @property
    def policy_load_event__autoinit(self) -> 'AppPolicyLoadEvent':
        """Return ``policy_load_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._policy_load_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``policy_load_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._policy_load_event is None:
            self._policy_load_event = self.policy_load_event__safederef

        return self._policy_load_event

    @property
    def policy_load_event__safederef(self) -> 'AppPolicyLoadEvent':
        """Return ``policy_load_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``policy_load_event`` stays unset and each
        call returns a new default. Use ``policy_load_event__autoinit`` to persist the default on
        first access.
        """
        if self._policy_load_event is None:
            from .. import models
            return models.AppPolicyLoadEvent._new_empty()

        return self._policy_load_event

    @property
    def policy_analyze_summary_event(self) -> 'AppPolicyAnalyzeSummaryEvent':
        return self._policy_analyze_summary_event

    @policy_analyze_summary_event.setter
    def policy_analyze_summary_event(self, policy_analyze_summary_event: 'AppPolicyAnalyzeSummaryEvent'):
        self._policy_analyze_summary_event = policy_analyze_summary_event

    @property
    def policy_analyze_summary_event__autoinit(self) -> 'AppPolicyAnalyzeSummaryEvent':
        """Return ``policy_analyze_summary_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._policy_analyze_summary_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``policy_analyze_summary_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._policy_analyze_summary_event is None:
            self._policy_analyze_summary_event = self.policy_analyze_summary_event__safederef

        return self._policy_analyze_summary_event

    @property
    def policy_analyze_summary_event__safederef(self) -> 'AppPolicyAnalyzeSummaryEvent':
        """Return ``policy_analyze_summary_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``policy_analyze_summary_event`` stays unset and each
        call returns a new default. Use ``policy_analyze_summary_event__autoinit`` to persist the default on
        first access.
        """
        if self._policy_analyze_summary_event is None:
            from .. import models
            return models.AppPolicyAnalyzeSummaryEvent._new_empty()

        return self._policy_analyze_summary_event

    @property
    def policy_remediate_summary_event(self) -> 'AppPolicyRemediateSummaryEvent':
        return self._policy_remediate_summary_event

    @policy_remediate_summary_event.setter
    def policy_remediate_summary_event(self, policy_remediate_summary_event: 'AppPolicyRemediateSummaryEvent'):
        self._policy_remediate_summary_event = policy_remediate_summary_event

    @property
    def policy_remediate_summary_event__autoinit(self) -> 'AppPolicyRemediateSummaryEvent':
        """Return ``policy_remediate_summary_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._policy_remediate_summary_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``policy_remediate_summary_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._policy_remediate_summary_event is None:
            self._policy_remediate_summary_event = self.policy_remediate_summary_event__safederef

        return self._policy_remediate_summary_event

    @property
    def policy_remediate_summary_event__safederef(self) -> 'AppPolicyRemediateSummaryEvent':
        """Return ``policy_remediate_summary_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``policy_remediate_summary_event`` stays unset and each
        call returns a new default. Use ``policy_remediate_summary_event__autoinit`` to persist the default on
        first access.
        """
        if self._policy_remediate_summary_event is None:
            from .. import models
            return models.AppPolicyRemediateSummaryEvent._new_empty()

        return self._policy_remediate_summary_event

    @property
    def policy_analyze_stack_summary_event(self) -> 'AppPolicyAnalyzeStackSummaryEvent':
        return self._policy_analyze_stack_summary_event

    @policy_analyze_stack_summary_event.setter
    def policy_analyze_stack_summary_event(self, policy_analyze_stack_summary_event: 'AppPolicyAnalyzeStackSummaryEvent'):
        self._policy_analyze_stack_summary_event = policy_analyze_stack_summary_event

    @property
    def policy_analyze_stack_summary_event__autoinit(self) -> 'AppPolicyAnalyzeStackSummaryEvent':
        """Return ``policy_analyze_stack_summary_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._policy_analyze_stack_summary_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``policy_analyze_stack_summary_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._policy_analyze_stack_summary_event is None:
            self._policy_analyze_stack_summary_event = self.policy_analyze_stack_summary_event__safederef

        return self._policy_analyze_stack_summary_event

    @property
    def policy_analyze_stack_summary_event__safederef(self) -> 'AppPolicyAnalyzeStackSummaryEvent':
        """Return ``policy_analyze_stack_summary_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``policy_analyze_stack_summary_event`` stays unset and each
        call returns a new default. Use ``policy_analyze_stack_summary_event__autoinit`` to persist the default on
        first access.
        """
        if self._policy_analyze_stack_summary_event is None:
            from .. import models
            return models.AppPolicyAnalyzeStackSummaryEvent._new_empty()

        return self._policy_analyze_stack_summary_event

    @property
    def start_debugging_event(self) -> 'AppStartDebuggingEvent':
        return self._start_debugging_event

    @start_debugging_event.setter
    def start_debugging_event(self, start_debugging_event: 'AppStartDebuggingEvent'):
        self._start_debugging_event = start_debugging_event

    @property
    def start_debugging_event__autoinit(self) -> 'AppStartDebuggingEvent':
        """Return ``start_debugging_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._start_debugging_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``start_debugging_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._start_debugging_event is None:
            self._start_debugging_event = self.start_debugging_event__safederef

        return self._start_debugging_event

    @property
    def start_debugging_event__safederef(self) -> 'AppStartDebuggingEvent':
        """Return ``start_debugging_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``start_debugging_event`` stays unset and each
        call returns a new default. Use ``start_debugging_event__autoinit`` to persist the default on
        first access.
        """
        if self._start_debugging_event is None:
            from .. import models
            return models.AppStartDebuggingEvent._new_empty()

        return self._start_debugging_event

    @property
    def progress_event(self) -> 'AppProgressEvent':
        return self._progress_event

    @progress_event.setter
    def progress_event(self, progress_event: 'AppProgressEvent'):
        self._progress_event = progress_event

    @property
    def progress_event__autoinit(self) -> 'AppProgressEvent':
        """Return ``progress_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._progress_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``progress_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._progress_event is None:
            self._progress_event = self.progress_event__safederef

        return self._progress_event

    @property
    def progress_event__safederef(self) -> 'AppProgressEvent':
        """Return ``progress_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``progress_event`` stays unset and each
        call returns a new default. Use ``progress_event__autoinit`` to persist the default on
        first access.
        """
        if self._progress_event is None:
            from .. import models
            return models.AppProgressEvent._new_empty()

        return self._progress_event

    @property
    def error_event(self) -> 'AppErrorEvent':
        return self._error_event

    @error_event.setter
    def error_event(self, error_event: 'AppErrorEvent'):
        self._error_event = error_event

    @property
    def error_event__autoinit(self) -> 'AppErrorEvent':
        """Return ``error_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._error_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``error_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._error_event is None:
            self._error_event = self.error_event__safederef

        return self._error_event

    @property
    def error_event__safederef(self) -> 'AppErrorEvent':
        """Return ``error_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``error_event`` stays unset and each
        call returns a new default. Use ``error_event__autoinit`` to persist the default on
        first access.
        """
        if self._error_event is None:
            from .. import models
            return models.AppErrorEvent._new_empty()

        return self._error_event

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AppEngineEvent):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
