# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class SourceContext(PulumiAutoModelEncoder):
    """
    :var git: SourceContextGit - declared
    :var hg: SourceContextHg - declared
    :var template: SourceContextTemplate - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'git': 'SourceContextGit',
        'hg': 'SourceContextHg',
        'template': 'SourceContextTemplate',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'git': 'git',
        'hg': 'hg',
        'template': 'template',
    }

    FIELDS_git = 'git'
    FIELDS_hg = 'hg'
    FIELDS_template = 'template'

    _git: 'SourceContextGit'
    _hg: 'SourceContextHg'
    _template: 'SourceContextTemplate'

    def __init__(
        self,
        git: 'SourceContextGit' = None,
        hg: 'SourceContextHg' = None,
        template: 'SourceContextTemplate' = None,
    ) -> None:
        super().__init__()

        self.git = git
        self.hg = hg
        self.template = template

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, SourceContext):
            self._git = source._git
            self._hg = source._hg
            self._template = source._template

    @property
    def git(self) -> 'SourceContextGit':
        return self._git

    @git.setter
    def git(self, git: 'SourceContextGit'):
        self._git = git

    @property
    def git__autoinit(self) -> 'SourceContextGit':
        """Return ``git``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._git``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``git__safederef`` when you only want to read a default without mutating self.
        """
        if self._git is None:
            self._git = self.git__safederef

        return self._git

    @property
    def git__safederef(self) -> 'SourceContextGit':
        """Return ``git``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``git`` stays unset and each
        call returns a new default. Use ``git__autoinit`` to persist the default on
        first access.
        """
        if self._git is None:
            from .. import models
            return models.SourceContextGit._new_empty()

        return self._git

    @property
    def hg(self) -> 'SourceContextHg':
        return self._hg

    @hg.setter
    def hg(self, hg: 'SourceContextHg'):
        self._hg = hg

    @property
    def hg__autoinit(self) -> 'SourceContextHg':
        """Return ``hg``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._hg``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``hg__safederef`` when you only want to read a default without mutating self.
        """
        if self._hg is None:
            self._hg = self.hg__safederef

        return self._hg

    @property
    def hg__safederef(self) -> 'SourceContextHg':
        """Return ``hg``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``hg`` stays unset and each
        call returns a new default. Use ``hg__autoinit`` to persist the default on
        first access.
        """
        if self._hg is None:
            from .. import models
            return models.SourceContextHg._new_empty()

        return self._hg

    @property
    def template(self) -> 'SourceContextTemplate':
        return self._template

    @template.setter
    def template(self, template: 'SourceContextTemplate'):
        self._template = template

    @property
    def template__autoinit(self) -> 'SourceContextTemplate':
        """Return ``template``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._template``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``template__safederef`` when you only want to read a default without mutating self.
        """
        if self._template is None:
            self._template = self.template__safederef

        return self._template

    @property
    def template__safederef(self) -> 'SourceContextTemplate':
        """Return ``template``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``template`` stays unset and each
        call returns a new default. Use ``template__autoinit`` to persist the default on
        first access.
        """
        if self._template is None:
            from .. import models
            return models.SourceContextTemplate._new_empty()

        return self._template

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, SourceContext):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
