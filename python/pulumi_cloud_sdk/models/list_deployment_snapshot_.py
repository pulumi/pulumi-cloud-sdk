# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .list_deployment_response_ import ListDeploymentResponse

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class ListDeploymentSnapshot(ListDeploymentResponse):
    """
    :var project_name: str - declared
    :var stack_name: str - declared
    :var paused: bool - declared
    :var pulumi_operation: PulumiOperation - declared
    :var updates: list[DeploymentNestedUpdate] - declared
    :var jobs: list[DeploymentJob] - declared
    :var initiator: str - declared
    :var agent_pool: ListDeploymentSnapshotAgentPool - declared
    :var id: str - inherited
    :var created: str - inherited
    :var modified: str - inherited
    :var status: JobStatus - inherited
    :var version: int - inherited
    :var requested_by: UserInfo - inherited
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'id': 'str',
        'created': 'str',
        'modified': 'str',
        'status': 'JobStatus',
        'version': 'int',
        'requested_by': 'UserInfo',
        'project_name': 'str',
        'stack_name': 'str',
        'paused': 'bool',
        'pulumi_operation': 'PulumiOperation',
        'updates': 'list[DeploymentNestedUpdate]',
        'jobs': 'list[DeploymentJob]',
        'initiator': 'str',
        'agent_pool': 'ListDeploymentSnapshotAgentPool',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'id': 'id',
        'created': 'created',
        'modified': 'modified',
        'status': 'status',
        'version': 'version',
        'requested_by': 'requestedBy',
        'project_name': 'projectName',
        'stack_name': 'stackName',
        'paused': 'paused',
        'pulumi_operation': 'pulumiOperation',
        'updates': 'updates',
        'jobs': 'jobs',
        'initiator': 'initiator',
        'agent_pool': 'agentPool',
    }

    FIELDS_project_name = 'projectName'
    FIELDS_stack_name = 'stackName'
    FIELDS_paused = 'paused'
    FIELDS_pulumi_operation = 'pulumiOperation'
    FIELDS_updates = 'updates'
    FIELDS_jobs = 'jobs'
    FIELDS_initiator = 'initiator'
    FIELDS_agent_pool = 'agentPool'

    _project_name: 'str'
    _stack_name: 'str'
    _paused: 'bool'
    _pulumi_operation: 'PulumiOperation'
    _updates: 'list[DeploymentNestedUpdate]'
    _jobs: 'list[DeploymentJob]'
    _initiator: 'str'
    _agent_pool: 'ListDeploymentSnapshotAgentPool'

    def __init__(
        self,
        id: 'str',
        created: 'str',
        modified: 'str',
        status: 'JobStatus',
        version: 'int',
        requested_by: 'UserInfo',
        pulumi_operation: 'PulumiOperation',
        updates: 'list[DeploymentNestedUpdate]',
        jobs: 'list[DeploymentJob]',
        project_name: 'str' = None,
        stack_name: 'str' = None,
        paused: 'bool' = False,
        initiator: 'str' = None,
        agent_pool: 'ListDeploymentSnapshotAgentPool' = None,
    ) -> None:
        super().__init__(id=id, created=created, modified=modified, status=status, version=version, requested_by=requested_by)

        self.project_name = project_name
        self.stack_name = stack_name
        self.paused = paused
        self.pulumi_operation = pulumi_operation
        self.updates = updates
        self.jobs = jobs
        self.initiator = initiator
        self.agent_pool = agent_pool

    def copy_common_fields(self, source: Any, /) -> None:
        super().copy_common_fields(source)
        if isinstance(source, ListDeploymentSnapshot):
            self._project_name = source._project_name
            self._stack_name = source._stack_name
            self._paused = source._paused
            self._pulumi_operation = source._pulumi_operation
            self._updates = source._updates
            self._jobs = source._jobs
            self._initiator = source._initiator
            self._agent_pool = source._agent_pool

    @property
    def project_name(self) -> 'str':
        return self._project_name

    @project_name.setter
    def project_name(self, project_name: 'str'):
        self._project_name = project_name

    @property
    def stack_name(self) -> 'str':
        return self._stack_name

    @stack_name.setter
    def stack_name(self, stack_name: 'str'):
        self._stack_name = stack_name

    @property
    def paused(self) -> 'bool':
        return self._paused

    @paused.setter
    def paused(self, paused: 'bool'):
        self._paused = paused

    @property
    def pulumi_operation(self) -> 'PulumiOperation':
        return self._pulumi_operation

    @pulumi_operation.setter
    def pulumi_operation(self, pulumi_operation: 'PulumiOperation'):
        if pulumi_operation is None:
            raise ValueError("Invalid value for `pulumi_operation`, must not be `None`")

        self._pulumi_operation = pulumi_operation

    @property
    def updates(self) -> 'list[DeploymentNestedUpdate]':
        return self._updates

    @updates.setter
    def updates(self, updates: 'list[DeploymentNestedUpdate]'):
        if updates is None:
            raise ValueError("Invalid value for `updates`, must not be `None`")

        self._updates = updates

    @property
    def updates__autoinit(self) -> 'list[DeploymentNestedUpdate]':
        """Return ``updates``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._updates``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``updates__safederef`` when you only want to read a default without mutating self.
        """
        if self._updates is None:
            self._updates = self.updates__safederef

        return self._updates

    @property
    def updates__safederef(self) -> 'list[DeploymentNestedUpdate]':
        """Return ``updates``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``updates`` stays unset and each
        call returns a new default. Use ``updates__autoinit`` to persist the default on
        first access.
        """
        if self._updates is None:
            return []

        return self._updates

    @property
    def jobs(self) -> 'list[DeploymentJob]':
        return self._jobs

    @jobs.setter
    def jobs(self, jobs: 'list[DeploymentJob]'):
        if jobs is None:
            raise ValueError("Invalid value for `jobs`, must not be `None`")

        self._jobs = jobs

    @property
    def jobs__autoinit(self) -> 'list[DeploymentJob]':
        """Return ``jobs``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._jobs``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``jobs__safederef`` when you only want to read a default without mutating self.
        """
        if self._jobs is None:
            self._jobs = self.jobs__safederef

        return self._jobs

    @property
    def jobs__safederef(self) -> 'list[DeploymentJob]':
        """Return ``jobs``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``jobs`` stays unset and each
        call returns a new default. Use ``jobs__autoinit`` to persist the default on
        first access.
        """
        if self._jobs is None:
            return []

        return self._jobs

    @property
    def initiator(self) -> 'str':
        return self._initiator

    @initiator.setter
    def initiator(self, initiator: 'str'):
        self._initiator = initiator

    @property
    def agent_pool(self) -> 'ListDeploymentSnapshotAgentPool':
        return self._agent_pool

    @agent_pool.setter
    def agent_pool(self, agent_pool: 'ListDeploymentSnapshotAgentPool'):
        self._agent_pool = agent_pool

    @property
    def agent_pool__autoinit(self) -> 'ListDeploymentSnapshotAgentPool':
        """Return ``agent_pool``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._agent_pool``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``agent_pool__safederef`` when you only want to read a default without mutating self.
        """
        if self._agent_pool is None:
            self._agent_pool = self.agent_pool__safederef

        return self._agent_pool

    @property
    def agent_pool__safederef(self) -> 'ListDeploymentSnapshotAgentPool':
        """Return ``agent_pool``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``agent_pool`` stays unset and each
        call returns a new default. Use ``agent_pool__autoinit`` to persist the default on
        first access.
        """
        if self._agent_pool is None:
            from .. import models
            return models.ListDeploymentSnapshotAgentPool._new_empty()

        return self._agent_pool

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, ListDeploymentSnapshot):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
