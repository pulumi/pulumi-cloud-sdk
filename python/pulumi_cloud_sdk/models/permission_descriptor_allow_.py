# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .permission_descriptor_ import PermissionDescriptor

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class PermissionDescriptorAllow(PermissionDescriptor):
    """
    :var permissions: list[RbacPermission] - declared
    :var constraints: RbacPermissionConstraints - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'permissions': 'list[RbacPermission]',
        'constraints': 'RbacPermissionConstraints',
        '__DISCRIMINATOR_VALUE__': 'str'
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'permissions': 'permissions',
        'constraints': 'constraints',
        '__DISCRIMINATOR_VALUE__': '__type'
    }

    __DISCRIMINATOR_VALUE__ = "PermissionDescriptorAllow"

    FIELDS_permissions = 'permissions'
    FIELDS_constraints = 'constraints'

    _permissions: 'list[RbacPermission]'
    _constraints: 'RbacPermissionConstraints'

    def __init__(
        self,
        permissions: 'list[RbacPermission]' = None,
        constraints: 'RbacPermissionConstraints' = None,
    ) -> None:
        super().__init__()

        self.permissions = permissions
        self.constraints = constraints

    def copy_common_fields(self, source: Any, /) -> None:
        super().copy_common_fields(source)
        if isinstance(source, PermissionDescriptorAllow):
            self._permissions = source._permissions
            self._constraints = source._constraints

    @property
    def permissions(self) -> 'list[RbacPermission]':
        return self._permissions

    @permissions.setter
    def permissions(self, permissions: 'list[RbacPermission]'):
        self._permissions = permissions

    @property
    def permissions__autoinit(self) -> 'list[RbacPermission]':
        """Return ``permissions``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._permissions``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``permissions__safederef`` when you only want to read a default without mutating self.
        """
        if self._permissions is None:
            self._permissions = self.permissions__safederef

        return self._permissions

    @property
    def permissions__safederef(self) -> 'list[RbacPermission]':
        """Return ``permissions``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``permissions`` stays unset and each
        call returns a new default. Use ``permissions__autoinit`` to persist the default on
        first access.
        """
        if self._permissions is None:
            return []

        return self._permissions

    @property
    def constraints(self) -> 'RbacPermissionConstraints':
        return self._constraints

    @constraints.setter
    def constraints(self, constraints: 'RbacPermissionConstraints'):
        self._constraints = constraints

    @property
    def constraints__autoinit(self) -> 'RbacPermissionConstraints':
        """Return ``constraints``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._constraints``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``constraints__safederef`` when you only want to read a default without mutating self.
        """
        if self._constraints is None:
            self._constraints = self.constraints__safederef

        return self._constraints

    @property
    def constraints__safederef(self) -> 'RbacPermissionConstraints':
        """Return ``constraints``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``constraints`` stays unset and each
        call returns a new default. Use ``constraints__autoinit`` to persist the default on
        first access.
        """
        if self._constraints is None:
            from .. import models
            return models.RbacPermissionConstraints._new_empty()

        return self._constraints

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, PermissionDescriptorAllow):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
