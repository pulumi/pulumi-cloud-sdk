# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class ListTemplatesResponse(PulumiAutoModelEncoder):
    """
    :var templates: list[Template] - declared
    :var continuation_token: str - declared
    :var diagnostics: list[str] - declared
    :var vcs_template_source_totals: list[OrgVcsTemplateSourceTotal] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'templates': 'list[Template]',
        'continuation_token': 'str',
        'diagnostics': 'list[str]',
        'vcs_template_source_totals': 'list[OrgVcsTemplateSourceTotal]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'templates': 'templates',
        'continuation_token': 'continuationToken',
        'diagnostics': 'diagnostics',
        'vcs_template_source_totals': 'vcsTemplateSourceTotals',
    }

    FIELDS_templates = 'templates'
    FIELDS_continuation_token = 'continuationToken'
    FIELDS_diagnostics = 'diagnostics'
    FIELDS_vcs_template_source_totals = 'vcsTemplateSourceTotals'

    _templates: 'list[Template]'
    _continuation_token: 'str'
    _diagnostics: 'list[str]'
    _vcs_template_source_totals: 'list[OrgVcsTemplateSourceTotal]'

    def __init__(
        self,
        templates: 'list[Template]',
        continuation_token: 'str' = None,
        diagnostics: 'list[str]' = None,
        vcs_template_source_totals: 'list[OrgVcsTemplateSourceTotal]' = None,
    ) -> None:
        super().__init__()

        self.templates = templates
        self.continuation_token = continuation_token
        self.diagnostics = diagnostics
        self.vcs_template_source_totals = vcs_template_source_totals

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, ListTemplatesResponse):
            self._templates = source._templates
            self._continuation_token = source._continuation_token
            self._diagnostics = source._diagnostics
            self._vcs_template_source_totals = source._vcs_template_source_totals

    @property
    def templates(self) -> 'list[Template]':
        return self._templates

    @templates.setter
    def templates(self, templates: 'list[Template]'):
        if templates is None:
            raise ValueError("Invalid value for `templates`, must not be `None`")

        self._templates = templates

    @property
    def templates__autoinit(self) -> 'list[Template]':
        """Return ``templates``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._templates``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``templates__safederef`` when you only want to read a default without mutating self.
        """
        if self._templates is None:
            self._templates = self.templates__safederef

        return self._templates

    @property
    def templates__safederef(self) -> 'list[Template]':
        """Return ``templates``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``templates`` stays unset and each
        call returns a new default. Use ``templates__autoinit`` to persist the default on
        first access.
        """
        if self._templates is None:
            return []

        return self._templates

    @property
    def continuation_token(self) -> 'str':
        return self._continuation_token

    @continuation_token.setter
    def continuation_token(self, continuation_token: 'str'):
        self._continuation_token = continuation_token

    @property
    def diagnostics(self) -> 'list[str]':
        return self._diagnostics

    @diagnostics.setter
    def diagnostics(self, diagnostics: 'list[str]'):
        self._diagnostics = diagnostics

    @property
    def diagnostics__autoinit(self) -> 'list[str]':
        """Return ``diagnostics``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._diagnostics``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``diagnostics__safederef`` when you only want to read a default without mutating self.
        """
        if self._diagnostics is None:
            self._diagnostics = self.diagnostics__safederef

        return self._diagnostics

    @property
    def diagnostics__safederef(self) -> 'list[str]':
        """Return ``diagnostics``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``diagnostics`` stays unset and each
        call returns a new default. Use ``diagnostics__autoinit`` to persist the default on
        first access.
        """
        if self._diagnostics is None:
            return []

        return self._diagnostics

    @property
    def vcs_template_source_totals(self) -> 'list[OrgVcsTemplateSourceTotal]':
        return self._vcs_template_source_totals

    @vcs_template_source_totals.setter
    def vcs_template_source_totals(self, vcs_template_source_totals: 'list[OrgVcsTemplateSourceTotal]'):
        self._vcs_template_source_totals = vcs_template_source_totals

    @property
    def vcs_template_source_totals__autoinit(self) -> 'list[OrgVcsTemplateSourceTotal]':
        """Return ``vcs_template_source_totals``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._vcs_template_source_totals``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``vcs_template_source_totals__safederef`` when you only want to read a default without mutating self.
        """
        if self._vcs_template_source_totals is None:
            self._vcs_template_source_totals = self.vcs_template_source_totals__safederef

        return self._vcs_template_source_totals

    @property
    def vcs_template_source_totals__safederef(self) -> 'list[OrgVcsTemplateSourceTotal]':
        """Return ``vcs_template_source_totals``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``vcs_template_source_totals`` stays unset and each
        call returns a new default. Use ``vcs_template_source_totals__autoinit`` to persist the default on
        first access.
        """
        if self._vcs_template_source_totals is None:
            return []

        return self._vcs_template_source_totals

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, ListTemplatesResponse):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
