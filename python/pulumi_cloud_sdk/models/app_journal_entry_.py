# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AppJournalEntry(PulumiAutoModelEncoder):
    """
    :var version: int - declared
    :var kind: AppJournalEntryKind - declared
    :var sequence_i_d: int - declared
    :var operation_i_d: int - declared
    :var remove_old: int - declared
    :var remove_new: int - declared
    :var pending_replacement_old: int - declared
    :var pending_replacement_new: int - declared
    :var delete_old: int - declared
    :var delete_new: int - declared
    :var state: AppResourceV3 - declared
    :var operation: AppOperationV2 - declared
    :var is_refresh: bool - declared
    :var secrets_provider: AppSecretsProvidersV1 - declared
    :var new_snapshot: AppDeploymentV3 - declared
    :var snippets: list[AppSnippetV1] - declared
    :var extension_ref: str - declared
    :var extension: AppExtension - declared
    :var requires_byte_string: bool - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'version': 'int',
        'kind': 'AppJournalEntryKind',
        'sequence_i_d': 'int',
        'operation_i_d': 'int',
        'remove_old': 'int',
        'remove_new': 'int',
        'pending_replacement_old': 'int',
        'pending_replacement_new': 'int',
        'delete_old': 'int',
        'delete_new': 'int',
        'state': 'AppResourceV3',
        'operation': 'AppOperationV2',
        'is_refresh': 'bool',
        'secrets_provider': 'AppSecretsProvidersV1',
        'new_snapshot': 'AppDeploymentV3',
        'snippets': 'list[AppSnippetV1]',
        'extension_ref': 'str',
        'extension': 'AppExtension',
        'requires_byte_string': 'bool',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'version': 'version',
        'kind': 'kind',
        'sequence_i_d': 'sequenceID',
        'operation_i_d': 'operationID',
        'remove_old': 'removeOld',
        'remove_new': 'removeNew',
        'pending_replacement_old': 'pendingReplacementOld',
        'pending_replacement_new': 'pendingReplacementNew',
        'delete_old': 'deleteOld',
        'delete_new': 'deleteNew',
        'state': 'state',
        'operation': 'operation',
        'is_refresh': 'isRefresh',
        'secrets_provider': 'secretsProvider',
        'new_snapshot': 'newSnapshot',
        'snippets': 'snippets',
        'extension_ref': 'extensionRef',
        'extension': 'extension',
        'requires_byte_string': 'requiresByteString',
    }

    FIELDS_version = 'version'
    FIELDS_kind = 'kind'
    FIELDS_sequence_i_d = 'sequenceID'
    FIELDS_operation_i_d = 'operationID'
    FIELDS_remove_old = 'removeOld'
    FIELDS_remove_new = 'removeNew'
    FIELDS_pending_replacement_old = 'pendingReplacementOld'
    FIELDS_pending_replacement_new = 'pendingReplacementNew'
    FIELDS_delete_old = 'deleteOld'
    FIELDS_delete_new = 'deleteNew'
    FIELDS_state = 'state'
    FIELDS_operation = 'operation'
    FIELDS_is_refresh = 'isRefresh'
    FIELDS_secrets_provider = 'secretsProvider'
    FIELDS_new_snapshot = 'newSnapshot'
    FIELDS_snippets = 'snippets'
    FIELDS_extension_ref = 'extensionRef'
    FIELDS_extension = 'extension'
    FIELDS_requires_byte_string = 'requiresByteString'

    _version: 'int'
    _kind: 'AppJournalEntryKind'
    _sequence_i_d: 'int'
    _operation_i_d: 'int'
    _remove_old: 'int'
    _remove_new: 'int'
    _pending_replacement_old: 'int'
    _pending_replacement_new: 'int'
    _delete_old: 'int'
    _delete_new: 'int'
    _state: 'AppResourceV3'
    _operation: 'AppOperationV2'
    _is_refresh: 'bool'
    _secrets_provider: 'AppSecretsProvidersV1'
    _new_snapshot: 'AppDeploymentV3'
    _snippets: 'list[AppSnippetV1]'
    _extension_ref: 'str'
    _extension: 'AppExtension'
    _requires_byte_string: 'bool'

    def __init__(
        self,
        version: 'int',
        kind: 'AppJournalEntryKind',
        sequence_i_d: 'int',
        operation_i_d: 'int',
        remove_old: 'int',
        remove_new: 'int',
        pending_replacement_old: 'int' = 0,
        pending_replacement_new: 'int' = 0,
        delete_old: 'int' = 0,
        delete_new: 'int' = 0,
        state: 'AppResourceV3' = None,
        operation: 'AppOperationV2' = None,
        is_refresh: 'bool' = False,
        secrets_provider: 'AppSecretsProvidersV1' = None,
        new_snapshot: 'AppDeploymentV3' = None,
        snippets: 'list[AppSnippetV1]' = None,
        extension_ref: 'str' = None,
        extension: 'AppExtension' = None,
        requires_byte_string: 'bool' = False,
    ) -> None:
        super().__init__()

        self.version = version
        self.kind = kind
        self.sequence_i_d = sequence_i_d
        self.operation_i_d = operation_i_d
        self.remove_old = remove_old
        self.remove_new = remove_new
        self.pending_replacement_old = pending_replacement_old
        self.pending_replacement_new = pending_replacement_new
        self.delete_old = delete_old
        self.delete_new = delete_new
        self.state = state
        self.operation = operation
        self.is_refresh = is_refresh
        self.secrets_provider = secrets_provider
        self.new_snapshot = new_snapshot
        self.snippets = snippets
        self.extension_ref = extension_ref
        self.extension = extension
        self.requires_byte_string = requires_byte_string

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AppJournalEntry):
            self._version = source._version
            self._kind = source._kind
            self._sequence_i_d = source._sequence_i_d
            self._operation_i_d = source._operation_i_d
            self._remove_old = source._remove_old
            self._remove_new = source._remove_new
            self._pending_replacement_old = source._pending_replacement_old
            self._pending_replacement_new = source._pending_replacement_new
            self._delete_old = source._delete_old
            self._delete_new = source._delete_new
            self._state = source._state
            self._operation = source._operation
            self._is_refresh = source._is_refresh
            self._secrets_provider = source._secrets_provider
            self._new_snapshot = source._new_snapshot
            self._snippets = source._snippets
            self._extension_ref = source._extension_ref
            self._extension = source._extension
            self._requires_byte_string = source._requires_byte_string

    @property
    def version(self) -> 'int':
        return self._version

    @version.setter
    def version(self, version: 'int'):
        if version is None:
            raise ValueError("Invalid value for `version`, must not be `None`")

        self._version = version

    @property
    def kind(self) -> 'AppJournalEntryKind':
        return self._kind

    @kind.setter
    def kind(self, kind: 'AppJournalEntryKind'):
        if kind is None:
            raise ValueError("Invalid value for `kind`, must not be `None`")

        self._kind = kind

    @property
    def sequence_i_d(self) -> 'int':
        return self._sequence_i_d

    @sequence_i_d.setter
    def sequence_i_d(self, sequence_i_d: 'int'):
        if sequence_i_d is None:
            raise ValueError("Invalid value for `sequence_i_d`, must not be `None`")

        self._sequence_i_d = sequence_i_d

    @property
    def operation_i_d(self) -> 'int':
        return self._operation_i_d

    @operation_i_d.setter
    def operation_i_d(self, operation_i_d: 'int'):
        if operation_i_d is None:
            raise ValueError("Invalid value for `operation_i_d`, must not be `None`")

        self._operation_i_d = operation_i_d

    @property
    def remove_old(self) -> 'int':
        return self._remove_old

    @remove_old.setter
    def remove_old(self, remove_old: 'int'):
        if remove_old is None:
            raise ValueError("Invalid value for `remove_old`, must not be `None`")

        self._remove_old = remove_old

    @property
    def remove_new(self) -> 'int':
        return self._remove_new

    @remove_new.setter
    def remove_new(self, remove_new: 'int'):
        if remove_new is None:
            raise ValueError("Invalid value for `remove_new`, must not be `None`")

        self._remove_new = remove_new

    @property
    def pending_replacement_old(self) -> 'int':
        return self._pending_replacement_old

    @pending_replacement_old.setter
    def pending_replacement_old(self, pending_replacement_old: 'int'):
        self._pending_replacement_old = pending_replacement_old

    @property
    def pending_replacement_new(self) -> 'int':
        return self._pending_replacement_new

    @pending_replacement_new.setter
    def pending_replacement_new(self, pending_replacement_new: 'int'):
        self._pending_replacement_new = pending_replacement_new

    @property
    def delete_old(self) -> 'int':
        return self._delete_old

    @delete_old.setter
    def delete_old(self, delete_old: 'int'):
        self._delete_old = delete_old

    @property
    def delete_new(self) -> 'int':
        return self._delete_new

    @delete_new.setter
    def delete_new(self, delete_new: 'int'):
        self._delete_new = delete_new

    @property
    def state(self) -> 'AppResourceV3':
        return self._state

    @state.setter
    def state(self, state: 'AppResourceV3'):
        self._state = state

    @property
    def state__autoinit(self) -> 'AppResourceV3':
        """Return ``state``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._state``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``state__safederef`` when you only want to read a default without mutating self.
        """
        if self._state is None:
            self._state = self.state__safederef

        return self._state

    @property
    def state__safederef(self) -> 'AppResourceV3':
        """Return ``state``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``state`` stays unset and each
        call returns a new default. Use ``state__autoinit`` to persist the default on
        first access.
        """
        if self._state is None:
            from .. import models
            return models.AppResourceV3._new_empty()

        return self._state

    @property
    def operation(self) -> 'AppOperationV2':
        return self._operation

    @operation.setter
    def operation(self, operation: 'AppOperationV2'):
        self._operation = operation

    @property
    def operation__autoinit(self) -> 'AppOperationV2':
        """Return ``operation``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._operation``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``operation__safederef`` when you only want to read a default without mutating self.
        """
        if self._operation is None:
            self._operation = self.operation__safederef

        return self._operation

    @property
    def operation__safederef(self) -> 'AppOperationV2':
        """Return ``operation``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``operation`` stays unset and each
        call returns a new default. Use ``operation__autoinit`` to persist the default on
        first access.
        """
        if self._operation is None:
            from .. import models
            return models.AppOperationV2._new_empty()

        return self._operation

    @property
    def is_refresh(self) -> 'bool':
        return self._is_refresh

    @is_refresh.setter
    def is_refresh(self, is_refresh: 'bool'):
        self._is_refresh = is_refresh

    @property
    def secrets_provider(self) -> 'AppSecretsProvidersV1':
        return self._secrets_provider

    @secrets_provider.setter
    def secrets_provider(self, secrets_provider: 'AppSecretsProvidersV1'):
        self._secrets_provider = secrets_provider

    @property
    def secrets_provider__autoinit(self) -> 'AppSecretsProvidersV1':
        """Return ``secrets_provider``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._secrets_provider``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``secrets_provider__safederef`` when you only want to read a default without mutating self.
        """
        if self._secrets_provider is None:
            self._secrets_provider = self.secrets_provider__safederef

        return self._secrets_provider

    @property
    def secrets_provider__safederef(self) -> 'AppSecretsProvidersV1':
        """Return ``secrets_provider``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``secrets_provider`` stays unset and each
        call returns a new default. Use ``secrets_provider__autoinit`` to persist the default on
        first access.
        """
        if self._secrets_provider is None:
            from .. import models
            return models.AppSecretsProvidersV1._new_empty()

        return self._secrets_provider

    @property
    def new_snapshot(self) -> 'AppDeploymentV3':
        return self._new_snapshot

    @new_snapshot.setter
    def new_snapshot(self, new_snapshot: 'AppDeploymentV3'):
        self._new_snapshot = new_snapshot

    @property
    def new_snapshot__autoinit(self) -> 'AppDeploymentV3':
        """Return ``new_snapshot``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._new_snapshot``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``new_snapshot__safederef`` when you only want to read a default without mutating self.
        """
        if self._new_snapshot is None:
            self._new_snapshot = self.new_snapshot__safederef

        return self._new_snapshot

    @property
    def new_snapshot__safederef(self) -> 'AppDeploymentV3':
        """Return ``new_snapshot``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``new_snapshot`` stays unset and each
        call returns a new default. Use ``new_snapshot__autoinit`` to persist the default on
        first access.
        """
        if self._new_snapshot is None:
            from .. import models
            return models.AppDeploymentV3._new_empty()

        return self._new_snapshot

    @property
    def snippets(self) -> 'list[AppSnippetV1]':
        return self._snippets

    @snippets.setter
    def snippets(self, snippets: 'list[AppSnippetV1]'):
        self._snippets = snippets

    @property
    def snippets__autoinit(self) -> 'list[AppSnippetV1]':
        """Return ``snippets``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._snippets``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``snippets__safederef`` when you only want to read a default without mutating self.
        """
        if self._snippets is None:
            self._snippets = self.snippets__safederef

        return self._snippets

    @property
    def snippets__safederef(self) -> 'list[AppSnippetV1]':
        """Return ``snippets``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``snippets`` stays unset and each
        call returns a new default. Use ``snippets__autoinit`` to persist the default on
        first access.
        """
        if self._snippets is None:
            return []

        return self._snippets

    @property
    def extension_ref(self) -> 'str':
        return self._extension_ref

    @extension_ref.setter
    def extension_ref(self, extension_ref: 'str'):
        self._extension_ref = extension_ref

    @property
    def extension(self) -> 'AppExtension':
        return self._extension

    @extension.setter
    def extension(self, extension: 'AppExtension'):
        self._extension = extension

    @property
    def extension__autoinit(self) -> 'AppExtension':
        """Return ``extension``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._extension``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``extension__safederef`` when you only want to read a default without mutating self.
        """
        if self._extension is None:
            self._extension = self.extension__safederef

        return self._extension

    @property
    def extension__safederef(self) -> 'AppExtension':
        """Return ``extension``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``extension`` stays unset and each
        call returns a new default. Use ``extension__autoinit`` to persist the default on
        first access.
        """
        if self._extension is None:
            from .. import models
            return models.AppExtension._new_empty()

        return self._extension

    @property
    def requires_byte_string(self) -> 'bool':
        return self._requires_byte_string

    @requires_byte_string.setter
    def requires_byte_string(self, requires_byte_string: 'bool'):
        self._requires_byte_string = requires_byte_string

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AppJournalEntry):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
