# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AppBeginUpdateResponse(PulumiAutoModelEncoder):
    """
    :var update_i_d: str - declared
    :var version: int - declared
    :var token: str - declared
    :var token_expiration: int - declared
    :var journal_version: int - declared
    :var required_policies: list[AppRequiredPolicy] - declared
    :var messages: list[AppMessage] - declared
    :var ai_settings: AppAISettingsForUpdate - declared
    :var deployment: AppUntypedDeployment - declared
    :var stack: AppStack - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'update_i_d': 'str',
        'version': 'int',
        'token': 'str',
        'token_expiration': 'int',
        'journal_version': 'int',
        'required_policies': 'list[AppRequiredPolicy]',
        'messages': 'list[AppMessage]',
        'ai_settings': 'AppAISettingsForUpdate',
        'deployment': 'AppUntypedDeployment',
        'stack': 'AppStack',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'update_i_d': 'updateID',
        'version': 'version',
        'token': 'token',
        'token_expiration': 'tokenExpiration',
        'journal_version': 'journalVersion',
        'required_policies': 'requiredPolicies',
        'messages': 'messages',
        'ai_settings': 'aiSettings',
        'deployment': 'deployment',
        'stack': 'stack',
    }

    FIELDS_update_i_d = 'updateID'
    FIELDS_version = 'version'
    FIELDS_token = 'token'
    FIELDS_token_expiration = 'tokenExpiration'
    FIELDS_journal_version = 'journalVersion'
    FIELDS_required_policies = 'requiredPolicies'
    FIELDS_messages = 'messages'
    FIELDS_ai_settings = 'aiSettings'
    FIELDS_deployment = 'deployment'
    FIELDS_stack = 'stack'

    _update_i_d: 'str'
    _version: 'int'
    _token: 'str'
    _token_expiration: 'int'
    _journal_version: 'int'
    _required_policies: 'list[AppRequiredPolicy]'
    _messages: 'list[AppMessage]'
    _ai_settings: 'AppAISettingsForUpdate'
    _deployment: 'AppUntypedDeployment'
    _stack: 'AppStack'

    def __init__(
        self,
        update_i_d: 'str',
        version: 'int',
        ai_settings: 'AppAISettingsForUpdate',
        stack: 'AppStack',
        token: 'str' = None,
        token_expiration: 'int' = 0,
        journal_version: 'int' = 0,
        required_policies: 'list[AppRequiredPolicy]' = None,
        messages: 'list[AppMessage]' = None,
        deployment: 'AppUntypedDeployment' = None,
    ) -> None:
        super().__init__()

        self.update_i_d = update_i_d
        self.version = version
        self.token = token
        self.token_expiration = token_expiration
        self.journal_version = journal_version
        self.required_policies = required_policies
        self.messages = messages
        self.ai_settings = ai_settings
        self.deployment = deployment
        self.stack = stack

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AppBeginUpdateResponse):
            self._update_i_d = source._update_i_d
            self._version = source._version
            self._token = source._token
            self._token_expiration = source._token_expiration
            self._journal_version = source._journal_version
            self._required_policies = source._required_policies
            self._messages = source._messages
            self._ai_settings = source._ai_settings
            self._deployment = source._deployment
            self._stack = source._stack

    @property
    def update_i_d(self) -> 'str':
        return self._update_i_d

    @update_i_d.setter
    def update_i_d(self, update_i_d: 'str'):
        if update_i_d is None:
            raise ValueError("Invalid value for `update_i_d`, must not be `None`")

        self._update_i_d = update_i_d

    @property
    def version(self) -> 'int':
        return self._version

    @version.setter
    def version(self, version: 'int'):
        if version is None:
            raise ValueError("Invalid value for `version`, must not be `None`")

        self._version = version

    @property
    def token(self) -> 'str':
        return self._token

    @token.setter
    def token(self, token: 'str'):
        self._token = token

    @property
    def token_expiration(self) -> 'int':
        return self._token_expiration

    @token_expiration.setter
    def token_expiration(self, token_expiration: 'int'):
        self._token_expiration = token_expiration

    @property
    def journal_version(self) -> 'int':
        return self._journal_version

    @journal_version.setter
    def journal_version(self, journal_version: 'int'):
        self._journal_version = journal_version

    @property
    def required_policies(self) -> 'list[AppRequiredPolicy]':
        return self._required_policies

    @required_policies.setter
    def required_policies(self, required_policies: 'list[AppRequiredPolicy]'):
        self._required_policies = required_policies

    @property
    def required_policies__autoinit(self) -> 'list[AppRequiredPolicy]':
        """Return ``required_policies``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._required_policies``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``required_policies__safederef`` when you only want to read a default without mutating self.
        """
        if self._required_policies is None:
            self._required_policies = self.required_policies__safederef

        return self._required_policies

    @property
    def required_policies__safederef(self) -> 'list[AppRequiredPolicy]':
        """Return ``required_policies``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``required_policies`` stays unset and each
        call returns a new default. Use ``required_policies__autoinit`` to persist the default on
        first access.
        """
        if self._required_policies is None:
            return []

        return self._required_policies

    @property
    def messages(self) -> 'list[AppMessage]':
        return self._messages

    @messages.setter
    def messages(self, messages: 'list[AppMessage]'):
        self._messages = messages

    @property
    def messages__autoinit(self) -> 'list[AppMessage]':
        """Return ``messages``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._messages``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``messages__safederef`` when you only want to read a default without mutating self.
        """
        if self._messages is None:
            self._messages = self.messages__safederef

        return self._messages

    @property
    def messages__safederef(self) -> 'list[AppMessage]':
        """Return ``messages``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``messages`` stays unset and each
        call returns a new default. Use ``messages__autoinit`` to persist the default on
        first access.
        """
        if self._messages is None:
            return []

        return self._messages

    @property
    def ai_settings(self) -> 'AppAISettingsForUpdate':
        return self._ai_settings

    @ai_settings.setter
    def ai_settings(self, ai_settings: 'AppAISettingsForUpdate'):
        if ai_settings is None:
            raise ValueError("Invalid value for `ai_settings`, must not be `None`")

        self._ai_settings = ai_settings

    @property
    def ai_settings__autoinit(self) -> 'AppAISettingsForUpdate':
        """Return ``ai_settings``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._ai_settings``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``ai_settings__safederef`` when you only want to read a default without mutating self.
        """
        if self._ai_settings is None:
            self._ai_settings = self.ai_settings__safederef

        return self._ai_settings

    @property
    def ai_settings__safederef(self) -> 'AppAISettingsForUpdate':
        """Return ``ai_settings``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``ai_settings`` stays unset and each
        call returns a new default. Use ``ai_settings__autoinit`` to persist the default on
        first access.
        """
        if self._ai_settings is None:
            from .. import models
            return models.AppAISettingsForUpdate._new_empty()

        return self._ai_settings

    @property
    def deployment(self) -> 'AppUntypedDeployment':
        return self._deployment

    @deployment.setter
    def deployment(self, deployment: 'AppUntypedDeployment'):
        self._deployment = deployment

    @property
    def deployment__autoinit(self) -> 'AppUntypedDeployment':
        """Return ``deployment``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._deployment``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``deployment__safederef`` when you only want to read a default without mutating self.
        """
        if self._deployment is None:
            self._deployment = self.deployment__safederef

        return self._deployment

    @property
    def deployment__safederef(self) -> 'AppUntypedDeployment':
        """Return ``deployment``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``deployment`` stays unset and each
        call returns a new default. Use ``deployment__autoinit`` to persist the default on
        first access.
        """
        if self._deployment is None:
            from .. import models
            return models.AppUntypedDeployment._new_empty()

        return self._deployment

    @property
    def stack(self) -> 'AppStack':
        return self._stack

    @stack.setter
    def stack(self, stack: 'AppStack'):
        if stack is None:
            raise ValueError("Invalid value for `stack`, must not be `None`")

        self._stack = stack

    @property
    def stack__autoinit(self) -> 'AppStack':
        """Return ``stack``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._stack``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``stack__safederef`` when you only want to read a default without mutating self.
        """
        if self._stack is None:
            self._stack = self.stack__safederef

        return self._stack

    @property
    def stack__safederef(self) -> 'AppStack':
        """Return ``stack``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``stack`` stays unset and each
        call returns a new default. Use ``stack__autoinit`` to persist the default on
        first access.
        """
        if self._stack is None:
            from .. import models
            return models.AppStack._new_empty()

        return self._stack

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AppBeginUpdateResponse):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
