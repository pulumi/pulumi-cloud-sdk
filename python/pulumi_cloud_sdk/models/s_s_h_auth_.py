# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class SSHAuth(PulumiAutoModelEncoder):
    """
    :var ssh_private_key: SecretValue - declared
    :var password: SecretValue - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'ssh_private_key': 'SecretValue',
        'password': 'SecretValue',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'ssh_private_key': 'sshPrivateKey',
        'password': 'password',
    }

    FIELDS_ssh_private_key = 'sshPrivateKey'
    FIELDS_password = 'password'

    _ssh_private_key: 'SecretValue'
    _password: 'SecretValue'

    def __init__(
        self,
        ssh_private_key: 'SecretValue',
        password: 'SecretValue' = None,
    ) -> None:
        super().__init__()

        self.ssh_private_key = ssh_private_key
        self.password = password

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, SSHAuth):
            self._ssh_private_key = source._ssh_private_key
            self._password = source._password

    @property
    def ssh_private_key(self) -> 'SecretValue':
        return self._ssh_private_key

    @ssh_private_key.setter
    def ssh_private_key(self, ssh_private_key: 'SecretValue'):
        if ssh_private_key is None:
            raise ValueError("Invalid value for `ssh_private_key`, must not be `None`")

        self._ssh_private_key = ssh_private_key

    @property
    def ssh_private_key__autoinit(self) -> 'SecretValue':
        """Return ``ssh_private_key``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._ssh_private_key``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``ssh_private_key__safederef`` when you only want to read a default without mutating self.
        """
        if self._ssh_private_key is None:
            self._ssh_private_key = self.ssh_private_key__safederef

        return self._ssh_private_key

    @property
    def ssh_private_key__safederef(self) -> 'SecretValue':
        """Return ``ssh_private_key``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``ssh_private_key`` stays unset and each
        call returns a new default. Use ``ssh_private_key__autoinit`` to persist the default on
        first access.
        """
        if self._ssh_private_key is None:
            from .. import models
            return models.SecretValue._new_empty()

        return self._ssh_private_key

    @property
    def password(self) -> 'SecretValue':
        return self._password

    @password.setter
    def password(self, password: 'SecretValue'):
        self._password = password

    @property
    def password__autoinit(self) -> 'SecretValue':
        """Return ``password``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._password``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``password__safederef`` when you only want to read a default without mutating self.
        """
        if self._password is None:
            self._password = self.password__safederef

        return self._password

    @property
    def password__safederef(self) -> 'SecretValue':
        """Return ``password``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``password`` stays unset and each
        call returns a new default. Use ``password__autoinit`` to persist the default on
        first access.
        """
        if self._password is None:
            from .. import models
            return models.SecretValue._new_empty()

        return self._password

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, SSHAuth):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
