# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class StackMetadata(PulumiAutoModelEncoder):
    """
    :var org_name: str - declared
    :var project_name: str - declared
    :var stack_name: str - declared
    :var owned_by: UserInfo - declared
    :var notification_settings: StackNotificationSettings - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'org_name': 'str',
        'project_name': 'str',
        'stack_name': 'str',
        'owned_by': 'UserInfo',
        'notification_settings': 'StackNotificationSettings',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'org_name': 'orgName',
        'project_name': 'projectName',
        'stack_name': 'stackName',
        'owned_by': 'ownedBy',
        'notification_settings': 'notificationSettings',
    }

    FIELDS_org_name = 'orgName'
    FIELDS_project_name = 'projectName'
    FIELDS_stack_name = 'stackName'
    FIELDS_owned_by = 'ownedBy'
    FIELDS_notification_settings = 'notificationSettings'

    _org_name: 'str'
    _project_name: 'str'
    _stack_name: 'str'
    _owned_by: 'UserInfo'
    _notification_settings: 'StackNotificationSettings'

    def __init__(
        self,
        org_name: 'str',
        project_name: 'str',
        stack_name: 'str',
        owned_by: 'UserInfo',
        notification_settings: 'StackNotificationSettings',
    ) -> None:
        super().__init__()

        self.org_name = org_name
        self.project_name = project_name
        self.stack_name = stack_name
        self.owned_by = owned_by
        self.notification_settings = notification_settings

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, StackMetadata):
            self._org_name = source._org_name
            self._project_name = source._project_name
            self._stack_name = source._stack_name
            self._owned_by = source._owned_by
            self._notification_settings = source._notification_settings

    @property
    def org_name(self) -> 'str':
        return self._org_name

    @org_name.setter
    def org_name(self, org_name: 'str'):
        if org_name is None:
            raise ValueError("Invalid value for `org_name`, must not be `None`")

        self._org_name = org_name

    @property
    def project_name(self) -> 'str':
        return self._project_name

    @project_name.setter
    def project_name(self, project_name: 'str'):
        if project_name is None:
            raise ValueError("Invalid value for `project_name`, must not be `None`")

        self._project_name = project_name

    @property
    def stack_name(self) -> 'str':
        return self._stack_name

    @stack_name.setter
    def stack_name(self, stack_name: 'str'):
        if stack_name is None:
            raise ValueError("Invalid value for `stack_name`, must not be `None`")

        self._stack_name = stack_name

    @property
    def owned_by(self) -> 'UserInfo':
        return self._owned_by

    @owned_by.setter
    def owned_by(self, owned_by: 'UserInfo'):
        if owned_by is None:
            raise ValueError("Invalid value for `owned_by`, must not be `None`")

        self._owned_by = owned_by

    @property
    def owned_by__autoinit(self) -> 'UserInfo':
        """Return ``owned_by``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._owned_by``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``owned_by__safederef`` when you only want to read a default without mutating self.
        """
        if self._owned_by is None:
            self._owned_by = self.owned_by__safederef

        return self._owned_by

    @property
    def owned_by__safederef(self) -> 'UserInfo':
        """Return ``owned_by``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``owned_by`` stays unset and each
        call returns a new default. Use ``owned_by__autoinit`` to persist the default on
        first access.
        """
        if self._owned_by is None:
            from .. import models
            return models.UserInfo._new_empty()

        return self._owned_by

    @property
    def notification_settings(self) -> 'StackNotificationSettings':
        return self._notification_settings

    @notification_settings.setter
    def notification_settings(self, notification_settings: 'StackNotificationSettings'):
        if notification_settings is None:
            raise ValueError("Invalid value for `notification_settings`, must not be `None`")

        self._notification_settings = notification_settings

    @property
    def notification_settings__autoinit(self) -> 'StackNotificationSettings':
        """Return ``notification_settings``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._notification_settings``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``notification_settings__safederef`` when you only want to read a default without mutating self.
        """
        if self._notification_settings is None:
            self._notification_settings = self.notification_settings__safederef

        return self._notification_settings

    @property
    def notification_settings__safederef(self) -> 'StackNotificationSettings':
        """Return ``notification_settings``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``notification_settings`` stays unset and each
        call returns a new default. Use ``notification_settings__autoinit`` to persist the default on
        first access.
        """
        if self._notification_settings is None:
            from .. import models
            return models.StackNotificationSettings._new_empty()

        return self._notification_settings

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, StackMetadata):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
