# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AppUpdateProgramRequest(PulumiAutoModelEncoder):
    """
    :var name: str - declared
    :var runtime: str - declared
    :var main: str - declared
    :var description: str - declared
    :var options: AppUpdateOptions - declared
    :var config: dict[str, AppConfigValue] - declared
    :var metadata: AppUpdateMetadata - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'name': 'str',
        'runtime': 'str',
        'main': 'str',
        'description': 'str',
        'options': 'AppUpdateOptions',
        'config': 'dict[str, AppConfigValue]',
        'metadata': 'AppUpdateMetadata',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'name': 'name',
        'runtime': 'runtime',
        'main': 'main',
        'description': 'description',
        'options': 'options',
        'config': 'config',
        'metadata': 'metadata',
    }

    FIELDS_name = 'name'
    FIELDS_runtime = 'runtime'
    FIELDS_main = 'main'
    FIELDS_description = 'description'
    FIELDS_options = 'options'
    FIELDS_config = 'config'
    FIELDS_metadata = 'metadata'

    _name: 'str'
    _runtime: 'str'
    _main: 'str'
    _description: 'str'
    _options: 'AppUpdateOptions'
    _config: 'dict[str, AppConfigValue]'
    _metadata: 'AppUpdateMetadata'

    def __init__(
        self,
        name: 'str',
        runtime: 'str',
        main: 'str',
        description: 'str',
        options: 'AppUpdateOptions',
        config: 'dict[str, AppConfigValue]',
        metadata: 'AppUpdateMetadata',
    ) -> None:
        super().__init__()

        self.name = name
        self.runtime = runtime
        self.main = main
        self.description = description
        self.options = options
        self.config = config
        self.metadata = metadata

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AppUpdateProgramRequest):
            self._name = source._name
            self._runtime = source._runtime
            self._main = source._main
            self._description = source._description
            self._options = source._options
            self._config = source._config
            self._metadata = source._metadata

    @property
    def name(self) -> 'str':
        return self._name

    @name.setter
    def name(self, name: 'str'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def runtime(self) -> 'str':
        return self._runtime

    @runtime.setter
    def runtime(self, runtime: 'str'):
        if runtime is None:
            raise ValueError("Invalid value for `runtime`, must not be `None`")

        self._runtime = runtime

    @property
    def main(self) -> 'str':
        return self._main

    @main.setter
    def main(self, main: 'str'):
        if main is None:
            raise ValueError("Invalid value for `main`, must not be `None`")

        self._main = main

    @property
    def description(self) -> 'str':
        return self._description

    @description.setter
    def description(self, description: 'str'):
        if description is None:
            raise ValueError("Invalid value for `description`, must not be `None`")

        self._description = description

    @property
    def options(self) -> 'AppUpdateOptions':
        return self._options

    @options.setter
    def options(self, options: 'AppUpdateOptions'):
        if options is None:
            raise ValueError("Invalid value for `options`, must not be `None`")

        self._options = options

    @property
    def options__autoinit(self) -> 'AppUpdateOptions':
        """Return ``options``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._options``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``options__safederef`` when you only want to read a default without mutating self.
        """
        if self._options is None:
            self._options = self.options__safederef

        return self._options

    @property
    def options__safederef(self) -> 'AppUpdateOptions':
        """Return ``options``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``options`` stays unset and each
        call returns a new default. Use ``options__autoinit`` to persist the default on
        first access.
        """
        if self._options is None:
            from .. import models
            return models.AppUpdateOptions._new_empty()

        return self._options

    @property
    def config(self) -> 'dict[str, AppConfigValue]':
        return self._config

    @config.setter
    def config(self, config: 'dict[str, AppConfigValue]'):
        if config is None:
            raise ValueError("Invalid value for `config`, must not be `None`")

        self._config = config

    @property
    def config__autoinit(self) -> 'dict[str, AppConfigValue]':
        """Return ``config``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._config``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``config__safederef`` when you only want to read a default without mutating self.
        """
        if self._config is None:
            self._config = self.config__safederef

        return self._config

    @property
    def config__safederef(self) -> 'dict[str, AppConfigValue]':
        """Return ``config``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``config`` stays unset and each
        call returns a new default. Use ``config__autoinit`` to persist the default on
        first access.
        """
        if self._config is None:
            return {}

        return self._config

    @property
    def metadata(self) -> 'AppUpdateMetadata':
        return self._metadata

    @metadata.setter
    def metadata(self, metadata: 'AppUpdateMetadata'):
        if metadata is None:
            raise ValueError("Invalid value for `metadata`, must not be `None`")

        self._metadata = metadata

    @property
    def metadata__autoinit(self) -> 'AppUpdateMetadata':
        """Return ``metadata``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._metadata``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``metadata__safederef`` when you only want to read a default without mutating self.
        """
        if self._metadata is None:
            self._metadata = self.metadata__safederef

        return self._metadata

    @property
    def metadata__safederef(self) -> 'AppUpdateMetadata':
        """Return ``metadata``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``metadata`` stays unset and each
        call returns a new default. Use ``metadata__autoinit`` to persist the default on
        first access.
        """
        if self._metadata is None:
            from .. import models
            return models.AppUpdateMetadata._new_empty()

        return self._metadata

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AppUpdateProgramRequest):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
