# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class EnvironmentMetadata(PulumiAutoModelEncoder):
    """
    :var id: str - declared
    :var owned_by: UserInfo - declared
    :var active_change_request: ChangeRequestRef - declared
    :var gated_actions: list[str] - declared
    :var open_request_needed: bool - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'id': 'str',
        'owned_by': 'UserInfo',
        'active_change_request': 'ChangeRequestRef',
        'gated_actions': 'list[str]',
        'open_request_needed': 'bool',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'id': 'id',
        'owned_by': 'ownedBy',
        'active_change_request': 'activeChangeRequest',
        'gated_actions': 'gatedActions',
        'open_request_needed': 'openRequestNeeded',
    }

    FIELDS_id = 'id'
    FIELDS_owned_by = 'ownedBy'
    FIELDS_active_change_request = 'activeChangeRequest'
    FIELDS_gated_actions = 'gatedActions'
    FIELDS_open_request_needed = 'openRequestNeeded'

    _id: 'str'
    _owned_by: 'UserInfo'
    _active_change_request: 'ChangeRequestRef'
    _gated_actions: 'list[str]'
    _open_request_needed: 'bool'

    def __init__(
        self,
        id: 'str',
        owned_by: 'UserInfo',
        active_change_request: 'ChangeRequestRef' = None,
        gated_actions: 'list[str]' = None,
        open_request_needed: 'bool' = False,
    ) -> None:
        super().__init__()

        self.id = id
        self.owned_by = owned_by
        self.active_change_request = active_change_request
        self.gated_actions = gated_actions
        self.open_request_needed = open_request_needed

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, EnvironmentMetadata):
            self._id = source._id
            self._owned_by = source._owned_by
            self._active_change_request = source._active_change_request
            self._gated_actions = source._gated_actions
            self._open_request_needed = source._open_request_needed

    @property
    def id(self) -> 'str':
        return self._id

    @id.setter
    def id(self, id: 'str'):
        if id is None:
            raise ValueError("Invalid value for `id`, must not be `None`")

        self._id = id

    @property
    def owned_by(self) -> 'UserInfo':
        return self._owned_by

    @owned_by.setter
    def owned_by(self, owned_by: 'UserInfo'):
        if owned_by is None:
            raise ValueError("Invalid value for `owned_by`, must not be `None`")

        self._owned_by = owned_by

    @property
    def owned_by__autoinit(self) -> 'UserInfo':
        """Return ``owned_by``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._owned_by``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``owned_by__safederef`` when you only want to read a default without mutating self.
        """
        if self._owned_by is None:
            self._owned_by = self.owned_by__safederef

        return self._owned_by

    @property
    def owned_by__safederef(self) -> 'UserInfo':
        """Return ``owned_by``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``owned_by`` stays unset and each
        call returns a new default. Use ``owned_by__autoinit`` to persist the default on
        first access.
        """
        if self._owned_by is None:
            from .. import models
            return models.UserInfo._new_empty()

        return self._owned_by

    @property
    def active_change_request(self) -> 'ChangeRequestRef':
        return self._active_change_request

    @active_change_request.setter
    def active_change_request(self, active_change_request: 'ChangeRequestRef'):
        self._active_change_request = active_change_request

    @property
    def active_change_request__autoinit(self) -> 'ChangeRequestRef':
        """Return ``active_change_request``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._active_change_request``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``active_change_request__safederef`` when you only want to read a default without mutating self.
        """
        if self._active_change_request is None:
            self._active_change_request = self.active_change_request__safederef

        return self._active_change_request

    @property
    def active_change_request__safederef(self) -> 'ChangeRequestRef':
        """Return ``active_change_request``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``active_change_request`` stays unset and each
        call returns a new default. Use ``active_change_request__autoinit`` to persist the default on
        first access.
        """
        if self._active_change_request is None:
            from .. import models
            return models.ChangeRequestRef._new_empty()

        return self._active_change_request

    @property
    def gated_actions(self) -> 'list[str]':
        return self._gated_actions

    @gated_actions.setter
    def gated_actions(self, gated_actions: 'list[str]'):
        self._gated_actions = gated_actions

    @property
    def gated_actions__autoinit(self) -> 'list[str]':
        """Return ``gated_actions``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._gated_actions``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``gated_actions__safederef`` when you only want to read a default without mutating self.
        """
        if self._gated_actions is None:
            self._gated_actions = self.gated_actions__safederef

        return self._gated_actions

    @property
    def gated_actions__safederef(self) -> 'list[str]':
        """Return ``gated_actions``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``gated_actions`` stays unset and each
        call returns a new default. Use ``gated_actions__autoinit`` to persist the default on
        first access.
        """
        if self._gated_actions is None:
            return []

        return self._gated_actions

    @property
    def open_request_needed(self) -> 'bool':
        return self._open_request_needed

    @open_request_needed.setter
    def open_request_needed(self, open_request_needed: 'bool'):
        self._open_request_needed = open_request_needed

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, EnvironmentMetadata):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
