# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class DeploymentConfiguration(PulumiAutoModelEncoder):
    """
    :var environment_variables: list[EnvironmentVariable] - declared
    :var source: DeploymentSource - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'environment_variables': 'list[EnvironmentVariable]',
        'source': 'DeploymentSource',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'environment_variables': 'environmentVariables',
        'source': 'source',
    }

    FIELDS_environment_variables = 'environmentVariables'
    FIELDS_source = 'source'

    _environment_variables: 'list[EnvironmentVariable]'
    _source: 'DeploymentSource'

    def __init__(
        self,
        environment_variables: 'list[EnvironmentVariable]',
        source: 'DeploymentSource' = None,
    ) -> None:
        super().__init__()

        self.environment_variables = environment_variables
        self.source = source

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, DeploymentConfiguration):
            self._environment_variables = source._environment_variables
            self._source = source._source

    @property
    def environment_variables(self) -> 'list[EnvironmentVariable]':
        return self._environment_variables

    @environment_variables.setter
    def environment_variables(self, environment_variables: 'list[EnvironmentVariable]'):
        if environment_variables is None:
            raise ValueError("Invalid value for `environment_variables`, must not be `None`")

        self._environment_variables = environment_variables

    @property
    def environment_variables__autoinit(self) -> 'list[EnvironmentVariable]':
        """Return ``environment_variables``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._environment_variables``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``environment_variables__safederef`` when you only want to read a default without mutating self.
        """
        if self._environment_variables is None:
            self._environment_variables = self.environment_variables__safederef

        return self._environment_variables

    @property
    def environment_variables__safederef(self) -> 'list[EnvironmentVariable]':
        """Return ``environment_variables``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``environment_variables`` stays unset and each
        call returns a new default. Use ``environment_variables__autoinit`` to persist the default on
        first access.
        """
        if self._environment_variables is None:
            return []

        return self._environment_variables

    @property
    def source(self) -> 'DeploymentSource':
        return self._source

    @source.setter
    def source(self, source: 'DeploymentSource'):
        self._source = source

    @property
    def source__autoinit(self) -> 'DeploymentSource':
        """Return ``source``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._source``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``source__safederef`` when you only want to read a default without mutating self.
        """
        if self._source is None:
            self._source = self.source__safederef

        return self._source

    @property
    def source__safederef(self) -> 'DeploymentSource':
        """Return ``source``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``source`` stays unset and each
        call returns a new default. Use ``source__autoinit`` to persist the default on
        first access.
        """
        if self._source is None:
            from .. import models
            return models.DeploymentSource._new_empty()

        return self._source

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, DeploymentConfiguration):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
