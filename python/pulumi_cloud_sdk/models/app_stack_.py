# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AppStack(PulumiAutoModelEncoder):
    """
    :var id: str - declared
    :var org_name: str - declared
    :var project_name: str - declared
    :var stack_name: str - declared
    :var current_operation: AppOperationStatus - declared
    :var active_update: str - declared
    :var tags: dict[str, str] - declared
    :var config: AppStackConfig - declared
    :var version: int - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'id': 'str',
        'org_name': 'str',
        'project_name': 'str',
        'stack_name': 'str',
        'current_operation': 'AppOperationStatus',
        'active_update': 'str',
        'tags': 'dict[str, str]',
        'config': 'AppStackConfig',
        'version': 'int',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'id': 'id',
        'org_name': 'orgName',
        'project_name': 'projectName',
        'stack_name': 'stackName',
        'current_operation': 'currentOperation',
        'active_update': 'activeUpdate',
        'tags': 'tags',
        'config': 'config',
        'version': 'version',
    }

    FIELDS_id = 'id'
    FIELDS_org_name = 'orgName'
    FIELDS_project_name = 'projectName'
    FIELDS_stack_name = 'stackName'
    FIELDS_current_operation = 'currentOperation'
    FIELDS_active_update = 'activeUpdate'
    FIELDS_tags = 'tags'
    FIELDS_config = 'config'
    FIELDS_version = 'version'

    _id: 'str'
    _org_name: 'str'
    _project_name: 'str'
    _stack_name: 'str'
    _current_operation: 'AppOperationStatus'
    _active_update: 'str'
    _tags: 'dict[str, str]'
    _config: 'AppStackConfig'
    _version: 'int'

    def __init__(
        self,
        id: 'str',
        org_name: 'str',
        project_name: 'str',
        stack_name: 'str',
        active_update: 'str',
        version: 'int',
        current_operation: 'AppOperationStatus' = None,
        tags: 'dict[str, str]' = None,
        config: 'AppStackConfig' = None,
    ) -> None:
        super().__init__()

        self.id = id
        self.org_name = org_name
        self.project_name = project_name
        self.stack_name = stack_name
        self.current_operation = current_operation
        self.active_update = active_update
        self.tags = tags
        self.config = config
        self.version = version

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AppStack):
            self._id = source._id
            self._org_name = source._org_name
            self._project_name = source._project_name
            self._stack_name = source._stack_name
            self._current_operation = source._current_operation
            self._active_update = source._active_update
            self._tags = source._tags
            self._config = source._config
            self._version = source._version

    @property
    def id(self) -> 'str':
        return self._id

    @id.setter
    def id(self, id: 'str'):
        if id is None:
            raise ValueError("Invalid value for `id`, must not be `None`")

        self._id = id

    @property
    def org_name(self) -> 'str':
        return self._org_name

    @org_name.setter
    def org_name(self, org_name: 'str'):
        if org_name is None:
            raise ValueError("Invalid value for `org_name`, must not be `None`")

        self._org_name = org_name

    @property
    def project_name(self) -> 'str':
        return self._project_name

    @project_name.setter
    def project_name(self, project_name: 'str'):
        if project_name is None:
            raise ValueError("Invalid value for `project_name`, must not be `None`")

        self._project_name = project_name

    @property
    def stack_name(self) -> 'str':
        return self._stack_name

    @stack_name.setter
    def stack_name(self, stack_name: 'str'):
        if stack_name is None:
            raise ValueError("Invalid value for `stack_name`, must not be `None`")

        self._stack_name = stack_name

    @property
    def current_operation(self) -> 'AppOperationStatus':
        return self._current_operation

    @current_operation.setter
    def current_operation(self, current_operation: 'AppOperationStatus'):
        self._current_operation = current_operation

    @property
    def current_operation__autoinit(self) -> 'AppOperationStatus':
        """Return ``current_operation``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._current_operation``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``current_operation__safederef`` when you only want to read a default without mutating self.
        """
        if self._current_operation is None:
            self._current_operation = self.current_operation__safederef

        return self._current_operation

    @property
    def current_operation__safederef(self) -> 'AppOperationStatus':
        """Return ``current_operation``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``current_operation`` stays unset and each
        call returns a new default. Use ``current_operation__autoinit`` to persist the default on
        first access.
        """
        if self._current_operation is None:
            from .. import models
            return models.AppOperationStatus._new_empty()

        return self._current_operation

    @property
    def active_update(self) -> 'str':
        return self._active_update

    @active_update.setter
    def active_update(self, active_update: 'str'):
        if active_update is None:
            raise ValueError("Invalid value for `active_update`, must not be `None`")

        self._active_update = active_update

    @property
    def tags(self) -> 'dict[str, str]':
        return self._tags

    @tags.setter
    def tags(self, tags: 'dict[str, str]'):
        self._tags = tags

    @property
    def tags__autoinit(self) -> 'dict[str, str]':
        """Return ``tags``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._tags``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``tags__safederef`` when you only want to read a default without mutating self.
        """
        if self._tags is None:
            self._tags = self.tags__safederef

        return self._tags

    @property
    def tags__safederef(self) -> 'dict[str, str]':
        """Return ``tags``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``tags`` stays unset and each
        call returns a new default. Use ``tags__autoinit`` to persist the default on
        first access.
        """
        if self._tags is None:
            return {}

        return self._tags

    @property
    def config(self) -> 'AppStackConfig':
        return self._config

    @config.setter
    def config(self, config: 'AppStackConfig'):
        self._config = config

    @property
    def config__autoinit(self) -> 'AppStackConfig':
        """Return ``config``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._config``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``config__safederef`` when you only want to read a default without mutating self.
        """
        if self._config is None:
            self._config = self.config__safederef

        return self._config

    @property
    def config__safederef(self) -> 'AppStackConfig':
        """Return ``config``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``config`` stays unset and each
        call returns a new default. Use ``config__autoinit`` to persist the default on
        first access.
        """
        if self._config is None:
            from .. import models
            return models.AppStackConfig._new_empty()

        return self._config

    @property
    def version(self) -> 'int':
        return self._version

    @version.setter
    def version(self, version: 'int'):
        if version is None:
            raise ValueError("Invalid value for `version`, must not be `None`")

        self._version = version

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AppStack):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
