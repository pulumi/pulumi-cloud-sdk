# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class Activity(PulumiAutoModelEncoder):
    """
    :var update: UpdateInfo - declared
    :var deployment: DeploymentWithRelatedUpdates - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'update': 'UpdateInfo',
        'deployment': 'DeploymentWithRelatedUpdates',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'update': 'update',
        'deployment': 'deployment',
    }

    FIELDS_update = 'update'
    FIELDS_deployment = 'deployment'

    _update: 'UpdateInfo'
    _deployment: 'DeploymentWithRelatedUpdates'

    def __init__(
        self,
        update: 'UpdateInfo' = None,
        deployment: 'DeploymentWithRelatedUpdates' = None,
    ) -> None:
        super().__init__()

        self.update = update
        self.deployment = deployment

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, Activity):
            self._update = source._update
            self._deployment = source._deployment

    @property
    def update(self) -> 'UpdateInfo':
        return self._update

    @update.setter
    def update(self, update: 'UpdateInfo'):
        self._update = update

    @property
    def update__autoinit(self) -> 'UpdateInfo':
        """Return ``update``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._update``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``update__safederef`` when you only want to read a default without mutating self.
        """
        if self._update is None:
            self._update = self.update__safederef

        return self._update

    @property
    def update__safederef(self) -> 'UpdateInfo':
        """Return ``update``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``update`` stays unset and each
        call returns a new default. Use ``update__autoinit`` to persist the default on
        first access.
        """
        if self._update is None:
            from .. import models
            return models.UpdateInfo._new_empty()

        return self._update

    @property
    def deployment(self) -> 'DeploymentWithRelatedUpdates':
        return self._deployment

    @deployment.setter
    def deployment(self, deployment: 'DeploymentWithRelatedUpdates'):
        self._deployment = deployment

    @property
    def deployment__autoinit(self) -> 'DeploymentWithRelatedUpdates':
        """Return ``deployment``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._deployment``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``deployment__safederef`` when you only want to read a default without mutating self.
        """
        if self._deployment is None:
            self._deployment = self.deployment__safederef

        return self._deployment

    @property
    def deployment__safederef(self) -> 'DeploymentWithRelatedUpdates':
        """Return ``deployment``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``deployment`` stays unset and each
        call returns a new default. Use ``deployment__autoinit`` to persist the default on
        first access.
        """
        if self._deployment is None:
            from .. import models
            return models.DeploymentWithRelatedUpdates._new_empty()

        return self._deployment

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, Activity):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
