# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class DiscoveredResourceInfo(PulumiAutoModelEncoder):
    """
    :var resource: Resource - declared
    :var name: str - declared
    :var origin_type: str - declared
    :var cloud_provider_id: str - declared
    :var managed_by: str - declared
    :var provider_type: str - declared
    :var virtual_state: dict[str, Any] - declared
    :var migration_status: MigrationStatus - declared
    :var hint: str - declared
    :var annotation: ResourceMigrationAnnotation - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'resource': 'Resource',
        'name': 'str',
        'origin_type': 'str',
        'cloud_provider_id': 'str',
        'managed_by': 'str',
        'provider_type': 'str',
        'virtual_state': 'dict[str, Any]',
        'migration_status': 'MigrationStatus',
        'hint': 'str',
        'annotation': 'ResourceMigrationAnnotation',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'resource': 'resource',
        'name': 'name',
        'origin_type': 'originType',
        'cloud_provider_id': 'cloudProviderId',
        'managed_by': 'managedBy',
        'provider_type': 'providerType',
        'virtual_state': 'virtualState',
        'migration_status': 'migrationStatus',
        'hint': 'hint',
        'annotation': 'annotation',
    }

    FIELDS_resource = 'resource'
    FIELDS_name = 'name'
    FIELDS_origin_type = 'originType'
    FIELDS_cloud_provider_id = 'cloudProviderId'
    FIELDS_managed_by = 'managedBy'
    FIELDS_provider_type = 'providerType'
    FIELDS_virtual_state = 'virtualState'
    FIELDS_migration_status = 'migrationStatus'
    FIELDS_hint = 'hint'
    FIELDS_annotation = 'annotation'

    _resource: 'Resource'
    _name: 'str'
    _origin_type: 'str'
    _cloud_provider_id: 'str'
    _managed_by: 'str'
    _provider_type: 'str'
    _virtual_state: 'dict[str, Any]'
    _migration_status: 'MigrationStatus'
    _hint: 'str'
    _annotation: 'ResourceMigrationAnnotation'

    def __init__(
        self,
        resource: 'Resource',
        name: 'str' = None,
        origin_type: 'str' = None,
        cloud_provider_id: 'str' = None,
        managed_by: 'str' = None,
        provider_type: 'str' = None,
        virtual_state: 'dict[str, Any]' = None,
        migration_status: 'MigrationStatus' = None,
        hint: 'str' = None,
        annotation: 'ResourceMigrationAnnotation' = None,
    ) -> None:
        super().__init__()

        self.resource = resource
        self.name = name
        self.origin_type = origin_type
        self.cloud_provider_id = cloud_provider_id
        self.managed_by = managed_by
        self.provider_type = provider_type
        self.virtual_state = virtual_state
        self.migration_status = migration_status
        self.hint = hint
        self.annotation = annotation

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, DiscoveredResourceInfo):
            self._resource = source._resource
            self._name = source._name
            self._origin_type = source._origin_type
            self._cloud_provider_id = source._cloud_provider_id
            self._managed_by = source._managed_by
            self._provider_type = source._provider_type
            self._virtual_state = source._virtual_state
            self._migration_status = source._migration_status
            self._hint = source._hint
            self._annotation = source._annotation

    @property
    def resource(self) -> 'Resource':
        return self._resource

    @resource.setter
    def resource(self, resource: 'Resource'):
        if resource is None:
            raise ValueError("Invalid value for `resource`, must not be `None`")

        self._resource = resource

    @property
    def resource__autoinit(self) -> 'Resource':
        """Return ``resource``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._resource``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``resource__safederef`` when you only want to read a default without mutating self.
        """
        if self._resource is None:
            self._resource = self.resource__safederef

        return self._resource

    @property
    def resource__safederef(self) -> 'Resource':
        """Return ``resource``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``resource`` stays unset and each
        call returns a new default. Use ``resource__autoinit`` to persist the default on
        first access.
        """
        if self._resource is None:
            from .. import models
            return models.Resource._new_empty()

        return self._resource

    @property
    def name(self) -> 'str':
        return self._name

    @name.setter
    def name(self, name: 'str'):
        self._name = name

    @property
    def origin_type(self) -> 'str':
        return self._origin_type

    @origin_type.setter
    def origin_type(self, origin_type: 'str'):
        self._origin_type = origin_type

    @property
    def cloud_provider_id(self) -> 'str':
        return self._cloud_provider_id

    @cloud_provider_id.setter
    def cloud_provider_id(self, cloud_provider_id: 'str'):
        self._cloud_provider_id = cloud_provider_id

    @property
    def managed_by(self) -> 'str':
        return self._managed_by

    @managed_by.setter
    def managed_by(self, managed_by: 'str'):
        self._managed_by = managed_by

    @property
    def provider_type(self) -> 'str':
        return self._provider_type

    @provider_type.setter
    def provider_type(self, provider_type: 'str'):
        self._provider_type = provider_type

    @property
    def virtual_state(self) -> 'dict[str, Any]':
        return self._virtual_state

    @virtual_state.setter
    def virtual_state(self, virtual_state: 'dict[str, Any]'):
        self._virtual_state = virtual_state

    @property
    def virtual_state__autoinit(self) -> 'dict[str, Any]':
        """Return ``virtual_state``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._virtual_state``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``virtual_state__safederef`` when you only want to read a default without mutating self.
        """
        if self._virtual_state is None:
            self._virtual_state = self.virtual_state__safederef

        return self._virtual_state

    @property
    def virtual_state__safederef(self) -> 'dict[str, Any]':
        """Return ``virtual_state``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``virtual_state`` stays unset and each
        call returns a new default. Use ``virtual_state__autoinit`` to persist the default on
        first access.
        """
        if self._virtual_state is None:
            return {}

        return self._virtual_state

    @property
    def migration_status(self) -> 'MigrationStatus':
        return self._migration_status

    @migration_status.setter
    def migration_status(self, migration_status: 'MigrationStatus'):
        self._migration_status = migration_status

    @property
    def hint(self) -> 'str':
        return self._hint

    @hint.setter
    def hint(self, hint: 'str'):
        self._hint = hint

    @property
    def annotation(self) -> 'ResourceMigrationAnnotation':
        return self._annotation

    @annotation.setter
    def annotation(self, annotation: 'ResourceMigrationAnnotation'):
        self._annotation = annotation

    @property
    def annotation__autoinit(self) -> 'ResourceMigrationAnnotation':
        """Return ``annotation``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._annotation``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``annotation__safederef`` when you only want to read a default without mutating self.
        """
        if self._annotation is None:
            self._annotation = self.annotation__safederef

        return self._annotation

    @property
    def annotation__safederef(self) -> 'ResourceMigrationAnnotation':
        """Return ``annotation``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``annotation`` stays unset and each
        call returns a new default. Use ``annotation__autoinit`` to persist the default on
        first access.
        """
        if self._annotation is None:
            from .. import models
            return models.ResourceMigrationAnnotation._new_empty()

        return self._annotation

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, DiscoveredResourceInfo):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
