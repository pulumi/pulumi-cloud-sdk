# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GitHubCommitInfo(PulumiAutoModelEncoder):
    """
    :var slug: str - declared
    :var sha: str - declared
    :var url: str - declared
    :var message: str - declared
    :var author: UserInfo - declared
    :var committer: UserInfo - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'slug': 'str',
        'sha': 'str',
        'url': 'str',
        'message': 'str',
        'author': 'UserInfo',
        'committer': 'UserInfo',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'slug': 'slug',
        'sha': 'sha',
        'url': 'url',
        'message': 'message',
        'author': 'author',
        'committer': 'committer',
    }

    FIELDS_slug = 'slug'
    FIELDS_sha = 'sha'
    FIELDS_url = 'url'
    FIELDS_message = 'message'
    FIELDS_author = 'author'
    FIELDS_committer = 'committer'

    _slug: 'str'
    _sha: 'str'
    _url: 'str'
    _message: 'str'
    _author: 'UserInfo'
    _committer: 'UserInfo'

    def __init__(
        self,
        slug: 'str' = None,
        sha: 'str' = None,
        url: 'str' = None,
        message: 'str' = None,
        author: 'UserInfo' = None,
        committer: 'UserInfo' = None,
    ) -> None:
        super().__init__()

        self.slug = slug
        self.sha = sha
        self.url = url
        self.message = message
        self.author = author
        self.committer = committer

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GitHubCommitInfo):
            self._slug = source._slug
            self._sha = source._sha
            self._url = source._url
            self._message = source._message
            self._author = source._author
            self._committer = source._committer

    @property
    def slug(self) -> 'str':
        return self._slug

    @slug.setter
    def slug(self, slug: 'str'):
        self._slug = slug

    @property
    def sha(self) -> 'str':
        return self._sha

    @sha.setter
    def sha(self, sha: 'str'):
        self._sha = sha

    @property
    def url(self) -> 'str':
        return self._url

    @url.setter
    def url(self, url: 'str'):
        self._url = url

    @property
    def message(self) -> 'str':
        return self._message

    @message.setter
    def message(self, message: 'str'):
        self._message = message

    @property
    def author(self) -> 'UserInfo':
        return self._author

    @author.setter
    def author(self, author: 'UserInfo'):
        self._author = author

    @property
    def author__autoinit(self) -> 'UserInfo':
        """Return ``author``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._author``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``author__safederef`` when you only want to read a default without mutating self.
        """
        if self._author is None:
            self._author = self.author__safederef

        return self._author

    @property
    def author__safederef(self) -> 'UserInfo':
        """Return ``author``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``author`` stays unset and each
        call returns a new default. Use ``author__autoinit`` to persist the default on
        first access.
        """
        if self._author is None:
            from .. import models
            return models.UserInfo._new_empty()

        return self._author

    @property
    def committer(self) -> 'UserInfo':
        return self._committer

    @committer.setter
    def committer(self, committer: 'UserInfo'):
        self._committer = committer

    @property
    def committer__autoinit(self) -> 'UserInfo':
        """Return ``committer``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._committer``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``committer__safederef`` when you only want to read a default without mutating self.
        """
        if self._committer is None:
            self._committer = self.committer__safederef

        return self._committer

    @property
    def committer__safederef(self) -> 'UserInfo':
        """Return ``committer``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``committer`` stays unset and each
        call returns a new default. Use ``committer__autoinit`` to persist the default on
        first access.
        """
        if self._committer is None:
            from .. import models
            return models.UserInfo._new_empty()

        return self._committer

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GitHubCommitInfo):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
