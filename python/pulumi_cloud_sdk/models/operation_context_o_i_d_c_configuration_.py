# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class OperationContextOIDCConfiguration(PulumiAutoModelEncoder):
    """
    :var aws: OperationContextAWSOIDCConfiguration - declared
    :var azure: OperationContextAzureOIDCConfiguration - declared
    :var gcp: OperationContextGCPOIDCConfiguration - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'aws': 'OperationContextAWSOIDCConfiguration',
        'azure': 'OperationContextAzureOIDCConfiguration',
        'gcp': 'OperationContextGCPOIDCConfiguration',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'aws': 'aws',
        'azure': 'azure',
        'gcp': 'gcp',
    }

    FIELDS_aws = 'aws'
    FIELDS_azure = 'azure'
    FIELDS_gcp = 'gcp'

    _aws: 'OperationContextAWSOIDCConfiguration'
    _azure: 'OperationContextAzureOIDCConfiguration'
    _gcp: 'OperationContextGCPOIDCConfiguration'

    def __init__(
        self,
        aws: 'OperationContextAWSOIDCConfiguration' = None,
        azure: 'OperationContextAzureOIDCConfiguration' = None,
        gcp: 'OperationContextGCPOIDCConfiguration' = None,
    ) -> None:
        super().__init__()

        self.aws = aws
        self.azure = azure
        self.gcp = gcp

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, OperationContextOIDCConfiguration):
            self._aws = source._aws
            self._azure = source._azure
            self._gcp = source._gcp

    @property
    def aws(self) -> 'OperationContextAWSOIDCConfiguration':
        return self._aws

    @aws.setter
    def aws(self, aws: 'OperationContextAWSOIDCConfiguration'):
        self._aws = aws

    @property
    def aws__autoinit(self) -> 'OperationContextAWSOIDCConfiguration':
        """Return ``aws``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._aws``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``aws__safederef`` when you only want to read a default without mutating self.
        """
        if self._aws is None:
            self._aws = self.aws__safederef

        return self._aws

    @property
    def aws__safederef(self) -> 'OperationContextAWSOIDCConfiguration':
        """Return ``aws``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``aws`` stays unset and each
        call returns a new default. Use ``aws__autoinit`` to persist the default on
        first access.
        """
        if self._aws is None:
            from .. import models
            return models.OperationContextAWSOIDCConfiguration._new_empty()

        return self._aws

    @property
    def azure(self) -> 'OperationContextAzureOIDCConfiguration':
        return self._azure

    @azure.setter
    def azure(self, azure: 'OperationContextAzureOIDCConfiguration'):
        self._azure = azure

    @property
    def azure__autoinit(self) -> 'OperationContextAzureOIDCConfiguration':
        """Return ``azure``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._azure``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``azure__safederef`` when you only want to read a default without mutating self.
        """
        if self._azure is None:
            self._azure = self.azure__safederef

        return self._azure

    @property
    def azure__safederef(self) -> 'OperationContextAzureOIDCConfiguration':
        """Return ``azure``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``azure`` stays unset and each
        call returns a new default. Use ``azure__autoinit`` to persist the default on
        first access.
        """
        if self._azure is None:
            from .. import models
            return models.OperationContextAzureOIDCConfiguration._new_empty()

        return self._azure

    @property
    def gcp(self) -> 'OperationContextGCPOIDCConfiguration':
        return self._gcp

    @gcp.setter
    def gcp(self, gcp: 'OperationContextGCPOIDCConfiguration'):
        self._gcp = gcp

    @property
    def gcp__autoinit(self) -> 'OperationContextGCPOIDCConfiguration':
        """Return ``gcp``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._gcp``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``gcp__safederef`` when you only want to read a default without mutating self.
        """
        if self._gcp is None:
            self._gcp = self.gcp__safederef

        return self._gcp

    @property
    def gcp__safederef(self) -> 'OperationContextGCPOIDCConfiguration':
        """Return ``gcp``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``gcp`` stays unset and each
        call returns a new default. Use ``gcp__autoinit`` to persist the default on
        first access.
        """
        if self._gcp is None:
            from .. import models
            return models.OperationContextGCPOIDCConfiguration._new_empty()

        return self._gcp

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, OperationContextOIDCConfiguration):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
