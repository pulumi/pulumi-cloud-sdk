# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class Team(PulumiAutoModelEncoder):
    """
    :var kind: TeamKind - declared
    :var name: str - declared
    :var display_name: str - declared
    :var description: str - declared
    :var members: list[TeamMemberInfo] - declared
    :var stacks: list[TeamStackPermission] - declared
    :var environments: list[TeamEnvironmentSettings] - declared
    :var accounts: list[TeamAccountPermission] - declared
    :var list_members_error: str - declared
    :var user_role: TeamRole - declared
    :var role_ids: list[str] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'kind': 'TeamKind',
        'name': 'str',
        'display_name': 'str',
        'description': 'str',
        'members': 'list[TeamMemberInfo]',
        'stacks': 'list[TeamStackPermission]',
        'environments': 'list[TeamEnvironmentSettings]',
        'accounts': 'list[TeamAccountPermission]',
        'list_members_error': 'str',
        'user_role': 'TeamRole',
        'role_ids': 'list[str]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'kind': 'kind',
        'name': 'name',
        'display_name': 'displayName',
        'description': 'description',
        'members': 'members',
        'stacks': 'stacks',
        'environments': 'environments',
        'accounts': 'accounts',
        'list_members_error': 'listMembersError',
        'user_role': 'userRole',
        'role_ids': 'roleIds',
    }

    FIELDS_kind = 'kind'
    FIELDS_name = 'name'
    FIELDS_display_name = 'displayName'
    FIELDS_description = 'description'
    FIELDS_members = 'members'
    FIELDS_stacks = 'stacks'
    FIELDS_environments = 'environments'
    FIELDS_accounts = 'accounts'
    FIELDS_list_members_error = 'listMembersError'
    FIELDS_user_role = 'userRole'
    FIELDS_role_ids = 'roleIds'

    _kind: 'TeamKind'
    _name: 'str'
    _display_name: 'str'
    _description: 'str'
    _members: 'list[TeamMemberInfo]'
    _stacks: 'list[TeamStackPermission]'
    _environments: 'list[TeamEnvironmentSettings]'
    _accounts: 'list[TeamAccountPermission]'
    _list_members_error: 'str'
    _user_role: 'TeamRole'
    _role_ids: 'list[str]'

    def __init__(
        self,
        kind: 'TeamKind',
        name: 'str',
        display_name: 'str',
        description: 'str',
        members: 'list[TeamMemberInfo]' = None,
        stacks: 'list[TeamStackPermission]' = None,
        environments: 'list[TeamEnvironmentSettings]' = None,
        accounts: 'list[TeamAccountPermission]' = None,
        list_members_error: 'str' = None,
        user_role: 'TeamRole' = None,
        role_ids: 'list[str]' = None,
    ) -> None:
        super().__init__()

        self.kind = kind
        self.name = name
        self.display_name = display_name
        self.description = description
        self.members = members
        self.stacks = stacks
        self.environments = environments
        self.accounts = accounts
        self.list_members_error = list_members_error
        self.user_role = user_role
        self.role_ids = role_ids

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, Team):
            self._kind = source._kind
            self._name = source._name
            self._display_name = source._display_name
            self._description = source._description
            self._members = source._members
            self._stacks = source._stacks
            self._environments = source._environments
            self._accounts = source._accounts
            self._list_members_error = source._list_members_error
            self._user_role = source._user_role
            self._role_ids = source._role_ids

    @property
    def kind(self) -> 'TeamKind':
        return self._kind

    @kind.setter
    def kind(self, kind: 'TeamKind'):
        if kind is None:
            raise ValueError("Invalid value for `kind`, must not be `None`")

        self._kind = kind

    @property
    def name(self) -> 'str':
        return self._name

    @name.setter
    def name(self, name: 'str'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def display_name(self) -> 'str':
        return self._display_name

    @display_name.setter
    def display_name(self, display_name: 'str'):
        if display_name is None:
            raise ValueError("Invalid value for `display_name`, must not be `None`")

        self._display_name = display_name

    @property
    def description(self) -> 'str':
        return self._description

    @description.setter
    def description(self, description: 'str'):
        if description is None:
            raise ValueError("Invalid value for `description`, must not be `None`")

        self._description = description

    @property
    def members(self) -> 'list[TeamMemberInfo]':
        return self._members

    @members.setter
    def members(self, members: 'list[TeamMemberInfo]'):
        self._members = members

    @property
    def members__autoinit(self) -> 'list[TeamMemberInfo]':
        """Return ``members``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._members``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``members__safederef`` when you only want to read a default without mutating self.
        """
        if self._members is None:
            self._members = self.members__safederef

        return self._members

    @property
    def members__safederef(self) -> 'list[TeamMemberInfo]':
        """Return ``members``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``members`` stays unset and each
        call returns a new default. Use ``members__autoinit`` to persist the default on
        first access.
        """
        if self._members is None:
            return []

        return self._members

    @property
    def stacks(self) -> 'list[TeamStackPermission]':
        return self._stacks

    @stacks.setter
    def stacks(self, stacks: 'list[TeamStackPermission]'):
        self._stacks = stacks

    @property
    def stacks__autoinit(self) -> 'list[TeamStackPermission]':
        """Return ``stacks``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._stacks``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``stacks__safederef`` when you only want to read a default without mutating self.
        """
        if self._stacks is None:
            self._stacks = self.stacks__safederef

        return self._stacks

    @property
    def stacks__safederef(self) -> 'list[TeamStackPermission]':
        """Return ``stacks``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``stacks`` stays unset and each
        call returns a new default. Use ``stacks__autoinit`` to persist the default on
        first access.
        """
        if self._stacks is None:
            return []

        return self._stacks

    @property
    def environments(self) -> 'list[TeamEnvironmentSettings]':
        return self._environments

    @environments.setter
    def environments(self, environments: 'list[TeamEnvironmentSettings]'):
        self._environments = environments

    @property
    def environments__autoinit(self) -> 'list[TeamEnvironmentSettings]':
        """Return ``environments``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._environments``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``environments__safederef`` when you only want to read a default without mutating self.
        """
        if self._environments is None:
            self._environments = self.environments__safederef

        return self._environments

    @property
    def environments__safederef(self) -> 'list[TeamEnvironmentSettings]':
        """Return ``environments``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``environments`` stays unset and each
        call returns a new default. Use ``environments__autoinit`` to persist the default on
        first access.
        """
        if self._environments is None:
            return []

        return self._environments

    @property
    def accounts(self) -> 'list[TeamAccountPermission]':
        return self._accounts

    @accounts.setter
    def accounts(self, accounts: 'list[TeamAccountPermission]'):
        self._accounts = accounts

    @property
    def accounts__autoinit(self) -> 'list[TeamAccountPermission]':
        """Return ``accounts``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._accounts``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``accounts__safederef`` when you only want to read a default without mutating self.
        """
        if self._accounts is None:
            self._accounts = self.accounts__safederef

        return self._accounts

    @property
    def accounts__safederef(self) -> 'list[TeamAccountPermission]':
        """Return ``accounts``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``accounts`` stays unset and each
        call returns a new default. Use ``accounts__autoinit`` to persist the default on
        first access.
        """
        if self._accounts is None:
            return []

        return self._accounts

    @property
    def list_members_error(self) -> 'str':
        return self._list_members_error

    @list_members_error.setter
    def list_members_error(self, list_members_error: 'str'):
        self._list_members_error = list_members_error

    @property
    def user_role(self) -> 'TeamRole':
        return self._user_role

    @user_role.setter
    def user_role(self, user_role: 'TeamRole'):
        self._user_role = user_role

    @property
    def role_ids(self) -> 'list[str]':
        return self._role_ids

    @role_ids.setter
    def role_ids(self, role_ids: 'list[str]'):
        self._role_ids = role_ids

    @property
    def role_ids__autoinit(self) -> 'list[str]':
        """Return ``role_ids``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._role_ids``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``role_ids__safederef`` when you only want to read a default without mutating self.
        """
        if self._role_ids is None:
            self._role_ids = self.role_ids__safederef

        return self._role_ids

    @property
    def role_ids__safederef(self) -> 'list[str]':
        """Return ``role_ids``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``role_ids`` stays unset and each
        call returns a new default. Use ``role_ids__autoinit`` to persist the default on
        first access.
        """
        if self._role_ids is None:
            return []

        return self._role_ids

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, Team):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
