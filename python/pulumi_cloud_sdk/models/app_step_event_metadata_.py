# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AppStepEventMetadata(PulumiAutoModelEncoder):
    """
    :var op: AppOpType - declared
    :var urn: str - declared
    :var type: str - declared
    :var old: AppStepEventStateMetadata - declared
    :var new: AppStepEventStateMetadata - declared
    :var keys: list[str] - declared
    :var diffs: list[str] - declared
    :var detailed_diff: dict[str, AppPropertyDiff] - declared
    :var logical: bool - declared
    :var provider: str - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'op': 'AppOpType',
        'urn': 'str',
        'type': 'str',
        'old': 'AppStepEventStateMetadata',
        'new': 'AppStepEventStateMetadata',
        'keys': 'list[str]',
        'diffs': 'list[str]',
        'detailed_diff': 'dict[str, AppPropertyDiff]',
        'logical': 'bool',
        'provider': 'str',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'op': 'op',
        'urn': 'urn',
        'type': 'type',
        'old': 'old',
        'new': 'new',
        'keys': 'keys',
        'diffs': 'diffs',
        'detailed_diff': 'detailedDiff',
        'logical': 'logical',
        'provider': 'provider',
    }

    FIELDS_op = 'op'
    FIELDS_urn = 'urn'
    FIELDS_type = 'type'
    FIELDS_old = 'old'
    FIELDS_new = 'new'
    FIELDS_keys = 'keys'
    FIELDS_diffs = 'diffs'
    FIELDS_detailed_diff = 'detailedDiff'
    FIELDS_logical = 'logical'
    FIELDS_provider = 'provider'

    _op: 'AppOpType'
    _urn: 'str'
    _type: 'str'
    _old: 'AppStepEventStateMetadata'
    _new: 'AppStepEventStateMetadata'
    _keys: 'list[str]'
    _diffs: 'list[str]'
    _detailed_diff: 'dict[str, AppPropertyDiff]'
    _logical: 'bool'
    _provider: 'str'

    def __init__(
        self,
        op: 'AppOpType',
        urn: 'str',
        type: 'str',
        old: 'AppStepEventStateMetadata',
        new: 'AppStepEventStateMetadata',
        detailed_diff: 'dict[str, AppPropertyDiff]',
        provider: 'str',
        keys: 'list[str]' = None,
        diffs: 'list[str]' = None,
        logical: 'bool' = False,
    ) -> None:
        super().__init__()

        self.op = op
        self.urn = urn
        self.type = type
        self.old = old
        self.new = new
        self.keys = keys
        self.diffs = diffs
        self.detailed_diff = detailed_diff
        self.logical = logical
        self.provider = provider

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AppStepEventMetadata):
            self._op = source._op
            self._urn = source._urn
            self._type = source._type
            self._old = source._old
            self._new = source._new
            self._keys = source._keys
            self._diffs = source._diffs
            self._detailed_diff = source._detailed_diff
            self._logical = source._logical
            self._provider = source._provider

    @property
    def op(self) -> 'AppOpType':
        return self._op

    @op.setter
    def op(self, op: 'AppOpType'):
        if op is None:
            raise ValueError("Invalid value for `op`, must not be `None`")

        self._op = op

    @property
    def urn(self) -> 'str':
        return self._urn

    @urn.setter
    def urn(self, urn: 'str'):
        if urn is None:
            raise ValueError("Invalid value for `urn`, must not be `None`")

        self._urn = urn

    @property
    def type(self) -> 'str':
        return self._type

    @type.setter
    def type(self, type: 'str'):
        if type is None:
            raise ValueError("Invalid value for `type`, must not be `None`")

        self._type = type

    @property
    def old(self) -> 'AppStepEventStateMetadata':
        return self._old

    @old.setter
    def old(self, old: 'AppStepEventStateMetadata'):
        if old is None:
            raise ValueError("Invalid value for `old`, must not be `None`")

        self._old = old

    @property
    def old__autoinit(self) -> 'AppStepEventStateMetadata':
        """Return ``old``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._old``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``old__safederef`` when you only want to read a default without mutating self.
        """
        if self._old is None:
            self._old = self.old__safederef

        return self._old

    @property
    def old__safederef(self) -> 'AppStepEventStateMetadata':
        """Return ``old``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``old`` stays unset and each
        call returns a new default. Use ``old__autoinit`` to persist the default on
        first access.
        """
        if self._old is None:
            from .. import models
            return models.AppStepEventStateMetadata._new_empty()

        return self._old

    @property
    def new(self) -> 'AppStepEventStateMetadata':
        return self._new

    @new.setter
    def new(self, new: 'AppStepEventStateMetadata'):
        if new is None:
            raise ValueError("Invalid value for `new`, must not be `None`")

        self._new = new

    @property
    def new__autoinit(self) -> 'AppStepEventStateMetadata':
        """Return ``new``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._new``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``new__safederef`` when you only want to read a default without mutating self.
        """
        if self._new is None:
            self._new = self.new__safederef

        return self._new

    @property
    def new__safederef(self) -> 'AppStepEventStateMetadata':
        """Return ``new``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``new`` stays unset and each
        call returns a new default. Use ``new__autoinit`` to persist the default on
        first access.
        """
        if self._new is None:
            from .. import models
            return models.AppStepEventStateMetadata._new_empty()

        return self._new

    @property
    def keys(self) -> 'list[str]':
        return self._keys

    @keys.setter
    def keys(self, keys: 'list[str]'):
        self._keys = keys

    @property
    def keys__autoinit(self) -> 'list[str]':
        """Return ``keys``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._keys``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``keys__safederef`` when you only want to read a default without mutating self.
        """
        if self._keys is None:
            self._keys = self.keys__safederef

        return self._keys

    @property
    def keys__safederef(self) -> 'list[str]':
        """Return ``keys``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``keys`` stays unset and each
        call returns a new default. Use ``keys__autoinit`` to persist the default on
        first access.
        """
        if self._keys is None:
            return []

        return self._keys

    @property
    def diffs(self) -> 'list[str]':
        return self._diffs

    @diffs.setter
    def diffs(self, diffs: 'list[str]'):
        self._diffs = diffs

    @property
    def diffs__autoinit(self) -> 'list[str]':
        """Return ``diffs``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._diffs``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``diffs__safederef`` when you only want to read a default without mutating self.
        """
        if self._diffs is None:
            self._diffs = self.diffs__safederef

        return self._diffs

    @property
    def diffs__safederef(self) -> 'list[str]':
        """Return ``diffs``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``diffs`` stays unset and each
        call returns a new default. Use ``diffs__autoinit`` to persist the default on
        first access.
        """
        if self._diffs is None:
            return []

        return self._diffs

    @property
    def detailed_diff(self) -> 'dict[str, AppPropertyDiff]':
        return self._detailed_diff

    @detailed_diff.setter
    def detailed_diff(self, detailed_diff: 'dict[str, AppPropertyDiff]'):
        if detailed_diff is None:
            raise ValueError("Invalid value for `detailed_diff`, must not be `None`")

        self._detailed_diff = detailed_diff

    @property
    def detailed_diff__autoinit(self) -> 'dict[str, AppPropertyDiff]':
        """Return ``detailed_diff``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._detailed_diff``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``detailed_diff__safederef`` when you only want to read a default without mutating self.
        """
        if self._detailed_diff is None:
            self._detailed_diff = self.detailed_diff__safederef

        return self._detailed_diff

    @property
    def detailed_diff__safederef(self) -> 'dict[str, AppPropertyDiff]':
        """Return ``detailed_diff``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``detailed_diff`` stays unset and each
        call returns a new default. Use ``detailed_diff__autoinit`` to persist the default on
        first access.
        """
        if self._detailed_diff is None:
            return {}

        return self._detailed_diff

    @property
    def logical(self) -> 'bool':
        return self._logical

    @logical.setter
    def logical(self, logical: 'bool'):
        self._logical = logical

    @property
    def provider(self) -> 'str':
        return self._provider

    @provider.setter
    def provider(self, provider: 'str'):
        if provider is None:
            raise ValueError("Invalid value for `provider`, must not be `None`")

        self._provider = provider

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AppStepEventMetadata):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
