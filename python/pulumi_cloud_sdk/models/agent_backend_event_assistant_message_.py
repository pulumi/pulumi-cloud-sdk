# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .agent_backend_event_ import AgentBackendEvent

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AgentBackendEventAssistantMessage(AgentBackendEvent):
    """
    :var content: str - declared
    :var tool_calls: list[AgentBackendEventToolCall] - declared
    :var is_final: bool - declared
    :var token_usage: dict[str, AgentBackendEventTokenUsage] - declared
    :var thinking_signatures: list[str] - declared
    :var timestamp: datetime - inherited
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'timestamp': 'datetime',
        'content': 'str',
        'tool_calls': 'list[AgentBackendEventToolCall]',
        'is_final': 'bool',
        'token_usage': 'dict[str, AgentBackendEventTokenUsage]',
        'thinking_signatures': 'list[str]',
        '__DISCRIMINATOR_VALUE__': 'str'
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'timestamp': 'timestamp',
        'content': 'content',
        'tool_calls': 'tool_calls',
        'is_final': 'is_final',
        'token_usage': 'token_usage',
        'thinking_signatures': 'thinking_signatures',
        '__DISCRIMINATOR_VALUE__': 'type'
    }

    __DISCRIMINATOR_VALUE__ = "assistant_message"

    FIELDS_content = 'content'
    FIELDS_tool_calls = 'tool_calls'
    FIELDS_is_final = 'is_final'
    FIELDS_token_usage = 'token_usage'
    FIELDS_thinking_signatures = 'thinking_signatures'

    _content: 'str'
    _tool_calls: 'list[AgentBackendEventToolCall]'
    _is_final: 'bool'
    _token_usage: 'dict[str, AgentBackendEventTokenUsage]'
    _thinking_signatures: 'list[str]'

    def __init__(
        self,
        timestamp: 'datetime',
        content: 'str',
        is_final: 'bool',
        token_usage: 'dict[str, AgentBackendEventTokenUsage]',
        tool_calls: 'list[AgentBackendEventToolCall]' = None,
        thinking_signatures: 'list[str]' = None,
    ) -> None:
        super().__init__(timestamp=timestamp)

        self.content = content
        self.tool_calls = tool_calls
        self.is_final = is_final
        self.token_usage = token_usage
        self.thinking_signatures = thinking_signatures

    def copy_common_fields(self, source: Any, /) -> None:
        super().copy_common_fields(source)
        if isinstance(source, AgentBackendEventAssistantMessage):
            self._content = source._content
            self._tool_calls = source._tool_calls
            self._is_final = source._is_final
            self._token_usage = source._token_usage
            self._thinking_signatures = source._thinking_signatures

    @property
    def content(self) -> 'str':
        return self._content

    @content.setter
    def content(self, content: 'str'):
        if content is None:
            raise ValueError("Invalid value for `content`, must not be `None`")

        self._content = content

    @property
    def tool_calls(self) -> 'list[AgentBackendEventToolCall]':
        return self._tool_calls

    @tool_calls.setter
    def tool_calls(self, tool_calls: 'list[AgentBackendEventToolCall]'):
        self._tool_calls = tool_calls

    @property
    def tool_calls__autoinit(self) -> 'list[AgentBackendEventToolCall]':
        """Return ``tool_calls``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._tool_calls``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``tool_calls__safederef`` when you only want to read a default without mutating self.
        """
        if self._tool_calls is None:
            self._tool_calls = self.tool_calls__safederef

        return self._tool_calls

    @property
    def tool_calls__safederef(self) -> 'list[AgentBackendEventToolCall]':
        """Return ``tool_calls``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``tool_calls`` stays unset and each
        call returns a new default. Use ``tool_calls__autoinit`` to persist the default on
        first access.
        """
        if self._tool_calls is None:
            return []

        return self._tool_calls

    @property
    def is_final(self) -> 'bool':
        return self._is_final

    @is_final.setter
    def is_final(self, is_final: 'bool'):
        if is_final is None:
            raise ValueError("Invalid value for `is_final`, must not be `None`")

        self._is_final = is_final

    @property
    def token_usage(self) -> 'dict[str, AgentBackendEventTokenUsage]':
        return self._token_usage

    @token_usage.setter
    def token_usage(self, token_usage: 'dict[str, AgentBackendEventTokenUsage]'):
        if token_usage is None:
            raise ValueError("Invalid value for `token_usage`, must not be `None`")

        self._token_usage = token_usage

    @property
    def token_usage__autoinit(self) -> 'dict[str, AgentBackendEventTokenUsage]':
        """Return ``token_usage``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._token_usage``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``token_usage__safederef`` when you only want to read a default without mutating self.
        """
        if self._token_usage is None:
            self._token_usage = self.token_usage__safederef

        return self._token_usage

    @property
    def token_usage__safederef(self) -> 'dict[str, AgentBackendEventTokenUsage]':
        """Return ``token_usage``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``token_usage`` stays unset and each
        call returns a new default. Use ``token_usage__autoinit`` to persist the default on
        first access.
        """
        if self._token_usage is None:
            return {}

        return self._token_usage

    @property
    def thinking_signatures(self) -> 'list[str]':
        return self._thinking_signatures

    @thinking_signatures.setter
    def thinking_signatures(self, thinking_signatures: 'list[str]'):
        self._thinking_signatures = thinking_signatures

    @property
    def thinking_signatures__autoinit(self) -> 'list[str]':
        """Return ``thinking_signatures``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._thinking_signatures``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``thinking_signatures__safederef`` when you only want to read a default without mutating self.
        """
        if self._thinking_signatures is None:
            self._thinking_signatures = self.thinking_signatures__safederef

        return self._thinking_signatures

    @property
    def thinking_signatures__safederef(self) -> 'list[str]':
        """Return ``thinking_signatures``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``thinking_signatures`` stays unset and each
        call returns a new default. Use ``thinking_signatures__autoinit`` to persist the default on
        first access.
        """
        if self._thinking_signatures is None:
            return []

        return self._thinking_signatures

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AgentBackendEventAssistantMessage):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
