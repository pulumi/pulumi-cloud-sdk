# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GraphSchemaResponse(PulumiAutoModelEncoder):
    """
    :var schema_version: str - declared
    :var node_types: list[GraphSchemaNodeType] - declared
    :var edge_types: list[GraphSchemaEdgeType] - declared
    :var metric_ops: list[GraphAggregateOp] - declared
    :var limits: GraphSchemaLimits - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'schema_version': 'str',
        'node_types': 'list[GraphSchemaNodeType]',
        'edge_types': 'list[GraphSchemaEdgeType]',
        'metric_ops': 'list[GraphAggregateOp]',
        'limits': 'GraphSchemaLimits',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'schema_version': 'schemaVersion',
        'node_types': 'nodeTypes',
        'edge_types': 'edgeTypes',
        'metric_ops': 'metricOps',
        'limits': 'limits',
    }

    FIELDS_schema_version = 'schemaVersion'
    FIELDS_node_types = 'nodeTypes'
    FIELDS_edge_types = 'edgeTypes'
    FIELDS_metric_ops = 'metricOps'
    FIELDS_limits = 'limits'

    _schema_version: 'str'
    _node_types: 'list[GraphSchemaNodeType]'
    _edge_types: 'list[GraphSchemaEdgeType]'
    _metric_ops: 'list[GraphAggregateOp]'
    _limits: 'GraphSchemaLimits'

    def __init__(
        self,
        schema_version: 'str',
        node_types: 'list[GraphSchemaNodeType]',
        edge_types: 'list[GraphSchemaEdgeType]',
        metric_ops: 'list[GraphAggregateOp]',
        limits: 'GraphSchemaLimits',
    ) -> None:
        super().__init__()

        self.schema_version = schema_version
        self.node_types = node_types
        self.edge_types = edge_types
        self.metric_ops = metric_ops
        self.limits = limits

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GraphSchemaResponse):
            self._schema_version = source._schema_version
            self._node_types = source._node_types
            self._edge_types = source._edge_types
            self._metric_ops = source._metric_ops
            self._limits = source._limits

    @property
    def schema_version(self) -> 'str':
        return self._schema_version

    @schema_version.setter
    def schema_version(self, schema_version: 'str'):
        if schema_version is None:
            raise ValueError("Invalid value for `schema_version`, must not be `None`")

        self._schema_version = schema_version

    @property
    def node_types(self) -> 'list[GraphSchemaNodeType]':
        return self._node_types

    @node_types.setter
    def node_types(self, node_types: 'list[GraphSchemaNodeType]'):
        if node_types is None:
            raise ValueError("Invalid value for `node_types`, must not be `None`")

        self._node_types = node_types

    @property
    def node_types__autoinit(self) -> 'list[GraphSchemaNodeType]':
        """Return ``node_types``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._node_types``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``node_types__safederef`` when you only want to read a default without mutating self.
        """
        if self._node_types is None:
            self._node_types = self.node_types__safederef

        return self._node_types

    @property
    def node_types__safederef(self) -> 'list[GraphSchemaNodeType]':
        """Return ``node_types``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``node_types`` stays unset and each
        call returns a new default. Use ``node_types__autoinit`` to persist the default on
        first access.
        """
        if self._node_types is None:
            return []

        return self._node_types

    @property
    def edge_types(self) -> 'list[GraphSchemaEdgeType]':
        return self._edge_types

    @edge_types.setter
    def edge_types(self, edge_types: 'list[GraphSchemaEdgeType]'):
        if edge_types is None:
            raise ValueError("Invalid value for `edge_types`, must not be `None`")

        self._edge_types = edge_types

    @property
    def edge_types__autoinit(self) -> 'list[GraphSchemaEdgeType]':
        """Return ``edge_types``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._edge_types``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``edge_types__safederef`` when you only want to read a default without mutating self.
        """
        if self._edge_types is None:
            self._edge_types = self.edge_types__safederef

        return self._edge_types

    @property
    def edge_types__safederef(self) -> 'list[GraphSchemaEdgeType]':
        """Return ``edge_types``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``edge_types`` stays unset and each
        call returns a new default. Use ``edge_types__autoinit`` to persist the default on
        first access.
        """
        if self._edge_types is None:
            return []

        return self._edge_types

    @property
    def metric_ops(self) -> 'list[GraphAggregateOp]':
        return self._metric_ops

    @metric_ops.setter
    def metric_ops(self, metric_ops: 'list[GraphAggregateOp]'):
        if metric_ops is None:
            raise ValueError("Invalid value for `metric_ops`, must not be `None`")

        self._metric_ops = metric_ops

    @property
    def metric_ops__autoinit(self) -> 'list[GraphAggregateOp]':
        """Return ``metric_ops``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._metric_ops``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``metric_ops__safederef`` when you only want to read a default without mutating self.
        """
        if self._metric_ops is None:
            self._metric_ops = self.metric_ops__safederef

        return self._metric_ops

    @property
    def metric_ops__safederef(self) -> 'list[GraphAggregateOp]':
        """Return ``metric_ops``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``metric_ops`` stays unset and each
        call returns a new default. Use ``metric_ops__autoinit`` to persist the default on
        first access.
        """
        if self._metric_ops is None:
            return []

        return self._metric_ops

    @property
    def limits(self) -> 'GraphSchemaLimits':
        return self._limits

    @limits.setter
    def limits(self, limits: 'GraphSchemaLimits'):
        if limits is None:
            raise ValueError("Invalid value for `limits`, must not be `None`")

        self._limits = limits

    @property
    def limits__autoinit(self) -> 'GraphSchemaLimits':
        """Return ``limits``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._limits``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``limits__safederef`` when you only want to read a default without mutating self.
        """
        if self._limits is None:
            self._limits = self.limits__safederef

        return self._limits

    @property
    def limits__safederef(self) -> 'GraphSchemaLimits':
        """Return ``limits``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``limits`` stays unset and each
        call returns a new default. Use ``limits__autoinit`` to persist the default on
        first access.
        """
        if self._limits is None:
            from .. import models
            return models.GraphSchemaLimits._new_empty()

        return self._limits

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GraphSchemaResponse):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
