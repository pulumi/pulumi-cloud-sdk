# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class OrganizationMember(PulumiAutoModelEncoder):
    """
    :var role: OrganizationRole - declared
    :var user: UserInfo - declared
    :var created: datetime - declared
    :var known_to_pulumi: bool - declared
    :var virtual_admin: bool - declared
    :var links: MemberLinks - declared
    :var fga_role: FGARole - declared
    :var teams: list[str] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'role': 'OrganizationRole',
        'user': 'UserInfo',
        'created': 'datetime',
        'known_to_pulumi': 'bool',
        'virtual_admin': 'bool',
        'links': 'MemberLinks',
        'fga_role': 'FGARole',
        'teams': 'list[str]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'role': 'role',
        'user': 'user',
        'created': 'created',
        'known_to_pulumi': 'knownToPulumi',
        'virtual_admin': 'virtualAdmin',
        'links': 'links',
        'fga_role': 'fgaRole',
        'teams': 'teams',
    }

    FIELDS_role = 'role'
    FIELDS_user = 'user'
    FIELDS_created = 'created'
    FIELDS_known_to_pulumi = 'knownToPulumi'
    FIELDS_virtual_admin = 'virtualAdmin'
    FIELDS_links = 'links'
    FIELDS_fga_role = 'fgaRole'
    FIELDS_teams = 'teams'

    _role: 'OrganizationRole'
    _user: 'UserInfo'
    _created: 'datetime'
    _known_to_pulumi: 'bool'
    _virtual_admin: 'bool'
    _links: 'MemberLinks'
    _fga_role: 'FGARole'
    _teams: 'list[str]'

    def __init__(
        self,
        role: 'OrganizationRole',
        user: 'UserInfo',
        created: 'datetime',
        known_to_pulumi: 'bool',
        virtual_admin: 'bool',
        fga_role: 'FGARole',
        links: 'MemberLinks' = None,
        teams: 'list[str]' = None,
    ) -> None:
        super().__init__()

        self.role = role
        self.user = user
        self.created = created
        self.known_to_pulumi = known_to_pulumi
        self.virtual_admin = virtual_admin
        self.links = links
        self.fga_role = fga_role
        self.teams = teams

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, OrganizationMember):
            self._role = source._role
            self._user = source._user
            self._created = source._created
            self._known_to_pulumi = source._known_to_pulumi
            self._virtual_admin = source._virtual_admin
            self._links = source._links
            self._fga_role = source._fga_role
            self._teams = source._teams

    @property
    def role(self) -> 'OrganizationRole':
        return self._role

    @role.setter
    def role(self, role: 'OrganizationRole'):
        if role is None:
            raise ValueError("Invalid value for `role`, must not be `None`")

        self._role = role

    @property
    def user(self) -> 'UserInfo':
        return self._user

    @user.setter
    def user(self, user: 'UserInfo'):
        if user is None:
            raise ValueError("Invalid value for `user`, must not be `None`")

        self._user = user

    @property
    def user__autoinit(self) -> 'UserInfo':
        """Return ``user``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._user``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``user__safederef`` when you only want to read a default without mutating self.
        """
        if self._user is None:
            self._user = self.user__safederef

        return self._user

    @property
    def user__safederef(self) -> 'UserInfo':
        """Return ``user``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``user`` stays unset and each
        call returns a new default. Use ``user__autoinit`` to persist the default on
        first access.
        """
        if self._user is None:
            from .. import models
            return models.UserInfo._new_empty()

        return self._user

    @property
    def created(self) -> 'datetime':
        return self._created

    @created.setter
    def created(self, created: 'datetime'):
        if created is None:
            raise ValueError("Invalid value for `created`, must not be `None`")

        self._created = created

    @property
    def known_to_pulumi(self) -> 'bool':
        return self._known_to_pulumi

    @known_to_pulumi.setter
    def known_to_pulumi(self, known_to_pulumi: 'bool'):
        if known_to_pulumi is None:
            raise ValueError("Invalid value for `known_to_pulumi`, must not be `None`")

        self._known_to_pulumi = known_to_pulumi

    @property
    def virtual_admin(self) -> 'bool':
        return self._virtual_admin

    @virtual_admin.setter
    def virtual_admin(self, virtual_admin: 'bool'):
        if virtual_admin is None:
            raise ValueError("Invalid value for `virtual_admin`, must not be `None`")

        self._virtual_admin = virtual_admin

    @property
    def links(self) -> 'MemberLinks':
        return self._links

    @links.setter
    def links(self, links: 'MemberLinks'):
        self._links = links

    @property
    def links__autoinit(self) -> 'MemberLinks':
        """Return ``links``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._links``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``links__safederef`` when you only want to read a default without mutating self.
        """
        if self._links is None:
            self._links = self.links__safederef

        return self._links

    @property
    def links__safederef(self) -> 'MemberLinks':
        """Return ``links``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``links`` stays unset and each
        call returns a new default. Use ``links__autoinit`` to persist the default on
        first access.
        """
        if self._links is None:
            from .. import models
            return models.MemberLinks._new_empty()

        return self._links

    @property
    def fga_role(self) -> 'FGARole':
        return self._fga_role

    @fga_role.setter
    def fga_role(self, fga_role: 'FGARole'):
        if fga_role is None:
            raise ValueError("Invalid value for `fga_role`, must not be `None`")

        self._fga_role = fga_role

    @property
    def fga_role__autoinit(self) -> 'FGARole':
        """Return ``fga_role``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._fga_role``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``fga_role__safederef`` when you only want to read a default without mutating self.
        """
        if self._fga_role is None:
            self._fga_role = self.fga_role__safederef

        return self._fga_role

    @property
    def fga_role__safederef(self) -> 'FGARole':
        """Return ``fga_role``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``fga_role`` stays unset and each
        call returns a new default. Use ``fga_role__autoinit`` to persist the default on
        first access.
        """
        if self._fga_role is None:
            from .. import models
            return models.FGARole._new_empty()

        return self._fga_role

    @property
    def teams(self) -> 'list[str]':
        return self._teams

    @teams.setter
    def teams(self, teams: 'list[str]'):
        self._teams = teams

    @property
    def teams__autoinit(self) -> 'list[str]':
        """Return ``teams``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._teams``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``teams__safederef`` when you only want to read a default without mutating self.
        """
        if self._teams is None:
            self._teams = self.teams__safederef

        return self._teams

    @property
    def teams__safederef(self) -> 'list[str]':
        """Return ``teams``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``teams`` stays unset and each
        call returns a new default. Use ``teams__autoinit`` to persist the default on
        first access.
        """
        if self._teams is None:
            return []

        return self._teams

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, OrganizationMember):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
