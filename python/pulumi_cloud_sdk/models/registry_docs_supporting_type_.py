# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class RegistryDocsSupportingType(PulumiAutoModelEncoder):
    """
    :var type_token: str - declared
    :var name: dict[str, str] - declared
    :var kind: str - declared
    :var properties: list[RegistryDocsProperty] - declared
    :var values: list[RegistryDocsEnumValue] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'type_token': 'str',
        'name': 'dict[str, str]',
        'kind': 'str',
        'properties': 'list[RegistryDocsProperty]',
        'values': 'list[RegistryDocsEnumValue]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'type_token': 'typeToken',
        'name': 'name',
        'kind': 'kind',
        'properties': 'properties',
        'values': 'values',
    }

    FIELDS_type_token = 'typeToken'
    FIELDS_name = 'name'
    FIELDS_kind = 'kind'
    FIELDS_properties = 'properties'
    FIELDS_values = 'values'

    _type_token: 'str'
    _name: 'dict[str, str]'
    _kind: 'str'
    _properties: 'list[RegistryDocsProperty]'
    _values: 'list[RegistryDocsEnumValue]'

    def __init__(
        self,
        type_token: 'str',
        name: 'dict[str, str]',
        kind: 'str',
        properties: 'list[RegistryDocsProperty]' = None,
        values: 'list[RegistryDocsEnumValue]' = None,
    ) -> None:
        super().__init__()

        self.type_token = type_token
        self.name = name
        self.kind = kind
        self.properties = properties
        self.values = values

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, RegistryDocsSupportingType):
            self._type_token = source._type_token
            self._name = source._name
            self._kind = source._kind
            self._properties = source._properties
            self._values = source._values

    @property
    def type_token(self) -> 'str':
        return self._type_token

    @type_token.setter
    def type_token(self, type_token: 'str'):
        if type_token is None:
            raise ValueError("Invalid value for `type_token`, must not be `None`")

        self._type_token = type_token

    @property
    def name(self) -> 'dict[str, str]':
        return self._name

    @name.setter
    def name(self, name: 'dict[str, str]'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def name__autoinit(self) -> 'dict[str, str]':
        """Return ``name``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._name``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``name__safederef`` when you only want to read a default without mutating self.
        """
        if self._name is None:
            self._name = self.name__safederef

        return self._name

    @property
    def name__safederef(self) -> 'dict[str, str]':
        """Return ``name``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``name`` stays unset and each
        call returns a new default. Use ``name__autoinit`` to persist the default on
        first access.
        """
        if self._name is None:
            return {}

        return self._name

    @property
    def kind(self) -> 'str':
        return self._kind

    @kind.setter
    def kind(self, kind: 'str'):
        if kind is None:
            raise ValueError("Invalid value for `kind`, must not be `None`")

        self._kind = kind

    @property
    def properties(self) -> 'list[RegistryDocsProperty]':
        return self._properties

    @properties.setter
    def properties(self, properties: 'list[RegistryDocsProperty]'):
        self._properties = properties

    @property
    def properties__autoinit(self) -> 'list[RegistryDocsProperty]':
        """Return ``properties``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._properties``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``properties__safederef`` when you only want to read a default without mutating self.
        """
        if self._properties is None:
            self._properties = self.properties__safederef

        return self._properties

    @property
    def properties__safederef(self) -> 'list[RegistryDocsProperty]':
        """Return ``properties``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``properties`` stays unset and each
        call returns a new default. Use ``properties__autoinit`` to persist the default on
        first access.
        """
        if self._properties is None:
            return []

        return self._properties

    @property
    def values(self) -> 'list[RegistryDocsEnumValue]':
        return self._values

    @values.setter
    def values(self, values: 'list[RegistryDocsEnumValue]'):
        self._values = values

    @property
    def values__autoinit(self) -> 'list[RegistryDocsEnumValue]':
        """Return ``values``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._values``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``values__safederef`` when you only want to read a default without mutating self.
        """
        if self._values is None:
            self._values = self.values__safederef

        return self._values

    @property
    def values__safederef(self) -> 'list[RegistryDocsEnumValue]':
        """Return ``values``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``values`` stays unset and each
        call returns a new default. Use ``values__autoinit`` to persist the default on
        first access.
        """
        if self._values is None:
            return []

        return self._values

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, RegistryDocsSupportingType):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
