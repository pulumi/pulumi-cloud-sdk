# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GraphAggregationBucket(PulumiAutoModelEncoder):
    """
    :var key: dict[str, str] - declared
    :var metrics: dict[str, float] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'key': 'dict[str, str]',
        'metrics': 'dict[str, float]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'key': 'key',
        'metrics': 'metrics',
    }

    FIELDS_key = 'key'
    FIELDS_metrics = 'metrics'

    _key: 'dict[str, str]'
    _metrics: 'dict[str, float]'

    def __init__(
        self,
        key: 'dict[str, str]',
        metrics: 'dict[str, float]',
    ) -> None:
        super().__init__()

        self.key = key
        self.metrics = metrics

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GraphAggregationBucket):
            self._key = source._key
            self._metrics = source._metrics

    @property
    def key(self) -> 'dict[str, str]':
        return self._key

    @key.setter
    def key(self, key: 'dict[str, str]'):
        if key is None:
            raise ValueError("Invalid value for `key`, must not be `None`")

        self._key = key

    @property
    def key__autoinit(self) -> 'dict[str, str]':
        """Return ``key``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._key``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``key__safederef`` when you only want to read a default without mutating self.
        """
        if self._key is None:
            self._key = self.key__safederef

        return self._key

    @property
    def key__safederef(self) -> 'dict[str, str]':
        """Return ``key``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``key`` stays unset and each
        call returns a new default. Use ``key__autoinit`` to persist the default on
        first access.
        """
        if self._key is None:
            return {}

        return self._key

    @property
    def metrics(self) -> 'dict[str, float]':
        return self._metrics

    @metrics.setter
    def metrics(self, metrics: 'dict[str, float]'):
        if metrics is None:
            raise ValueError("Invalid value for `metrics`, must not be `None`")

        self._metrics = metrics

    @property
    def metrics__autoinit(self) -> 'dict[str, float]':
        """Return ``metrics``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._metrics``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``metrics__safederef`` when you only want to read a default without mutating self.
        """
        if self._metrics is None:
            self._metrics = self.metrics__safederef

        return self._metrics

    @property
    def metrics__safederef(self) -> 'dict[str, float]':
        """Return ``metrics``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``metrics`` stays unset and each
        call returns a new default. Use ``metrics__autoinit`` to persist the default on
        first access.
        """
        if self._metrics is None:
            return {}

        return self._metrics

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GraphAggregationBucket):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
