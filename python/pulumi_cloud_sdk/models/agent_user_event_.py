# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AgentUserEvent(PulumiAutoModelEncoder):
    """
    :var timestamp: datetime - declared
    :var entity_diff: AgentEntityDiff - declared
    :var page_context: PageContext - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'timestamp': 'datetime',
        'entity_diff': 'AgentEntityDiff',
        'page_context': 'PageContext',
        '__DISCRIMINATOR_VALUE__': 'str'
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'timestamp': 'timestamp',
        'entity_diff': 'entity_diff',
        'page_context': 'pageContext',
        '__DISCRIMINATOR_VALUE__': 'type'
    }

    DISCRIMINATOR = 'type'
    __DISCRIMINATOR_VALUE__ = "AgentUserEvent"

    FIELDS_timestamp = 'timestamp'
    FIELDS_entity_diff = 'entity_diff'
    FIELDS_page_context = 'pageContext'

    _timestamp: 'datetime'
    _entity_diff: 'AgentEntityDiff'
    _page_context: 'PageContext'

    def __init__(
        self,
        timestamp: 'datetime',
        entity_diff: 'AgentEntityDiff' = None,
        page_context: 'PageContext' = None,
    ) -> None:
        super().__init__()

        self.timestamp = timestamp
        self.entity_diff = entity_diff
        self.page_context = page_context

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AgentUserEvent):
            self._timestamp = source._timestamp
            self._entity_diff = source._entity_diff
            self._page_context = source._page_context

    @classmethod
    def compute_discriminator_closure(cls) -> list[str]:
        switcher = AgentUserEvent._ensure_discriminator_hierarchy()

        return [k for k, v in switcher.items() if issubclass(v, cls)]

    @staticmethod
    def get_concrete_subtypes() -> list[Type]:
        switcher = AgentUserEvent._ensure_discriminator_hierarchy()

        return list(switcher.values())

    @staticmethod
    def fixup_prototype(data, default_value=None) -> Type:
        klass_name = getattr(data, "type", None)
        if not klass_name:
            klass_name = data.get("type", default_value)

        switcher = AgentUserEvent._ensure_discriminator_hierarchy()

        klass = switcher.get(klass_name)
        if klass:
            return klass

        raise ValueError(f"Unable to deserialize unknown type '{klass_name}'")

    @staticmethod
    def _ensure_discriminator_hierarchy() -> dict[str, Type]:
        switcher = AgentUserEvent.__dict__.get("__DISCRIMINATOR_SWITCHER__")
        if not switcher:
            from .. import models
            switcher = {
                "exec_tool_call": models.AgentUserEventExecToolCall,
                "tool_result": models.AgentUserEventToolResult,
                "user_cancel": models.AgentUserEventCancel,
                "user_confirmation": models.AgentUserEventConfirmation,
                "user_message": models.AgentUserEventMessage,
            }
            setattr(AgentUserEvent, "__DISCRIMINATOR_SWITCHER__", switcher)
        return switcher

    @property
    def timestamp(self) -> 'datetime':
        return self._timestamp

    @timestamp.setter
    def timestamp(self, timestamp: 'datetime'):
        if timestamp is None:
            raise ValueError("Invalid value for `timestamp`, must not be `None`")

        self._timestamp = timestamp

    @property
    def entity_diff(self) -> 'AgentEntityDiff':
        return self._entity_diff

    @entity_diff.setter
    def entity_diff(self, entity_diff: 'AgentEntityDiff'):
        self._entity_diff = entity_diff

    @property
    def entity_diff__autoinit(self) -> 'AgentEntityDiff':
        """Return ``entity_diff``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._entity_diff``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``entity_diff__safederef`` when you only want to read a default without mutating self.
        """
        if self._entity_diff is None:
            self._entity_diff = self.entity_diff__safederef

        return self._entity_diff

    @property
    def entity_diff__safederef(self) -> 'AgentEntityDiff':
        """Return ``entity_diff``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``entity_diff`` stays unset and each
        call returns a new default. Use ``entity_diff__autoinit`` to persist the default on
        first access.
        """
        if self._entity_diff is None:
            from .. import models
            return models.AgentEntityDiff._new_empty()

        return self._entity_diff

    @property
    def page_context(self) -> 'PageContext':
        return self._page_context

    @page_context.setter
    def page_context(self, page_context: 'PageContext'):
        self._page_context = page_context

    @property
    def page_context__autoinit(self) -> 'PageContext':
        """Return ``page_context``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._page_context``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``page_context__safederef`` when you only want to read a default without mutating self.
        """
        if self._page_context is None:
            self._page_context = self.page_context__safederef

        return self._page_context

    @property
    def page_context__safederef(self) -> 'PageContext':
        """Return ``page_context``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``page_context`` stays unset and each
        call returns a new default. Use ``page_context__autoinit`` to persist the default on
        first access.
        """
        if self._page_context is None:
            from .. import models
            return models.PageContext._new_empty()

        return self._page_context

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AgentUserEvent):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
