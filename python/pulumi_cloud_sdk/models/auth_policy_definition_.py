# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AuthPolicyDefinition(PulumiAutoModelEncoder):
    """
    :var decision: str - declared
    :var token_type: str - declared
    :var team_name: str - declared
    :var user_login: str - declared
    :var runner_i_d: str - declared
    :var role_i_d: str - declared
    :var authorized_permissions: list[str] - declared
    :var rules: dict[str, Any] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'decision': 'str',
        'token_type': 'str',
        'team_name': 'str',
        'user_login': 'str',
        'runner_i_d': 'str',
        'role_i_d': 'str',
        'authorized_permissions': 'list[str]',
        'rules': 'dict[str, Any]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'decision': 'decision',
        'token_type': 'tokenType',
        'team_name': 'teamName',
        'user_login': 'userLogin',
        'runner_i_d': 'runnerID',
        'role_i_d': 'roleID',
        'authorized_permissions': 'authorizedPermissions',
        'rules': 'rules',
    }

    FIELDS_decision = 'decision'
    FIELDS_token_type = 'tokenType'
    FIELDS_team_name = 'teamName'
    FIELDS_user_login = 'userLogin'
    FIELDS_runner_i_d = 'runnerID'
    FIELDS_role_i_d = 'roleID'
    FIELDS_authorized_permissions = 'authorizedPermissions'
    FIELDS_rules = 'rules'

    _decision: 'str'
    _token_type: 'str'
    _team_name: 'str'
    _user_login: 'str'
    _runner_i_d: 'str'
    _role_i_d: 'str'
    _authorized_permissions: 'list[str]'
    _rules: 'dict[str, Any]'

    def __init__(
        self,
        decision: 'str',
        token_type: 'str',
        authorized_permissions: 'list[str]',
        rules: 'dict[str, Any]',
        team_name: 'str' = None,
        user_login: 'str' = None,
        runner_i_d: 'str' = None,
        role_i_d: 'str' = None,
    ) -> None:
        super().__init__()

        self.decision = decision
        self.token_type = token_type
        self.team_name = team_name
        self.user_login = user_login
        self.runner_i_d = runner_i_d
        self.role_i_d = role_i_d
        self.authorized_permissions = authorized_permissions
        self.rules = rules

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AuthPolicyDefinition):
            self._decision = source._decision
            self._token_type = source._token_type
            self._team_name = source._team_name
            self._user_login = source._user_login
            self._runner_i_d = source._runner_i_d
            self._role_i_d = source._role_i_d
            self._authorized_permissions = source._authorized_permissions
            self._rules = source._rules

    @property
    def decision(self) -> 'str':
        return self._decision

    @decision.setter
    def decision(self, decision: 'str'):
        if decision is None:
            raise ValueError("Invalid value for `decision`, must not be `None`")

        self._decision = decision

    @property
    def token_type(self) -> 'str':
        return self._token_type

    @token_type.setter
    def token_type(self, token_type: 'str'):
        if token_type is None:
            raise ValueError("Invalid value for `token_type`, must not be `None`")

        self._token_type = token_type

    @property
    def team_name(self) -> 'str':
        return self._team_name

    @team_name.setter
    def team_name(self, team_name: 'str'):
        self._team_name = team_name

    @property
    def user_login(self) -> 'str':
        return self._user_login

    @user_login.setter
    def user_login(self, user_login: 'str'):
        self._user_login = user_login

    @property
    def runner_i_d(self) -> 'str':
        return self._runner_i_d

    @runner_i_d.setter
    def runner_i_d(self, runner_i_d: 'str'):
        self._runner_i_d = runner_i_d

    @property
    def role_i_d(self) -> 'str':
        return self._role_i_d

    @role_i_d.setter
    def role_i_d(self, role_i_d: 'str'):
        self._role_i_d = role_i_d

    @property
    def authorized_permissions(self) -> 'list[str]':
        return self._authorized_permissions

    @authorized_permissions.setter
    def authorized_permissions(self, authorized_permissions: 'list[str]'):
        if authorized_permissions is None:
            raise ValueError("Invalid value for `authorized_permissions`, must not be `None`")

        self._authorized_permissions = authorized_permissions

    @property
    def authorized_permissions__autoinit(self) -> 'list[str]':
        """Return ``authorized_permissions``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._authorized_permissions``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``authorized_permissions__safederef`` when you only want to read a default without mutating self.
        """
        if self._authorized_permissions is None:
            self._authorized_permissions = self.authorized_permissions__safederef

        return self._authorized_permissions

    @property
    def authorized_permissions__safederef(self) -> 'list[str]':
        """Return ``authorized_permissions``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``authorized_permissions`` stays unset and each
        call returns a new default. Use ``authorized_permissions__autoinit`` to persist the default on
        first access.
        """
        if self._authorized_permissions is None:
            return []

        return self._authorized_permissions

    @property
    def rules(self) -> 'dict[str, Any]':
        return self._rules

    @rules.setter
    def rules(self, rules: 'dict[str, Any]'):
        if rules is None:
            raise ValueError("Invalid value for `rules`, must not be `None`")

        self._rules = rules

    @property
    def rules__autoinit(self) -> 'dict[str, Any]':
        """Return ``rules``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._rules``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``rules__safederef`` when you only want to read a default without mutating self.
        """
        if self._rules is None:
            self._rules = self.rules__safederef

        return self._rules

    @property
    def rules__safederef(self) -> 'dict[str, Any]':
        """Return ``rules``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``rules`` stays unset and each
        call returns a new default. Use ``rules__autoinit`` to persist the default on
        first access.
        """
        if self._rules is None:
            return {}

        return self._rules

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AuthPolicyDefinition):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
