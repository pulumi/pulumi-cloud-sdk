# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class StackOverviewResponse(PulumiAutoModelEncoder):
    """
    :var tags: dict[str, str] - declared
    :var referenced_stacks: list[StackReference] - declared
    :var resources: GetStackResourcesResponse - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'tags': 'dict[str, str]',
        'referenced_stacks': 'list[StackReference]',
        'resources': 'GetStackResourcesResponse',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'tags': 'tags',
        'referenced_stacks': 'referencedStacks',
        'resources': 'resources',
    }

    FIELDS_tags = 'tags'
    FIELDS_referenced_stacks = 'referencedStacks'
    FIELDS_resources = 'resources'

    _tags: 'dict[str, str]'
    _referenced_stacks: 'list[StackReference]'
    _resources: 'GetStackResourcesResponse'

    def __init__(
        self,
        tags: 'dict[str, str]',
        referenced_stacks: 'list[StackReference]',
        resources: 'GetStackResourcesResponse',
    ) -> None:
        super().__init__()

        self.tags = tags
        self.referenced_stacks = referenced_stacks
        self.resources = resources

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, StackOverviewResponse):
            self._tags = source._tags
            self._referenced_stacks = source._referenced_stacks
            self._resources = source._resources

    @property
    def tags(self) -> 'dict[str, str]':
        return self._tags

    @tags.setter
    def tags(self, tags: 'dict[str, str]'):
        if tags is None:
            raise ValueError("Invalid value for `tags`, must not be `None`")

        self._tags = tags

    @property
    def tags__autoinit(self) -> 'dict[str, str]':
        """Return ``tags``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._tags``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``tags__safederef`` when you only want to read a default without mutating self.
        """
        if self._tags is None:
            self._tags = self.tags__safederef

        return self._tags

    @property
    def tags__safederef(self) -> 'dict[str, str]':
        """Return ``tags``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``tags`` stays unset and each
        call returns a new default. Use ``tags__autoinit`` to persist the default on
        first access.
        """
        if self._tags is None:
            return {}

        return self._tags

    @property
    def referenced_stacks(self) -> 'list[StackReference]':
        return self._referenced_stacks

    @referenced_stacks.setter
    def referenced_stacks(self, referenced_stacks: 'list[StackReference]'):
        if referenced_stacks is None:
            raise ValueError("Invalid value for `referenced_stacks`, must not be `None`")

        self._referenced_stacks = referenced_stacks

    @property
    def referenced_stacks__autoinit(self) -> 'list[StackReference]':
        """Return ``referenced_stacks``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._referenced_stacks``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``referenced_stacks__safederef`` when you only want to read a default without mutating self.
        """
        if self._referenced_stacks is None:
            self._referenced_stacks = self.referenced_stacks__safederef

        return self._referenced_stacks

    @property
    def referenced_stacks__safederef(self) -> 'list[StackReference]':
        """Return ``referenced_stacks``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``referenced_stacks`` stays unset and each
        call returns a new default. Use ``referenced_stacks__autoinit`` to persist the default on
        first access.
        """
        if self._referenced_stacks is None:
            return []

        return self._referenced_stacks

    @property
    def resources(self) -> 'GetStackResourcesResponse':
        return self._resources

    @resources.setter
    def resources(self, resources: 'GetStackResourcesResponse'):
        if resources is None:
            raise ValueError("Invalid value for `resources`, must not be `None`")

        self._resources = resources

    @property
    def resources__autoinit(self) -> 'GetStackResourcesResponse':
        """Return ``resources``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._resources``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``resources__safederef`` when you only want to read a default without mutating self.
        """
        if self._resources is None:
            self._resources = self.resources__safederef

        return self._resources

    @property
    def resources__safederef(self) -> 'GetStackResourcesResponse':
        """Return ``resources``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``resources`` stays unset and each
        call returns a new default. Use ``resources__autoinit`` to persist the default on
        first access.
        """
        if self._resources is None:
            from .. import models
            return models.GetStackResourcesResponse._new_empty()

        return self._resources

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, StackOverviewResponse):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
