# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GetUpdateTimelineResponse(PulumiAutoModelEncoder):
    """
    :var update: UpdateInfo - declared
    :var collated_update_events: list[UpdateInfo] - declared
    :var collated_pull_request: GitHubPullRequest - declared
    :var previews: list[UpdateInfo] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'update': 'UpdateInfo',
        'collated_update_events': 'list[UpdateInfo]',
        'collated_pull_request': 'GitHubPullRequest',
        'previews': 'list[UpdateInfo]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'update': 'update',
        'collated_update_events': 'collatedUpdateEvents',
        'collated_pull_request': 'collatedPullRequest',
        'previews': 'previews',
    }

    FIELDS_update = 'update'
    FIELDS_collated_update_events = 'collatedUpdateEvents'
    FIELDS_collated_pull_request = 'collatedPullRequest'
    FIELDS_previews = 'previews'

    _update: 'UpdateInfo'
    _collated_update_events: 'list[UpdateInfo]'
    _collated_pull_request: 'GitHubPullRequest'
    _previews: 'list[UpdateInfo]'

    def __init__(
        self,
        update: 'UpdateInfo',
        collated_update_events: 'list[UpdateInfo]',
        previews: 'list[UpdateInfo]',
        collated_pull_request: 'GitHubPullRequest' = None,
    ) -> None:
        super().__init__()

        self.update = update
        self.collated_update_events = collated_update_events
        self.collated_pull_request = collated_pull_request
        self.previews = previews

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GetUpdateTimelineResponse):
            self._update = source._update
            self._collated_update_events = source._collated_update_events
            self._collated_pull_request = source._collated_pull_request
            self._previews = source._previews

    @property
    def update(self) -> 'UpdateInfo':
        return self._update

    @update.setter
    def update(self, update: 'UpdateInfo'):
        if update is None:
            raise ValueError("Invalid value for `update`, must not be `None`")

        self._update = update

    @property
    def update__autoinit(self) -> 'UpdateInfo':
        """Return ``update``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._update``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``update__safederef`` when you only want to read a default without mutating self.
        """
        if self._update is None:
            self._update = self.update__safederef

        return self._update

    @property
    def update__safederef(self) -> 'UpdateInfo':
        """Return ``update``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``update`` stays unset and each
        call returns a new default. Use ``update__autoinit`` to persist the default on
        first access.
        """
        if self._update is None:
            from .. import models
            return models.UpdateInfo._new_empty()

        return self._update

    @property
    def collated_update_events(self) -> 'list[UpdateInfo]':
        return self._collated_update_events

    @collated_update_events.setter
    def collated_update_events(self, collated_update_events: 'list[UpdateInfo]'):
        if collated_update_events is None:
            raise ValueError("Invalid value for `collated_update_events`, must not be `None`")

        self._collated_update_events = collated_update_events

    @property
    def collated_update_events__autoinit(self) -> 'list[UpdateInfo]':
        """Return ``collated_update_events``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._collated_update_events``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``collated_update_events__safederef`` when you only want to read a default without mutating self.
        """
        if self._collated_update_events is None:
            self._collated_update_events = self.collated_update_events__safederef

        return self._collated_update_events

    @property
    def collated_update_events__safederef(self) -> 'list[UpdateInfo]':
        """Return ``collated_update_events``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``collated_update_events`` stays unset and each
        call returns a new default. Use ``collated_update_events__autoinit`` to persist the default on
        first access.
        """
        if self._collated_update_events is None:
            return []

        return self._collated_update_events

    @property
    def collated_pull_request(self) -> 'GitHubPullRequest':
        return self._collated_pull_request

    @collated_pull_request.setter
    def collated_pull_request(self, collated_pull_request: 'GitHubPullRequest'):
        self._collated_pull_request = collated_pull_request

    @property
    def collated_pull_request__autoinit(self) -> 'GitHubPullRequest':
        """Return ``collated_pull_request``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._collated_pull_request``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``collated_pull_request__safederef`` when you only want to read a default without mutating self.
        """
        if self._collated_pull_request is None:
            self._collated_pull_request = self.collated_pull_request__safederef

        return self._collated_pull_request

    @property
    def collated_pull_request__safederef(self) -> 'GitHubPullRequest':
        """Return ``collated_pull_request``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``collated_pull_request`` stays unset and each
        call returns a new default. Use ``collated_pull_request__autoinit`` to persist the default on
        first access.
        """
        if self._collated_pull_request is None:
            from .. import models
            return models.GitHubPullRequest._new_empty()

        return self._collated_pull_request

    @property
    def previews(self) -> 'list[UpdateInfo]':
        return self._previews

    @previews.setter
    def previews(self, previews: 'list[UpdateInfo]'):
        if previews is None:
            raise ValueError("Invalid value for `previews`, must not be `None`")

        self._previews = previews

    @property
    def previews__autoinit(self) -> 'list[UpdateInfo]':
        """Return ``previews``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._previews``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``previews__safederef`` when you only want to read a default without mutating self.
        """
        if self._previews is None:
            self._previews = self.previews__safederef

        return self._previews

    @property
    def previews__safederef(self) -> 'list[UpdateInfo]':
        """Return ``previews``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``previews`` stays unset and each
        call returns a new default. Use ``previews__autoinit`` to persist the default on
        first access.
        """
        if self._previews is None:
            return []

        return self._previews

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GetUpdateTimelineResponse):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
