# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AppBeginUpdateRequest(PulumiAutoModelEncoder):
    """
    :var update_kind: AppUpdateKind - declared
    :var program: AppUpdateProgramRequest - declared
    :var tags: dict[str, str] - declared
    :var journal_version: int - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'update_kind': 'AppUpdateKind',
        'program': 'AppUpdateProgramRequest',
        'tags': 'dict[str, str]',
        'journal_version': 'int',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'update_kind': 'updateKind',
        'program': 'program',
        'tags': 'tags',
        'journal_version': 'journalVersion',
    }

    FIELDS_update_kind = 'updateKind'
    FIELDS_program = 'program'
    FIELDS_tags = 'tags'
    FIELDS_journal_version = 'journalVersion'

    _update_kind: 'AppUpdateKind'
    _program: 'AppUpdateProgramRequest'
    _tags: 'dict[str, str]'
    _journal_version: 'int'

    def __init__(
        self,
        update_kind: 'AppUpdateKind',
        program: 'AppUpdateProgramRequest',
        tags: 'dict[str, str]' = None,
        journal_version: 'int' = 0,
    ) -> None:
        super().__init__()

        self.update_kind = update_kind
        self.program = program
        self.tags = tags
        self.journal_version = journal_version

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AppBeginUpdateRequest):
            self._update_kind = source._update_kind
            self._program = source._program
            self._tags = source._tags
            self._journal_version = source._journal_version

    @property
    def update_kind(self) -> 'AppUpdateKind':
        return self._update_kind

    @update_kind.setter
    def update_kind(self, update_kind: 'AppUpdateKind'):
        if update_kind is None:
            raise ValueError("Invalid value for `update_kind`, must not be `None`")

        self._update_kind = update_kind

    @property
    def program(self) -> 'AppUpdateProgramRequest':
        return self._program

    @program.setter
    def program(self, program: 'AppUpdateProgramRequest'):
        if program is None:
            raise ValueError("Invalid value for `program`, must not be `None`")

        self._program = program

    @property
    def program__autoinit(self) -> 'AppUpdateProgramRequest':
        """Return ``program``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._program``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``program__safederef`` when you only want to read a default without mutating self.
        """
        if self._program is None:
            self._program = self.program__safederef

        return self._program

    @property
    def program__safederef(self) -> 'AppUpdateProgramRequest':
        """Return ``program``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``program`` stays unset and each
        call returns a new default. Use ``program__autoinit`` to persist the default on
        first access.
        """
        if self._program is None:
            from .. import models
            return models.AppUpdateProgramRequest._new_empty()

        return self._program

    @property
    def tags(self) -> 'dict[str, str]':
        return self._tags

    @tags.setter
    def tags(self, tags: 'dict[str, str]'):
        self._tags = tags

    @property
    def tags__autoinit(self) -> 'dict[str, str]':
        """Return ``tags``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._tags``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``tags__safederef`` when you only want to read a default without mutating self.
        """
        if self._tags is None:
            self._tags = self.tags__safederef

        return self._tags

    @property
    def tags__safederef(self) -> 'dict[str, str]':
        """Return ``tags``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``tags`` stays unset and each
        call returns a new default. Use ``tags__autoinit`` to persist the default on
        first access.
        """
        if self._tags is None:
            return {}

        return self._tags

    @property
    def journal_version(self) -> 'int':
        return self._journal_version

    @journal_version.setter
    def journal_version(self, journal_version: 'int'):
        self._journal_version = journal_version

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AppBeginUpdateRequest):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
