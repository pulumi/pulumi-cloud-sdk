# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AppPolicyPackWithVersions(PulumiAutoModelEncoder):
    """
    :var name: str - declared
    :var display_name: str - declared
    :var versions: list[int] - declared
    :var version_tags: list[str] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'name': 'str',
        'display_name': 'str',
        'versions': 'list[int]',
        'version_tags': 'list[str]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'name': 'name',
        'display_name': 'displayName',
        'versions': 'versions',
        'version_tags': 'versionTags',
    }

    FIELDS_name = 'name'
    FIELDS_display_name = 'displayName'
    FIELDS_versions = 'versions'
    FIELDS_version_tags = 'versionTags'

    _name: 'str'
    _display_name: 'str'
    _versions: 'list[int]'
    _version_tags: 'list[str]'

    def __init__(
        self,
        name: 'str',
        display_name: 'str',
        versions: 'list[int]',
        version_tags: 'list[str]',
    ) -> None:
        super().__init__()

        self.name = name
        self.display_name = display_name
        self.versions = versions
        self.version_tags = version_tags

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AppPolicyPackWithVersions):
            self._name = source._name
            self._display_name = source._display_name
            self._versions = source._versions
            self._version_tags = source._version_tags

    @property
    def name(self) -> 'str':
        return self._name

    @name.setter
    def name(self, name: 'str'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def display_name(self) -> 'str':
        return self._display_name

    @display_name.setter
    def display_name(self, display_name: 'str'):
        if display_name is None:
            raise ValueError("Invalid value for `display_name`, must not be `None`")

        self._display_name = display_name

    @property
    def versions(self) -> 'list[int]':
        return self._versions

    @versions.setter
    def versions(self, versions: 'list[int]'):
        if versions is None:
            raise ValueError("Invalid value for `versions`, must not be `None`")

        self._versions = versions

    @property
    def versions__autoinit(self) -> 'list[int]':
        """Return ``versions``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._versions``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``versions__safederef`` when you only want to read a default without mutating self.
        """
        if self._versions is None:
            self._versions = self.versions__safederef

        return self._versions

    @property
    def versions__safederef(self) -> 'list[int]':
        """Return ``versions``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``versions`` stays unset and each
        call returns a new default. Use ``versions__autoinit`` to persist the default on
        first access.
        """
        if self._versions is None:
            return []

        return self._versions

    @property
    def version_tags(self) -> 'list[str]':
        return self._version_tags

    @version_tags.setter
    def version_tags(self, version_tags: 'list[str]'):
        if version_tags is None:
            raise ValueError("Invalid value for `version_tags`, must not be `None`")

        self._version_tags = version_tags

    @property
    def version_tags__autoinit(self) -> 'list[str]':
        """Return ``version_tags``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._version_tags``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``version_tags__safederef`` when you only want to read a default without mutating self.
        """
        if self._version_tags is None:
            self._version_tags = self.version_tags__safederef

        return self._version_tags

    @property
    def version_tags__safederef(self) -> 'list[str]':
        """Return ``version_tags``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``version_tags`` stays unset and each
        call returns a new default. Use ``version_tags__autoinit`` to persist the default on
        first access.
        """
        if self._version_tags is None:
            return []

        return self._version_tags

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AppPolicyPackWithVersions):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
