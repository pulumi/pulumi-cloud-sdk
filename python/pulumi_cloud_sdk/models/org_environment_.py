# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class OrgEnvironment(PulumiAutoModelEncoder):
    """
    :var id: str - declared
    :var organization: str - declared
    :var project: str - declared
    :var name: str - declared
    :var created: str - declared
    :var modified: str - declared
    :var owned_by: UserInfo - declared
    :var tags: dict[str, str] - declared
    :var deleted_at: str - declared
    :var links: EnvironmentLinks - declared
    :var referrer_metadata: EnvironmentReferrerMetadata - declared
    :var settings: EnvironmentSettings - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'id': 'str',
        'organization': 'str',
        'project': 'str',
        'name': 'str',
        'created': 'str',
        'modified': 'str',
        'owned_by': 'UserInfo',
        'tags': 'dict[str, str]',
        'deleted_at': 'str',
        'links': 'EnvironmentLinks',
        'referrer_metadata': 'EnvironmentReferrerMetadata',
        'settings': 'EnvironmentSettings',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'id': 'id',
        'organization': 'organization',
        'project': 'project',
        'name': 'name',
        'created': 'created',
        'modified': 'modified',
        'owned_by': 'ownedBy',
        'tags': 'tags',
        'deleted_at': 'deletedAt',
        'links': 'links',
        'referrer_metadata': 'referrerMetadata',
        'settings': 'settings',
    }

    FIELDS_id = 'id'
    FIELDS_organization = 'organization'
    FIELDS_project = 'project'
    FIELDS_name = 'name'
    FIELDS_created = 'created'
    FIELDS_modified = 'modified'
    FIELDS_owned_by = 'ownedBy'
    FIELDS_tags = 'tags'
    FIELDS_deleted_at = 'deletedAt'
    FIELDS_links = 'links'
    FIELDS_referrer_metadata = 'referrerMetadata'
    FIELDS_settings = 'settings'

    _id: 'str'
    _organization: 'str'
    _project: 'str'
    _name: 'str'
    _created: 'str'
    _modified: 'str'
    _owned_by: 'UserInfo'
    _tags: 'dict[str, str]'
    _deleted_at: 'str'
    _links: 'EnvironmentLinks'
    _referrer_metadata: 'EnvironmentReferrerMetadata'
    _settings: 'EnvironmentSettings'

    def __init__(
        self,
        id: 'str',
        organization: 'str',
        name: 'str',
        created: 'str',
        modified: 'str',
        owned_by: 'UserInfo',
        tags: 'dict[str, str]',
        referrer_metadata: 'EnvironmentReferrerMetadata',
        settings: 'EnvironmentSettings',
        project: 'str' = None,
        deleted_at: 'str' = None,
        links: 'EnvironmentLinks' = None,
    ) -> None:
        super().__init__()

        self.id = id
        self.organization = organization
        self.project = project
        self.name = name
        self.created = created
        self.modified = modified
        self.owned_by = owned_by
        self.tags = tags
        self.deleted_at = deleted_at
        self.links = links
        self.referrer_metadata = referrer_metadata
        self.settings = settings

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, OrgEnvironment):
            self._id = source._id
            self._organization = source._organization
            self._project = source._project
            self._name = source._name
            self._created = source._created
            self._modified = source._modified
            self._owned_by = source._owned_by
            self._tags = source._tags
            self._deleted_at = source._deleted_at
            self._links = source._links
            self._referrer_metadata = source._referrer_metadata
            self._settings = source._settings

    @property
    def id(self) -> 'str':
        return self._id

    @id.setter
    def id(self, id: 'str'):
        if id is None:
            raise ValueError("Invalid value for `id`, must not be `None`")

        self._id = id

    @property
    def organization(self) -> 'str':
        return self._organization

    @organization.setter
    def organization(self, organization: 'str'):
        if organization is None:
            raise ValueError("Invalid value for `organization`, must not be `None`")

        self._organization = organization

    @property
    def project(self) -> 'str':
        return self._project

    @project.setter
    def project(self, project: 'str'):
        self._project = project

    @property
    def name(self) -> 'str':
        return self._name

    @name.setter
    def name(self, name: 'str'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def created(self) -> 'str':
        return self._created

    @created.setter
    def created(self, created: 'str'):
        if created is None:
            raise ValueError("Invalid value for `created`, must not be `None`")

        self._created = created

    @property
    def modified(self) -> 'str':
        return self._modified

    @modified.setter
    def modified(self, modified: 'str'):
        if modified is None:
            raise ValueError("Invalid value for `modified`, must not be `None`")

        self._modified = modified

    @property
    def owned_by(self) -> 'UserInfo':
        return self._owned_by

    @owned_by.setter
    def owned_by(self, owned_by: 'UserInfo'):
        if owned_by is None:
            raise ValueError("Invalid value for `owned_by`, must not be `None`")

        self._owned_by = owned_by

    @property
    def owned_by__autoinit(self) -> 'UserInfo':
        """Return ``owned_by``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._owned_by``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``owned_by__safederef`` when you only want to read a default without mutating self.
        """
        if self._owned_by is None:
            self._owned_by = self.owned_by__safederef

        return self._owned_by

    @property
    def owned_by__safederef(self) -> 'UserInfo':
        """Return ``owned_by``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``owned_by`` stays unset and each
        call returns a new default. Use ``owned_by__autoinit`` to persist the default on
        first access.
        """
        if self._owned_by is None:
            from .. import models
            return models.UserInfo._new_empty()

        return self._owned_by

    @property
    def tags(self) -> 'dict[str, str]':
        return self._tags

    @tags.setter
    def tags(self, tags: 'dict[str, str]'):
        if tags is None:
            raise ValueError("Invalid value for `tags`, must not be `None`")

        self._tags = tags

    @property
    def tags__autoinit(self) -> 'dict[str, str]':
        """Return ``tags``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._tags``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``tags__safederef`` when you only want to read a default without mutating self.
        """
        if self._tags is None:
            self._tags = self.tags__safederef

        return self._tags

    @property
    def tags__safederef(self) -> 'dict[str, str]':
        """Return ``tags``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``tags`` stays unset and each
        call returns a new default. Use ``tags__autoinit`` to persist the default on
        first access.
        """
        if self._tags is None:
            return {}

        return self._tags

    @property
    def deleted_at(self) -> 'str':
        return self._deleted_at

    @deleted_at.setter
    def deleted_at(self, deleted_at: 'str'):
        self._deleted_at = deleted_at

    @property
    def links(self) -> 'EnvironmentLinks':
        return self._links

    @links.setter
    def links(self, links: 'EnvironmentLinks'):
        self._links = links

    @property
    def links__autoinit(self) -> 'EnvironmentLinks':
        """Return ``links``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._links``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``links__safederef`` when you only want to read a default without mutating self.
        """
        if self._links is None:
            self._links = self.links__safederef

        return self._links

    @property
    def links__safederef(self) -> 'EnvironmentLinks':
        """Return ``links``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``links`` stays unset and each
        call returns a new default. Use ``links__autoinit`` to persist the default on
        first access.
        """
        if self._links is None:
            from .. import models
            return models.EnvironmentLinks._new_empty()

        return self._links

    @property
    def referrer_metadata(self) -> 'EnvironmentReferrerMetadata':
        return self._referrer_metadata

    @referrer_metadata.setter
    def referrer_metadata(self, referrer_metadata: 'EnvironmentReferrerMetadata'):
        if referrer_metadata is None:
            raise ValueError("Invalid value for `referrer_metadata`, must not be `None`")

        self._referrer_metadata = referrer_metadata

    @property
    def referrer_metadata__autoinit(self) -> 'EnvironmentReferrerMetadata':
        """Return ``referrer_metadata``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._referrer_metadata``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``referrer_metadata__safederef`` when you only want to read a default without mutating self.
        """
        if self._referrer_metadata is None:
            self._referrer_metadata = self.referrer_metadata__safederef

        return self._referrer_metadata

    @property
    def referrer_metadata__safederef(self) -> 'EnvironmentReferrerMetadata':
        """Return ``referrer_metadata``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``referrer_metadata`` stays unset and each
        call returns a new default. Use ``referrer_metadata__autoinit`` to persist the default on
        first access.
        """
        if self._referrer_metadata is None:
            from .. import models
            return models.EnvironmentReferrerMetadata._new_empty()

        return self._referrer_metadata

    @property
    def settings(self) -> 'EnvironmentSettings':
        return self._settings

    @settings.setter
    def settings(self, settings: 'EnvironmentSettings'):
        if settings is None:
            raise ValueError("Invalid value for `settings`, must not be `None`")

        self._settings = settings

    @property
    def settings__autoinit(self) -> 'EnvironmentSettings':
        """Return ``settings``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._settings``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``settings__safederef`` when you only want to read a default without mutating self.
        """
        if self._settings is None:
            self._settings = self.settings__safederef

        return self._settings

    @property
    def settings__safederef(self) -> 'EnvironmentSettings':
        """Return ``settings``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``settings`` stays unset and each
        call returns a new default. Use ``settings__autoinit`` to persist the default on
        first access.
        """
        if self._settings is None:
            from .. import models
            return models.EnvironmentSettings._new_empty()

        return self._settings

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, OrgEnvironment):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
