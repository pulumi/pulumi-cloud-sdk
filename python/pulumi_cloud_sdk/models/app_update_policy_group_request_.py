# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AppUpdatePolicyGroupRequest(PulumiAutoModelEncoder):
    """
    :var new_name: str - declared
    :var add_stack: AppPulumiStackReference - declared
    :var remove_stack: AppPulumiStackReference - declared
    :var add_policy_pack: AppPolicyPackMetadata - declared
    :var remove_policy_pack: AppPolicyPackMetadata - declared
    :var add_insights_account: AppInsightsAccountReference - declared
    :var remove_insights_account: AppInsightsAccountReference - declared
    :var stacks: list[AppPulumiStackReference] - declared
    :var policy_packs: list[AppPolicyPackMetadata] - declared
    :var insights_accounts: list[str] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'new_name': 'str',
        'add_stack': 'AppPulumiStackReference',
        'remove_stack': 'AppPulumiStackReference',
        'add_policy_pack': 'AppPolicyPackMetadata',
        'remove_policy_pack': 'AppPolicyPackMetadata',
        'add_insights_account': 'AppInsightsAccountReference',
        'remove_insights_account': 'AppInsightsAccountReference',
        'stacks': 'list[AppPulumiStackReference]',
        'policy_packs': 'list[AppPolicyPackMetadata]',
        'insights_accounts': 'list[str]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'new_name': 'newName',
        'add_stack': 'addStack',
        'remove_stack': 'removeStack',
        'add_policy_pack': 'addPolicyPack',
        'remove_policy_pack': 'removePolicyPack',
        'add_insights_account': 'addInsightsAccount',
        'remove_insights_account': 'removeInsightsAccount',
        'stacks': 'stacks',
        'policy_packs': 'policyPacks',
        'insights_accounts': 'insightsAccounts',
    }

    FIELDS_new_name = 'newName'
    FIELDS_add_stack = 'addStack'
    FIELDS_remove_stack = 'removeStack'
    FIELDS_add_policy_pack = 'addPolicyPack'
    FIELDS_remove_policy_pack = 'removePolicyPack'
    FIELDS_add_insights_account = 'addInsightsAccount'
    FIELDS_remove_insights_account = 'removeInsightsAccount'
    FIELDS_stacks = 'stacks'
    FIELDS_policy_packs = 'policyPacks'
    FIELDS_insights_accounts = 'insightsAccounts'

    _new_name: 'str'
    _add_stack: 'AppPulumiStackReference'
    _remove_stack: 'AppPulumiStackReference'
    _add_policy_pack: 'AppPolicyPackMetadata'
    _remove_policy_pack: 'AppPolicyPackMetadata'
    _add_insights_account: 'AppInsightsAccountReference'
    _remove_insights_account: 'AppInsightsAccountReference'
    _stacks: 'list[AppPulumiStackReference]'
    _policy_packs: 'list[AppPolicyPackMetadata]'
    _insights_accounts: 'list[str]'

    def __init__(
        self,
        new_name: 'str' = None,
        add_stack: 'AppPulumiStackReference' = None,
        remove_stack: 'AppPulumiStackReference' = None,
        add_policy_pack: 'AppPolicyPackMetadata' = None,
        remove_policy_pack: 'AppPolicyPackMetadata' = None,
        add_insights_account: 'AppInsightsAccountReference' = None,
        remove_insights_account: 'AppInsightsAccountReference' = None,
        stacks: 'list[AppPulumiStackReference]' = None,
        policy_packs: 'list[AppPolicyPackMetadata]' = None,
        insights_accounts: 'list[str]' = None,
    ) -> None:
        super().__init__()

        self.new_name = new_name
        self.add_stack = add_stack
        self.remove_stack = remove_stack
        self.add_policy_pack = add_policy_pack
        self.remove_policy_pack = remove_policy_pack
        self.add_insights_account = add_insights_account
        self.remove_insights_account = remove_insights_account
        self.stacks = stacks
        self.policy_packs = policy_packs
        self.insights_accounts = insights_accounts

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AppUpdatePolicyGroupRequest):
            self._new_name = source._new_name
            self._add_stack = source._add_stack
            self._remove_stack = source._remove_stack
            self._add_policy_pack = source._add_policy_pack
            self._remove_policy_pack = source._remove_policy_pack
            self._add_insights_account = source._add_insights_account
            self._remove_insights_account = source._remove_insights_account
            self._stacks = source._stacks
            self._policy_packs = source._policy_packs
            self._insights_accounts = source._insights_accounts

    @property
    def new_name(self) -> 'str':
        return self._new_name

    @new_name.setter
    def new_name(self, new_name: 'str'):
        self._new_name = new_name

    @property
    def add_stack(self) -> 'AppPulumiStackReference':
        return self._add_stack

    @add_stack.setter
    def add_stack(self, add_stack: 'AppPulumiStackReference'):
        self._add_stack = add_stack

    @property
    def add_stack__autoinit(self) -> 'AppPulumiStackReference':
        """Return ``add_stack``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._add_stack``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``add_stack__safederef`` when you only want to read a default without mutating self.
        """
        if self._add_stack is None:
            self._add_stack = self.add_stack__safederef

        return self._add_stack

    @property
    def add_stack__safederef(self) -> 'AppPulumiStackReference':
        """Return ``add_stack``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``add_stack`` stays unset and each
        call returns a new default. Use ``add_stack__autoinit`` to persist the default on
        first access.
        """
        if self._add_stack is None:
            from .. import models
            return models.AppPulumiStackReference._new_empty()

        return self._add_stack

    @property
    def remove_stack(self) -> 'AppPulumiStackReference':
        return self._remove_stack

    @remove_stack.setter
    def remove_stack(self, remove_stack: 'AppPulumiStackReference'):
        self._remove_stack = remove_stack

    @property
    def remove_stack__autoinit(self) -> 'AppPulumiStackReference':
        """Return ``remove_stack``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._remove_stack``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``remove_stack__safederef`` when you only want to read a default without mutating self.
        """
        if self._remove_stack is None:
            self._remove_stack = self.remove_stack__safederef

        return self._remove_stack

    @property
    def remove_stack__safederef(self) -> 'AppPulumiStackReference':
        """Return ``remove_stack``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``remove_stack`` stays unset and each
        call returns a new default. Use ``remove_stack__autoinit`` to persist the default on
        first access.
        """
        if self._remove_stack is None:
            from .. import models
            return models.AppPulumiStackReference._new_empty()

        return self._remove_stack

    @property
    def add_policy_pack(self) -> 'AppPolicyPackMetadata':
        return self._add_policy_pack

    @add_policy_pack.setter
    def add_policy_pack(self, add_policy_pack: 'AppPolicyPackMetadata'):
        self._add_policy_pack = add_policy_pack

    @property
    def add_policy_pack__autoinit(self) -> 'AppPolicyPackMetadata':
        """Return ``add_policy_pack``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._add_policy_pack``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``add_policy_pack__safederef`` when you only want to read a default without mutating self.
        """
        if self._add_policy_pack is None:
            self._add_policy_pack = self.add_policy_pack__safederef

        return self._add_policy_pack

    @property
    def add_policy_pack__safederef(self) -> 'AppPolicyPackMetadata':
        """Return ``add_policy_pack``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``add_policy_pack`` stays unset and each
        call returns a new default. Use ``add_policy_pack__autoinit`` to persist the default on
        first access.
        """
        if self._add_policy_pack is None:
            from .. import models
            return models.AppPolicyPackMetadata._new_empty()

        return self._add_policy_pack

    @property
    def remove_policy_pack(self) -> 'AppPolicyPackMetadata':
        return self._remove_policy_pack

    @remove_policy_pack.setter
    def remove_policy_pack(self, remove_policy_pack: 'AppPolicyPackMetadata'):
        self._remove_policy_pack = remove_policy_pack

    @property
    def remove_policy_pack__autoinit(self) -> 'AppPolicyPackMetadata':
        """Return ``remove_policy_pack``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._remove_policy_pack``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``remove_policy_pack__safederef`` when you only want to read a default without mutating self.
        """
        if self._remove_policy_pack is None:
            self._remove_policy_pack = self.remove_policy_pack__safederef

        return self._remove_policy_pack

    @property
    def remove_policy_pack__safederef(self) -> 'AppPolicyPackMetadata':
        """Return ``remove_policy_pack``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``remove_policy_pack`` stays unset and each
        call returns a new default. Use ``remove_policy_pack__autoinit`` to persist the default on
        first access.
        """
        if self._remove_policy_pack is None:
            from .. import models
            return models.AppPolicyPackMetadata._new_empty()

        return self._remove_policy_pack

    @property
    def add_insights_account(self) -> 'AppInsightsAccountReference':
        return self._add_insights_account

    @add_insights_account.setter
    def add_insights_account(self, add_insights_account: 'AppInsightsAccountReference'):
        self._add_insights_account = add_insights_account

    @property
    def add_insights_account__autoinit(self) -> 'AppInsightsAccountReference':
        """Return ``add_insights_account``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._add_insights_account``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``add_insights_account__safederef`` when you only want to read a default without mutating self.
        """
        if self._add_insights_account is None:
            self._add_insights_account = self.add_insights_account__safederef

        return self._add_insights_account

    @property
    def add_insights_account__safederef(self) -> 'AppInsightsAccountReference':
        """Return ``add_insights_account``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``add_insights_account`` stays unset and each
        call returns a new default. Use ``add_insights_account__autoinit`` to persist the default on
        first access.
        """
        if self._add_insights_account is None:
            from .. import models
            return models.AppInsightsAccountReference._new_empty()

        return self._add_insights_account

    @property
    def remove_insights_account(self) -> 'AppInsightsAccountReference':
        return self._remove_insights_account

    @remove_insights_account.setter
    def remove_insights_account(self, remove_insights_account: 'AppInsightsAccountReference'):
        self._remove_insights_account = remove_insights_account

    @property
    def remove_insights_account__autoinit(self) -> 'AppInsightsAccountReference':
        """Return ``remove_insights_account``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._remove_insights_account``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``remove_insights_account__safederef`` when you only want to read a default without mutating self.
        """
        if self._remove_insights_account is None:
            self._remove_insights_account = self.remove_insights_account__safederef

        return self._remove_insights_account

    @property
    def remove_insights_account__safederef(self) -> 'AppInsightsAccountReference':
        """Return ``remove_insights_account``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``remove_insights_account`` stays unset and each
        call returns a new default. Use ``remove_insights_account__autoinit`` to persist the default on
        first access.
        """
        if self._remove_insights_account is None:
            from .. import models
            return models.AppInsightsAccountReference._new_empty()

        return self._remove_insights_account

    @property
    def stacks(self) -> 'list[AppPulumiStackReference]':
        return self._stacks

    @stacks.setter
    def stacks(self, stacks: 'list[AppPulumiStackReference]'):
        self._stacks = stacks

    @property
    def stacks__autoinit(self) -> 'list[AppPulumiStackReference]':
        """Return ``stacks``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._stacks``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``stacks__safederef`` when you only want to read a default without mutating self.
        """
        if self._stacks is None:
            self._stacks = self.stacks__safederef

        return self._stacks

    @property
    def stacks__safederef(self) -> 'list[AppPulumiStackReference]':
        """Return ``stacks``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``stacks`` stays unset and each
        call returns a new default. Use ``stacks__autoinit`` to persist the default on
        first access.
        """
        if self._stacks is None:
            return []

        return self._stacks

    @property
    def policy_packs(self) -> 'list[AppPolicyPackMetadata]':
        return self._policy_packs

    @policy_packs.setter
    def policy_packs(self, policy_packs: 'list[AppPolicyPackMetadata]'):
        self._policy_packs = policy_packs

    @property
    def policy_packs__autoinit(self) -> 'list[AppPolicyPackMetadata]':
        """Return ``policy_packs``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._policy_packs``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``policy_packs__safederef`` when you only want to read a default without mutating self.
        """
        if self._policy_packs is None:
            self._policy_packs = self.policy_packs__safederef

        return self._policy_packs

    @property
    def policy_packs__safederef(self) -> 'list[AppPolicyPackMetadata]':
        """Return ``policy_packs``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``policy_packs`` stays unset and each
        call returns a new default. Use ``policy_packs__autoinit`` to persist the default on
        first access.
        """
        if self._policy_packs is None:
            return []

        return self._policy_packs

    @property
    def insights_accounts(self) -> 'list[str]':
        return self._insights_accounts

    @insights_accounts.setter
    def insights_accounts(self, insights_accounts: 'list[str]'):
        self._insights_accounts = insights_accounts

    @property
    def insights_accounts__autoinit(self) -> 'list[str]':
        """Return ``insights_accounts``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._insights_accounts``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``insights_accounts__safederef`` when you only want to read a default without mutating self.
        """
        if self._insights_accounts is None:
            self._insights_accounts = self.insights_accounts__safederef

        return self._insights_accounts

    @property
    def insights_accounts__safederef(self) -> 'list[str]':
        """Return ``insights_accounts``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``insights_accounts`` stays unset and each
        call returns a new default. Use ``insights_accounts__autoinit`` to persist the default on
        first access.
        """
        if self._insights_accounts is None:
            return []

        return self._insights_accounts

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AppUpdatePolicyGroupRequest):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
