# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GraphTraverseStep(PulumiAutoModelEncoder):
    """
    :var edge_types: list[str] - declared
    :var direction: GraphTraverseDirection - declared
    :var depth: GraphDepthRange - declared
    :var target: GraphTraverseTarget - declared
    :var alias: str - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'edge_types': 'list[str]',
        'direction': 'GraphTraverseDirection',
        'depth': 'GraphDepthRange',
        'target': 'GraphTraverseTarget',
        'alias': 'str',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'edge_types': 'edgeTypes',
        'direction': 'direction',
        'depth': 'depth',
        'target': 'target',
        'alias': 'alias',
    }

    FIELDS_edge_types = 'edgeTypes'
    FIELDS_direction = 'direction'
    FIELDS_depth = 'depth'
    FIELDS_target = 'target'
    FIELDS_alias = 'alias'

    _edge_types: 'list[str]'
    _direction: 'GraphTraverseDirection'
    _depth: 'GraphDepthRange'
    _target: 'GraphTraverseTarget'
    _alias: 'str'

    def __init__(
        self,
        edge_types: 'list[str]',
        direction: 'GraphTraverseDirection',
        depth: 'GraphDepthRange' = None,
        target: 'GraphTraverseTarget' = None,
        alias: 'str' = None,
    ) -> None:
        super().__init__()

        self.edge_types = edge_types
        self.direction = direction
        self.depth = depth
        self.target = target
        self.alias = alias

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GraphTraverseStep):
            self._edge_types = source._edge_types
            self._direction = source._direction
            self._depth = source._depth
            self._target = source._target
            self._alias = source._alias

    @property
    def edge_types(self) -> 'list[str]':
        return self._edge_types

    @edge_types.setter
    def edge_types(self, edge_types: 'list[str]'):
        if edge_types is None:
            raise ValueError("Invalid value for `edge_types`, must not be `None`")

        self._edge_types = edge_types

    @property
    def edge_types__autoinit(self) -> 'list[str]':
        """Return ``edge_types``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._edge_types``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``edge_types__safederef`` when you only want to read a default without mutating self.
        """
        if self._edge_types is None:
            self._edge_types = self.edge_types__safederef

        return self._edge_types

    @property
    def edge_types__safederef(self) -> 'list[str]':
        """Return ``edge_types``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``edge_types`` stays unset and each
        call returns a new default. Use ``edge_types__autoinit`` to persist the default on
        first access.
        """
        if self._edge_types is None:
            return []

        return self._edge_types

    @property
    def direction(self) -> 'GraphTraverseDirection':
        return self._direction

    @direction.setter
    def direction(self, direction: 'GraphTraverseDirection'):
        if direction is None:
            raise ValueError("Invalid value for `direction`, must not be `None`")

        self._direction = direction

    @property
    def depth(self) -> 'GraphDepthRange':
        return self._depth

    @depth.setter
    def depth(self, depth: 'GraphDepthRange'):
        self._depth = depth

    @property
    def depth__autoinit(self) -> 'GraphDepthRange':
        """Return ``depth``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._depth``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``depth__safederef`` when you only want to read a default without mutating self.
        """
        if self._depth is None:
            self._depth = self.depth__safederef

        return self._depth

    @property
    def depth__safederef(self) -> 'GraphDepthRange':
        """Return ``depth``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``depth`` stays unset and each
        call returns a new default. Use ``depth__autoinit`` to persist the default on
        first access.
        """
        if self._depth is None:
            from .. import models
            return models.GraphDepthRange._new_empty()

        return self._depth

    @property
    def target(self) -> 'GraphTraverseTarget':
        return self._target

    @target.setter
    def target(self, target: 'GraphTraverseTarget'):
        self._target = target

    @property
    def target__autoinit(self) -> 'GraphTraverseTarget':
        """Return ``target``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._target``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``target__safederef`` when you only want to read a default without mutating self.
        """
        if self._target is None:
            self._target = self.target__safederef

        return self._target

    @property
    def target__safederef(self) -> 'GraphTraverseTarget':
        """Return ``target``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``target`` stays unset and each
        call returns a new default. Use ``target__autoinit`` to persist the default on
        first access.
        """
        if self._target is None:
            from .. import models
            return models.GraphTraverseTarget._new_empty()

        return self._target

    @property
    def alias(self) -> 'str':
        return self._alias

    @alias.setter
    def alias(self, alias: 'str'):
        self._alias = alias

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GraphTraverseStep):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
