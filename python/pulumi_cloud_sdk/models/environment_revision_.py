# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class EnvironmentRevision(PulumiAutoModelEncoder):
    """
    :var number: int - declared
    :var created: datetime - declared
    :var creator_login: str - declared
    :var creator_name: str - declared
    :var tags: list[str] - declared
    :var retracted: EnvironmentRevisionRetracted - declared
    :var source_change_request: EnvironmentRevisionChangeRequest - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'number': 'int',
        'created': 'datetime',
        'creator_login': 'str',
        'creator_name': 'str',
        'tags': 'list[str]',
        'retracted': 'EnvironmentRevisionRetracted',
        'source_change_request': 'EnvironmentRevisionChangeRequest',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'number': 'number',
        'created': 'created',
        'creator_login': 'creatorLogin',
        'creator_name': 'creatorName',
        'tags': 'tags',
        'retracted': 'retracted',
        'source_change_request': 'sourceChangeRequest',
    }

    FIELDS_number = 'number'
    FIELDS_created = 'created'
    FIELDS_creator_login = 'creatorLogin'
    FIELDS_creator_name = 'creatorName'
    FIELDS_tags = 'tags'
    FIELDS_retracted = 'retracted'
    FIELDS_source_change_request = 'sourceChangeRequest'

    _number: 'int'
    _created: 'datetime'
    _creator_login: 'str'
    _creator_name: 'str'
    _tags: 'list[str]'
    _retracted: 'EnvironmentRevisionRetracted'
    _source_change_request: 'EnvironmentRevisionChangeRequest'

    def __init__(
        self,
        number: 'int',
        created: 'datetime',
        creator_login: 'str' = None,
        creator_name: 'str' = None,
        tags: 'list[str]' = None,
        retracted: 'EnvironmentRevisionRetracted' = None,
        source_change_request: 'EnvironmentRevisionChangeRequest' = None,
    ) -> None:
        super().__init__()

        self.number = number
        self.created = created
        self.creator_login = creator_login
        self.creator_name = creator_name
        self.tags = tags
        self.retracted = retracted
        self.source_change_request = source_change_request

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, EnvironmentRevision):
            self._number = source._number
            self._created = source._created
            self._creator_login = source._creator_login
            self._creator_name = source._creator_name
            self._tags = source._tags
            self._retracted = source._retracted
            self._source_change_request = source._source_change_request

    @property
    def number(self) -> 'int':
        return self._number

    @number.setter
    def number(self, number: 'int'):
        if number is None:
            raise ValueError("Invalid value for `number`, must not be `None`")

        self._number = number

    @property
    def created(self) -> 'datetime':
        return self._created

    @created.setter
    def created(self, created: 'datetime'):
        if created is None:
            raise ValueError("Invalid value for `created`, must not be `None`")

        self._created = created

    @property
    def creator_login(self) -> 'str':
        return self._creator_login

    @creator_login.setter
    def creator_login(self, creator_login: 'str'):
        self._creator_login = creator_login

    @property
    def creator_name(self) -> 'str':
        return self._creator_name

    @creator_name.setter
    def creator_name(self, creator_name: 'str'):
        self._creator_name = creator_name

    @property
    def tags(self) -> 'list[str]':
        return self._tags

    @tags.setter
    def tags(self, tags: 'list[str]'):
        self._tags = tags

    @property
    def tags__autoinit(self) -> 'list[str]':
        """Return ``tags``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._tags``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``tags__safederef`` when you only want to read a default without mutating self.
        """
        if self._tags is None:
            self._tags = self.tags__safederef

        return self._tags

    @property
    def tags__safederef(self) -> 'list[str]':
        """Return ``tags``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``tags`` stays unset and each
        call returns a new default. Use ``tags__autoinit`` to persist the default on
        first access.
        """
        if self._tags is None:
            return []

        return self._tags

    @property
    def retracted(self) -> 'EnvironmentRevisionRetracted':
        return self._retracted

    @retracted.setter
    def retracted(self, retracted: 'EnvironmentRevisionRetracted'):
        self._retracted = retracted

    @property
    def retracted__autoinit(self) -> 'EnvironmentRevisionRetracted':
        """Return ``retracted``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._retracted``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``retracted__safederef`` when you only want to read a default without mutating self.
        """
        if self._retracted is None:
            self._retracted = self.retracted__safederef

        return self._retracted

    @property
    def retracted__safederef(self) -> 'EnvironmentRevisionRetracted':
        """Return ``retracted``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``retracted`` stays unset and each
        call returns a new default. Use ``retracted__autoinit`` to persist the default on
        first access.
        """
        if self._retracted is None:
            from .. import models
            return models.EnvironmentRevisionRetracted._new_empty()

        return self._retracted

    @property
    def source_change_request(self) -> 'EnvironmentRevisionChangeRequest':
        return self._source_change_request

    @source_change_request.setter
    def source_change_request(self, source_change_request: 'EnvironmentRevisionChangeRequest'):
        self._source_change_request = source_change_request

    @property
    def source_change_request__autoinit(self) -> 'EnvironmentRevisionChangeRequest':
        """Return ``source_change_request``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._source_change_request``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``source_change_request__safederef`` when you only want to read a default without mutating self.
        """
        if self._source_change_request is None:
            self._source_change_request = self.source_change_request__safederef

        return self._source_change_request

    @property
    def source_change_request__safederef(self) -> 'EnvironmentRevisionChangeRequest':
        """Return ``source_change_request``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``source_change_request`` stays unset and each
        call returns a new default. Use ``source_change_request__autoinit`` to persist the default on
        first access.
        """
        if self._source_change_request is None:
            from .. import models
            return models.EnvironmentRevisionChangeRequest._new_empty()

        return self._source_change_request

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, EnvironmentRevision):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
