# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class OperationContextRequest(PulumiAutoModelEncoder):
    """
    :var oidc: OperationContextOIDCConfigurationRequest - declared
    :var pre_run_commands: list[str] - declared
    :var operation: PulumiOperation - declared
    :var environment_variables: dict[str, SecretValue] - declared
    :var options: OperationContextOptionsRequest - declared
    :var role: DeploymentRoleRequest - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'oidc': 'OperationContextOIDCConfigurationRequest',
        'pre_run_commands': 'list[str]',
        'operation': 'PulumiOperation',
        'environment_variables': 'dict[str, SecretValue]',
        'options': 'OperationContextOptionsRequest',
        'role': 'DeploymentRoleRequest',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'oidc': 'oidc',
        'pre_run_commands': 'preRunCommands',
        'operation': 'operation',
        'environment_variables': 'environmentVariables',
        'options': 'options',
        'role': 'role',
    }

    FIELDS_oidc = 'oidc'
    FIELDS_pre_run_commands = 'preRunCommands'
    FIELDS_operation = 'operation'
    FIELDS_environment_variables = 'environmentVariables'
    FIELDS_options = 'options'
    FIELDS_role = 'role'

    _oidc: 'OperationContextOIDCConfigurationRequest'
    _pre_run_commands: 'list[str]'
    _operation: 'PulumiOperation'
    _environment_variables: 'dict[str, SecretValue]'
    _options: 'OperationContextOptionsRequest'
    _role: 'DeploymentRoleRequest'

    def __init__(
        self,
        oidc: 'OperationContextOIDCConfigurationRequest' = None,
        pre_run_commands: 'list[str]' = None,
        operation: 'PulumiOperation' = None,
        environment_variables: 'dict[str, SecretValue]' = None,
        options: 'OperationContextOptionsRequest' = None,
        role: 'DeploymentRoleRequest' = None,
    ) -> None:
        super().__init__()

        self.oidc = oidc
        self.pre_run_commands = pre_run_commands
        self.operation = operation
        self.environment_variables = environment_variables
        self.options = options
        self.role = role

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, OperationContextRequest):
            self._oidc = source._oidc
            self._pre_run_commands = source._pre_run_commands
            self._operation = source._operation
            self._environment_variables = source._environment_variables
            self._options = source._options
            self._role = source._role

    @property
    def oidc(self) -> 'OperationContextOIDCConfigurationRequest':
        return self._oidc

    @oidc.setter
    def oidc(self, oidc: 'OperationContextOIDCConfigurationRequest'):
        self._oidc = oidc

    @property
    def oidc__autoinit(self) -> 'OperationContextOIDCConfigurationRequest':
        """Return ``oidc``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._oidc``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``oidc__safederef`` when you only want to read a default without mutating self.
        """
        if self._oidc is None:
            self._oidc = self.oidc__safederef

        return self._oidc

    @property
    def oidc__safederef(self) -> 'OperationContextOIDCConfigurationRequest':
        """Return ``oidc``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``oidc`` stays unset and each
        call returns a new default. Use ``oidc__autoinit`` to persist the default on
        first access.
        """
        if self._oidc is None:
            from .. import models
            return models.OperationContextOIDCConfigurationRequest._new_empty()

        return self._oidc

    @property
    def pre_run_commands(self) -> 'list[str]':
        return self._pre_run_commands

    @pre_run_commands.setter
    def pre_run_commands(self, pre_run_commands: 'list[str]'):
        self._pre_run_commands = pre_run_commands

    @property
    def pre_run_commands__autoinit(self) -> 'list[str]':
        """Return ``pre_run_commands``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._pre_run_commands``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``pre_run_commands__safederef`` when you only want to read a default without mutating self.
        """
        if self._pre_run_commands is None:
            self._pre_run_commands = self.pre_run_commands__safederef

        return self._pre_run_commands

    @property
    def pre_run_commands__safederef(self) -> 'list[str]':
        """Return ``pre_run_commands``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``pre_run_commands`` stays unset and each
        call returns a new default. Use ``pre_run_commands__autoinit`` to persist the default on
        first access.
        """
        if self._pre_run_commands is None:
            return []

        return self._pre_run_commands

    @property
    def operation(self) -> 'PulumiOperation':
        return self._operation

    @operation.setter
    def operation(self, operation: 'PulumiOperation'):
        self._operation = operation

    @property
    def environment_variables(self) -> 'dict[str, SecretValue]':
        return self._environment_variables

    @environment_variables.setter
    def environment_variables(self, environment_variables: 'dict[str, SecretValue]'):
        self._environment_variables = environment_variables

    @property
    def environment_variables__autoinit(self) -> 'dict[str, SecretValue]':
        """Return ``environment_variables``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._environment_variables``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``environment_variables__safederef`` when you only want to read a default without mutating self.
        """
        if self._environment_variables is None:
            self._environment_variables = self.environment_variables__safederef

        return self._environment_variables

    @property
    def environment_variables__safederef(self) -> 'dict[str, SecretValue]':
        """Return ``environment_variables``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``environment_variables`` stays unset and each
        call returns a new default. Use ``environment_variables__autoinit`` to persist the default on
        first access.
        """
        if self._environment_variables is None:
            return {}

        return self._environment_variables

    @property
    def options(self) -> 'OperationContextOptionsRequest':
        return self._options

    @options.setter
    def options(self, options: 'OperationContextOptionsRequest'):
        self._options = options

    @property
    def options__autoinit(self) -> 'OperationContextOptionsRequest':
        """Return ``options``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._options``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``options__safederef`` when you only want to read a default without mutating self.
        """
        if self._options is None:
            self._options = self.options__safederef

        return self._options

    @property
    def options__safederef(self) -> 'OperationContextOptionsRequest':
        """Return ``options``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``options`` stays unset and each
        call returns a new default. Use ``options__autoinit`` to persist the default on
        first access.
        """
        if self._options is None:
            from .. import models
            return models.OperationContextOptionsRequest._new_empty()

        return self._options

    @property
    def role(self) -> 'DeploymentRoleRequest':
        return self._role

    @role.setter
    def role(self, role: 'DeploymentRoleRequest'):
        self._role = role

    @property
    def role__autoinit(self) -> 'DeploymentRoleRequest':
        """Return ``role``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._role``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``role__safederef`` when you only want to read a default without mutating self.
        """
        if self._role is None:
            self._role = self.role__safederef

        return self._role

    @property
    def role__safederef(self) -> 'DeploymentRoleRequest':
        """Return ``role``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``role`` stays unset and each
        call returns a new default. Use ``role__autoinit`` to persist the default on
        first access.
        """
        if self._role is None:
            from .. import models
            return models.DeploymentRoleRequest._new_empty()

        return self._role

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, OperationContextRequest):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
