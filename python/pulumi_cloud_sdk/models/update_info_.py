# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class UpdateInfo(PulumiAutoModelEncoder):
    """
    :var info: AppUpdateInfo - declared
    :var update_i_d: str - declared
    :var github_commit_info: GitHubCommitInfo - declared
    :var version: int - declared
    :var latest_version: int - declared
    :var requested_by: UserInfo - declared
    :var policy_packs: list[AppPolicyPackMetadata] - declared
    :var requested_by_token: str - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'info': 'AppUpdateInfo',
        'update_i_d': 'str',
        'github_commit_info': 'GitHubCommitInfo',
        'version': 'int',
        'latest_version': 'int',
        'requested_by': 'UserInfo',
        'policy_packs': 'list[AppPolicyPackMetadata]',
        'requested_by_token': 'str',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'info': 'info',
        'update_i_d': 'updateID',
        'github_commit_info': 'githubCommitInfo',
        'version': 'version',
        'latest_version': 'latestVersion',
        'requested_by': 'requestedBy',
        'policy_packs': 'policyPacks',
        'requested_by_token': 'requestedByToken',
    }

    FIELDS_info = 'info'
    FIELDS_update_i_d = 'updateID'
    FIELDS_github_commit_info = 'githubCommitInfo'
    FIELDS_version = 'version'
    FIELDS_latest_version = 'latestVersion'
    FIELDS_requested_by = 'requestedBy'
    FIELDS_policy_packs = 'policyPacks'
    FIELDS_requested_by_token = 'requestedByToken'

    _info: 'AppUpdateInfo'
    _update_i_d: 'str'
    _github_commit_info: 'GitHubCommitInfo'
    _version: 'int'
    _latest_version: 'int'
    _requested_by: 'UserInfo'
    _policy_packs: 'list[AppPolicyPackMetadata]'
    _requested_by_token: 'str'

    def __init__(
        self,
        info: 'AppUpdateInfo',
        update_i_d: 'str',
        version: 'int',
        latest_version: 'int',
        requested_by: 'UserInfo',
        github_commit_info: 'GitHubCommitInfo' = None,
        policy_packs: 'list[AppPolicyPackMetadata]' = None,
        requested_by_token: 'str' = None,
    ) -> None:
        super().__init__()

        self.info = info
        self.update_i_d = update_i_d
        self.github_commit_info = github_commit_info
        self.version = version
        self.latest_version = latest_version
        self.requested_by = requested_by
        self.policy_packs = policy_packs
        self.requested_by_token = requested_by_token

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, UpdateInfo):
            self._info = source._info
            self._update_i_d = source._update_i_d
            self._github_commit_info = source._github_commit_info
            self._version = source._version
            self._latest_version = source._latest_version
            self._requested_by = source._requested_by
            self._policy_packs = source._policy_packs
            self._requested_by_token = source._requested_by_token

    @property
    def info(self) -> 'AppUpdateInfo':
        return self._info

    @info.setter
    def info(self, info: 'AppUpdateInfo'):
        if info is None:
            raise ValueError("Invalid value for `info`, must not be `None`")

        self._info = info

    @property
    def info__autoinit(self) -> 'AppUpdateInfo':
        """Return ``info``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._info``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``info__safederef`` when you only want to read a default without mutating self.
        """
        if self._info is None:
            self._info = self.info__safederef

        return self._info

    @property
    def info__safederef(self) -> 'AppUpdateInfo':
        """Return ``info``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``info`` stays unset and each
        call returns a new default. Use ``info__autoinit`` to persist the default on
        first access.
        """
        if self._info is None:
            from .. import models
            return models.AppUpdateInfo._new_empty()

        return self._info

    @property
    def update_i_d(self) -> 'str':
        return self._update_i_d

    @update_i_d.setter
    def update_i_d(self, update_i_d: 'str'):
        if update_i_d is None:
            raise ValueError("Invalid value for `update_i_d`, must not be `None`")

        self._update_i_d = update_i_d

    @property
    def github_commit_info(self) -> 'GitHubCommitInfo':
        return self._github_commit_info

    @github_commit_info.setter
    def github_commit_info(self, github_commit_info: 'GitHubCommitInfo'):
        self._github_commit_info = github_commit_info

    @property
    def github_commit_info__autoinit(self) -> 'GitHubCommitInfo':
        """Return ``github_commit_info``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._github_commit_info``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``github_commit_info__safederef`` when you only want to read a default without mutating self.
        """
        if self._github_commit_info is None:
            self._github_commit_info = self.github_commit_info__safederef

        return self._github_commit_info

    @property
    def github_commit_info__safederef(self) -> 'GitHubCommitInfo':
        """Return ``github_commit_info``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``github_commit_info`` stays unset and each
        call returns a new default. Use ``github_commit_info__autoinit`` to persist the default on
        first access.
        """
        if self._github_commit_info is None:
            from .. import models
            return models.GitHubCommitInfo._new_empty()

        return self._github_commit_info

    @property
    def version(self) -> 'int':
        return self._version

    @version.setter
    def version(self, version: 'int'):
        if version is None:
            raise ValueError("Invalid value for `version`, must not be `None`")

        self._version = version

    @property
    def latest_version(self) -> 'int':
        return self._latest_version

    @latest_version.setter
    def latest_version(self, latest_version: 'int'):
        if latest_version is None:
            raise ValueError("Invalid value for `latest_version`, must not be `None`")

        self._latest_version = latest_version

    @property
    def requested_by(self) -> 'UserInfo':
        return self._requested_by

    @requested_by.setter
    def requested_by(self, requested_by: 'UserInfo'):
        if requested_by is None:
            raise ValueError("Invalid value for `requested_by`, must not be `None`")

        self._requested_by = requested_by

    @property
    def requested_by__autoinit(self) -> 'UserInfo':
        """Return ``requested_by``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._requested_by``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``requested_by__safederef`` when you only want to read a default without mutating self.
        """
        if self._requested_by is None:
            self._requested_by = self.requested_by__safederef

        return self._requested_by

    @property
    def requested_by__safederef(self) -> 'UserInfo':
        """Return ``requested_by``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``requested_by`` stays unset and each
        call returns a new default. Use ``requested_by__autoinit`` to persist the default on
        first access.
        """
        if self._requested_by is None:
            from .. import models
            return models.UserInfo._new_empty()

        return self._requested_by

    @property
    def policy_packs(self) -> 'list[AppPolicyPackMetadata]':
        return self._policy_packs

    @policy_packs.setter
    def policy_packs(self, policy_packs: 'list[AppPolicyPackMetadata]'):
        self._policy_packs = policy_packs

    @property
    def policy_packs__autoinit(self) -> 'list[AppPolicyPackMetadata]':
        """Return ``policy_packs``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._policy_packs``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``policy_packs__safederef`` when you only want to read a default without mutating self.
        """
        if self._policy_packs is None:
            self._policy_packs = self.policy_packs__safederef

        return self._policy_packs

    @property
    def policy_packs__safederef(self) -> 'list[AppPolicyPackMetadata]':
        """Return ``policy_packs``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``policy_packs`` stays unset and each
        call returns a new default. Use ``policy_packs__autoinit`` to persist the default on
        first access.
        """
        if self._policy_packs is None:
            return []

        return self._policy_packs

    @property
    def requested_by_token(self) -> 'str':
        return self._requested_by_token

    @requested_by_token.setter
    def requested_by_token(self, requested_by_token: 'str'):
        self._requested_by_token = requested_by_token

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, UpdateInfo):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
