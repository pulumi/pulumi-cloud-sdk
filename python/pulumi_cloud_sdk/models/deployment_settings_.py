# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class DeploymentSettings(PulumiAutoModelEncoder):
    """
    :var version: int - declared
    :var tag: str - declared
    :var executor_context: ExecutorContext - declared
    :var source_context: SourceContext - declared
    :var git_hub: DeploymentSettingsGitHub - declared
    :var vcs: DeploymentSettingsVCS - declared
    :var operation_context: OperationContext - declared
    :var source: DeploymentSettingsSource - declared
    :var agent_pool_i_d: str - declared
    :var cache_options: CacheOptions - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'version': 'int',
        'tag': 'str',
        'executor_context': 'ExecutorContext',
        'source_context': 'SourceContext',
        'git_hub': 'DeploymentSettingsGitHub',
        'vcs': 'DeploymentSettingsVCS',
        'operation_context': 'OperationContext',
        'source': 'DeploymentSettingsSource',
        'agent_pool_i_d': 'str',
        'cache_options': 'CacheOptions',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'version': 'version',
        'tag': 'tag',
        'executor_context': 'executorContext',
        'source_context': 'sourceContext',
        'git_hub': 'gitHub',
        'vcs': 'vcs',
        'operation_context': 'operationContext',
        'source': 'source',
        'agent_pool_i_d': 'agentPoolID',
        'cache_options': 'cacheOptions',
    }

    FIELDS_version = 'version'
    FIELDS_tag = 'tag'
    FIELDS_executor_context = 'executorContext'
    FIELDS_source_context = 'sourceContext'
    FIELDS_git_hub = 'gitHub'
    FIELDS_vcs = 'vcs'
    FIELDS_operation_context = 'operationContext'
    FIELDS_source = 'source'
    FIELDS_agent_pool_i_d = 'agentPoolID'
    FIELDS_cache_options = 'cacheOptions'

    _version: 'int'
    _tag: 'str'
    _executor_context: 'ExecutorContext'
    _source_context: 'SourceContext'
    _git_hub: 'DeploymentSettingsGitHub'
    _vcs: 'DeploymentSettingsVCS'
    _operation_context: 'OperationContext'
    _source: 'DeploymentSettingsSource'
    _agent_pool_i_d: 'str'
    _cache_options: 'CacheOptions'

    def __init__(
        self,
        version: 'int' = 0,
        tag: 'str' = None,
        executor_context: 'ExecutorContext' = None,
        source_context: 'SourceContext' = None,
        git_hub: 'DeploymentSettingsGitHub' = None,
        vcs: 'DeploymentSettingsVCS' = None,
        operation_context: 'OperationContext' = None,
        source: 'DeploymentSettingsSource' = None,
        agent_pool_i_d: 'str' = None,
        cache_options: 'CacheOptions' = None,
    ) -> None:
        super().__init__()

        self.version = version
        self.tag = tag
        self.executor_context = executor_context
        self.source_context = source_context
        self.git_hub = git_hub
        self.vcs = vcs
        self.operation_context = operation_context
        self.source = source
        self.agent_pool_i_d = agent_pool_i_d
        self.cache_options = cache_options

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, DeploymentSettings):
            self._version = source._version
            self._tag = source._tag
            self._executor_context = source._executor_context
            self._source_context = source._source_context
            self._git_hub = source._git_hub
            self._vcs = source._vcs
            self._operation_context = source._operation_context
            self._source = source._source
            self._agent_pool_i_d = source._agent_pool_i_d
            self._cache_options = source._cache_options

    @property
    def version(self) -> 'int':
        return self._version

    @version.setter
    def version(self, version: 'int'):
        self._version = version

    @property
    def tag(self) -> 'str':
        return self._tag

    @tag.setter
    def tag(self, tag: 'str'):
        self._tag = tag

    @property
    def executor_context(self) -> 'ExecutorContext':
        return self._executor_context

    @executor_context.setter
    def executor_context(self, executor_context: 'ExecutorContext'):
        self._executor_context = executor_context

    @property
    def executor_context__autoinit(self) -> 'ExecutorContext':
        """Return ``executor_context``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._executor_context``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``executor_context__safederef`` when you only want to read a default without mutating self.
        """
        if self._executor_context is None:
            self._executor_context = self.executor_context__safederef

        return self._executor_context

    @property
    def executor_context__safederef(self) -> 'ExecutorContext':
        """Return ``executor_context``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``executor_context`` stays unset and each
        call returns a new default. Use ``executor_context__autoinit`` to persist the default on
        first access.
        """
        if self._executor_context is None:
            from .. import models
            return models.ExecutorContext._new_empty()

        return self._executor_context

    @property
    def source_context(self) -> 'SourceContext':
        return self._source_context

    @source_context.setter
    def source_context(self, source_context: 'SourceContext'):
        self._source_context = source_context

    @property
    def source_context__autoinit(self) -> 'SourceContext':
        """Return ``source_context``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._source_context``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``source_context__safederef`` when you only want to read a default without mutating self.
        """
        if self._source_context is None:
            self._source_context = self.source_context__safederef

        return self._source_context

    @property
    def source_context__safederef(self) -> 'SourceContext':
        """Return ``source_context``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``source_context`` stays unset and each
        call returns a new default. Use ``source_context__autoinit`` to persist the default on
        first access.
        """
        if self._source_context is None:
            from .. import models
            return models.SourceContext._new_empty()

        return self._source_context

    @property
    def git_hub(self) -> 'DeploymentSettingsGitHub':
        return self._git_hub

    @git_hub.setter
    def git_hub(self, git_hub: 'DeploymentSettingsGitHub'):
        self._git_hub = git_hub

    @property
    def git_hub__autoinit(self) -> 'DeploymentSettingsGitHub':
        """Return ``git_hub``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._git_hub``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``git_hub__safederef`` when you only want to read a default without mutating self.
        """
        if self._git_hub is None:
            self._git_hub = self.git_hub__safederef

        return self._git_hub

    @property
    def git_hub__safederef(self) -> 'DeploymentSettingsGitHub':
        """Return ``git_hub``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``git_hub`` stays unset and each
        call returns a new default. Use ``git_hub__autoinit`` to persist the default on
        first access.
        """
        if self._git_hub is None:
            from .. import models
            return models.DeploymentSettingsGitHub._new_empty()

        return self._git_hub

    @property
    def vcs(self) -> 'DeploymentSettingsVCS':
        return self._vcs

    @vcs.setter
    def vcs(self, vcs: 'DeploymentSettingsVCS'):
        self._vcs = vcs

    @property
    def operation_context(self) -> 'OperationContext':
        return self._operation_context

    @operation_context.setter
    def operation_context(self, operation_context: 'OperationContext'):
        self._operation_context = operation_context

    @property
    def operation_context__autoinit(self) -> 'OperationContext':
        """Return ``operation_context``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._operation_context``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``operation_context__safederef`` when you only want to read a default without mutating self.
        """
        if self._operation_context is None:
            self._operation_context = self.operation_context__safederef

        return self._operation_context

    @property
    def operation_context__safederef(self) -> 'OperationContext':
        """Return ``operation_context``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``operation_context`` stays unset and each
        call returns a new default. Use ``operation_context__autoinit`` to persist the default on
        first access.
        """
        if self._operation_context is None:
            from .. import models
            return models.OperationContext._new_empty()

        return self._operation_context

    @property
    def source(self) -> 'DeploymentSettingsSource':
        return self._source

    @source.setter
    def source(self, source: 'DeploymentSettingsSource'):
        self._source = source

    @property
    def agent_pool_i_d(self) -> 'str':
        return self._agent_pool_i_d

    @agent_pool_i_d.setter
    def agent_pool_i_d(self, agent_pool_i_d: 'str'):
        self._agent_pool_i_d = agent_pool_i_d

    @property
    def cache_options(self) -> 'CacheOptions':
        return self._cache_options

    @cache_options.setter
    def cache_options(self, cache_options: 'CacheOptions'):
        self._cache_options = cache_options

    @property
    def cache_options__autoinit(self) -> 'CacheOptions':
        """Return ``cache_options``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._cache_options``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``cache_options__safederef`` when you only want to read a default without mutating self.
        """
        if self._cache_options is None:
            self._cache_options = self.cache_options__safederef

        return self._cache_options

    @property
    def cache_options__safederef(self) -> 'CacheOptions':
        """Return ``cache_options``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``cache_options`` stays unset and each
        call returns a new default. Use ``cache_options__autoinit`` to persist the default on
        first access.
        """
        if self._cache_options is None:
            from .. import models
            return models.CacheOptions._new_empty()

        return self._cache_options

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, DeploymentSettings):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
