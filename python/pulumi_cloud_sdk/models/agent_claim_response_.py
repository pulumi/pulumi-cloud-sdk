# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AgentClaimResponse(PulumiAutoModelEncoder):
    """
    :var agent: AgentAccountSummary - declared
    :var entities: list[TransferEntity] - declared
    :var conflicts: list[TransferEntity] - declared
    :var failures: list[TransferEntityFailure] - declared
    :var claim_expires_at: datetime - declared
    :var transfer_token: str - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'agent': 'AgentAccountSummary',
        'entities': 'list[TransferEntity]',
        'conflicts': 'list[TransferEntity]',
        'failures': 'list[TransferEntityFailure]',
        'claim_expires_at': 'datetime',
        'transfer_token': 'str',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'agent': 'agent',
        'entities': 'entities',
        'conflicts': 'conflicts',
        'failures': 'failures',
        'claim_expires_at': 'claimExpiresAt',
        'transfer_token': 'transferToken',
    }

    FIELDS_agent = 'agent'
    FIELDS_entities = 'entities'
    FIELDS_conflicts = 'conflicts'
    FIELDS_failures = 'failures'
    FIELDS_claim_expires_at = 'claimExpiresAt'
    FIELDS_transfer_token = 'transferToken'

    _agent: 'AgentAccountSummary'
    _entities: 'list[TransferEntity]'
    _conflicts: 'list[TransferEntity]'
    _failures: 'list[TransferEntityFailure]'
    _claim_expires_at: 'datetime'
    _transfer_token: 'str'

    def __init__(
        self,
        agent: 'AgentAccountSummary',
        entities: 'list[TransferEntity]',
        conflicts: 'list[TransferEntity]',
        failures: 'list[TransferEntityFailure]',
        claim_expires_at: 'datetime',
        transfer_token: 'str' = None,
    ) -> None:
        super().__init__()

        self.agent = agent
        self.entities = entities
        self.conflicts = conflicts
        self.failures = failures
        self.claim_expires_at = claim_expires_at
        self.transfer_token = transfer_token

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AgentClaimResponse):
            self._agent = source._agent
            self._entities = source._entities
            self._conflicts = source._conflicts
            self._failures = source._failures
            self._claim_expires_at = source._claim_expires_at
            self._transfer_token = source._transfer_token

    @property
    def agent(self) -> 'AgentAccountSummary':
        return self._agent

    @agent.setter
    def agent(self, agent: 'AgentAccountSummary'):
        if agent is None:
            raise ValueError("Invalid value for `agent`, must not be `None`")

        self._agent = agent

    @property
    def agent__autoinit(self) -> 'AgentAccountSummary':
        """Return ``agent``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._agent``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``agent__safederef`` when you only want to read a default without mutating self.
        """
        if self._agent is None:
            self._agent = self.agent__safederef

        return self._agent

    @property
    def agent__safederef(self) -> 'AgentAccountSummary':
        """Return ``agent``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``agent`` stays unset and each
        call returns a new default. Use ``agent__autoinit`` to persist the default on
        first access.
        """
        if self._agent is None:
            from .. import models
            return models.AgentAccountSummary._new_empty()

        return self._agent

    @property
    def entities(self) -> 'list[TransferEntity]':
        return self._entities

    @entities.setter
    def entities(self, entities: 'list[TransferEntity]'):
        if entities is None:
            raise ValueError("Invalid value for `entities`, must not be `None`")

        self._entities = entities

    @property
    def entities__autoinit(self) -> 'list[TransferEntity]':
        """Return ``entities``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._entities``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``entities__safederef`` when you only want to read a default without mutating self.
        """
        if self._entities is None:
            self._entities = self.entities__safederef

        return self._entities

    @property
    def entities__safederef(self) -> 'list[TransferEntity]':
        """Return ``entities``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``entities`` stays unset and each
        call returns a new default. Use ``entities__autoinit`` to persist the default on
        first access.
        """
        if self._entities is None:
            return []

        return self._entities

    @property
    def conflicts(self) -> 'list[TransferEntity]':
        return self._conflicts

    @conflicts.setter
    def conflicts(self, conflicts: 'list[TransferEntity]'):
        if conflicts is None:
            raise ValueError("Invalid value for `conflicts`, must not be `None`")

        self._conflicts = conflicts

    @property
    def conflicts__autoinit(self) -> 'list[TransferEntity]':
        """Return ``conflicts``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._conflicts``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``conflicts__safederef`` when you only want to read a default without mutating self.
        """
        if self._conflicts is None:
            self._conflicts = self.conflicts__safederef

        return self._conflicts

    @property
    def conflicts__safederef(self) -> 'list[TransferEntity]':
        """Return ``conflicts``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``conflicts`` stays unset and each
        call returns a new default. Use ``conflicts__autoinit`` to persist the default on
        first access.
        """
        if self._conflicts is None:
            return []

        return self._conflicts

    @property
    def failures(self) -> 'list[TransferEntityFailure]':
        return self._failures

    @failures.setter
    def failures(self, failures: 'list[TransferEntityFailure]'):
        if failures is None:
            raise ValueError("Invalid value for `failures`, must not be `None`")

        self._failures = failures

    @property
    def failures__autoinit(self) -> 'list[TransferEntityFailure]':
        """Return ``failures``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._failures``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``failures__safederef`` when you only want to read a default without mutating self.
        """
        if self._failures is None:
            self._failures = self.failures__safederef

        return self._failures

    @property
    def failures__safederef(self) -> 'list[TransferEntityFailure]':
        """Return ``failures``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``failures`` stays unset and each
        call returns a new default. Use ``failures__autoinit`` to persist the default on
        first access.
        """
        if self._failures is None:
            return []

        return self._failures

    @property
    def claim_expires_at(self) -> 'datetime':
        return self._claim_expires_at

    @claim_expires_at.setter
    def claim_expires_at(self, claim_expires_at: 'datetime'):
        if claim_expires_at is None:
            raise ValueError("Invalid value for `claim_expires_at`, must not be `None`")

        self._claim_expires_at = claim_expires_at

    @property
    def transfer_token(self) -> 'str':
        return self._transfer_token

    @transfer_token.setter
    def transfer_token(self, transfer_token: 'str'):
        self._transfer_token = transfer_token

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AgentClaimResponse):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
