# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class EnvironmentReferrer(PulumiAutoModelEncoder):
    """
    :var environment: EnvironmentImportReferrer - declared
    :var stack: EnvironmentStackReferrer - declared
    :var insights_account: EnvironmentInsightsAccountReferrer - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'environment': 'EnvironmentImportReferrer',
        'stack': 'EnvironmentStackReferrer',
        'insights_account': 'EnvironmentInsightsAccountReferrer',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'environment': 'environment',
        'stack': 'stack',
        'insights_account': 'insightsAccount',
    }

    FIELDS_environment = 'environment'
    FIELDS_stack = 'stack'
    FIELDS_insights_account = 'insightsAccount'

    _environment: 'EnvironmentImportReferrer'
    _stack: 'EnvironmentStackReferrer'
    _insights_account: 'EnvironmentInsightsAccountReferrer'

    def __init__(
        self,
        environment: 'EnvironmentImportReferrer' = None,
        stack: 'EnvironmentStackReferrer' = None,
        insights_account: 'EnvironmentInsightsAccountReferrer' = None,
    ) -> None:
        super().__init__()

        self.environment = environment
        self.stack = stack
        self.insights_account = insights_account

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, EnvironmentReferrer):
            self._environment = source._environment
            self._stack = source._stack
            self._insights_account = source._insights_account

    @property
    def environment(self) -> 'EnvironmentImportReferrer':
        return self._environment

    @environment.setter
    def environment(self, environment: 'EnvironmentImportReferrer'):
        self._environment = environment

    @property
    def environment__autoinit(self) -> 'EnvironmentImportReferrer':
        """Return ``environment``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._environment``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``environment__safederef`` when you only want to read a default without mutating self.
        """
        if self._environment is None:
            self._environment = self.environment__safederef

        return self._environment

    @property
    def environment__safederef(self) -> 'EnvironmentImportReferrer':
        """Return ``environment``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``environment`` stays unset and each
        call returns a new default. Use ``environment__autoinit`` to persist the default on
        first access.
        """
        if self._environment is None:
            from .. import models
            return models.EnvironmentImportReferrer._new_empty()

        return self._environment

    @property
    def stack(self) -> 'EnvironmentStackReferrer':
        return self._stack

    @stack.setter
    def stack(self, stack: 'EnvironmentStackReferrer'):
        self._stack = stack

    @property
    def stack__autoinit(self) -> 'EnvironmentStackReferrer':
        """Return ``stack``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._stack``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``stack__safederef`` when you only want to read a default without mutating self.
        """
        if self._stack is None:
            self._stack = self.stack__safederef

        return self._stack

    @property
    def stack__safederef(self) -> 'EnvironmentStackReferrer':
        """Return ``stack``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``stack`` stays unset and each
        call returns a new default. Use ``stack__autoinit`` to persist the default on
        first access.
        """
        if self._stack is None:
            from .. import models
            return models.EnvironmentStackReferrer._new_empty()

        return self._stack

    @property
    def insights_account(self) -> 'EnvironmentInsightsAccountReferrer':
        return self._insights_account

    @insights_account.setter
    def insights_account(self, insights_account: 'EnvironmentInsightsAccountReferrer'):
        self._insights_account = insights_account

    @property
    def insights_account__autoinit(self) -> 'EnvironmentInsightsAccountReferrer':
        """Return ``insights_account``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._insights_account``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``insights_account__safederef`` when you only want to read a default without mutating self.
        """
        if self._insights_account is None:
            self._insights_account = self.insights_account__safederef

        return self._insights_account

    @property
    def insights_account__safederef(self) -> 'EnvironmentInsightsAccountReferrer':
        """Return ``insights_account``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``insights_account`` stays unset and each
        call returns a new default. Use ``insights_account__autoinit`` to persist the default on
        first access.
        """
        if self._insights_account is None:
            from .. import models
            return models.EnvironmentInsightsAccountReferrer._new_empty()

        return self._insights_account

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, EnvironmentReferrer):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
