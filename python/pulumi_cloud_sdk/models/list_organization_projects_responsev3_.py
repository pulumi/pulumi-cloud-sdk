# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class ListOrganizationProjectsResponsev3(PulumiAutoModelEncoder):
    """
    :var row_data: list[dict[str, Any]] - declared
    :var row_count: int - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'row_data': 'list[dict[str, Any]]',
        'row_count': 'int',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'row_data': 'rowData',
        'row_count': 'rowCount',
    }

    FIELDS_row_data = 'rowData'
    FIELDS_row_count = 'rowCount'

    _row_data: 'list[dict[str, Any]]'
    _row_count: 'int'

    def __init__(
        self,
        row_data: 'list[dict[str, Any]]',
        row_count: 'int' = 0,
    ) -> None:
        super().__init__()

        self.row_data = row_data
        self.row_count = row_count

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, ListOrganizationProjectsResponsev3):
            self._row_data = source._row_data
            self._row_count = source._row_count

    @property
    def row_data(self) -> 'list[dict[str, Any]]':
        return self._row_data

    @row_data.setter
    def row_data(self, row_data: 'list[dict[str, Any]]'):
        if row_data is None:
            raise ValueError("Invalid value for `row_data`, must not be `None`")

        self._row_data = row_data

    @property
    def row_data__autoinit(self) -> 'list[dict[str, Any]]':
        """Return ``row_data``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._row_data``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``row_data__safederef`` when you only want to read a default without mutating self.
        """
        if self._row_data is None:
            self._row_data = self.row_data__safederef

        return self._row_data

    @property
    def row_data__safederef(self) -> 'list[dict[str, Any]]':
        """Return ``row_data``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``row_data`` stays unset and each
        call returns a new default. Use ``row_data__autoinit`` to persist the default on
        first access.
        """
        if self._row_data is None:
            return []

        return self._row_data

    @property
    def row_count(self) -> 'int':
        return self._row_count

    @row_count.setter
    def row_count(self, row_count: 'int'):
        self._row_count = row_count

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, ListOrganizationProjectsResponsev3):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
