# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GetResourceCountSummaryResponse(PulumiAutoModelEncoder):
    """
    :var summary: list[ResourceCountSummary] - declared
    :var tag_keys: list[str] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'summary': 'list[ResourceCountSummary]',
        'tag_keys': 'list[str]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'summary': 'summary',
        'tag_keys': 'tagKeys',
    }

    FIELDS_summary = 'summary'
    FIELDS_tag_keys = 'tagKeys'

    _summary: 'list[ResourceCountSummary]'
    _tag_keys: 'list[str]'

    def __init__(
        self,
        summary: 'list[ResourceCountSummary]',
        tag_keys: 'list[str]' = None,
    ) -> None:
        super().__init__()

        self.summary = summary
        self.tag_keys = tag_keys

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GetResourceCountSummaryResponse):
            self._summary = source._summary
            self._tag_keys = source._tag_keys

    @property
    def summary(self) -> 'list[ResourceCountSummary]':
        return self._summary

    @summary.setter
    def summary(self, summary: 'list[ResourceCountSummary]'):
        if summary is None:
            raise ValueError("Invalid value for `summary`, must not be `None`")

        self._summary = summary

    @property
    def summary__autoinit(self) -> 'list[ResourceCountSummary]':
        """Return ``summary``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._summary``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``summary__safederef`` when you only want to read a default without mutating self.
        """
        if self._summary is None:
            self._summary = self.summary__safederef

        return self._summary

    @property
    def summary__safederef(self) -> 'list[ResourceCountSummary]':
        """Return ``summary``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``summary`` stays unset and each
        call returns a new default. Use ``summary__autoinit`` to persist the default on
        first access.
        """
        if self._summary is None:
            return []

        return self._summary

    @property
    def tag_keys(self) -> 'list[str]':
        return self._tag_keys

    @tag_keys.setter
    def tag_keys(self, tag_keys: 'list[str]'):
        self._tag_keys = tag_keys

    @property
    def tag_keys__autoinit(self) -> 'list[str]':
        """Return ``tag_keys``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._tag_keys``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``tag_keys__safederef`` when you only want to read a default without mutating self.
        """
        if self._tag_keys is None:
            self._tag_keys = self.tag_keys__safederef

        return self._tag_keys

    @property
    def tag_keys__safederef(self) -> 'list[str]':
        """Return ``tag_keys``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``tag_keys`` stays unset and each
        call returns a new default. Use ``tag_keys__autoinit`` to persist the default on
        first access.
        """
        if self._tag_keys is None:
            return []

        return self._tag_keys

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GetResourceCountSummaryResponse):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
