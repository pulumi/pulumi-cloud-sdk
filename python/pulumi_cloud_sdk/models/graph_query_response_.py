# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GraphQueryResponse(PulumiAutoModelEncoder):
    """
    :var nodes: list[GraphNode] - declared
    :var edges: list[GraphEdge] - declared
    :var paths: list[GraphPath] - declared
    :var aggregations: GraphAggregations - declared
    :var page_info: GraphPageInfo - declared
    :var meta: GraphQueryMeta - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'nodes': 'list[GraphNode]',
        'edges': 'list[GraphEdge]',
        'paths': 'list[GraphPath]',
        'aggregations': 'GraphAggregations',
        'page_info': 'GraphPageInfo',
        'meta': 'GraphQueryMeta',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'nodes': 'nodes',
        'edges': 'edges',
        'paths': 'paths',
        'aggregations': 'aggregations',
        'page_info': 'pageInfo',
        'meta': 'meta',
    }

    FIELDS_nodes = 'nodes'
    FIELDS_edges = 'edges'
    FIELDS_paths = 'paths'
    FIELDS_aggregations = 'aggregations'
    FIELDS_page_info = 'pageInfo'
    FIELDS_meta = 'meta'

    _nodes: 'list[GraphNode]'
    _edges: 'list[GraphEdge]'
    _paths: 'list[GraphPath]'
    _aggregations: 'GraphAggregations'
    _page_info: 'GraphPageInfo'
    _meta: 'GraphQueryMeta'

    def __init__(
        self,
        nodes: 'list[GraphNode]',
        edges: 'list[GraphEdge]',
        aggregations: 'GraphAggregations',
        page_info: 'GraphPageInfo',
        meta: 'GraphQueryMeta',
        paths: 'list[GraphPath]' = None,
    ) -> None:
        super().__init__()

        self.nodes = nodes
        self.edges = edges
        self.paths = paths
        self.aggregations = aggregations
        self.page_info = page_info
        self.meta = meta

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GraphQueryResponse):
            self._nodes = source._nodes
            self._edges = source._edges
            self._paths = source._paths
            self._aggregations = source._aggregations
            self._page_info = source._page_info
            self._meta = source._meta

    @property
    def nodes(self) -> 'list[GraphNode]':
        return self._nodes

    @nodes.setter
    def nodes(self, nodes: 'list[GraphNode]'):
        if nodes is None:
            raise ValueError("Invalid value for `nodes`, must not be `None`")

        self._nodes = nodes

    @property
    def nodes__autoinit(self) -> 'list[GraphNode]':
        """Return ``nodes``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._nodes``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``nodes__safederef`` when you only want to read a default without mutating self.
        """
        if self._nodes is None:
            self._nodes = self.nodes__safederef

        return self._nodes

    @property
    def nodes__safederef(self) -> 'list[GraphNode]':
        """Return ``nodes``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``nodes`` stays unset and each
        call returns a new default. Use ``nodes__autoinit`` to persist the default on
        first access.
        """
        if self._nodes is None:
            return []

        return self._nodes

    @property
    def edges(self) -> 'list[GraphEdge]':
        return self._edges

    @edges.setter
    def edges(self, edges: 'list[GraphEdge]'):
        if edges is None:
            raise ValueError("Invalid value for `edges`, must not be `None`")

        self._edges = edges

    @property
    def edges__autoinit(self) -> 'list[GraphEdge]':
        """Return ``edges``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._edges``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``edges__safederef`` when you only want to read a default without mutating self.
        """
        if self._edges is None:
            self._edges = self.edges__safederef

        return self._edges

    @property
    def edges__safederef(self) -> 'list[GraphEdge]':
        """Return ``edges``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``edges`` stays unset and each
        call returns a new default. Use ``edges__autoinit`` to persist the default on
        first access.
        """
        if self._edges is None:
            return []

        return self._edges

    @property
    def paths(self) -> 'list[GraphPath]':
        return self._paths

    @paths.setter
    def paths(self, paths: 'list[GraphPath]'):
        self._paths = paths

    @property
    def paths__autoinit(self) -> 'list[GraphPath]':
        """Return ``paths``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._paths``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``paths__safederef`` when you only want to read a default without mutating self.
        """
        if self._paths is None:
            self._paths = self.paths__safederef

        return self._paths

    @property
    def paths__safederef(self) -> 'list[GraphPath]':
        """Return ``paths``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``paths`` stays unset and each
        call returns a new default. Use ``paths__autoinit`` to persist the default on
        first access.
        """
        if self._paths is None:
            return []

        return self._paths

    @property
    def aggregations(self) -> 'GraphAggregations':
        return self._aggregations

    @aggregations.setter
    def aggregations(self, aggregations: 'GraphAggregations'):
        if aggregations is None:
            raise ValueError("Invalid value for `aggregations`, must not be `None`")

        self._aggregations = aggregations

    @property
    def aggregations__autoinit(self) -> 'GraphAggregations':
        """Return ``aggregations``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._aggregations``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``aggregations__safederef`` when you only want to read a default without mutating self.
        """
        if self._aggregations is None:
            self._aggregations = self.aggregations__safederef

        return self._aggregations

    @property
    def aggregations__safederef(self) -> 'GraphAggregations':
        """Return ``aggregations``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``aggregations`` stays unset and each
        call returns a new default. Use ``aggregations__autoinit`` to persist the default on
        first access.
        """
        if self._aggregations is None:
            from .. import models
            return models.GraphAggregations._new_empty()

        return self._aggregations

    @property
    def page_info(self) -> 'GraphPageInfo':
        return self._page_info

    @page_info.setter
    def page_info(self, page_info: 'GraphPageInfo'):
        if page_info is None:
            raise ValueError("Invalid value for `page_info`, must not be `None`")

        self._page_info = page_info

    @property
    def page_info__autoinit(self) -> 'GraphPageInfo':
        """Return ``page_info``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._page_info``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``page_info__safederef`` when you only want to read a default without mutating self.
        """
        if self._page_info is None:
            self._page_info = self.page_info__safederef

        return self._page_info

    @property
    def page_info__safederef(self) -> 'GraphPageInfo':
        """Return ``page_info``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``page_info`` stays unset and each
        call returns a new default. Use ``page_info__autoinit`` to persist the default on
        first access.
        """
        if self._page_info is None:
            from .. import models
            return models.GraphPageInfo._new_empty()

        return self._page_info

    @property
    def meta(self) -> 'GraphQueryMeta':
        return self._meta

    @meta.setter
    def meta(self, meta: 'GraphQueryMeta'):
        if meta is None:
            raise ValueError("Invalid value for `meta`, must not be `None`")

        self._meta = meta

    @property
    def meta__autoinit(self) -> 'GraphQueryMeta':
        """Return ``meta``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._meta``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``meta__safederef`` when you only want to read a default without mutating self.
        """
        if self._meta is None:
            self._meta = self.meta__safederef

        return self._meta

    @property
    def meta__safederef(self) -> 'GraphQueryMeta':
        """Return ``meta``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``meta`` stays unset and each
        call returns a new default. Use ``meta__autoinit`` to persist the default on
        first access.
        """
        if self._meta is None:
            from .. import models
            return models.GraphQueryMeta._new_empty()

        return self._meta

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GraphQueryResponse):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
