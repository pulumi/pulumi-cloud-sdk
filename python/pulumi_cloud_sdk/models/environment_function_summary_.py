# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class EnvironmentFunctionSummary(PulumiAutoModelEncoder):
    """
    :var func_counts: dict[str, int] - declared
    :var rotation_paths: list[str] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'func_counts': 'dict[str, int]',
        'rotation_paths': 'list[str]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'func_counts': 'funcCounts',
        'rotation_paths': 'rotationPaths',
    }

    FIELDS_func_counts = 'funcCounts'
    FIELDS_rotation_paths = 'rotationPaths'

    _func_counts: 'dict[str, int]'
    _rotation_paths: 'list[str]'

    def __init__(
        self,
        func_counts: 'dict[str, int]',
        rotation_paths: 'list[str]',
    ) -> None:
        super().__init__()

        self.func_counts = func_counts
        self.rotation_paths = rotation_paths

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, EnvironmentFunctionSummary):
            self._func_counts = source._func_counts
            self._rotation_paths = source._rotation_paths

    @property
    def func_counts(self) -> 'dict[str, int]':
        return self._func_counts

    @func_counts.setter
    def func_counts(self, func_counts: 'dict[str, int]'):
        if func_counts is None:
            raise ValueError("Invalid value for `func_counts`, must not be `None`")

        self._func_counts = func_counts

    @property
    def func_counts__autoinit(self) -> 'dict[str, int]':
        """Return ``func_counts``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._func_counts``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``func_counts__safederef`` when you only want to read a default without mutating self.
        """
        if self._func_counts is None:
            self._func_counts = self.func_counts__safederef

        return self._func_counts

    @property
    def func_counts__safederef(self) -> 'dict[str, int]':
        """Return ``func_counts``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``func_counts`` stays unset and each
        call returns a new default. Use ``func_counts__autoinit`` to persist the default on
        first access.
        """
        if self._func_counts is None:
            return {}

        return self._func_counts

    @property
    def rotation_paths(self) -> 'list[str]':
        return self._rotation_paths

    @rotation_paths.setter
    def rotation_paths(self, rotation_paths: 'list[str]'):
        if rotation_paths is None:
            raise ValueError("Invalid value for `rotation_paths`, must not be `None`")

        self._rotation_paths = rotation_paths

    @property
    def rotation_paths__autoinit(self) -> 'list[str]':
        """Return ``rotation_paths``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._rotation_paths``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``rotation_paths__safederef`` when you only want to read a default without mutating self.
        """
        if self._rotation_paths is None:
            self._rotation_paths = self.rotation_paths__safederef

        return self._rotation_paths

    @property
    def rotation_paths__safederef(self) -> 'list[str]':
        """Return ``rotation_paths``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``rotation_paths`` stays unset and each
        call returns a new default. Use ``rotation_paths__autoinit`` to persist the default on
        first access.
        """
        if self._rotation_paths is None:
            return []

        return self._rotation_paths

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, EnvironmentFunctionSummary):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
