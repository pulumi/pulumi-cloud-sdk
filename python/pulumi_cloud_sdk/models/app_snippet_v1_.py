# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AppSnippetV1(PulumiAutoModelEncoder):
    """
    :var uuid: str - declared
    :var name: str - declared
    :var type: str - declared
    :var code: str - declared
    :var descriptor: AppPackageDescriptorV1 - declared
    :var references: dict[str, str] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'uuid': 'str',
        'name': 'str',
        'type': 'str',
        'code': 'str',
        'descriptor': 'AppPackageDescriptorV1',
        'references': 'dict[str, str]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'uuid': 'uuid',
        'name': 'name',
        'type': 'type',
        'code': 'code',
        'descriptor': 'descriptor',
        'references': 'references',
    }

    FIELDS_uuid = 'uuid'
    FIELDS_name = 'name'
    FIELDS_type = 'type'
    FIELDS_code = 'code'
    FIELDS_descriptor = 'descriptor'
    FIELDS_references = 'references'

    _uuid: 'str'
    _name: 'str'
    _type: 'str'
    _code: 'str'
    _descriptor: 'AppPackageDescriptorV1'
    _references: 'dict[str, str]'

    def __init__(
        self,
        uuid: 'str',
        name: 'str',
        type: 'str',
        code: 'str',
        descriptor: 'AppPackageDescriptorV1',
        references: 'dict[str, str]' = None,
    ) -> None:
        super().__init__()

        self.uuid = uuid
        self.name = name
        self.type = type
        self.code = code
        self.descriptor = descriptor
        self.references = references

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AppSnippetV1):
            self._uuid = source._uuid
            self._name = source._name
            self._type = source._type
            self._code = source._code
            self._descriptor = source._descriptor
            self._references = source._references

    @property
    def uuid(self) -> 'str':
        return self._uuid

    @uuid.setter
    def uuid(self, uuid: 'str'):
        if uuid is None:
            raise ValueError("Invalid value for `uuid`, must not be `None`")

        self._uuid = uuid

    @property
    def name(self) -> 'str':
        return self._name

    @name.setter
    def name(self, name: 'str'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def type(self) -> 'str':
        return self._type

    @type.setter
    def type(self, type: 'str'):
        if type is None:
            raise ValueError("Invalid value for `type`, must not be `None`")

        self._type = type

    @property
    def code(self) -> 'str':
        return self._code

    @code.setter
    def code(self, code: 'str'):
        if code is None:
            raise ValueError("Invalid value for `code`, must not be `None`")

        self._code = code

    @property
    def descriptor(self) -> 'AppPackageDescriptorV1':
        return self._descriptor

    @descriptor.setter
    def descriptor(self, descriptor: 'AppPackageDescriptorV1'):
        if descriptor is None:
            raise ValueError("Invalid value for `descriptor`, must not be `None`")

        self._descriptor = descriptor

    @property
    def descriptor__autoinit(self) -> 'AppPackageDescriptorV1':
        """Return ``descriptor``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._descriptor``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``descriptor__safederef`` when you only want to read a default without mutating self.
        """
        if self._descriptor is None:
            self._descriptor = self.descriptor__safederef

        return self._descriptor

    @property
    def descriptor__safederef(self) -> 'AppPackageDescriptorV1':
        """Return ``descriptor``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``descriptor`` stays unset and each
        call returns a new default. Use ``descriptor__autoinit`` to persist the default on
        first access.
        """
        if self._descriptor is None:
            from .. import models
            return models.AppPackageDescriptorV1._new_empty()

        return self._descriptor

    @property
    def references(self) -> 'dict[str, str]':
        return self._references

    @references.setter
    def references(self, references: 'dict[str, str]'):
        self._references = references

    @property
    def references__autoinit(self) -> 'dict[str, str]':
        """Return ``references``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._references``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``references__safederef`` when you only want to read a default without mutating self.
        """
        if self._references is None:
            self._references = self.references__safederef

        return self._references

    @property
    def references__safederef(self) -> 'dict[str, str]':
        """Return ``references``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``references`` stays unset and each
        call returns a new default. Use ``references__autoinit`` to persist the default on
        first access.
        """
        if self._references is None:
            return {}

        return self._references

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AppSnippetV1):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
