# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class EscEnvironment(PulumiAutoModelEncoder):
    """
    :var exprs: dict[str, EscExpr] - declared
    :var properties: dict[str, EscValue] - declared
    :var schema: EscSchemaSchema - declared
    :var execution_context: EscEvaluatedExecutionContext - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'exprs': 'dict[str, EscExpr]',
        'properties': 'dict[str, EscValue]',
        'schema': 'EscSchemaSchema',
        'execution_context': 'EscEvaluatedExecutionContext',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'exprs': 'exprs',
        'properties': 'properties',
        'schema': 'schema',
        'execution_context': 'executionContext',
    }

    FIELDS_exprs = 'exprs'
    FIELDS_properties = 'properties'
    FIELDS_schema = 'schema'
    FIELDS_execution_context = 'executionContext'

    _exprs: 'dict[str, EscExpr]'
    _properties: 'dict[str, EscValue]'
    _schema: 'EscSchemaSchema'
    _execution_context: 'EscEvaluatedExecutionContext'

    def __init__(
        self,
        exprs: 'dict[str, EscExpr]' = None,
        properties: 'dict[str, EscValue]' = None,
        schema: 'EscSchemaSchema' = None,
        execution_context: 'EscEvaluatedExecutionContext' = None,
    ) -> None:
        super().__init__()

        self.exprs = exprs
        self.properties = properties
        self.schema = schema
        self.execution_context = execution_context

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, EscEnvironment):
            self._exprs = source._exprs
            self._properties = source._properties
            self._schema = source._schema
            self._execution_context = source._execution_context

    @property
    def exprs(self) -> 'dict[str, EscExpr]':
        return self._exprs

    @exprs.setter
    def exprs(self, exprs: 'dict[str, EscExpr]'):
        self._exprs = exprs

    @property
    def exprs__autoinit(self) -> 'dict[str, EscExpr]':
        """Return ``exprs``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._exprs``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``exprs__safederef`` when you only want to read a default without mutating self.
        """
        if self._exprs is None:
            self._exprs = self.exprs__safederef

        return self._exprs

    @property
    def exprs__safederef(self) -> 'dict[str, EscExpr]':
        """Return ``exprs``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``exprs`` stays unset and each
        call returns a new default. Use ``exprs__autoinit`` to persist the default on
        first access.
        """
        if self._exprs is None:
            return {}

        return self._exprs

    @property
    def properties(self) -> 'dict[str, EscValue]':
        return self._properties

    @properties.setter
    def properties(self, properties: 'dict[str, EscValue]'):
        self._properties = properties

    @property
    def properties__autoinit(self) -> 'dict[str, EscValue]':
        """Return ``properties``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._properties``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``properties__safederef`` when you only want to read a default without mutating self.
        """
        if self._properties is None:
            self._properties = self.properties__safederef

        return self._properties

    @property
    def properties__safederef(self) -> 'dict[str, EscValue]':
        """Return ``properties``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``properties`` stays unset and each
        call returns a new default. Use ``properties__autoinit`` to persist the default on
        first access.
        """
        if self._properties is None:
            return {}

        return self._properties

    @property
    def schema(self) -> 'EscSchemaSchema':
        return self._schema

    @schema.setter
    def schema(self, schema: 'EscSchemaSchema'):
        self._schema = schema

    @property
    def schema__autoinit(self) -> 'EscSchemaSchema':
        """Return ``schema``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._schema``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``schema__safederef`` when you only want to read a default without mutating self.
        """
        if self._schema is None:
            self._schema = self.schema__safederef

        return self._schema

    @property
    def schema__safederef(self) -> 'EscSchemaSchema':
        """Return ``schema``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``schema`` stays unset and each
        call returns a new default. Use ``schema__autoinit`` to persist the default on
        first access.
        """
        if self._schema is None:
            from .. import models
            return models.EscSchemaSchema._new_empty()

        return self._schema

    @property
    def execution_context(self) -> 'EscEvaluatedExecutionContext':
        return self._execution_context

    @execution_context.setter
    def execution_context(self, execution_context: 'EscEvaluatedExecutionContext'):
        self._execution_context = execution_context

    @property
    def execution_context__autoinit(self) -> 'EscEvaluatedExecutionContext':
        """Return ``execution_context``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._execution_context``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``execution_context__safederef`` when you only want to read a default without mutating self.
        """
        if self._execution_context is None:
            self._execution_context = self.execution_context__safederef

        return self._execution_context

    @property
    def execution_context__safederef(self) -> 'EscEvaluatedExecutionContext':
        """Return ``execution_context``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``execution_context`` stays unset and each
        call returns a new default. Use ``execution_context__autoinit`` to persist the default on
        first access.
        """
        if self._execution_context is None:
            from .. import models
            return models.EscEvaluatedExecutionContext._new_empty()

        return self._execution_context

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, EscEnvironment):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
