# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class EscTrace(PulumiAutoModelEncoder):
    """
    :var def_: EscRange - declared
    :var base: EscValue - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'def_': 'EscRange',
        'base': 'EscValue',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'def_': 'def',
        'base': 'base',
    }

    FIELDS_def_ = 'def'
    FIELDS_base = 'base'

    _def_: 'EscRange'
    _base: 'EscValue'

    def __init__(
        self,
        def_: 'EscRange',
        base: 'EscValue' = None,
    ) -> None:
        super().__init__()

        self.def_ = def_
        self.base = base

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, EscTrace):
            self._def_ = source._def_
            self._base = source._base

    @property
    def def_(self) -> 'EscRange':
        return self._def_

    @def_.setter
    def def_(self, def_: 'EscRange'):
        if def_ is None:
            raise ValueError("Invalid value for `def_`, must not be `None`")

        self._def_ = def_

    @property
    def def___autoinit(self) -> 'EscRange':
        """Return ``def_``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._def_``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``def___safederef`` when you only want to read a default without mutating self.
        """
        if self._def_ is None:
            self._def_ = self.def___safederef

        return self._def_

    @property
    def def___safederef(self) -> 'EscRange':
        """Return ``def_``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``def_`` stays unset and each
        call returns a new default. Use ``def___autoinit`` to persist the default on
        first access.
        """
        if self._def_ is None:
            from .. import models
            return models.EscRange._new_empty()

        return self._def_

    @property
    def base(self) -> 'EscValue':
        return self._base

    @base.setter
    def base(self, base: 'EscValue'):
        self._base = base

    @property
    def base__autoinit(self) -> 'EscValue':
        """Return ``base``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._base``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``base__safederef`` when you only want to read a default without mutating self.
        """
        if self._base is None:
            self._base = self.base__safederef

        return self._base

    @property
    def base__safederef(self) -> 'EscValue':
        """Return ``base``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``base`` stays unset and each
        call returns a new default. Use ``base__autoinit`` to persist the default on
        first access.
        """
        if self._base is None:
            from .. import models
            return models.EscValue._new_empty()

        return self._base

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, EscTrace):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
