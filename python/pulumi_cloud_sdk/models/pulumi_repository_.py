# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class PulumiRepository(PulumiAutoModelEncoder):
    """
    :var org_name: str - declared
    :var name: str - declared
    :var vcs_info: VCSInfo - declared
    :var projects: list[PulumiProject] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'org_name': 'str',
        'name': 'str',
        'vcs_info': 'VCSInfo',
        'projects': 'list[PulumiProject]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'org_name': 'orgName',
        'name': 'name',
        'vcs_info': 'vcsInfo',
        'projects': 'projects',
    }

    FIELDS_org_name = 'orgName'
    FIELDS_name = 'name'
    FIELDS_vcs_info = 'vcsInfo'
    FIELDS_projects = 'projects'

    _org_name: 'str'
    _name: 'str'
    _vcs_info: 'VCSInfo'
    _projects: 'list[PulumiProject]'

    def __init__(
        self,
        org_name: 'str',
        name: 'str',
        projects: 'list[PulumiProject]',
        vcs_info: 'VCSInfo' = None,
    ) -> None:
        super().__init__()

        self.org_name = org_name
        self.name = name
        self.vcs_info = vcs_info
        self.projects = projects

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, PulumiRepository):
            self._org_name = source._org_name
            self._name = source._name
            self._vcs_info = source._vcs_info
            self._projects = source._projects

    @property
    def org_name(self) -> 'str':
        return self._org_name

    @org_name.setter
    def org_name(self, org_name: 'str'):
        if org_name is None:
            raise ValueError("Invalid value for `org_name`, must not be `None`")

        self._org_name = org_name

    @property
    def name(self) -> 'str':
        return self._name

    @name.setter
    def name(self, name: 'str'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def vcs_info(self) -> 'VCSInfo':
        return self._vcs_info

    @vcs_info.setter
    def vcs_info(self, vcs_info: 'VCSInfo'):
        self._vcs_info = vcs_info

    @property
    def vcs_info__autoinit(self) -> 'VCSInfo':
        """Return ``vcs_info``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._vcs_info``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``vcs_info__safederef`` when you only want to read a default without mutating self.
        """
        if self._vcs_info is None:
            self._vcs_info = self.vcs_info__safederef

        return self._vcs_info

    @property
    def vcs_info__safederef(self) -> 'VCSInfo':
        """Return ``vcs_info``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``vcs_info`` stays unset and each
        call returns a new default. Use ``vcs_info__autoinit`` to persist the default on
        first access.
        """
        if self._vcs_info is None:
            from .. import models
            return models.VCSInfo._new_empty()

        return self._vcs_info

    @property
    def projects(self) -> 'list[PulumiProject]':
        return self._projects

    @projects.setter
    def projects(self, projects: 'list[PulumiProject]'):
        if projects is None:
            raise ValueError("Invalid value for `projects`, must not be `None`")

        self._projects = projects

    @property
    def projects__autoinit(self) -> 'list[PulumiProject]':
        """Return ``projects``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._projects``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``projects__safederef`` when you only want to read a default without mutating self.
        """
        if self._projects is None:
            self._projects = self.projects__safederef

        return self._projects

    @property
    def projects__safederef(self) -> 'list[PulumiProject]':
        """Return ``projects``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``projects`` stays unset and each
        call returns a new default. Use ``projects__autoinit`` to persist the default on
        first access.
        """
        if self._projects is None:
            return []

        return self._projects

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, PulumiRepository):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
