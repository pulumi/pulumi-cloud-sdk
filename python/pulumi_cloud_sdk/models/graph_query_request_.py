# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GraphQueryRequest(PulumiAutoModelEncoder):
    """
    :var scope: GraphQueryScope - declared
    :var anchor: GraphQueryAnchor - declared
    :var traverse: list[GraphTraverseStep] - declared
    :var aggregate: GraphQueryAggregate - declared
    :var return_: GraphQueryReturn - declared
    :var page: GraphQueryPage - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'scope': 'GraphQueryScope',
        'anchor': 'GraphQueryAnchor',
        'traverse': 'list[GraphTraverseStep]',
        'aggregate': 'GraphQueryAggregate',
        'return_': 'GraphQueryReturn',
        'page': 'GraphQueryPage',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'scope': 'scope',
        'anchor': 'anchor',
        'traverse': 'traverse',
        'aggregate': 'aggregate',
        'return_': 'return',
        'page': 'page',
    }

    FIELDS_scope = 'scope'
    FIELDS_anchor = 'anchor'
    FIELDS_traverse = 'traverse'
    FIELDS_aggregate = 'aggregate'
    FIELDS_return_ = 'return'
    FIELDS_page = 'page'

    _scope: 'GraphQueryScope'
    _anchor: 'GraphQueryAnchor'
    _traverse: 'list[GraphTraverseStep]'
    _aggregate: 'GraphQueryAggregate'
    _return_: 'GraphQueryReturn'
    _page: 'GraphQueryPage'

    def __init__(
        self,
        anchor: 'GraphQueryAnchor',
        scope: 'GraphQueryScope' = None,
        traverse: 'list[GraphTraverseStep]' = None,
        aggregate: 'GraphQueryAggregate' = None,
        return_: 'GraphQueryReturn' = None,
        page: 'GraphQueryPage' = None,
    ) -> None:
        super().__init__()

        self.scope = scope
        self.anchor = anchor
        self.traverse = traverse
        self.aggregate = aggregate
        self.return_ = return_
        self.page = page

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GraphQueryRequest):
            self._scope = source._scope
            self._anchor = source._anchor
            self._traverse = source._traverse
            self._aggregate = source._aggregate
            self._return_ = source._return_
            self._page = source._page

    @property
    def scope(self) -> 'GraphQueryScope':
        return self._scope

    @scope.setter
    def scope(self, scope: 'GraphQueryScope'):
        self._scope = scope

    @property
    def scope__autoinit(self) -> 'GraphQueryScope':
        """Return ``scope``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._scope``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``scope__safederef`` when you only want to read a default without mutating self.
        """
        if self._scope is None:
            self._scope = self.scope__safederef

        return self._scope

    @property
    def scope__safederef(self) -> 'GraphQueryScope':
        """Return ``scope``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``scope`` stays unset and each
        call returns a new default. Use ``scope__autoinit`` to persist the default on
        first access.
        """
        if self._scope is None:
            from .. import models
            return models.GraphQueryScope._new_empty()

        return self._scope

    @property
    def anchor(self) -> 'GraphQueryAnchor':
        return self._anchor

    @anchor.setter
    def anchor(self, anchor: 'GraphQueryAnchor'):
        if anchor is None:
            raise ValueError("Invalid value for `anchor`, must not be `None`")

        self._anchor = anchor

    @property
    def anchor__autoinit(self) -> 'GraphQueryAnchor':
        """Return ``anchor``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._anchor``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``anchor__safederef`` when you only want to read a default without mutating self.
        """
        if self._anchor is None:
            self._anchor = self.anchor__safederef

        return self._anchor

    @property
    def anchor__safederef(self) -> 'GraphQueryAnchor':
        """Return ``anchor``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``anchor`` stays unset and each
        call returns a new default. Use ``anchor__autoinit`` to persist the default on
        first access.
        """
        if self._anchor is None:
            from .. import models
            return models.GraphQueryAnchor._new_empty()

        return self._anchor

    @property
    def traverse(self) -> 'list[GraphTraverseStep]':
        return self._traverse

    @traverse.setter
    def traverse(self, traverse: 'list[GraphTraverseStep]'):
        self._traverse = traverse

    @property
    def traverse__autoinit(self) -> 'list[GraphTraverseStep]':
        """Return ``traverse``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._traverse``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``traverse__safederef`` when you only want to read a default without mutating self.
        """
        if self._traverse is None:
            self._traverse = self.traverse__safederef

        return self._traverse

    @property
    def traverse__safederef(self) -> 'list[GraphTraverseStep]':
        """Return ``traverse``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``traverse`` stays unset and each
        call returns a new default. Use ``traverse__autoinit`` to persist the default on
        first access.
        """
        if self._traverse is None:
            return []

        return self._traverse

    @property
    def aggregate(self) -> 'GraphQueryAggregate':
        return self._aggregate

    @aggregate.setter
    def aggregate(self, aggregate: 'GraphQueryAggregate'):
        self._aggregate = aggregate

    @property
    def aggregate__autoinit(self) -> 'GraphQueryAggregate':
        """Return ``aggregate``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._aggregate``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``aggregate__safederef`` when you only want to read a default without mutating self.
        """
        if self._aggregate is None:
            self._aggregate = self.aggregate__safederef

        return self._aggregate

    @property
    def aggregate__safederef(self) -> 'GraphQueryAggregate':
        """Return ``aggregate``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``aggregate`` stays unset and each
        call returns a new default. Use ``aggregate__autoinit`` to persist the default on
        first access.
        """
        if self._aggregate is None:
            from .. import models
            return models.GraphQueryAggregate._new_empty()

        return self._aggregate

    @property
    def return_(self) -> 'GraphQueryReturn':
        return self._return_

    @return_.setter
    def return_(self, return_: 'GraphQueryReturn'):
        self._return_ = return_

    @property
    def return___autoinit(self) -> 'GraphQueryReturn':
        """Return ``return_``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._return_``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``return___safederef`` when you only want to read a default without mutating self.
        """
        if self._return_ is None:
            self._return_ = self.return___safederef

        return self._return_

    @property
    def return___safederef(self) -> 'GraphQueryReturn':
        """Return ``return_``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``return_`` stays unset and each
        call returns a new default. Use ``return___autoinit`` to persist the default on
        first access.
        """
        if self._return_ is None:
            from .. import models
            return models.GraphQueryReturn._new_empty()

        return self._return_

    @property
    def page(self) -> 'GraphQueryPage':
        return self._page

    @page.setter
    def page(self, page: 'GraphQueryPage'):
        self._page = page

    @property
    def page__autoinit(self) -> 'GraphQueryPage':
        """Return ``page``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._page``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``page__safederef`` when you only want to read a default without mutating self.
        """
        if self._page is None:
            self._page = self.page__safederef

        return self._page

    @property
    def page__safederef(self) -> 'GraphQueryPage':
        """Return ``page``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``page`` stays unset and each
        call returns a new default. Use ``page__autoinit`` to persist the default on
        first access.
        """
        if self._page is None:
            from .. import models
            return models.GraphQueryPage._new_empty()

        return self._page

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GraphQueryRequest):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
