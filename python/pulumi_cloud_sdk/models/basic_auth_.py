# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class BasicAuth(PulumiAutoModelEncoder):
    """
    :var user_name: SecretValue - declared
    :var password: SecretValue - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'user_name': 'SecretValue',
        'password': 'SecretValue',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'user_name': 'userName',
        'password': 'password',
    }

    FIELDS_user_name = 'userName'
    FIELDS_password = 'password'

    _user_name: 'SecretValue'
    _password: 'SecretValue'

    def __init__(
        self,
        user_name: 'SecretValue',
        password: 'SecretValue',
    ) -> None:
        super().__init__()

        self.user_name = user_name
        self.password = password

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, BasicAuth):
            self._user_name = source._user_name
            self._password = source._password

    @property
    def user_name(self) -> 'SecretValue':
        return self._user_name

    @user_name.setter
    def user_name(self, user_name: 'SecretValue'):
        if user_name is None:
            raise ValueError("Invalid value for `user_name`, must not be `None`")

        self._user_name = user_name

    @property
    def user_name__autoinit(self) -> 'SecretValue':
        """Return ``user_name``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._user_name``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``user_name__safederef`` when you only want to read a default without mutating self.
        """
        if self._user_name is None:
            self._user_name = self.user_name__safederef

        return self._user_name

    @property
    def user_name__safederef(self) -> 'SecretValue':
        """Return ``user_name``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``user_name`` stays unset and each
        call returns a new default. Use ``user_name__autoinit`` to persist the default on
        first access.
        """
        if self._user_name is None:
            from .. import models
            return models.SecretValue._new_empty()

        return self._user_name

    @property
    def password(self) -> 'SecretValue':
        return self._password

    @password.setter
    def password(self, password: 'SecretValue'):
        if password is None:
            raise ValueError("Invalid value for `password`, must not be `None`")

        self._password = password

    @property
    def password__autoinit(self) -> 'SecretValue':
        """Return ``password``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._password``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``password__safederef`` when you only want to read a default without mutating self.
        """
        if self._password is None:
            self._password = self.password__safederef

        return self._password

    @property
    def password__safederef(self) -> 'SecretValue':
        """Return ``password``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``password`` stays unset and each
        call returns a new default. Use ``password__autoinit`` to persist the default on
        first access.
        """
        if self._password is None:
            from .. import models
            return models.SecretValue._new_empty()

        return self._password

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, BasicAuth):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
