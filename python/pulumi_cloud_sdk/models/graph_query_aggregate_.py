# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GraphQueryAggregate(PulumiAutoModelEncoder):
    """
    :var group_by: list[str] - declared
    :var metrics: list[GraphAggregateMetric] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'group_by': 'list[str]',
        'metrics': 'list[GraphAggregateMetric]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'group_by': 'groupBy',
        'metrics': 'metrics',
    }

    FIELDS_group_by = 'groupBy'
    FIELDS_metrics = 'metrics'

    _group_by: 'list[str]'
    _metrics: 'list[GraphAggregateMetric]'

    def __init__(
        self,
        group_by: 'list[str]',
        metrics: 'list[GraphAggregateMetric]',
    ) -> None:
        super().__init__()

        self.group_by = group_by
        self.metrics = metrics

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GraphQueryAggregate):
            self._group_by = source._group_by
            self._metrics = source._metrics

    @property
    def group_by(self) -> 'list[str]':
        return self._group_by

    @group_by.setter
    def group_by(self, group_by: 'list[str]'):
        if group_by is None:
            raise ValueError("Invalid value for `group_by`, must not be `None`")

        self._group_by = group_by

    @property
    def group_by__autoinit(self) -> 'list[str]':
        """Return ``group_by``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._group_by``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``group_by__safederef`` when you only want to read a default without mutating self.
        """
        if self._group_by is None:
            self._group_by = self.group_by__safederef

        return self._group_by

    @property
    def group_by__safederef(self) -> 'list[str]':
        """Return ``group_by``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``group_by`` stays unset and each
        call returns a new default. Use ``group_by__autoinit`` to persist the default on
        first access.
        """
        if self._group_by is None:
            return []

        return self._group_by

    @property
    def metrics(self) -> 'list[GraphAggregateMetric]':
        return self._metrics

    @metrics.setter
    def metrics(self, metrics: 'list[GraphAggregateMetric]'):
        if metrics is None:
            raise ValueError("Invalid value for `metrics`, must not be `None`")

        self._metrics = metrics

    @property
    def metrics__autoinit(self) -> 'list[GraphAggregateMetric]':
        """Return ``metrics``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._metrics``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``metrics__safederef`` when you only want to read a default without mutating self.
        """
        if self._metrics is None:
            self._metrics = self.metrics__safederef

        return self._metrics

    @property
    def metrics__safederef(self) -> 'list[GraphAggregateMetric]':
        """Return ``metrics``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``metrics`` stays unset and each
        call returns a new default. Use ``metrics__autoinit`` to persist the default on
        first access.
        """
        if self._metrics is None:
            return []

        return self._metrics

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GraphQueryAggregate):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
