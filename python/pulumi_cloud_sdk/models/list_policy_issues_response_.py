# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class ListPolicyIssuesResponse(PulumiAutoModelEncoder):
    """
    :var policy_issues: list[PolicyIssue] - declared
    :var group_data: list[dict[str, Any]] - declared
    :var row_count: int - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'policy_issues': 'list[PolicyIssue]',
        'group_data': 'list[dict[str, Any]]',
        'row_count': 'int',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'policy_issues': 'policyIssues',
        'group_data': 'groupData',
        'row_count': 'rowCount',
    }

    FIELDS_policy_issues = 'policyIssues'
    FIELDS_group_data = 'groupData'
    FIELDS_row_count = 'rowCount'

    _policy_issues: 'list[PolicyIssue]'
    _group_data: 'list[dict[str, Any]]'
    _row_count: 'int'

    def __init__(
        self,
        policy_issues: 'list[PolicyIssue]',
        group_data: 'list[dict[str, Any]]',
        row_count: 'int' = 0,
    ) -> None:
        super().__init__()

        self.policy_issues = policy_issues
        self.group_data = group_data
        self.row_count = row_count

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, ListPolicyIssuesResponse):
            self._policy_issues = source._policy_issues
            self._group_data = source._group_data
            self._row_count = source._row_count

    @property
    def policy_issues(self) -> 'list[PolicyIssue]':
        return self._policy_issues

    @policy_issues.setter
    def policy_issues(self, policy_issues: 'list[PolicyIssue]'):
        if policy_issues is None:
            raise ValueError("Invalid value for `policy_issues`, must not be `None`")

        self._policy_issues = policy_issues

    @property
    def policy_issues__autoinit(self) -> 'list[PolicyIssue]':
        """Return ``policy_issues``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._policy_issues``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``policy_issues__safederef`` when you only want to read a default without mutating self.
        """
        if self._policy_issues is None:
            self._policy_issues = self.policy_issues__safederef

        return self._policy_issues

    @property
    def policy_issues__safederef(self) -> 'list[PolicyIssue]':
        """Return ``policy_issues``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``policy_issues`` stays unset and each
        call returns a new default. Use ``policy_issues__autoinit`` to persist the default on
        first access.
        """
        if self._policy_issues is None:
            return []

        return self._policy_issues

    @property
    def group_data(self) -> 'list[dict[str, Any]]':
        return self._group_data

    @group_data.setter
    def group_data(self, group_data: 'list[dict[str, Any]]'):
        if group_data is None:
            raise ValueError("Invalid value for `group_data`, must not be `None`")

        self._group_data = group_data

    @property
    def group_data__autoinit(self) -> 'list[dict[str, Any]]':
        """Return ``group_data``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._group_data``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``group_data__safederef`` when you only want to read a default without mutating self.
        """
        if self._group_data is None:
            self._group_data = self.group_data__safederef

        return self._group_data

    @property
    def group_data__safederef(self) -> 'list[dict[str, Any]]':
        """Return ``group_data``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``group_data`` stays unset and each
        call returns a new default. Use ``group_data__autoinit`` to persist the default on
        first access.
        """
        if self._group_data is None:
            return []

        return self._group_data

    @property
    def row_count(self) -> 'int':
        return self._row_count

    @row_count.setter
    def row_count(self, row_count: 'int'):
        self._row_count = row_count

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, ListPolicyIssuesResponse):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
