# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class OrganizationAuditLogExportSettings(PulumiAutoModelEncoder):
    """
    :var enabled: bool - declared
    :var s3_config: AuditLogsExportS3Config - declared
    :var last_result: AuditLogExportResult - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'enabled': 'bool',
        's3_config': 'AuditLogsExportS3Config',
        'last_result': 'AuditLogExportResult',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'enabled': 'enabled',
        's3_config': 's3Config',
        'last_result': 'lastResult',
    }

    FIELDS_enabled = 'enabled'
    FIELDS_s3_config = 's3Config'
    FIELDS_last_result = 'lastResult'

    _enabled: 'bool'
    _s3_config: 'AuditLogsExportS3Config'
    _last_result: 'AuditLogExportResult'

    def __init__(
        self,
        enabled: 'bool',
        s3_config: 'AuditLogsExportS3Config',
        last_result: 'AuditLogExportResult',
    ) -> None:
        super().__init__()

        self.enabled = enabled
        self.s3_config = s3_config
        self.last_result = last_result

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, OrganizationAuditLogExportSettings):
            self._enabled = source._enabled
            self._s3_config = source._s3_config
            self._last_result = source._last_result

    @property
    def enabled(self) -> 'bool':
        return self._enabled

    @enabled.setter
    def enabled(self, enabled: 'bool'):
        if enabled is None:
            raise ValueError("Invalid value for `enabled`, must not be `None`")

        self._enabled = enabled

    @property
    def s3_config(self) -> 'AuditLogsExportS3Config':
        return self._s3_config

    @s3_config.setter
    def s3_config(self, s3_config: 'AuditLogsExportS3Config'):
        if s3_config is None:
            raise ValueError("Invalid value for `s3_config`, must not be `None`")

        self._s3_config = s3_config

    @property
    def s3_config__autoinit(self) -> 'AuditLogsExportS3Config':
        """Return ``s3_config``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._s3_config``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``s3_config__safederef`` when you only want to read a default without mutating self.
        """
        if self._s3_config is None:
            self._s3_config = self.s3_config__safederef

        return self._s3_config

    @property
    def s3_config__safederef(self) -> 'AuditLogsExportS3Config':
        """Return ``s3_config``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``s3_config`` stays unset and each
        call returns a new default. Use ``s3_config__autoinit`` to persist the default on
        first access.
        """
        if self._s3_config is None:
            from .. import models
            return models.AuditLogsExportS3Config._new_empty()

        return self._s3_config

    @property
    def last_result(self) -> 'AuditLogExportResult':
        return self._last_result

    @last_result.setter
    def last_result(self, last_result: 'AuditLogExportResult'):
        if last_result is None:
            raise ValueError("Invalid value for `last_result`, must not be `None`")

        self._last_result = last_result

    @property
    def last_result__autoinit(self) -> 'AuditLogExportResult':
        """Return ``last_result``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._last_result``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``last_result__safederef`` when you only want to read a default without mutating self.
        """
        if self._last_result is None:
            self._last_result = self.last_result__safederef

        return self._last_result

    @property
    def last_result__safederef(self) -> 'AuditLogExportResult':
        """Return ``last_result``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``last_result`` stays unset and each
        call returns a new default. Use ``last_result__autoinit`` to persist the default on
        first access.
        """
        if self._last_result is None:
            from .. import models
            return models.AuditLogExportResult._new_empty()

        return self._last_result

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, OrganizationAuditLogExportSettings):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
