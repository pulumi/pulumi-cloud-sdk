# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GraphSchemaNodeType(PulumiAutoModelEncoder):
    """
    :var name: GraphNodeType - declared
    :var selectable_fields: list[str] - declared
    :var projectable_fields: list[str] - declared
    :var group_by_fields: list[str] - declared
    :var field_values: dict[str, list[str]] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'name': 'GraphNodeType',
        'selectable_fields': 'list[str]',
        'projectable_fields': 'list[str]',
        'group_by_fields': 'list[str]',
        'field_values': 'dict[str, list[str]]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'name': 'name',
        'selectable_fields': 'selectableFields',
        'projectable_fields': 'projectableFields',
        'group_by_fields': 'groupByFields',
        'field_values': 'fieldValues',
    }

    FIELDS_name = 'name'
    FIELDS_selectable_fields = 'selectableFields'
    FIELDS_projectable_fields = 'projectableFields'
    FIELDS_group_by_fields = 'groupByFields'
    FIELDS_field_values = 'fieldValues'

    _name: 'GraphNodeType'
    _selectable_fields: 'list[str]'
    _projectable_fields: 'list[str]'
    _group_by_fields: 'list[str]'
    _field_values: 'dict[str, list[str]]'

    def __init__(
        self,
        name: 'GraphNodeType',
        selectable_fields: 'list[str]',
        projectable_fields: 'list[str]',
        group_by_fields: 'list[str]',
        field_values: 'dict[str, list[str]]',
    ) -> None:
        super().__init__()

        self.name = name
        self.selectable_fields = selectable_fields
        self.projectable_fields = projectable_fields
        self.group_by_fields = group_by_fields
        self.field_values = field_values

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GraphSchemaNodeType):
            self._name = source._name
            self._selectable_fields = source._selectable_fields
            self._projectable_fields = source._projectable_fields
            self._group_by_fields = source._group_by_fields
            self._field_values = source._field_values

    @property
    def name(self) -> 'GraphNodeType':
        return self._name

    @name.setter
    def name(self, name: 'GraphNodeType'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def selectable_fields(self) -> 'list[str]':
        return self._selectable_fields

    @selectable_fields.setter
    def selectable_fields(self, selectable_fields: 'list[str]'):
        if selectable_fields is None:
            raise ValueError("Invalid value for `selectable_fields`, must not be `None`")

        self._selectable_fields = selectable_fields

    @property
    def selectable_fields__autoinit(self) -> 'list[str]':
        """Return ``selectable_fields``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._selectable_fields``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``selectable_fields__safederef`` when you only want to read a default without mutating self.
        """
        if self._selectable_fields is None:
            self._selectable_fields = self.selectable_fields__safederef

        return self._selectable_fields

    @property
    def selectable_fields__safederef(self) -> 'list[str]':
        """Return ``selectable_fields``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``selectable_fields`` stays unset and each
        call returns a new default. Use ``selectable_fields__autoinit`` to persist the default on
        first access.
        """
        if self._selectable_fields is None:
            return []

        return self._selectable_fields

    @property
    def projectable_fields(self) -> 'list[str]':
        return self._projectable_fields

    @projectable_fields.setter
    def projectable_fields(self, projectable_fields: 'list[str]'):
        if projectable_fields is None:
            raise ValueError("Invalid value for `projectable_fields`, must not be `None`")

        self._projectable_fields = projectable_fields

    @property
    def projectable_fields__autoinit(self) -> 'list[str]':
        """Return ``projectable_fields``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._projectable_fields``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``projectable_fields__safederef`` when you only want to read a default without mutating self.
        """
        if self._projectable_fields is None:
            self._projectable_fields = self.projectable_fields__safederef

        return self._projectable_fields

    @property
    def projectable_fields__safederef(self) -> 'list[str]':
        """Return ``projectable_fields``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``projectable_fields`` stays unset and each
        call returns a new default. Use ``projectable_fields__autoinit`` to persist the default on
        first access.
        """
        if self._projectable_fields is None:
            return []

        return self._projectable_fields

    @property
    def group_by_fields(self) -> 'list[str]':
        return self._group_by_fields

    @group_by_fields.setter
    def group_by_fields(self, group_by_fields: 'list[str]'):
        if group_by_fields is None:
            raise ValueError("Invalid value for `group_by_fields`, must not be `None`")

        self._group_by_fields = group_by_fields

    @property
    def group_by_fields__autoinit(self) -> 'list[str]':
        """Return ``group_by_fields``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._group_by_fields``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``group_by_fields__safederef`` when you only want to read a default without mutating self.
        """
        if self._group_by_fields is None:
            self._group_by_fields = self.group_by_fields__safederef

        return self._group_by_fields

    @property
    def group_by_fields__safederef(self) -> 'list[str]':
        """Return ``group_by_fields``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``group_by_fields`` stays unset and each
        call returns a new default. Use ``group_by_fields__autoinit`` to persist the default on
        first access.
        """
        if self._group_by_fields is None:
            return []

        return self._group_by_fields

    @property
    def field_values(self) -> 'dict[str, list[str]]':
        return self._field_values

    @field_values.setter
    def field_values(self, field_values: 'dict[str, list[str]]'):
        if field_values is None:
            raise ValueError("Invalid value for `field_values`, must not be `None`")

        self._field_values = field_values

    @property
    def field_values__autoinit(self) -> 'dict[str, list[str]]':
        """Return ``field_values``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._field_values``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``field_values__safederef`` when you only want to read a default without mutating self.
        """
        if self._field_values is None:
            self._field_values = self.field_values__safederef

        return self._field_values

    @property
    def field_values__safederef(self) -> 'dict[str, list[str]]':
        """Return ``field_values``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``field_values`` stays unset and each
        call returns a new default. Use ``field_values__autoinit`` to persist the default on
        first access.
        """
        if self._field_values is None:
            return {}

        return self._field_values

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GraphSchemaNodeType):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
