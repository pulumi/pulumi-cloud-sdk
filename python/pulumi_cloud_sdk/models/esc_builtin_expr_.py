# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class EscBuiltinExpr(PulumiAutoModelEncoder):
    """
    :var name: str - declared
    :var name_range: EscRange - declared
    :var arg_schema: EscSchemaSchema - declared
    :var arg: EscExpr - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'name': 'str',
        'name_range': 'EscRange',
        'arg_schema': 'EscSchemaSchema',
        'arg': 'EscExpr',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'name': 'name',
        'name_range': 'nameRange',
        'arg_schema': 'argSchema',
        'arg': 'arg',
    }

    FIELDS_name = 'name'
    FIELDS_name_range = 'nameRange'
    FIELDS_arg_schema = 'argSchema'
    FIELDS_arg = 'arg'

    _name: 'str'
    _name_range: 'EscRange'
    _arg_schema: 'EscSchemaSchema'
    _arg: 'EscExpr'

    def __init__(
        self,
        name: 'str',
        name_range: 'EscRange',
        arg_schema: 'EscSchemaSchema',
        arg: 'EscExpr',
    ) -> None:
        super().__init__()

        self.name = name
        self.name_range = name_range
        self.arg_schema = arg_schema
        self.arg = arg

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, EscBuiltinExpr):
            self._name = source._name
            self._name_range = source._name_range
            self._arg_schema = source._arg_schema
            self._arg = source._arg

    @property
    def name(self) -> 'str':
        return self._name

    @name.setter
    def name(self, name: 'str'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def name_range(self) -> 'EscRange':
        return self._name_range

    @name_range.setter
    def name_range(self, name_range: 'EscRange'):
        if name_range is None:
            raise ValueError("Invalid value for `name_range`, must not be `None`")

        self._name_range = name_range

    @property
    def name_range__autoinit(self) -> 'EscRange':
        """Return ``name_range``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._name_range``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``name_range__safederef`` when you only want to read a default without mutating self.
        """
        if self._name_range is None:
            self._name_range = self.name_range__safederef

        return self._name_range

    @property
    def name_range__safederef(self) -> 'EscRange':
        """Return ``name_range``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``name_range`` stays unset and each
        call returns a new default. Use ``name_range__autoinit`` to persist the default on
        first access.
        """
        if self._name_range is None:
            from .. import models
            return models.EscRange._new_empty()

        return self._name_range

    @property
    def arg_schema(self) -> 'EscSchemaSchema':
        return self._arg_schema

    @arg_schema.setter
    def arg_schema(self, arg_schema: 'EscSchemaSchema'):
        if arg_schema is None:
            raise ValueError("Invalid value for `arg_schema`, must not be `None`")

        self._arg_schema = arg_schema

    @property
    def arg_schema__autoinit(self) -> 'EscSchemaSchema':
        """Return ``arg_schema``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._arg_schema``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``arg_schema__safederef`` when you only want to read a default without mutating self.
        """
        if self._arg_schema is None:
            self._arg_schema = self.arg_schema__safederef

        return self._arg_schema

    @property
    def arg_schema__safederef(self) -> 'EscSchemaSchema':
        """Return ``arg_schema``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``arg_schema`` stays unset and each
        call returns a new default. Use ``arg_schema__autoinit`` to persist the default on
        first access.
        """
        if self._arg_schema is None:
            from .. import models
            return models.EscSchemaSchema._new_empty()

        return self._arg_schema

    @property
    def arg(self) -> 'EscExpr':
        return self._arg

    @arg.setter
    def arg(self, arg: 'EscExpr'):
        if arg is None:
            raise ValueError("Invalid value for `arg`, must not be `None`")

        self._arg = arg

    @property
    def arg__autoinit(self) -> 'EscExpr':
        """Return ``arg``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._arg``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``arg__safederef`` when you only want to read a default without mutating self.
        """
        if self._arg is None:
            self._arg = self.arg__safederef

        return self._arg

    @property
    def arg__safederef(self) -> 'EscExpr':
        """Return ``arg``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``arg`` stays unset and each
        call returns a new default. Use ``arg__autoinit`` to persist the default on
        first access.
        """
        if self._arg is None:
            from .. import models
            return models.EscExpr._new_empty()

        return self._arg

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, EscBuiltinExpr):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
