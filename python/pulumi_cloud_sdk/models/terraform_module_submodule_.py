# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class TerraformModuleSubmodule(PulumiAutoModelEncoder):
    """
    :var name: str - declared
    :var readme: str - declared
    :var inputs: list[TerraformModuleInput] - declared
    :var outputs: list[TerraformModuleOutput] - declared
    :var provider_requirements: list[TerraformModuleProviderRequirement] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'name': 'str',
        'readme': 'str',
        'inputs': 'list[TerraformModuleInput]',
        'outputs': 'list[TerraformModuleOutput]',
        'provider_requirements': 'list[TerraformModuleProviderRequirement]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'name': 'name',
        'readme': 'readme',
        'inputs': 'inputs',
        'outputs': 'outputs',
        'provider_requirements': 'providerRequirements',
    }

    FIELDS_name = 'name'
    FIELDS_readme = 'readme'
    FIELDS_inputs = 'inputs'
    FIELDS_outputs = 'outputs'
    FIELDS_provider_requirements = 'providerRequirements'

    _name: 'str'
    _readme: 'str'
    _inputs: 'list[TerraformModuleInput]'
    _outputs: 'list[TerraformModuleOutput]'
    _provider_requirements: 'list[TerraformModuleProviderRequirement]'

    def __init__(
        self,
        name: 'str',
        inputs: 'list[TerraformModuleInput]',
        outputs: 'list[TerraformModuleOutput]',
        provider_requirements: 'list[TerraformModuleProviderRequirement]',
        readme: 'str' = None,
    ) -> None:
        super().__init__()

        self.name = name
        self.readme = readme
        self.inputs = inputs
        self.outputs = outputs
        self.provider_requirements = provider_requirements

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, TerraformModuleSubmodule):
            self._name = source._name
            self._readme = source._readme
            self._inputs = source._inputs
            self._outputs = source._outputs
            self._provider_requirements = source._provider_requirements

    @property
    def name(self) -> 'str':
        return self._name

    @name.setter
    def name(self, name: 'str'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def readme(self) -> 'str':
        return self._readme

    @readme.setter
    def readme(self, readme: 'str'):
        self._readme = readme

    @property
    def inputs(self) -> 'list[TerraformModuleInput]':
        return self._inputs

    @inputs.setter
    def inputs(self, inputs: 'list[TerraformModuleInput]'):
        if inputs is None:
            raise ValueError("Invalid value for `inputs`, must not be `None`")

        self._inputs = inputs

    @property
    def inputs__autoinit(self) -> 'list[TerraformModuleInput]':
        """Return ``inputs``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._inputs``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``inputs__safederef`` when you only want to read a default without mutating self.
        """
        if self._inputs is None:
            self._inputs = self.inputs__safederef

        return self._inputs

    @property
    def inputs__safederef(self) -> 'list[TerraformModuleInput]':
        """Return ``inputs``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``inputs`` stays unset and each
        call returns a new default. Use ``inputs__autoinit`` to persist the default on
        first access.
        """
        if self._inputs is None:
            return []

        return self._inputs

    @property
    def outputs(self) -> 'list[TerraformModuleOutput]':
        return self._outputs

    @outputs.setter
    def outputs(self, outputs: 'list[TerraformModuleOutput]'):
        if outputs is None:
            raise ValueError("Invalid value for `outputs`, must not be `None`")

        self._outputs = outputs

    @property
    def outputs__autoinit(self) -> 'list[TerraformModuleOutput]':
        """Return ``outputs``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._outputs``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``outputs__safederef`` when you only want to read a default without mutating self.
        """
        if self._outputs is None:
            self._outputs = self.outputs__safederef

        return self._outputs

    @property
    def outputs__safederef(self) -> 'list[TerraformModuleOutput]':
        """Return ``outputs``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``outputs`` stays unset and each
        call returns a new default. Use ``outputs__autoinit`` to persist the default on
        first access.
        """
        if self._outputs is None:
            return []

        return self._outputs

    @property
    def provider_requirements(self) -> 'list[TerraformModuleProviderRequirement]':
        return self._provider_requirements

    @provider_requirements.setter
    def provider_requirements(self, provider_requirements: 'list[TerraformModuleProviderRequirement]'):
        if provider_requirements is None:
            raise ValueError("Invalid value for `provider_requirements`, must not be `None`")

        self._provider_requirements = provider_requirements

    @property
    def provider_requirements__autoinit(self) -> 'list[TerraformModuleProviderRequirement]':
        """Return ``provider_requirements``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._provider_requirements``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``provider_requirements__safederef`` when you only want to read a default without mutating self.
        """
        if self._provider_requirements is None:
            self._provider_requirements = self.provider_requirements__safederef

        return self._provider_requirements

    @property
    def provider_requirements__safederef(self) -> 'list[TerraformModuleProviderRequirement]':
        """Return ``provider_requirements``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``provider_requirements`` stays unset and each
        call returns a new default. Use ``provider_requirements__autoinit`` to persist the default on
        first access.
        """
        if self._provider_requirements is None:
            return []

        return self._provider_requirements

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, TerraformModuleSubmodule):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
