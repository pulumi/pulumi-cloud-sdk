# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class EscRange(PulumiAutoModelEncoder):
    """
    :var environment: str - declared
    :var begin: EscPos - declared
    :var end: EscPos - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'environment': 'str',
        'begin': 'EscPos',
        'end': 'EscPos',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'environment': 'environment',
        'begin': 'begin',
        'end': 'end',
    }

    FIELDS_environment = 'environment'
    FIELDS_begin = 'begin'
    FIELDS_end = 'end'

    _environment: 'str'
    _begin: 'EscPos'
    _end: 'EscPos'

    def __init__(
        self,
        environment: 'str',
        begin: 'EscPos',
        end: 'EscPos',
    ) -> None:
        super().__init__()

        self.environment = environment
        self.begin = begin
        self.end = end

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, EscRange):
            self._environment = source._environment
            self._begin = source._begin
            self._end = source._end

    @property
    def environment(self) -> 'str':
        return self._environment

    @environment.setter
    def environment(self, environment: 'str'):
        if environment is None:
            raise ValueError("Invalid value for `environment`, must not be `None`")

        self._environment = environment

    @property
    def begin(self) -> 'EscPos':
        return self._begin

    @begin.setter
    def begin(self, begin: 'EscPos'):
        if begin is None:
            raise ValueError("Invalid value for `begin`, must not be `None`")

        self._begin = begin

    @property
    def begin__autoinit(self) -> 'EscPos':
        """Return ``begin``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._begin``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``begin__safederef`` when you only want to read a default without mutating self.
        """
        if self._begin is None:
            self._begin = self.begin__safederef

        return self._begin

    @property
    def begin__safederef(self) -> 'EscPos':
        """Return ``begin``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``begin`` stays unset and each
        call returns a new default. Use ``begin__autoinit`` to persist the default on
        first access.
        """
        if self._begin is None:
            from .. import models
            return models.EscPos._new_empty()

        return self._begin

    @property
    def end(self) -> 'EscPos':
        return self._end

    @end.setter
    def end(self, end: 'EscPos'):
        if end is None:
            raise ValueError("Invalid value for `end`, must not be `None`")

        self._end = end

    @property
    def end__autoinit(self) -> 'EscPos':
        """Return ``end``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._end``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``end__safederef`` when you only want to read a default without mutating self.
        """
        if self._end is None:
            self._end = self.end__safederef

        return self._end

    @property
    def end__safederef(self) -> 'EscPos':
        """Return ``end``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``end`` stays unset and each
        call returns a new default. Use ``end__autoinit`` to persist the default on
        first access.
        """
        if self._end is None:
            from .. import models
            return models.EscPos._new_empty()

        return self._end

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, EscRange):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
