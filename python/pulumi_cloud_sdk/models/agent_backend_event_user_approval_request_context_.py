# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AgentBackendEventUserApprovalRequestContext(PulumiAutoModelEncoder):
    """
    :var tool_call_id: str - declared
    :var tool_name: str - declared
    :var git_diff: dict[str, str] - declared
    :var last_approval_diff: dict[str, str] - declared
    :var plan_description: str - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'tool_call_id': 'str',
        'tool_name': 'str',
        'git_diff': 'dict[str, str]',
        'last_approval_diff': 'dict[str, str]',
        'plan_description': 'str',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'tool_call_id': 'tool_call_id',
        'tool_name': 'tool_name',
        'git_diff': 'git_diff',
        'last_approval_diff': 'last_approval_diff',
        'plan_description': 'plan_description',
    }

    FIELDS_tool_call_id = 'tool_call_id'
    FIELDS_tool_name = 'tool_name'
    FIELDS_git_diff = 'git_diff'
    FIELDS_last_approval_diff = 'last_approval_diff'
    FIELDS_plan_description = 'plan_description'

    _tool_call_id: 'str'
    _tool_name: 'str'
    _git_diff: 'dict[str, str]'
    _last_approval_diff: 'dict[str, str]'
    _plan_description: 'str'

    def __init__(
        self,
        tool_call_id: 'str' = None,
        tool_name: 'str' = None,
        git_diff: 'dict[str, str]' = None,
        last_approval_diff: 'dict[str, str]' = None,
        plan_description: 'str' = None,
    ) -> None:
        super().__init__()

        self.tool_call_id = tool_call_id
        self.tool_name = tool_name
        self.git_diff = git_diff
        self.last_approval_diff = last_approval_diff
        self.plan_description = plan_description

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AgentBackendEventUserApprovalRequestContext):
            self._tool_call_id = source._tool_call_id
            self._tool_name = source._tool_name
            self._git_diff = source._git_diff
            self._last_approval_diff = source._last_approval_diff
            self._plan_description = source._plan_description

    @property
    def tool_call_id(self) -> 'str':
        return self._tool_call_id

    @tool_call_id.setter
    def tool_call_id(self, tool_call_id: 'str'):
        self._tool_call_id = tool_call_id

    @property
    def tool_name(self) -> 'str':
        return self._tool_name

    @tool_name.setter
    def tool_name(self, tool_name: 'str'):
        self._tool_name = tool_name

    @property
    def git_diff(self) -> 'dict[str, str]':
        return self._git_diff

    @git_diff.setter
    def git_diff(self, git_diff: 'dict[str, str]'):
        self._git_diff = git_diff

    @property
    def git_diff__autoinit(self) -> 'dict[str, str]':
        """Return ``git_diff``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._git_diff``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``git_diff__safederef`` when you only want to read a default without mutating self.
        """
        if self._git_diff is None:
            self._git_diff = self.git_diff__safederef

        return self._git_diff

    @property
    def git_diff__safederef(self) -> 'dict[str, str]':
        """Return ``git_diff``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``git_diff`` stays unset and each
        call returns a new default. Use ``git_diff__autoinit`` to persist the default on
        first access.
        """
        if self._git_diff is None:
            return {}

        return self._git_diff

    @property
    def last_approval_diff(self) -> 'dict[str, str]':
        return self._last_approval_diff

    @last_approval_diff.setter
    def last_approval_diff(self, last_approval_diff: 'dict[str, str]'):
        self._last_approval_diff = last_approval_diff

    @property
    def last_approval_diff__autoinit(self) -> 'dict[str, str]':
        """Return ``last_approval_diff``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._last_approval_diff``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``last_approval_diff__safederef`` when you only want to read a default without mutating self.
        """
        if self._last_approval_diff is None:
            self._last_approval_diff = self.last_approval_diff__safederef

        return self._last_approval_diff

    @property
    def last_approval_diff__safederef(self) -> 'dict[str, str]':
        """Return ``last_approval_diff``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``last_approval_diff`` stays unset and each
        call returns a new default. Use ``last_approval_diff__autoinit`` to persist the default on
        first access.
        """
        if self._last_approval_diff is None:
            return {}

        return self._last_approval_diff

    @property
    def plan_description(self) -> 'str':
        return self._plan_description

    @plan_description.setter
    def plan_description(self, plan_description: 'str'):
        self._plan_description = plan_description

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AgentBackendEventUserApprovalRequestContext):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
