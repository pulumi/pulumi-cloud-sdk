# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .agent_user_event_ import AgentUserEvent

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AgentUserEventMessage(AgentUserEvent):
    """
    :var content: str - declared
    :var commands: dict[str, AgentSlashCommand] - declared
    :var timestamp: datetime - inherited
    :var entity_diff: AgentEntityDiff - inherited
    :var page_context: PageContext - inherited
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'timestamp': 'datetime',
        'entity_diff': 'AgentEntityDiff',
        'page_context': 'PageContext',
        'content': 'str',
        'commands': 'dict[str, AgentSlashCommand]',
        '__DISCRIMINATOR_VALUE__': 'str'
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'timestamp': 'timestamp',
        'entity_diff': 'entity_diff',
        'page_context': 'pageContext',
        'content': 'content',
        'commands': 'commands',
        '__DISCRIMINATOR_VALUE__': 'type'
    }

    __DISCRIMINATOR_VALUE__ = "user_message"

    FIELDS_content = 'content'
    FIELDS_commands = 'commands'

    _content: 'str'
    _commands: 'dict[str, AgentSlashCommand]'

    def __init__(
        self,
        timestamp: 'datetime',
        content: 'str',
        entity_diff: 'AgentEntityDiff' = None,
        page_context: 'PageContext' = None,
        commands: 'dict[str, AgentSlashCommand]' = None,
    ) -> None:
        super().__init__(timestamp=timestamp, entity_diff=entity_diff, page_context=page_context)

        self.content = content
        self.commands = commands

    def copy_common_fields(self, source: Any, /) -> None:
        super().copy_common_fields(source)
        if isinstance(source, AgentUserEventMessage):
            self._content = source._content
            self._commands = source._commands

    @property
    def content(self) -> 'str':
        return self._content

    @content.setter
    def content(self, content: 'str'):
        if content is None:
            raise ValueError("Invalid value for `content`, must not be `None`")

        self._content = content

    @property
    def commands(self) -> 'dict[str, AgentSlashCommand]':
        return self._commands

    @commands.setter
    def commands(self, commands: 'dict[str, AgentSlashCommand]'):
        self._commands = commands

    @property
    def commands__autoinit(self) -> 'dict[str, AgentSlashCommand]':
        """Return ``commands``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._commands``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``commands__safederef`` when you only want to read a default without mutating self.
        """
        if self._commands is None:
            self._commands = self.commands__safederef

        return self._commands

    @property
    def commands__safederef(self) -> 'dict[str, AgentSlashCommand]':
        """Return ``commands``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``commands`` stays unset and each
        call returns a new default. Use ``commands__autoinit`` to persist the default on
        first access.
        """
        if self._commands is None:
            return {}

        return self._commands

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AgentUserEventMessage):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
