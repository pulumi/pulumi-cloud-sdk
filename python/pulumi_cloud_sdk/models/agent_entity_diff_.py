# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AgentEntityDiff(PulumiAutoModelEncoder):
    """
    :var add: list[AgentEntity] - declared
    :var remove: list[AgentEntity] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'add': 'list[AgentEntity]',
        'remove': 'list[AgentEntity]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'add': 'add',
        'remove': 'remove',
    }

    FIELDS_add = 'add'
    FIELDS_remove = 'remove'

    _add: 'list[AgentEntity]'
    _remove: 'list[AgentEntity]'

    def __init__(
        self,
        add: 'list[AgentEntity]' = None,
        remove: 'list[AgentEntity]' = None,
    ) -> None:
        super().__init__()

        self.add = add
        self.remove = remove

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AgentEntityDiff):
            self._add = source._add
            self._remove = source._remove

    @property
    def add(self) -> 'list[AgentEntity]':
        return self._add

    @add.setter
    def add(self, add: 'list[AgentEntity]'):
        self._add = add

    @property
    def add__autoinit(self) -> 'list[AgentEntity]':
        """Return ``add``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._add``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``add__safederef`` when you only want to read a default without mutating self.
        """
        if self._add is None:
            self._add = self.add__safederef

        return self._add

    @property
    def add__safederef(self) -> 'list[AgentEntity]':
        """Return ``add``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``add`` stays unset and each
        call returns a new default. Use ``add__autoinit`` to persist the default on
        first access.
        """
        if self._add is None:
            return []

        return self._add

    @property
    def remove(self) -> 'list[AgentEntity]':
        return self._remove

    @remove.setter
    def remove(self, remove: 'list[AgentEntity]'):
        self._remove = remove

    @property
    def remove__autoinit(self) -> 'list[AgentEntity]':
        """Return ``remove``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._remove``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``remove__safederef`` when you only want to read a default without mutating self.
        """
        if self._remove is None:
            self._remove = self.remove__safederef

        return self._remove

    @property
    def remove__safederef(self) -> 'list[AgentEntity]':
        """Return ``remove``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``remove`` stays unset and each
        call returns a new default. Use ``remove__autoinit`` to persist the default on
        first access.
        """
        if self._remove is None:
            return []

        return self._remove

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AgentEntityDiff):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
