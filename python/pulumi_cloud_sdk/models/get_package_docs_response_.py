# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GetPackageDocsResponse(PulumiAutoModelEncoder):
    """
    :var version: str - declared
    :var nav_url: str - declared
    :var type_token: str - declared
    :var kind: str - declared
    :var language: str - declared
    :var name: dict[str, str] - declared
    :var description: list[RegistryContentNode] - declared
    :var constructor_syntax: list[RegistryContentNode] - declared
    :var inputs: list[RegistryDocsProperty] - declared
    :var outputs: list[RegistryDocsProperty] - declared
    :var supporting_types: list[RegistryDocsSupportingType] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'version': 'str',
        'nav_url': 'str',
        'type_token': 'str',
        'kind': 'str',
        'language': 'str',
        'name': 'dict[str, str]',
        'description': 'list[RegistryContentNode]',
        'constructor_syntax': 'list[RegistryContentNode]',
        'inputs': 'list[RegistryDocsProperty]',
        'outputs': 'list[RegistryDocsProperty]',
        'supporting_types': 'list[RegistryDocsSupportingType]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'version': 'version',
        'nav_url': 'navUrl',
        'type_token': 'typeToken',
        'kind': 'kind',
        'language': 'language',
        'name': 'name',
        'description': 'description',
        'constructor_syntax': 'constructorSyntax',
        'inputs': 'inputs',
        'outputs': 'outputs',
        'supporting_types': 'supportingTypes',
    }

    FIELDS_version = 'version'
    FIELDS_nav_url = 'navUrl'
    FIELDS_type_token = 'typeToken'
    FIELDS_kind = 'kind'
    FIELDS_language = 'language'
    FIELDS_name = 'name'
    FIELDS_description = 'description'
    FIELDS_constructor_syntax = 'constructorSyntax'
    FIELDS_inputs = 'inputs'
    FIELDS_outputs = 'outputs'
    FIELDS_supporting_types = 'supportingTypes'

    _version: 'str'
    _nav_url: 'str'
    _type_token: 'str'
    _kind: 'str'
    _language: 'str'
    _name: 'dict[str, str]'
    _description: 'list[RegistryContentNode]'
    _constructor_syntax: 'list[RegistryContentNode]'
    _inputs: 'list[RegistryDocsProperty]'
    _outputs: 'list[RegistryDocsProperty]'
    _supporting_types: 'list[RegistryDocsSupportingType]'

    def __init__(
        self,
        version: 'str',
        nav_url: 'str',
        type_token: 'str',
        kind: 'str',
        language: 'str',
        name: 'dict[str, str]',
        description: 'list[RegistryContentNode]',
        constructor_syntax: 'list[RegistryContentNode]' = None,
        inputs: 'list[RegistryDocsProperty]' = None,
        outputs: 'list[RegistryDocsProperty]' = None,
        supporting_types: 'list[RegistryDocsSupportingType]' = None,
    ) -> None:
        super().__init__()

        self.version = version
        self.nav_url = nav_url
        self.type_token = type_token
        self.kind = kind
        self.language = language
        self.name = name
        self.description = description
        self.constructor_syntax = constructor_syntax
        self.inputs = inputs
        self.outputs = outputs
        self.supporting_types = supporting_types

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GetPackageDocsResponse):
            self._version = source._version
            self._nav_url = source._nav_url
            self._type_token = source._type_token
            self._kind = source._kind
            self._language = source._language
            self._name = source._name
            self._description = source._description
            self._constructor_syntax = source._constructor_syntax
            self._inputs = source._inputs
            self._outputs = source._outputs
            self._supporting_types = source._supporting_types

    @property
    def version(self) -> 'str':
        return self._version

    @version.setter
    def version(self, version: 'str'):
        if version is None:
            raise ValueError("Invalid value for `version`, must not be `None`")

        self._version = version

    @property
    def nav_url(self) -> 'str':
        return self._nav_url

    @nav_url.setter
    def nav_url(self, nav_url: 'str'):
        if nav_url is None:
            raise ValueError("Invalid value for `nav_url`, must not be `None`")

        self._nav_url = nav_url

    @property
    def type_token(self) -> 'str':
        return self._type_token

    @type_token.setter
    def type_token(self, type_token: 'str'):
        if type_token is None:
            raise ValueError("Invalid value for `type_token`, must not be `None`")

        self._type_token = type_token

    @property
    def kind(self) -> 'str':
        return self._kind

    @kind.setter
    def kind(self, kind: 'str'):
        if kind is None:
            raise ValueError("Invalid value for `kind`, must not be `None`")

        self._kind = kind

    @property
    def language(self) -> 'str':
        return self._language

    @language.setter
    def language(self, language: 'str'):
        if language is None:
            raise ValueError("Invalid value for `language`, must not be `None`")

        self._language = language

    @property
    def name(self) -> 'dict[str, str]':
        return self._name

    @name.setter
    def name(self, name: 'dict[str, str]'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def name__autoinit(self) -> 'dict[str, str]':
        """Return ``name``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._name``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``name__safederef`` when you only want to read a default without mutating self.
        """
        if self._name is None:
            self._name = self.name__safederef

        return self._name

    @property
    def name__safederef(self) -> 'dict[str, str]':
        """Return ``name``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``name`` stays unset and each
        call returns a new default. Use ``name__autoinit`` to persist the default on
        first access.
        """
        if self._name is None:
            return {}

        return self._name

    @property
    def description(self) -> 'list[RegistryContentNode]':
        return self._description

    @description.setter
    def description(self, description: 'list[RegistryContentNode]'):
        if description is None:
            raise ValueError("Invalid value for `description`, must not be `None`")

        self._description = description

    @property
    def description__autoinit(self) -> 'list[RegistryContentNode]':
        """Return ``description``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._description``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``description__safederef`` when you only want to read a default without mutating self.
        """
        if self._description is None:
            self._description = self.description__safederef

        return self._description

    @property
    def description__safederef(self) -> 'list[RegistryContentNode]':
        """Return ``description``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``description`` stays unset and each
        call returns a new default. Use ``description__autoinit`` to persist the default on
        first access.
        """
        if self._description is None:
            return []

        return self._description

    @property
    def constructor_syntax(self) -> 'list[RegistryContentNode]':
        return self._constructor_syntax

    @constructor_syntax.setter
    def constructor_syntax(self, constructor_syntax: 'list[RegistryContentNode]'):
        self._constructor_syntax = constructor_syntax

    @property
    def constructor_syntax__autoinit(self) -> 'list[RegistryContentNode]':
        """Return ``constructor_syntax``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._constructor_syntax``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``constructor_syntax__safederef`` when you only want to read a default without mutating self.
        """
        if self._constructor_syntax is None:
            self._constructor_syntax = self.constructor_syntax__safederef

        return self._constructor_syntax

    @property
    def constructor_syntax__safederef(self) -> 'list[RegistryContentNode]':
        """Return ``constructor_syntax``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``constructor_syntax`` stays unset and each
        call returns a new default. Use ``constructor_syntax__autoinit`` to persist the default on
        first access.
        """
        if self._constructor_syntax is None:
            return []

        return self._constructor_syntax

    @property
    def inputs(self) -> 'list[RegistryDocsProperty]':
        return self._inputs

    @inputs.setter
    def inputs(self, inputs: 'list[RegistryDocsProperty]'):
        self._inputs = inputs

    @property
    def inputs__autoinit(self) -> 'list[RegistryDocsProperty]':
        """Return ``inputs``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._inputs``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``inputs__safederef`` when you only want to read a default without mutating self.
        """
        if self._inputs is None:
            self._inputs = self.inputs__safederef

        return self._inputs

    @property
    def inputs__safederef(self) -> 'list[RegistryDocsProperty]':
        """Return ``inputs``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``inputs`` stays unset and each
        call returns a new default. Use ``inputs__autoinit`` to persist the default on
        first access.
        """
        if self._inputs is None:
            return []

        return self._inputs

    @property
    def outputs(self) -> 'list[RegistryDocsProperty]':
        return self._outputs

    @outputs.setter
    def outputs(self, outputs: 'list[RegistryDocsProperty]'):
        self._outputs = outputs

    @property
    def outputs__autoinit(self) -> 'list[RegistryDocsProperty]':
        """Return ``outputs``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._outputs``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``outputs__safederef`` when you only want to read a default without mutating self.
        """
        if self._outputs is None:
            self._outputs = self.outputs__safederef

        return self._outputs

    @property
    def outputs__safederef(self) -> 'list[RegistryDocsProperty]':
        """Return ``outputs``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``outputs`` stays unset and each
        call returns a new default. Use ``outputs__autoinit`` to persist the default on
        first access.
        """
        if self._outputs is None:
            return []

        return self._outputs

    @property
    def supporting_types(self) -> 'list[RegistryDocsSupportingType]':
        return self._supporting_types

    @supporting_types.setter
    def supporting_types(self, supporting_types: 'list[RegistryDocsSupportingType]'):
        self._supporting_types = supporting_types

    @property
    def supporting_types__autoinit(self) -> 'list[RegistryDocsSupportingType]':
        """Return ``supporting_types``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._supporting_types``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``supporting_types__safederef`` when you only want to read a default without mutating self.
        """
        if self._supporting_types is None:
            self._supporting_types = self.supporting_types__safederef

        return self._supporting_types

    @property
    def supporting_types__safederef(self) -> 'list[RegistryDocsSupportingType]':
        """Return ``supporting_types``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``supporting_types`` stays unset and each
        call returns a new default. Use ``supporting_types__autoinit`` to persist the default on
        first access.
        """
        if self._supporting_types is None:
            return []

        return self._supporting_types

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GetPackageDocsResponse):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
