# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GraphQueryReturn(PulumiAutoModelEncoder):
    """
    :var select: list[str] - declared
    :var fields: list[str] - declared
    :var paths: bool - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'select': 'list[str]',
        'fields': 'list[str]',
        'paths': 'bool',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'select': 'select',
        'fields': 'fields',
        'paths': 'paths',
    }

    FIELDS_select = 'select'
    FIELDS_fields = 'fields'
    FIELDS_paths = 'paths'

    _select: 'list[str]'
    _fields: 'list[str]'
    _paths: 'bool'

    def __init__(
        self,
        select: 'list[str]' = None,
        fields: 'list[str]' = None,
        paths: 'bool' = False,
    ) -> None:
        super().__init__()

        self.select = select
        self.fields = fields
        self.paths = paths

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GraphQueryReturn):
            self._select = source._select
            self._fields = source._fields
            self._paths = source._paths

    @property
    def select(self) -> 'list[str]':
        return self._select

    @select.setter
    def select(self, select: 'list[str]'):
        self._select = select

    @property
    def select__autoinit(self) -> 'list[str]':
        """Return ``select``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._select``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``select__safederef`` when you only want to read a default without mutating self.
        """
        if self._select is None:
            self._select = self.select__safederef

        return self._select

    @property
    def select__safederef(self) -> 'list[str]':
        """Return ``select``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``select`` stays unset and each
        call returns a new default. Use ``select__autoinit`` to persist the default on
        first access.
        """
        if self._select is None:
            return []

        return self._select

    @property
    def fields(self) -> 'list[str]':
        return self._fields

    @fields.setter
    def fields(self, fields: 'list[str]'):
        self._fields = fields

    @property
    def fields__autoinit(self) -> 'list[str]':
        """Return ``fields``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._fields``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``fields__safederef`` when you only want to read a default without mutating self.
        """
        if self._fields is None:
            self._fields = self.fields__safederef

        return self._fields

    @property
    def fields__safederef(self) -> 'list[str]':
        """Return ``fields``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``fields`` stays unset and each
        call returns a new default. Use ``fields__autoinit`` to persist the default on
        first access.
        """
        if self._fields is None:
            return []

        return self._fields

    @property
    def paths(self) -> 'bool':
        return self._paths

    @paths.setter
    def paths(self, paths: 'bool'):
        self._paths = paths

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GraphQueryReturn):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
