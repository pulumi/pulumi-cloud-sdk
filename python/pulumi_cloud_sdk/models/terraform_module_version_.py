# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .terraform_module_ import TerraformModule

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class TerraformModuleVersion(TerraformModule):
    """
    :var version: str - declared
    :var sha256: str - declared
    :var size_bytes: int - declared
    :var readme: str - declared
    :var inputs: list[TerraformModuleInput] - declared
    :var outputs: list[TerraformModuleOutput] - declared
    :var provider_requirements: list[TerraformModuleProviderRequirement] - declared
    :var submodules: list[TerraformModuleSubmodule] - declared
    :var examples: list[TerraformModuleExample] - declared
    :var package_conversion: TerraformModulePackageConversion - declared
    :var name: str - inherited
    :var publisher: str - inherited
    :var source: str - inherited
    :var system: str - inherited
    :var description: str - inherited
    :var latest_version: str - inherited
    :var updated_at: datetime - inherited
    :var url: str - inherited
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'name': 'str',
        'publisher': 'str',
        'source': 'str',
        'system': 'str',
        'description': 'str',
        'latest_version': 'str',
        'updated_at': 'datetime',
        'url': 'str',
        'version': 'str',
        'sha256': 'str',
        'size_bytes': 'int',
        'readme': 'str',
        'inputs': 'list[TerraformModuleInput]',
        'outputs': 'list[TerraformModuleOutput]',
        'provider_requirements': 'list[TerraformModuleProviderRequirement]',
        'submodules': 'list[TerraformModuleSubmodule]',
        'examples': 'list[TerraformModuleExample]',
        'package_conversion': 'TerraformModulePackageConversion',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'name': 'name',
        'publisher': 'publisher',
        'source': 'source',
        'system': 'system',
        'description': 'description',
        'latest_version': 'latestVersion',
        'updated_at': 'updatedAt',
        'url': 'url',
        'version': 'version',
        'sha256': 'sha256',
        'size_bytes': 'sizeBytes',
        'readme': 'readme',
        'inputs': 'inputs',
        'outputs': 'outputs',
        'provider_requirements': 'providerRequirements',
        'submodules': 'submodules',
        'examples': 'examples',
        'package_conversion': 'packageConversion',
    }

    FIELDS_version = 'version'
    FIELDS_sha256 = 'sha256'
    FIELDS_size_bytes = 'sizeBytes'
    FIELDS_readme = 'readme'
    FIELDS_inputs = 'inputs'
    FIELDS_outputs = 'outputs'
    FIELDS_provider_requirements = 'providerRequirements'
    FIELDS_submodules = 'submodules'
    FIELDS_examples = 'examples'
    FIELDS_package_conversion = 'packageConversion'

    _version: 'str'
    _sha256: 'str'
    _size_bytes: 'int'
    _readme: 'str'
    _inputs: 'list[TerraformModuleInput]'
    _outputs: 'list[TerraformModuleOutput]'
    _provider_requirements: 'list[TerraformModuleProviderRequirement]'
    _submodules: 'list[TerraformModuleSubmodule]'
    _examples: 'list[TerraformModuleExample]'
    _package_conversion: 'TerraformModulePackageConversion'

    def __init__(
        self,
        name: 'str',
        publisher: 'str',
        source: 'str',
        system: 'str',
        latest_version: 'str',
        updated_at: 'datetime',
        url: 'str',
        version: 'str',
        sha256: 'str',
        size_bytes: 'int',
        inputs: 'list[TerraformModuleInput]',
        outputs: 'list[TerraformModuleOutput]',
        provider_requirements: 'list[TerraformModuleProviderRequirement]',
        description: 'str' = None,
        readme: 'str' = None,
        submodules: 'list[TerraformModuleSubmodule]' = None,
        examples: 'list[TerraformModuleExample]' = None,
        package_conversion: 'TerraformModulePackageConversion' = None,
    ) -> None:
        super().__init__(name=name, publisher=publisher, source=source, system=system, description=description, latest_version=latest_version, updated_at=updated_at, url=url)

        self.version = version
        self.sha256 = sha256
        self.size_bytes = size_bytes
        self.readme = readme
        self.inputs = inputs
        self.outputs = outputs
        self.provider_requirements = provider_requirements
        self.submodules = submodules
        self.examples = examples
        self.package_conversion = package_conversion

    def copy_common_fields(self, source: Any, /) -> None:
        super().copy_common_fields(source)
        if isinstance(source, TerraformModuleVersion):
            self._version = source._version
            self._sha256 = source._sha256
            self._size_bytes = source._size_bytes
            self._readme = source._readme
            self._inputs = source._inputs
            self._outputs = source._outputs
            self._provider_requirements = source._provider_requirements
            self._submodules = source._submodules
            self._examples = source._examples
            self._package_conversion = source._package_conversion

    @property
    def version(self) -> 'str':
        return self._version

    @version.setter
    def version(self, version: 'str'):
        if version is None:
            raise ValueError("Invalid value for `version`, must not be `None`")

        self._version = version

    @property
    def sha256(self) -> 'str':
        return self._sha256

    @sha256.setter
    def sha256(self, sha256: 'str'):
        if sha256 is None:
            raise ValueError("Invalid value for `sha256`, must not be `None`")

        self._sha256 = sha256

    @property
    def size_bytes(self) -> 'int':
        return self._size_bytes

    @size_bytes.setter
    def size_bytes(self, size_bytes: 'int'):
        if size_bytes is None:
            raise ValueError("Invalid value for `size_bytes`, must not be `None`")

        self._size_bytes = size_bytes

    @property
    def readme(self) -> 'str':
        return self._readme

    @readme.setter
    def readme(self, readme: 'str'):
        self._readme = readme

    @property
    def inputs(self) -> 'list[TerraformModuleInput]':
        return self._inputs

    @inputs.setter
    def inputs(self, inputs: 'list[TerraformModuleInput]'):
        if inputs is None:
            raise ValueError("Invalid value for `inputs`, must not be `None`")

        self._inputs = inputs

    @property
    def inputs__autoinit(self) -> 'list[TerraformModuleInput]':
        """Return ``inputs``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._inputs``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``inputs__safederef`` when you only want to read a default without mutating self.
        """
        if self._inputs is None:
            self._inputs = self.inputs__safederef

        return self._inputs

    @property
    def inputs__safederef(self) -> 'list[TerraformModuleInput]':
        """Return ``inputs``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``inputs`` stays unset and each
        call returns a new default. Use ``inputs__autoinit`` to persist the default on
        first access.
        """
        if self._inputs is None:
            return []

        return self._inputs

    @property
    def outputs(self) -> 'list[TerraformModuleOutput]':
        return self._outputs

    @outputs.setter
    def outputs(self, outputs: 'list[TerraformModuleOutput]'):
        if outputs is None:
            raise ValueError("Invalid value for `outputs`, must not be `None`")

        self._outputs = outputs

    @property
    def outputs__autoinit(self) -> 'list[TerraformModuleOutput]':
        """Return ``outputs``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._outputs``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``outputs__safederef`` when you only want to read a default without mutating self.
        """
        if self._outputs is None:
            self._outputs = self.outputs__safederef

        return self._outputs

    @property
    def outputs__safederef(self) -> 'list[TerraformModuleOutput]':
        """Return ``outputs``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``outputs`` stays unset and each
        call returns a new default. Use ``outputs__autoinit`` to persist the default on
        first access.
        """
        if self._outputs is None:
            return []

        return self._outputs

    @property
    def provider_requirements(self) -> 'list[TerraformModuleProviderRequirement]':
        return self._provider_requirements

    @provider_requirements.setter
    def provider_requirements(self, provider_requirements: 'list[TerraformModuleProviderRequirement]'):
        if provider_requirements is None:
            raise ValueError("Invalid value for `provider_requirements`, must not be `None`")

        self._provider_requirements = provider_requirements

    @property
    def provider_requirements__autoinit(self) -> 'list[TerraformModuleProviderRequirement]':
        """Return ``provider_requirements``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._provider_requirements``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``provider_requirements__safederef`` when you only want to read a default without mutating self.
        """
        if self._provider_requirements is None:
            self._provider_requirements = self.provider_requirements__safederef

        return self._provider_requirements

    @property
    def provider_requirements__safederef(self) -> 'list[TerraformModuleProviderRequirement]':
        """Return ``provider_requirements``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``provider_requirements`` stays unset and each
        call returns a new default. Use ``provider_requirements__autoinit`` to persist the default on
        first access.
        """
        if self._provider_requirements is None:
            return []

        return self._provider_requirements

    @property
    def submodules(self) -> 'list[TerraformModuleSubmodule]':
        return self._submodules

    @submodules.setter
    def submodules(self, submodules: 'list[TerraformModuleSubmodule]'):
        self._submodules = submodules

    @property
    def submodules__autoinit(self) -> 'list[TerraformModuleSubmodule]':
        """Return ``submodules``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._submodules``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``submodules__safederef`` when you only want to read a default without mutating self.
        """
        if self._submodules is None:
            self._submodules = self.submodules__safederef

        return self._submodules

    @property
    def submodules__safederef(self) -> 'list[TerraformModuleSubmodule]':
        """Return ``submodules``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``submodules`` stays unset and each
        call returns a new default. Use ``submodules__autoinit`` to persist the default on
        first access.
        """
        if self._submodules is None:
            return []

        return self._submodules

    @property
    def examples(self) -> 'list[TerraformModuleExample]':
        return self._examples

    @examples.setter
    def examples(self, examples: 'list[TerraformModuleExample]'):
        self._examples = examples

    @property
    def examples__autoinit(self) -> 'list[TerraformModuleExample]':
        """Return ``examples``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._examples``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``examples__safederef`` when you only want to read a default without mutating self.
        """
        if self._examples is None:
            self._examples = self.examples__safederef

        return self._examples

    @property
    def examples__safederef(self) -> 'list[TerraformModuleExample]':
        """Return ``examples``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``examples`` stays unset and each
        call returns a new default. Use ``examples__autoinit`` to persist the default on
        first access.
        """
        if self._examples is None:
            return []

        return self._examples

    @property
    def package_conversion(self) -> 'TerraformModulePackageConversion':
        return self._package_conversion

    @package_conversion.setter
    def package_conversion(self, package_conversion: 'TerraformModulePackageConversion'):
        self._package_conversion = package_conversion

    @property
    def package_conversion__autoinit(self) -> 'TerraformModulePackageConversion':
        """Return ``package_conversion``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._package_conversion``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``package_conversion__safederef`` when you only want to read a default without mutating self.
        """
        if self._package_conversion is None:
            self._package_conversion = self.package_conversion__safederef

        return self._package_conversion

    @property
    def package_conversion__safederef(self) -> 'TerraformModulePackageConversion':
        """Return ``package_conversion``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``package_conversion`` stays unset and each
        call returns a new default. Use ``package_conversion__autoinit`` to persist the default on
        first access.
        """
        if self._package_conversion is None:
            from .. import models
            return models.TerraformModulePackageConversion._new_empty()

        return self._package_conversion

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, TerraformModuleVersion):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
