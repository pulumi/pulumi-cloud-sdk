# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class InsightsResourceWithReferences(PulumiAutoModelEncoder):
    """
    :var resource: InsightsResourceWithVersion - declared
    :var indexed_resource: ResourceResult - declared
    :var urn: str - declared
    :var from_: list[InsightsResourceWithReferences] - declared
    :var to: list[InsightsResourceWithReferences] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'resource': 'InsightsResourceWithVersion',
        'indexed_resource': 'ResourceResult',
        'urn': 'str',
        'from_': 'list[InsightsResourceWithReferences]',
        'to': 'list[InsightsResourceWithReferences]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'resource': 'resource',
        'indexed_resource': 'indexedResource',
        'urn': 'urn',
        'from_': 'from',
        'to': 'to',
    }

    FIELDS_resource = 'resource'
    FIELDS_indexed_resource = 'indexedResource'
    FIELDS_urn = 'urn'
    FIELDS_from_ = 'from'
    FIELDS_to = 'to'

    _resource: 'InsightsResourceWithVersion'
    _indexed_resource: 'ResourceResult'
    _urn: 'str'
    _from_: 'list[InsightsResourceWithReferences]'
    _to: 'list[InsightsResourceWithReferences]'

    def __init__(
        self,
        resource: 'InsightsResourceWithVersion',
        indexed_resource: 'ResourceResult',
        urn: 'str',
        from_: 'list[InsightsResourceWithReferences]',
        to: 'list[InsightsResourceWithReferences]',
    ) -> None:
        super().__init__()

        self.resource = resource
        self.indexed_resource = indexed_resource
        self.urn = urn
        self.from_ = from_
        self.to = to

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, InsightsResourceWithReferences):
            self._resource = source._resource
            self._indexed_resource = source._indexed_resource
            self._urn = source._urn
            self._from_ = source._from_
            self._to = source._to

    @property
    def resource(self) -> 'InsightsResourceWithVersion':
        return self._resource

    @resource.setter
    def resource(self, resource: 'InsightsResourceWithVersion'):
        if resource is None:
            raise ValueError("Invalid value for `resource`, must not be `None`")

        self._resource = resource

    @property
    def resource__autoinit(self) -> 'InsightsResourceWithVersion':
        """Return ``resource``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._resource``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``resource__safederef`` when you only want to read a default without mutating self.
        """
        if self._resource is None:
            self._resource = self.resource__safederef

        return self._resource

    @property
    def resource__safederef(self) -> 'InsightsResourceWithVersion':
        """Return ``resource``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``resource`` stays unset and each
        call returns a new default. Use ``resource__autoinit`` to persist the default on
        first access.
        """
        if self._resource is None:
            from .. import models
            return models.InsightsResourceWithVersion._new_empty()

        return self._resource

    @property
    def indexed_resource(self) -> 'ResourceResult':
        return self._indexed_resource

    @indexed_resource.setter
    def indexed_resource(self, indexed_resource: 'ResourceResult'):
        if indexed_resource is None:
            raise ValueError("Invalid value for `indexed_resource`, must not be `None`")

        self._indexed_resource = indexed_resource

    @property
    def indexed_resource__autoinit(self) -> 'ResourceResult':
        """Return ``indexed_resource``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._indexed_resource``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``indexed_resource__safederef`` when you only want to read a default without mutating self.
        """
        if self._indexed_resource is None:
            self._indexed_resource = self.indexed_resource__safederef

        return self._indexed_resource

    @property
    def indexed_resource__safederef(self) -> 'ResourceResult':
        """Return ``indexed_resource``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``indexed_resource`` stays unset and each
        call returns a new default. Use ``indexed_resource__autoinit`` to persist the default on
        first access.
        """
        if self._indexed_resource is None:
            from .. import models
            return models.ResourceResult._new_empty()

        return self._indexed_resource

    @property
    def urn(self) -> 'str':
        return self._urn

    @urn.setter
    def urn(self, urn: 'str'):
        if urn is None:
            raise ValueError("Invalid value for `urn`, must not be `None`")

        self._urn = urn

    @property
    def from_(self) -> 'list[InsightsResourceWithReferences]':
        return self._from_

    @from_.setter
    def from_(self, from_: 'list[InsightsResourceWithReferences]'):
        if from_ is None:
            raise ValueError("Invalid value for `from_`, must not be `None`")

        self._from_ = from_

    @property
    def from___autoinit(self) -> 'list[InsightsResourceWithReferences]':
        """Return ``from_``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._from_``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``from___safederef`` when you only want to read a default without mutating self.
        """
        if self._from_ is None:
            self._from_ = self.from___safederef

        return self._from_

    @property
    def from___safederef(self) -> 'list[InsightsResourceWithReferences]':
        """Return ``from_``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``from_`` stays unset and each
        call returns a new default. Use ``from___autoinit`` to persist the default on
        first access.
        """
        if self._from_ is None:
            return []

        return self._from_

    @property
    def to(self) -> 'list[InsightsResourceWithReferences]':
        return self._to

    @to.setter
    def to(self, to: 'list[InsightsResourceWithReferences]'):
        if to is None:
            raise ValueError("Invalid value for `to`, must not be `None`")

        self._to = to

    @property
    def to__autoinit(self) -> 'list[InsightsResourceWithReferences]':
        """Return ``to``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._to``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``to__safederef`` when you only want to read a default without mutating self.
        """
        if self._to is None:
            self._to = self.to__safederef

        return self._to

    @property
    def to__safederef(self) -> 'list[InsightsResourceWithReferences]':
        """Return ``to``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``to`` stays unset and each
        call returns a new default. Use ``to__autoinit`` to persist the default on
        first access.
        """
        if self._to is None:
            return []

        return self._to

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, InsightsResourceWithReferences):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
