# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class GetServiceResponse(PulumiAutoModelEncoder):
    """
    :var service: Service - declared
    :var items: list[ServiceItem] - declared
    :var continuation_token: str - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'service': 'Service',
        'items': 'list[ServiceItem]',
        'continuation_token': 'str',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'service': 'service',
        'items': 'items',
        'continuation_token': 'continuationToken',
    }

    FIELDS_service = 'service'
    FIELDS_items = 'items'
    FIELDS_continuation_token = 'continuationToken'

    _service: 'Service'
    _items: 'list[ServiceItem]'
    _continuation_token: 'str'

    def __init__(
        self,
        service: 'Service',
        items: 'list[ServiceItem]',
        continuation_token: 'str' = None,
    ) -> None:
        super().__init__()

        self.service = service
        self.items = items
        self.continuation_token = continuation_token

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, GetServiceResponse):
            self._service = source._service
            self._items = source._items
            self._continuation_token = source._continuation_token

    @property
    def service(self) -> 'Service':
        return self._service

    @service.setter
    def service(self, service: 'Service'):
        if service is None:
            raise ValueError("Invalid value for `service`, must not be `None`")

        self._service = service

    @property
    def service__autoinit(self) -> 'Service':
        """Return ``service``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._service``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``service__safederef`` when you only want to read a default without mutating self.
        """
        if self._service is None:
            self._service = self.service__safederef

        return self._service

    @property
    def service__safederef(self) -> 'Service':
        """Return ``service``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``service`` stays unset and each
        call returns a new default. Use ``service__autoinit`` to persist the default on
        first access.
        """
        if self._service is None:
            from .. import models
            return models.Service._new_empty()

        return self._service

    @property
    def items(self) -> 'list[ServiceItem]':
        return self._items

    @items.setter
    def items(self, items: 'list[ServiceItem]'):
        if items is None:
            raise ValueError("Invalid value for `items`, must not be `None`")

        self._items = items

    @property
    def items__autoinit(self) -> 'list[ServiceItem]':
        """Return ``items``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._items``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``items__safederef`` when you only want to read a default without mutating self.
        """
        if self._items is None:
            self._items = self.items__safederef

        return self._items

    @property
    def items__safederef(self) -> 'list[ServiceItem]':
        """Return ``items``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``items`` stays unset and each
        call returns a new default. Use ``items__autoinit`` to persist the default on
        first access.
        """
        if self._items is None:
            return []

        return self._items

    @property
    def continuation_token(self) -> 'str':
        return self._continuation_token

    @continuation_token.setter
    def continuation_token(self, continuation_token: 'str'):
        self._continuation_token = continuation_token

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, GetServiceResponse):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
