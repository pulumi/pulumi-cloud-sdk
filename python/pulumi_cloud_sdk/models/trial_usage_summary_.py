# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class TrialUsageSummary(PulumiAutoModelEncoder):
    """
    :var dhrum: ConsumptionUsage - declared
    :var insights_scans: ConsumptionUsage - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'dhrum': 'ConsumptionUsage',
        'insights_scans': 'ConsumptionUsage',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'dhrum': 'dhrum',
        'insights_scans': 'insightsScans',
    }

    FIELDS_dhrum = 'dhrum'
    FIELDS_insights_scans = 'insightsScans'

    _dhrum: 'ConsumptionUsage'
    _insights_scans: 'ConsumptionUsage'

    def __init__(
        self,
        dhrum: 'ConsumptionUsage' = None,
        insights_scans: 'ConsumptionUsage' = None,
    ) -> None:
        super().__init__()

        self.dhrum = dhrum
        self.insights_scans = insights_scans

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, TrialUsageSummary):
            self._dhrum = source._dhrum
            self._insights_scans = source._insights_scans

    @property
    def dhrum(self) -> 'ConsumptionUsage':
        return self._dhrum

    @dhrum.setter
    def dhrum(self, dhrum: 'ConsumptionUsage'):
        self._dhrum = dhrum

    @property
    def dhrum__autoinit(self) -> 'ConsumptionUsage':
        """Return ``dhrum``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._dhrum``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``dhrum__safederef`` when you only want to read a default without mutating self.
        """
        if self._dhrum is None:
            self._dhrum = self.dhrum__safederef

        return self._dhrum

    @property
    def dhrum__safederef(self) -> 'ConsumptionUsage':
        """Return ``dhrum``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``dhrum`` stays unset and each
        call returns a new default. Use ``dhrum__autoinit`` to persist the default on
        first access.
        """
        if self._dhrum is None:
            from .. import models
            return models.ConsumptionUsage._new_empty()

        return self._dhrum

    @property
    def insights_scans(self) -> 'ConsumptionUsage':
        return self._insights_scans

    @insights_scans.setter
    def insights_scans(self, insights_scans: 'ConsumptionUsage'):
        self._insights_scans = insights_scans

    @property
    def insights_scans__autoinit(self) -> 'ConsumptionUsage':
        """Return ``insights_scans``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._insights_scans``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``insights_scans__safederef`` when you only want to read a default without mutating self.
        """
        if self._insights_scans is None:
            self._insights_scans = self.insights_scans__safederef

        return self._insights_scans

    @property
    def insights_scans__safederef(self) -> 'ConsumptionUsage':
        """Return ``insights_scans``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``insights_scans`` stays unset and each
        call returns a new default. Use ``insights_scans__autoinit`` to persist the default on
        first access.
        """
        if self._insights_scans is None:
            from .. import models
            return models.ConsumptionUsage._new_empty()

        return self._insights_scans

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, TrialUsageSummary):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
