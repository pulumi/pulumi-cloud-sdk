# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class AppRequiredPolicy(PulumiAutoModelEncoder):
    """
    :var name: str - declared
    :var version: int - declared
    :var version_tag: str - declared
    :var display_name: str - declared
    :var pack_location: str - declared
    :var config: dict[str, Any] - declared
    :var environments: list[str] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'name': 'str',
        'version': 'int',
        'version_tag': 'str',
        'display_name': 'str',
        'pack_location': 'str',
        'config': 'dict[str, Any]',
        'environments': 'list[str]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'name': 'name',
        'version': 'version',
        'version_tag': 'versionTag',
        'display_name': 'displayName',
        'pack_location': 'packLocation',
        'config': 'config',
        'environments': 'environments',
    }

    FIELDS_name = 'name'
    FIELDS_version = 'version'
    FIELDS_version_tag = 'versionTag'
    FIELDS_display_name = 'displayName'
    FIELDS_pack_location = 'packLocation'
    FIELDS_config = 'config'
    FIELDS_environments = 'environments'

    _name: 'str'
    _version: 'int'
    _version_tag: 'str'
    _display_name: 'str'
    _pack_location: 'str'
    _config: 'dict[str, Any]'
    _environments: 'list[str]'

    def __init__(
        self,
        name: 'str',
        version: 'int',
        version_tag: 'str',
        display_name: 'str',
        pack_location: 'str' = None,
        config: 'dict[str, Any]' = None,
        environments: 'list[str]' = None,
    ) -> None:
        super().__init__()

        self.name = name
        self.version = version
        self.version_tag = version_tag
        self.display_name = display_name
        self.pack_location = pack_location
        self.config = config
        self.environments = environments

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, AppRequiredPolicy):
            self._name = source._name
            self._version = source._version
            self._version_tag = source._version_tag
            self._display_name = source._display_name
            self._pack_location = source._pack_location
            self._config = source._config
            self._environments = source._environments

    @property
    def name(self) -> 'str':
        return self._name

    @name.setter
    def name(self, name: 'str'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def version(self) -> 'int':
        return self._version

    @version.setter
    def version(self, version: 'int'):
        if version is None:
            raise ValueError("Invalid value for `version`, must not be `None`")

        self._version = version

    @property
    def version_tag(self) -> 'str':
        return self._version_tag

    @version_tag.setter
    def version_tag(self, version_tag: 'str'):
        if version_tag is None:
            raise ValueError("Invalid value for `version_tag`, must not be `None`")

        self._version_tag = version_tag

    @property
    def display_name(self) -> 'str':
        return self._display_name

    @display_name.setter
    def display_name(self, display_name: 'str'):
        if display_name is None:
            raise ValueError("Invalid value for `display_name`, must not be `None`")

        self._display_name = display_name

    @property
    def pack_location(self) -> 'str':
        return self._pack_location

    @pack_location.setter
    def pack_location(self, pack_location: 'str'):
        self._pack_location = pack_location

    @property
    def config(self) -> 'dict[str, Any]':
        return self._config

    @config.setter
    def config(self, config: 'dict[str, Any]'):
        self._config = config

    @property
    def config__autoinit(self) -> 'dict[str, Any]':
        """Return ``config``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._config``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``config__safederef`` when you only want to read a default without mutating self.
        """
        if self._config is None:
            self._config = self.config__safederef

        return self._config

    @property
    def config__safederef(self) -> 'dict[str, Any]':
        """Return ``config``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``config`` stays unset and each
        call returns a new default. Use ``config__autoinit`` to persist the default on
        first access.
        """
        if self._config is None:
            return {}

        return self._config

    @property
    def environments(self) -> 'list[str]':
        return self._environments

    @environments.setter
    def environments(self, environments: 'list[str]'):
        self._environments = environments

    @property
    def environments__autoinit(self) -> 'list[str]':
        """Return ``environments``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._environments``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``environments__safederef`` when you only want to read a default without mutating self.
        """
        if self._environments is None:
            self._environments = self.environments__safederef

        return self._environments

    @property
    def environments__safederef(self) -> 'list[str]':
        """Return ``environments``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``environments`` stays unset and each
        call returns a new default. Use ``environments__autoinit`` to persist the default on
        first access.
        """
        if self._environments is None:
            return []

        return self._environments

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, AppRequiredPolicy):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
