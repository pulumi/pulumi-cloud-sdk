# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class UpdateEnvironmentTagRequest(PulumiAutoModelEncoder):
    """
    :var current_tag: UpdateEnvironmentTagRequestCurrentTag - declared
    :var new_tag: UpdateEnvironmentTagRequestNewTag - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'current_tag': 'UpdateEnvironmentTagRequestCurrentTag',
        'new_tag': 'UpdateEnvironmentTagRequestNewTag',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'current_tag': 'currentTag',
        'new_tag': 'newTag',
    }

    FIELDS_current_tag = 'currentTag'
    FIELDS_new_tag = 'newTag'

    _current_tag: 'UpdateEnvironmentTagRequestCurrentTag'
    _new_tag: 'UpdateEnvironmentTagRequestNewTag'

    def __init__(
        self,
        current_tag: 'UpdateEnvironmentTagRequestCurrentTag',
        new_tag: 'UpdateEnvironmentTagRequestNewTag',
    ) -> None:
        super().__init__()

        self.current_tag = current_tag
        self.new_tag = new_tag

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, UpdateEnvironmentTagRequest):
            self._current_tag = source._current_tag
            self._new_tag = source._new_tag

    @property
    def current_tag(self) -> 'UpdateEnvironmentTagRequestCurrentTag':
        return self._current_tag

    @current_tag.setter
    def current_tag(self, current_tag: 'UpdateEnvironmentTagRequestCurrentTag'):
        if current_tag is None:
            raise ValueError("Invalid value for `current_tag`, must not be `None`")

        self._current_tag = current_tag

    @property
    def current_tag__autoinit(self) -> 'UpdateEnvironmentTagRequestCurrentTag':
        """Return ``current_tag``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._current_tag``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``current_tag__safederef`` when you only want to read a default without mutating self.
        """
        if self._current_tag is None:
            self._current_tag = self.current_tag__safederef

        return self._current_tag

    @property
    def current_tag__safederef(self) -> 'UpdateEnvironmentTagRequestCurrentTag':
        """Return ``current_tag``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``current_tag`` stays unset and each
        call returns a new default. Use ``current_tag__autoinit`` to persist the default on
        first access.
        """
        if self._current_tag is None:
            from .. import models
            return models.UpdateEnvironmentTagRequestCurrentTag._new_empty()

        return self._current_tag

    @property
    def new_tag(self) -> 'UpdateEnvironmentTagRequestNewTag':
        return self._new_tag

    @new_tag.setter
    def new_tag(self, new_tag: 'UpdateEnvironmentTagRequestNewTag'):
        if new_tag is None:
            raise ValueError("Invalid value for `new_tag`, must not be `None`")

        self._new_tag = new_tag

    @property
    def new_tag__autoinit(self) -> 'UpdateEnvironmentTagRequestNewTag':
        """Return ``new_tag``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._new_tag``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``new_tag__safederef`` when you only want to read a default without mutating self.
        """
        if self._new_tag is None:
            self._new_tag = self.new_tag__safederef

        return self._new_tag

    @property
    def new_tag__safederef(self) -> 'UpdateEnvironmentTagRequestNewTag':
        """Return ``new_tag``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``new_tag`` stays unset and each
        call returns a new default. Use ``new_tag__autoinit`` to persist the default on
        first access.
        """
        if self._new_tag is None:
            from .. import models
            return models.UpdateEnvironmentTagRequestNewTag._new_empty()

        return self._new_tag

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, UpdateEnvironmentTagRequest):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
