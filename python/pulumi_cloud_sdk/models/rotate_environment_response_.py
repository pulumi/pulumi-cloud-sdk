# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class RotateEnvironmentResponse(PulumiAutoModelEncoder):
    """
    :var id: str - declared
    :var diagnostics: list[EnvironmentDiagnostic] - declared
    :var secret_rotation_event: SecretRotationEvent - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'id': 'str',
        'diagnostics': 'list[EnvironmentDiagnostic]',
        'secret_rotation_event': 'SecretRotationEvent',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'id': 'id',
        'diagnostics': 'diagnostics',
        'secret_rotation_event': 'secretRotationEvent',
    }

    FIELDS_id = 'id'
    FIELDS_diagnostics = 'diagnostics'
    FIELDS_secret_rotation_event = 'secretRotationEvent'

    _id: 'str'
    _diagnostics: 'list[EnvironmentDiagnostic]'
    _secret_rotation_event: 'SecretRotationEvent'

    def __init__(
        self,
        id: 'str',
        secret_rotation_event: 'SecretRotationEvent',
        diagnostics: 'list[EnvironmentDiagnostic]' = None,
    ) -> None:
        super().__init__()

        self.id = id
        self.diagnostics = diagnostics
        self.secret_rotation_event = secret_rotation_event

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, RotateEnvironmentResponse):
            self._id = source._id
            self._diagnostics = source._diagnostics
            self._secret_rotation_event = source._secret_rotation_event

    @property
    def id(self) -> 'str':
        return self._id

    @id.setter
    def id(self, id: 'str'):
        if id is None:
            raise ValueError("Invalid value for `id`, must not be `None`")

        self._id = id

    @property
    def diagnostics(self) -> 'list[EnvironmentDiagnostic]':
        return self._diagnostics

    @diagnostics.setter
    def diagnostics(self, diagnostics: 'list[EnvironmentDiagnostic]'):
        self._diagnostics = diagnostics

    @property
    def diagnostics__autoinit(self) -> 'list[EnvironmentDiagnostic]':
        """Return ``diagnostics``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._diagnostics``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``diagnostics__safederef`` when you only want to read a default without mutating self.
        """
        if self._diagnostics is None:
            self._diagnostics = self.diagnostics__safederef

        return self._diagnostics

    @property
    def diagnostics__safederef(self) -> 'list[EnvironmentDiagnostic]':
        """Return ``diagnostics``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``diagnostics`` stays unset and each
        call returns a new default. Use ``diagnostics__autoinit`` to persist the default on
        first access.
        """
        if self._diagnostics is None:
            return []

        return self._diagnostics

    @property
    def secret_rotation_event(self) -> 'SecretRotationEvent':
        return self._secret_rotation_event

    @secret_rotation_event.setter
    def secret_rotation_event(self, secret_rotation_event: 'SecretRotationEvent'):
        if secret_rotation_event is None:
            raise ValueError("Invalid value for `secret_rotation_event`, must not be `None`")

        self._secret_rotation_event = secret_rotation_event

    @property
    def secret_rotation_event__autoinit(self) -> 'SecretRotationEvent':
        """Return ``secret_rotation_event``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._secret_rotation_event``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``secret_rotation_event__safederef`` when you only want to read a default without mutating self.
        """
        if self._secret_rotation_event is None:
            self._secret_rotation_event = self.secret_rotation_event__safederef

        return self._secret_rotation_event

    @property
    def secret_rotation_event__safederef(self) -> 'SecretRotationEvent':
        """Return ``secret_rotation_event``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``secret_rotation_event`` stays unset and each
        call returns a new default. Use ``secret_rotation_event__autoinit`` to persist the default on
        first access.
        """
        if self._secret_rotation_event is None:
            from .. import models
            return models.SecretRotationEvent._new_empty()

        return self._secret_rotation_event

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, RotateEnvironmentResponse):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
