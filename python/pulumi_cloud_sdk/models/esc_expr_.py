# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class EscExpr(PulumiAutoModelEncoder):
    """
    :var range: EscRange - declared
    :var schema: EscSchemaSchema - declared
    :var base: EscExpr - declared
    :var key_ranges: dict[str, EscRange] - declared
    :var literal: Any - declared
    :var interpolate: list[EscInterpolation] - declared
    :var symbol: list[EscPropertyAccessor] - declared
    :var access: EscAccessExpr - declared
    :var list: list[EscExpr] - declared
    :var object: dict[str, EscExpr] - declared
    :var builtin: EscBuiltinExpr - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'range': 'EscRange',
        'schema': 'EscSchemaSchema',
        'base': 'EscExpr',
        'key_ranges': 'dict[str, EscRange]',
        'literal': 'Any',
        'interpolate': 'list[EscInterpolation]',
        'symbol': 'list[EscPropertyAccessor]',
        'access': 'EscAccessExpr',
        'list': 'list[EscExpr]',
        'object': 'dict[str, EscExpr]',
        'builtin': 'EscBuiltinExpr',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'range': 'range',
        'schema': 'schema',
        'base': 'base',
        'key_ranges': 'keyRanges',
        'literal': 'literal',
        'interpolate': 'interpolate',
        'symbol': 'symbol',
        'access': 'access',
        'list': 'list',
        'object': 'object',
        'builtin': 'builtin',
    }

    FIELDS_range = 'range'
    FIELDS_schema = 'schema'
    FIELDS_base = 'base'
    FIELDS_key_ranges = 'keyRanges'
    FIELDS_literal = 'literal'
    FIELDS_interpolate = 'interpolate'
    FIELDS_symbol = 'symbol'
    FIELDS_access = 'access'
    FIELDS_list = 'list'
    FIELDS_object = 'object'
    FIELDS_builtin = 'builtin'

    _range: 'EscRange'
    _schema: 'EscSchemaSchema'
    _base: 'EscExpr'
    _key_ranges: 'dict[str, EscRange]'
    _literal: 'Any'
    _interpolate: 'list[EscInterpolation]'
    _symbol: 'list[EscPropertyAccessor]'
    _access: 'EscAccessExpr'
    _list: 'list[EscExpr]'
    _object: 'dict[str, EscExpr]'
    _builtin: 'EscBuiltinExpr'

    def __init__(
        self,
        range: 'EscRange',
        schema: 'EscSchemaSchema' = None,
        base: 'EscExpr' = None,
        key_ranges: 'dict[str, EscRange]' = None,
        literal: 'Any' = None,
        interpolate: 'list[EscInterpolation]' = None,
        symbol: 'list[EscPropertyAccessor]' = None,
        access: 'EscAccessExpr' = None,
        list: 'list[EscExpr]' = None,
        object: 'dict[str, EscExpr]' = None,
        builtin: 'EscBuiltinExpr' = None,
    ) -> None:
        super().__init__()

        self.range = range
        self.schema = schema
        self.base = base
        self.key_ranges = key_ranges
        self.literal = literal
        self.interpolate = interpolate
        self.symbol = symbol
        self.access = access
        self.list = list
        self.object = object
        self.builtin = builtin

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, EscExpr):
            self._range = source._range
            self._schema = source._schema
            self._base = source._base
            self._key_ranges = source._key_ranges
            self._literal = source._literal
            self._interpolate = source._interpolate
            self._symbol = source._symbol
            self._access = source._access
            self._list = source._list
            self._object = source._object
            self._builtin = source._builtin

    @property
    def range(self) -> 'EscRange':
        return self._range

    @range.setter
    def range(self, range: 'EscRange'):
        if range is None:
            raise ValueError("Invalid value for `range`, must not be `None`")

        self._range = range

    @property
    def range__autoinit(self) -> 'EscRange':
        """Return ``range``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._range``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``range__safederef`` when you only want to read a default without mutating self.
        """
        if self._range is None:
            self._range = self.range__safederef

        return self._range

    @property
    def range__safederef(self) -> 'EscRange':
        """Return ``range``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``range`` stays unset and each
        call returns a new default. Use ``range__autoinit`` to persist the default on
        first access.
        """
        if self._range is None:
            from .. import models
            return models.EscRange._new_empty()

        return self._range

    @property
    def schema(self) -> 'EscSchemaSchema':
        return self._schema

    @schema.setter
    def schema(self, schema: 'EscSchemaSchema'):
        self._schema = schema

    @property
    def schema__autoinit(self) -> 'EscSchemaSchema':
        """Return ``schema``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._schema``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``schema__safederef`` when you only want to read a default without mutating self.
        """
        if self._schema is None:
            self._schema = self.schema__safederef

        return self._schema

    @property
    def schema__safederef(self) -> 'EscSchemaSchema':
        """Return ``schema``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``schema`` stays unset and each
        call returns a new default. Use ``schema__autoinit`` to persist the default on
        first access.
        """
        if self._schema is None:
            from .. import models
            return models.EscSchemaSchema._new_empty()

        return self._schema

    @property
    def base(self) -> 'EscExpr':
        return self._base

    @base.setter
    def base(self, base: 'EscExpr'):
        self._base = base

    @property
    def base__autoinit(self) -> 'EscExpr':
        """Return ``base``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._base``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``base__safederef`` when you only want to read a default without mutating self.
        """
        if self._base is None:
            self._base = self.base__safederef

        return self._base

    @property
    def base__safederef(self) -> 'EscExpr':
        """Return ``base``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``base`` stays unset and each
        call returns a new default. Use ``base__autoinit`` to persist the default on
        first access.
        """
        if self._base is None:
            from .. import models
            return models.EscExpr._new_empty()

        return self._base

    @property
    def key_ranges(self) -> 'dict[str, EscRange]':
        return self._key_ranges

    @key_ranges.setter
    def key_ranges(self, key_ranges: 'dict[str, EscRange]'):
        self._key_ranges = key_ranges

    @property
    def key_ranges__autoinit(self) -> 'dict[str, EscRange]':
        """Return ``key_ranges``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._key_ranges``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``key_ranges__safederef`` when you only want to read a default without mutating self.
        """
        if self._key_ranges is None:
            self._key_ranges = self.key_ranges__safederef

        return self._key_ranges

    @property
    def key_ranges__safederef(self) -> 'dict[str, EscRange]':
        """Return ``key_ranges``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``key_ranges`` stays unset and each
        call returns a new default. Use ``key_ranges__autoinit`` to persist the default on
        first access.
        """
        if self._key_ranges is None:
            return {}

        return self._key_ranges

    @property
    def literal(self) -> 'Any':
        return self._literal

    @literal.setter
    def literal(self, literal: 'Any'):
        self._literal = literal

    @property
    def interpolate(self) -> 'list[EscInterpolation]':
        return self._interpolate

    @interpolate.setter
    def interpolate(self, interpolate: 'list[EscInterpolation]'):
        self._interpolate = interpolate

    @property
    def interpolate__autoinit(self) -> 'list[EscInterpolation]':
        """Return ``interpolate``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._interpolate``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``interpolate__safederef`` when you only want to read a default without mutating self.
        """
        if self._interpolate is None:
            self._interpolate = self.interpolate__safederef

        return self._interpolate

    @property
    def interpolate__safederef(self) -> 'list[EscInterpolation]':
        """Return ``interpolate``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``interpolate`` stays unset and each
        call returns a new default. Use ``interpolate__autoinit`` to persist the default on
        first access.
        """
        if self._interpolate is None:
            return []

        return self._interpolate

    @property
    def symbol(self) -> 'list[EscPropertyAccessor]':
        return self._symbol

    @symbol.setter
    def symbol(self, symbol: 'list[EscPropertyAccessor]'):
        self._symbol = symbol

    @property
    def symbol__autoinit(self) -> 'list[EscPropertyAccessor]':
        """Return ``symbol``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._symbol``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``symbol__safederef`` when you only want to read a default without mutating self.
        """
        if self._symbol is None:
            self._symbol = self.symbol__safederef

        return self._symbol

    @property
    def symbol__safederef(self) -> 'list[EscPropertyAccessor]':
        """Return ``symbol``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``symbol`` stays unset and each
        call returns a new default. Use ``symbol__autoinit`` to persist the default on
        first access.
        """
        if self._symbol is None:
            return []

        return self._symbol

    @property
    def access(self) -> 'EscAccessExpr':
        return self._access

    @access.setter
    def access(self, access: 'EscAccessExpr'):
        self._access = access

    @property
    def access__autoinit(self) -> 'EscAccessExpr':
        """Return ``access``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._access``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``access__safederef`` when you only want to read a default without mutating self.
        """
        if self._access is None:
            self._access = self.access__safederef

        return self._access

    @property
    def access__safederef(self) -> 'EscAccessExpr':
        """Return ``access``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``access`` stays unset and each
        call returns a new default. Use ``access__autoinit`` to persist the default on
        first access.
        """
        if self._access is None:
            from .. import models
            return models.EscAccessExpr._new_empty()

        return self._access

    @property
    def list(self) -> 'list[EscExpr]':
        return self._list

    @list.setter
    def list(self, list: 'list[EscExpr]'):
        self._list = list

    @property
    def list__autoinit(self) -> 'list[EscExpr]':
        """Return ``list``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._list``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``list__safederef`` when you only want to read a default without mutating self.
        """
        if self._list is None:
            self._list = self.list__safederef

        return self._list

    @property
    def list__safederef(self) -> 'list[EscExpr]':
        """Return ``list``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``list`` stays unset and each
        call returns a new default. Use ``list__autoinit`` to persist the default on
        first access.
        """
        if self._list is None:
            return []

        return self._list

    @property
    def object(self) -> 'dict[str, EscExpr]':
        return self._object

    @object.setter
    def object(self, object: 'dict[str, EscExpr]'):
        self._object = object

    @property
    def object__autoinit(self) -> 'dict[str, EscExpr]':
        """Return ``object``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._object``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``object__safederef`` when you only want to read a default without mutating self.
        """
        if self._object is None:
            self._object = self.object__safederef

        return self._object

    @property
    def object__safederef(self) -> 'dict[str, EscExpr]':
        """Return ``object``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``object`` stays unset and each
        call returns a new default. Use ``object__autoinit`` to persist the default on
        first access.
        """
        if self._object is None:
            return {}

        return self._object

    @property
    def builtin(self) -> 'EscBuiltinExpr':
        return self._builtin

    @builtin.setter
    def builtin(self, builtin: 'EscBuiltinExpr'):
        self._builtin = builtin

    @property
    def builtin__autoinit(self) -> 'EscBuiltinExpr':
        """Return ``builtin``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._builtin``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``builtin__safederef`` when you only want to read a default without mutating self.
        """
        if self._builtin is None:
            self._builtin = self.builtin__safederef

        return self._builtin

    @property
    def builtin__safederef(self) -> 'EscBuiltinExpr':
        """Return ``builtin``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``builtin`` stays unset and each
        call returns a new default. Use ``builtin__autoinit`` to persist the default on
        first access.
        """
        if self._builtin is None:
            from .. import models
            return models.EscBuiltinExpr._new_empty()

        return self._builtin

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, EscExpr):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
