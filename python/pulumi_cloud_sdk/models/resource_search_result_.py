# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class ResourceSearchResult(PulumiAutoModelEncoder):
    """
    :var total: int - declared
    :var resources: list[ResourceResult] - declared
    :var aggregations: dict[str, Aggregation] - declared
    :var pagination: ResourceSearchPagination - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'total': 'int',
        'resources': 'list[ResourceResult]',
        'aggregations': 'dict[str, Aggregation]',
        'pagination': 'ResourceSearchPagination',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'total': 'total',
        'resources': 'resources',
        'aggregations': 'aggregations',
        'pagination': 'pagination',
    }

    FIELDS_total = 'total'
    FIELDS_resources = 'resources'
    FIELDS_aggregations = 'aggregations'
    FIELDS_pagination = 'pagination'

    _total: 'int'
    _resources: 'list[ResourceResult]'
    _aggregations: 'dict[str, Aggregation]'
    _pagination: 'ResourceSearchPagination'

    def __init__(
        self,
        total: 'int' = 0,
        resources: 'list[ResourceResult]' = None,
        aggregations: 'dict[str, Aggregation]' = None,
        pagination: 'ResourceSearchPagination' = None,
    ) -> None:
        super().__init__()

        self.total = total
        self.resources = resources
        self.aggregations = aggregations
        self.pagination = pagination

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, ResourceSearchResult):
            self._total = source._total
            self._resources = source._resources
            self._aggregations = source._aggregations
            self._pagination = source._pagination

    @property
    def total(self) -> 'int':
        return self._total

    @total.setter
    def total(self, total: 'int'):
        self._total = total

    @property
    def resources(self) -> 'list[ResourceResult]':
        return self._resources

    @resources.setter
    def resources(self, resources: 'list[ResourceResult]'):
        self._resources = resources

    @property
    def resources__autoinit(self) -> 'list[ResourceResult]':
        """Return ``resources``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._resources``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``resources__safederef`` when you only want to read a default without mutating self.
        """
        if self._resources is None:
            self._resources = self.resources__safederef

        return self._resources

    @property
    def resources__safederef(self) -> 'list[ResourceResult]':
        """Return ``resources``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``resources`` stays unset and each
        call returns a new default. Use ``resources__autoinit`` to persist the default on
        first access.
        """
        if self._resources is None:
            return []

        return self._resources

    @property
    def aggregations(self) -> 'dict[str, Aggregation]':
        return self._aggregations

    @aggregations.setter
    def aggregations(self, aggregations: 'dict[str, Aggregation]'):
        self._aggregations = aggregations

    @property
    def aggregations__autoinit(self) -> 'dict[str, Aggregation]':
        """Return ``aggregations``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._aggregations``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``aggregations__safederef`` when you only want to read a default without mutating self.
        """
        if self._aggregations is None:
            self._aggregations = self.aggregations__safederef

        return self._aggregations

    @property
    def aggregations__safederef(self) -> 'dict[str, Aggregation]':
        """Return ``aggregations``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``aggregations`` stays unset and each
        call returns a new default. Use ``aggregations__autoinit`` to persist the default on
        first access.
        """
        if self._aggregations is None:
            return {}

        return self._aggregations

    @property
    def pagination(self) -> 'ResourceSearchPagination':
        return self._pagination

    @pagination.setter
    def pagination(self, pagination: 'ResourceSearchPagination'):
        self._pagination = pagination

    @property
    def pagination__autoinit(self) -> 'ResourceSearchPagination':
        """Return ``pagination``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._pagination``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``pagination__safederef`` when you only want to read a default without mutating self.
        """
        if self._pagination is None:
            self._pagination = self.pagination__safederef

        return self._pagination

    @property
    def pagination__safederef(self) -> 'ResourceSearchPagination':
        """Return ``pagination``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``pagination`` stays unset and each
        call returns a new default. Use ``pagination__autoinit`` to persist the default on
        first access.
        """
        if self._pagination is None:
            from .. import models
            return models.ResourceSearchPagination._new_empty()

        return self._pagination

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, ResourceSearchResult):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
