# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class PkixName(PulumiAutoModelEncoder):
    """
    :var country: list[str] - declared
    :var organization: list[str] - declared
    :var organizational_unit: list[str] - declared
    :var locality: list[str] - declared
    :var province: list[str] - declared
    :var street_address: list[str] - declared
    :var postal_code: list[str] - declared
    :var serial_number: str - declared
    :var common_name: str - declared
    :var names: list[PkixAttributeTypeAndValue] - declared
    :var extra_names: list[PkixAttributeTypeAndValue] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'country': 'list[str]',
        'organization': 'list[str]',
        'organizational_unit': 'list[str]',
        'locality': 'list[str]',
        'province': 'list[str]',
        'street_address': 'list[str]',
        'postal_code': 'list[str]',
        'serial_number': 'str',
        'common_name': 'str',
        'names': 'list[PkixAttributeTypeAndValue]',
        'extra_names': 'list[PkixAttributeTypeAndValue]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'country': 'Country',
        'organization': 'Organization',
        'organizational_unit': 'OrganizationalUnit',
        'locality': 'Locality',
        'province': 'Province',
        'street_address': 'StreetAddress',
        'postal_code': 'PostalCode',
        'serial_number': 'SerialNumber',
        'common_name': 'CommonName',
        'names': 'Names',
        'extra_names': 'ExtraNames',
    }

    FIELDS_country = 'Country'
    FIELDS_organization = 'Organization'
    FIELDS_organizational_unit = 'OrganizationalUnit'
    FIELDS_locality = 'Locality'
    FIELDS_province = 'Province'
    FIELDS_street_address = 'StreetAddress'
    FIELDS_postal_code = 'PostalCode'
    FIELDS_serial_number = 'SerialNumber'
    FIELDS_common_name = 'CommonName'
    FIELDS_names = 'Names'
    FIELDS_extra_names = 'ExtraNames'

    _country: 'list[str]'
    _organization: 'list[str]'
    _organizational_unit: 'list[str]'
    _locality: 'list[str]'
    _province: 'list[str]'
    _street_address: 'list[str]'
    _postal_code: 'list[str]'
    _serial_number: 'str'
    _common_name: 'str'
    _names: 'list[PkixAttributeTypeAndValue]'
    _extra_names: 'list[PkixAttributeTypeAndValue]'

    def __init__(
        self,
        country: 'list[str]',
        organization: 'list[str]',
        organizational_unit: 'list[str]',
        locality: 'list[str]',
        province: 'list[str]',
        street_address: 'list[str]',
        postal_code: 'list[str]',
        serial_number: 'str',
        common_name: 'str',
        names: 'list[PkixAttributeTypeAndValue]',
        extra_names: 'list[PkixAttributeTypeAndValue]',
    ) -> None:
        super().__init__()

        self.country = country
        self.organization = organization
        self.organizational_unit = organizational_unit
        self.locality = locality
        self.province = province
        self.street_address = street_address
        self.postal_code = postal_code
        self.serial_number = serial_number
        self.common_name = common_name
        self.names = names
        self.extra_names = extra_names

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, PkixName):
            self._country = source._country
            self._organization = source._organization
            self._organizational_unit = source._organizational_unit
            self._locality = source._locality
            self._province = source._province
            self._street_address = source._street_address
            self._postal_code = source._postal_code
            self._serial_number = source._serial_number
            self._common_name = source._common_name
            self._names = source._names
            self._extra_names = source._extra_names

    @property
    def country(self) -> 'list[str]':
        return self._country

    @country.setter
    def country(self, country: 'list[str]'):
        if country is None:
            raise ValueError("Invalid value for `country`, must not be `None`")

        self._country = country

    @property
    def country__autoinit(self) -> 'list[str]':
        """Return ``country``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._country``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``country__safederef`` when you only want to read a default without mutating self.
        """
        if self._country is None:
            self._country = self.country__safederef

        return self._country

    @property
    def country__safederef(self) -> 'list[str]':
        """Return ``country``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``country`` stays unset and each
        call returns a new default. Use ``country__autoinit`` to persist the default on
        first access.
        """
        if self._country is None:
            return []

        return self._country

    @property
    def organization(self) -> 'list[str]':
        return self._organization

    @organization.setter
    def organization(self, organization: 'list[str]'):
        if organization is None:
            raise ValueError("Invalid value for `organization`, must not be `None`")

        self._organization = organization

    @property
    def organization__autoinit(self) -> 'list[str]':
        """Return ``organization``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._organization``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``organization__safederef`` when you only want to read a default without mutating self.
        """
        if self._organization is None:
            self._organization = self.organization__safederef

        return self._organization

    @property
    def organization__safederef(self) -> 'list[str]':
        """Return ``organization``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``organization`` stays unset and each
        call returns a new default. Use ``organization__autoinit`` to persist the default on
        first access.
        """
        if self._organization is None:
            return []

        return self._organization

    @property
    def organizational_unit(self) -> 'list[str]':
        return self._organizational_unit

    @organizational_unit.setter
    def organizational_unit(self, organizational_unit: 'list[str]'):
        if organizational_unit is None:
            raise ValueError("Invalid value for `organizational_unit`, must not be `None`")

        self._organizational_unit = organizational_unit

    @property
    def organizational_unit__autoinit(self) -> 'list[str]':
        """Return ``organizational_unit``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._organizational_unit``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``organizational_unit__safederef`` when you only want to read a default without mutating self.
        """
        if self._organizational_unit is None:
            self._organizational_unit = self.organizational_unit__safederef

        return self._organizational_unit

    @property
    def organizational_unit__safederef(self) -> 'list[str]':
        """Return ``organizational_unit``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``organizational_unit`` stays unset and each
        call returns a new default. Use ``organizational_unit__autoinit`` to persist the default on
        first access.
        """
        if self._organizational_unit is None:
            return []

        return self._organizational_unit

    @property
    def locality(self) -> 'list[str]':
        return self._locality

    @locality.setter
    def locality(self, locality: 'list[str]'):
        if locality is None:
            raise ValueError("Invalid value for `locality`, must not be `None`")

        self._locality = locality

    @property
    def locality__autoinit(self) -> 'list[str]':
        """Return ``locality``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._locality``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``locality__safederef`` when you only want to read a default without mutating self.
        """
        if self._locality is None:
            self._locality = self.locality__safederef

        return self._locality

    @property
    def locality__safederef(self) -> 'list[str]':
        """Return ``locality``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``locality`` stays unset and each
        call returns a new default. Use ``locality__autoinit`` to persist the default on
        first access.
        """
        if self._locality is None:
            return []

        return self._locality

    @property
    def province(self) -> 'list[str]':
        return self._province

    @province.setter
    def province(self, province: 'list[str]'):
        if province is None:
            raise ValueError("Invalid value for `province`, must not be `None`")

        self._province = province

    @property
    def province__autoinit(self) -> 'list[str]':
        """Return ``province``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._province``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``province__safederef`` when you only want to read a default without mutating self.
        """
        if self._province is None:
            self._province = self.province__safederef

        return self._province

    @property
    def province__safederef(self) -> 'list[str]':
        """Return ``province``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``province`` stays unset and each
        call returns a new default. Use ``province__autoinit`` to persist the default on
        first access.
        """
        if self._province is None:
            return []

        return self._province

    @property
    def street_address(self) -> 'list[str]':
        return self._street_address

    @street_address.setter
    def street_address(self, street_address: 'list[str]'):
        if street_address is None:
            raise ValueError("Invalid value for `street_address`, must not be `None`")

        self._street_address = street_address

    @property
    def street_address__autoinit(self) -> 'list[str]':
        """Return ``street_address``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._street_address``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``street_address__safederef`` when you only want to read a default without mutating self.
        """
        if self._street_address is None:
            self._street_address = self.street_address__safederef

        return self._street_address

    @property
    def street_address__safederef(self) -> 'list[str]':
        """Return ``street_address``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``street_address`` stays unset and each
        call returns a new default. Use ``street_address__autoinit`` to persist the default on
        first access.
        """
        if self._street_address is None:
            return []

        return self._street_address

    @property
    def postal_code(self) -> 'list[str]':
        return self._postal_code

    @postal_code.setter
    def postal_code(self, postal_code: 'list[str]'):
        if postal_code is None:
            raise ValueError("Invalid value for `postal_code`, must not be `None`")

        self._postal_code = postal_code

    @property
    def postal_code__autoinit(self) -> 'list[str]':
        """Return ``postal_code``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._postal_code``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``postal_code__safederef`` when you only want to read a default without mutating self.
        """
        if self._postal_code is None:
            self._postal_code = self.postal_code__safederef

        return self._postal_code

    @property
    def postal_code__safederef(self) -> 'list[str]':
        """Return ``postal_code``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``postal_code`` stays unset and each
        call returns a new default. Use ``postal_code__autoinit`` to persist the default on
        first access.
        """
        if self._postal_code is None:
            return []

        return self._postal_code

    @property
    def serial_number(self) -> 'str':
        return self._serial_number

    @serial_number.setter
    def serial_number(self, serial_number: 'str'):
        if serial_number is None:
            raise ValueError("Invalid value for `serial_number`, must not be `None`")

        self._serial_number = serial_number

    @property
    def common_name(self) -> 'str':
        return self._common_name

    @common_name.setter
    def common_name(self, common_name: 'str'):
        if common_name is None:
            raise ValueError("Invalid value for `common_name`, must not be `None`")

        self._common_name = common_name

    @property
    def names(self) -> 'list[PkixAttributeTypeAndValue]':
        return self._names

    @names.setter
    def names(self, names: 'list[PkixAttributeTypeAndValue]'):
        if names is None:
            raise ValueError("Invalid value for `names`, must not be `None`")

        self._names = names

    @property
    def names__autoinit(self) -> 'list[PkixAttributeTypeAndValue]':
        """Return ``names``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._names``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``names__safederef`` when you only want to read a default without mutating self.
        """
        if self._names is None:
            self._names = self.names__safederef

        return self._names

    @property
    def names__safederef(self) -> 'list[PkixAttributeTypeAndValue]':
        """Return ``names``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``names`` stays unset and each
        call returns a new default. Use ``names__autoinit`` to persist the default on
        first access.
        """
        if self._names is None:
            return []

        return self._names

    @property
    def extra_names(self) -> 'list[PkixAttributeTypeAndValue]':
        return self._extra_names

    @extra_names.setter
    def extra_names(self, extra_names: 'list[PkixAttributeTypeAndValue]'):
        if extra_names is None:
            raise ValueError("Invalid value for `extra_names`, must not be `None`")

        self._extra_names = extra_names

    @property
    def extra_names__autoinit(self) -> 'list[PkixAttributeTypeAndValue]':
        """Return ``extra_names``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._extra_names``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``extra_names__safederef`` when you only want to read a default without mutating self.
        """
        if self._extra_names is None:
            self._extra_names = self.extra_names__safederef

        return self._extra_names

    @property
    def extra_names__safederef(self) -> 'list[PkixAttributeTypeAndValue]':
        """Return ``extra_names``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``extra_names`` stays unset and each
        call returns a new default. Use ``extra_names__autoinit`` to persist the default on
        first access.
        """
        if self._extra_names is None:
            return []

        return self._extra_names

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, PkixName):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
