# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class OidcIssuerRegistrationRequest(PulumiAutoModelEncoder):
    """
    :var name: str - declared
    :var url: str - declared
    :var thumbprints: list[str] - declared
    :var max_expiration: int - declared
    :var jwks: JSONWebKeySet - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'name': 'str',
        'url': 'str',
        'thumbprints': 'list[str]',
        'max_expiration': 'int',
        'jwks': 'JSONWebKeySet',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'name': 'name',
        'url': 'url',
        'thumbprints': 'thumbprints',
        'max_expiration': 'maxExpiration',
        'jwks': 'jwks',
    }

    FIELDS_name = 'name'
    FIELDS_url = 'url'
    FIELDS_thumbprints = 'thumbprints'
    FIELDS_max_expiration = 'maxExpiration'
    FIELDS_jwks = 'jwks'

    _name: 'str'
    _url: 'str'
    _thumbprints: 'list[str]'
    _max_expiration: 'int'
    _jwks: 'JSONWebKeySet'

    def __init__(
        self,
        name: 'str',
        url: 'str',
        thumbprints: 'list[str]' = None,
        max_expiration: 'int' = 0,
        jwks: 'JSONWebKeySet' = None,
    ) -> None:
        super().__init__()

        self.name = name
        self.url = url
        self.thumbprints = thumbprints
        self.max_expiration = max_expiration
        self.jwks = jwks

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, OidcIssuerRegistrationRequest):
            self._name = source._name
            self._url = source._url
            self._thumbprints = source._thumbprints
            self._max_expiration = source._max_expiration
            self._jwks = source._jwks

    @property
    def name(self) -> 'str':
        return self._name

    @name.setter
    def name(self, name: 'str'):
        if name is None:
            raise ValueError("Invalid value for `name`, must not be `None`")

        self._name = name

    @property
    def url(self) -> 'str':
        return self._url

    @url.setter
    def url(self, url: 'str'):
        if url is None:
            raise ValueError("Invalid value for `url`, must not be `None`")

        self._url = url

    @property
    def thumbprints(self) -> 'list[str]':
        return self._thumbprints

    @thumbprints.setter
    def thumbprints(self, thumbprints: 'list[str]'):
        self._thumbprints = thumbprints

    @property
    def thumbprints__autoinit(self) -> 'list[str]':
        """Return ``thumbprints``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._thumbprints``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``thumbprints__safederef`` when you only want to read a default without mutating self.
        """
        if self._thumbprints is None:
            self._thumbprints = self.thumbprints__safederef

        return self._thumbprints

    @property
    def thumbprints__safederef(self) -> 'list[str]':
        """Return ``thumbprints``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``thumbprints`` stays unset and each
        call returns a new default. Use ``thumbprints__autoinit`` to persist the default on
        first access.
        """
        if self._thumbprints is None:
            return []

        return self._thumbprints

    @property
    def max_expiration(self) -> 'int':
        return self._max_expiration

    @max_expiration.setter
    def max_expiration(self, max_expiration: 'int'):
        self._max_expiration = max_expiration

    @property
    def jwks(self) -> 'JSONWebKeySet':
        return self._jwks

    @jwks.setter
    def jwks(self, jwks: 'JSONWebKeySet'):
        self._jwks = jwks

    @property
    def jwks__autoinit(self) -> 'JSONWebKeySet':
        """Return ``jwks``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._jwks``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``jwks__safederef`` when you only want to read a default without mutating self.
        """
        if self._jwks is None:
            self._jwks = self.jwks__safederef

        return self._jwks

    @property
    def jwks__safederef(self) -> 'JSONWebKeySet':
        """Return ``jwks``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``jwks`` stays unset and each
        call returns a new default. Use ``jwks__autoinit`` to persist the default on
        first access.
        """
        if self._jwks is None:
            from .. import models
            return models.JSONWebKeySet._new_empty()

        return self._jwks

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, OidcIssuerRegistrationRequest):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
