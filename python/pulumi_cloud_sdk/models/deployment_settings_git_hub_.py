# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .._support import PulumiAutoModelEncoder

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class DeploymentSettingsGitHub(PulumiAutoModelEncoder):
    """
    :var repository: str - declared
    :var pull_request_template: bool - declared
    :var deploy_commits: bool - declared
    :var preview_pull_requests: bool - declared
    :var deploy_pull_request: int - declared
    :var paths: list[str] - declared
    :var installation_id: str - declared
    :var review_stack_labels: list[str] - declared
    :var deploy_tags: bool - declared
    :var tag_filters: list[str] - declared
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'repository': 'str',
        'pull_request_template': 'bool',
        'deploy_commits': 'bool',
        'preview_pull_requests': 'bool',
        'deploy_pull_request': 'int',
        'paths': 'list[str]',
        'installation_id': 'str',
        'review_stack_labels': 'list[str]',
        'deploy_tags': 'bool',
        'tag_filters': 'list[str]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'repository': 'repository',
        'pull_request_template': 'pullRequestTemplate',
        'deploy_commits': 'deployCommits',
        'preview_pull_requests': 'previewPullRequests',
        'deploy_pull_request': 'deployPullRequest',
        'paths': 'paths',
        'installation_id': 'installationId',
        'review_stack_labels': 'reviewStackLabels',
        'deploy_tags': 'deployTags',
        'tag_filters': 'tagFilters',
    }

    FIELDS_repository = 'repository'
    FIELDS_pull_request_template = 'pullRequestTemplate'
    FIELDS_deploy_commits = 'deployCommits'
    FIELDS_preview_pull_requests = 'previewPullRequests'
    FIELDS_deploy_pull_request = 'deployPullRequest'
    FIELDS_paths = 'paths'
    FIELDS_installation_id = 'installationId'
    FIELDS_review_stack_labels = 'reviewStackLabels'
    FIELDS_deploy_tags = 'deployTags'
    FIELDS_tag_filters = 'tagFilters'

    _repository: 'str'
    _pull_request_template: 'bool'
    _deploy_commits: 'bool'
    _preview_pull_requests: 'bool'
    _deploy_pull_request: 'int'
    _paths: 'list[str]'
    _installation_id: 'str'
    _review_stack_labels: 'list[str]'
    _deploy_tags: 'bool'
    _tag_filters: 'list[str]'

    def __init__(
        self,
        repository: 'str' = None,
        pull_request_template: 'bool' = False,
        deploy_commits: 'bool' = False,
        preview_pull_requests: 'bool' = False,
        deploy_pull_request: 'int' = 0,
        paths: 'list[str]' = None,
        installation_id: 'str' = None,
        review_stack_labels: 'list[str]' = None,
        deploy_tags: 'bool' = False,
        tag_filters: 'list[str]' = None,
    ) -> None:
        super().__init__()

        self.repository = repository
        self.pull_request_template = pull_request_template
        self.deploy_commits = deploy_commits
        self.preview_pull_requests = preview_pull_requests
        self.deploy_pull_request = deploy_pull_request
        self.paths = paths
        self.installation_id = installation_id
        self.review_stack_labels = review_stack_labels
        self.deploy_tags = deploy_tags
        self.tag_filters = tag_filters

    def copy_common_fields(self, source: Any, /) -> None:
        if isinstance(source, DeploymentSettingsGitHub):
            self._repository = source._repository
            self._pull_request_template = source._pull_request_template
            self._deploy_commits = source._deploy_commits
            self._preview_pull_requests = source._preview_pull_requests
            self._deploy_pull_request = source._deploy_pull_request
            self._paths = source._paths
            self._installation_id = source._installation_id
            self._review_stack_labels = source._review_stack_labels
            self._deploy_tags = source._deploy_tags
            self._tag_filters = source._tag_filters

    @property
    def repository(self) -> 'str':
        return self._repository

    @repository.setter
    def repository(self, repository: 'str'):
        self._repository = repository

    @property
    def pull_request_template(self) -> 'bool':
        return self._pull_request_template

    @pull_request_template.setter
    def pull_request_template(self, pull_request_template: 'bool'):
        self._pull_request_template = pull_request_template

    @property
    def deploy_commits(self) -> 'bool':
        return self._deploy_commits

    @deploy_commits.setter
    def deploy_commits(self, deploy_commits: 'bool'):
        self._deploy_commits = deploy_commits

    @property
    def preview_pull_requests(self) -> 'bool':
        return self._preview_pull_requests

    @preview_pull_requests.setter
    def preview_pull_requests(self, preview_pull_requests: 'bool'):
        self._preview_pull_requests = preview_pull_requests

    @property
    def deploy_pull_request(self) -> 'int':
        return self._deploy_pull_request

    @deploy_pull_request.setter
    def deploy_pull_request(self, deploy_pull_request: 'int'):
        self._deploy_pull_request = deploy_pull_request

    @property
    def paths(self) -> 'list[str]':
        return self._paths

    @paths.setter
    def paths(self, paths: 'list[str]'):
        self._paths = paths

    @property
    def paths__autoinit(self) -> 'list[str]':
        """Return ``paths``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._paths``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``paths__safederef`` when you only want to read a default without mutating self.
        """
        if self._paths is None:
            self._paths = self.paths__safederef

        return self._paths

    @property
    def paths__safederef(self) -> 'list[str]':
        """Return ``paths``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``paths`` stays unset and each
        call returns a new default. Use ``paths__autoinit`` to persist the default on
        first access.
        """
        if self._paths is None:
            return []

        return self._paths

    @property
    def installation_id(self) -> 'str':
        return self._installation_id

    @installation_id.setter
    def installation_id(self, installation_id: 'str'):
        self._installation_id = installation_id

    @property
    def review_stack_labels(self) -> 'list[str]':
        return self._review_stack_labels

    @review_stack_labels.setter
    def review_stack_labels(self, review_stack_labels: 'list[str]'):
        self._review_stack_labels = review_stack_labels

    @property
    def review_stack_labels__autoinit(self) -> 'list[str]':
        """Return ``review_stack_labels``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._review_stack_labels``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``review_stack_labels__safederef`` when you only want to read a default without mutating self.
        """
        if self._review_stack_labels is None:
            self._review_stack_labels = self.review_stack_labels__safederef

        return self._review_stack_labels

    @property
    def review_stack_labels__safederef(self) -> 'list[str]':
        """Return ``review_stack_labels``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``review_stack_labels`` stays unset and each
        call returns a new default. Use ``review_stack_labels__autoinit`` to persist the default on
        first access.
        """
        if self._review_stack_labels is None:
            return []

        return self._review_stack_labels

    @property
    def deploy_tags(self) -> 'bool':
        return self._deploy_tags

    @deploy_tags.setter
    def deploy_tags(self, deploy_tags: 'bool'):
        self._deploy_tags = deploy_tags

    @property
    def tag_filters(self) -> 'list[str]':
        return self._tag_filters

    @tag_filters.setter
    def tag_filters(self, tag_filters: 'list[str]'):
        self._tag_filters = tag_filters

    @property
    def tag_filters__autoinit(self) -> 'list[str]':
        """Return ``tag_filters``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._tag_filters``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``tag_filters__safederef`` when you only want to read a default without mutating self.
        """
        if self._tag_filters is None:
            self._tag_filters = self.tag_filters__safederef

        return self._tag_filters

    @property
    def tag_filters__safederef(self) -> 'list[str]':
        """Return ``tag_filters``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``tag_filters`` stays unset and each
        call returns a new default. Use ``tag_filters__autoinit`` to persist the default on
        first access.
        """
        if self._tag_filters is None:
            return []

        return self._tag_filters

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, DeploymentSettingsGitHub):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
