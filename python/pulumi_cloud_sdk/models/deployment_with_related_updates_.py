# coding: utf-8

# Copyright 2016-2026, Pulumi Corporation. All rights reserved.
# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
# RUN 'make openapi_python' TO REFRESH.

# noinspection PyUnresolvedReferences
from datetime import datetime, date, time
# noinspection PyUnresolvedReferences
from typing import Optional, Any, Type
from .get_deployment_response_ import GetDeploymentResponse

# noinspection PyUnresolvedReferences
from . import *


# noinspection PyShadowingBuiltins,PyShadowingNames
class DeploymentWithRelatedUpdates(GetDeploymentResponse):
    """
    :var updates: list[UpdateInfo] - declared
    :var id: str - inherited
    :var created: str - inherited
    :var modified: str - inherited
    :var status: JobStatus - inherited
    :var version: int - inherited
    :var requested_by: UserInfo - inherited
    :var jobs: list[DeploymentJob] - inherited
    :var latest_version: int - inherited
    :var configuration: DeploymentConfiguration - inherited
    :var initiator: str - inherited
    :var pulumi_operation: PulumiOperation - inherited
    """
    __swagger_types__ = {  # The key is attribute name and the value is attribute type.
        'id': 'str',
        'created': 'str',
        'modified': 'str',
        'status': 'JobStatus',
        'version': 'int',
        'requested_by': 'UserInfo',
        'jobs': 'list[DeploymentJob]',
        'latest_version': 'int',
        'configuration': 'DeploymentConfiguration',
        'initiator': 'str',
        'pulumi_operation': 'PulumiOperation',
        'updates': 'list[UpdateInfo]',
    }

    __attribute_map__ = {  # The key is attribute name and the value is json key in definition.
        'id': 'id',
        'created': 'created',
        'modified': 'modified',
        'status': 'status',
        'version': 'version',
        'requested_by': 'requestedBy',
        'jobs': 'jobs',
        'latest_version': 'latestVersion',
        'configuration': 'configuration',
        'initiator': 'initiator',
        'pulumi_operation': 'pulumiOperation',
        'updates': 'updates',
    }

    FIELDS_updates = 'updates'

    _updates: 'list[UpdateInfo]'

    def __init__(
        self,
        id: 'str',
        created: 'str',
        modified: 'str',
        status: 'JobStatus',
        version: 'int',
        requested_by: 'UserInfo',
        latest_version: 'int',
        updates: 'list[UpdateInfo]',
        jobs: 'list[DeploymentJob]' = None,
        configuration: 'DeploymentConfiguration' = None,
        initiator: 'str' = None,
        pulumi_operation: 'PulumiOperation' = None,
    ) -> None:
        super().__init__(id=id, created=created, modified=modified, status=status, version=version, requested_by=requested_by, jobs=jobs, latest_version=latest_version, configuration=configuration, initiator=initiator, pulumi_operation=pulumi_operation)

        self.updates = updates

    def copy_common_fields(self, source: Any, /) -> None:
        super().copy_common_fields(source)
        if isinstance(source, DeploymentWithRelatedUpdates):
            self._updates = source._updates

    @property
    def updates(self) -> 'list[UpdateInfo]':
        return self._updates

    @updates.setter
    def updates(self, updates: 'list[UpdateInfo]'):
        if updates is None:
            raise ValueError("Invalid value for `updates`, must not be `None`")

        self._updates = updates

    @property
    def updates__autoinit(self) -> 'list[UpdateInfo]':
        """Return ``updates``, creating and storing an empty default when it is unset.

        Side effect: the created default is written back to ``self._updates``, so it
        persists on the model and is included when the model is serialized. Prefer
        ``updates__safederef`` when you only want to read a default without mutating self.
        """
        if self._updates is None:
            self._updates = self.updates__safederef

        return self._updates

    @property
    def updates__safederef(self) -> 'list[UpdateInfo]':
        """Return ``updates``, or a freshly built empty default when it is unset.

        No side effect: the default is not stored, so ``updates`` stays unset and each
        call returns a new default. Use ``updates__autoinit`` to persist the default on
        first access.
        """
        if self._updates is None:
            return []

        return self._updates

    def __eq__(self, other: Any) -> bool:
        if not isinstance(other, DeploymentWithRelatedUpdates):
            return False

        return self.__dict__ == other.__dict__

    def __ne__(self, other: Any) -> bool:
        return not self == other
