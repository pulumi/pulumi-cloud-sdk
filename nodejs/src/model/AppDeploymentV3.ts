// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { AppExtension } from "./AppExtension";
import type { AppManifestV1 } from "./AppManifestV1";
import type { AppOperationV2 } from "./AppOperationV2";
import type { AppResourceV3 } from "./AppResourceV3";
import type { AppSecretsProvidersV1 } from "./AppSecretsProvidersV1";
import type { AppSnapshotMetadataV1 } from "./AppSnapshotMetadataV1";
import type { AppSnippetV1 } from "./AppSnippetV1";


export class AppDeploymentV3 {
    manifest: AppManifestV1;
    secrets_providers?: AppSecretsProvidersV1;
    resources?: AppResourceV3[];
    pending_operations?: AppOperationV2[];
    metadata?: AppSnapshotMetadataV1;
    snippets?: AppSnippetV1[];
    extensions?: { [key: string]: AppExtension };

    getFixupPrototypeFunction() {
        return AppDeploymentV3.fixupPrototype;
    }

    static newInstance(model: Partial<AppDeploymentV3>): AppDeploymentV3 {
        let obj = Object.assign(new AppDeploymentV3(), model);
        AppDeploymentV3.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<AppDeploymentV3>): AppDeploymentV3 | null {
        if (!model) return null;
        return AppDeploymentV3.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: AppDeploymentV3) {
        if (!obj) return;

        AppDeploymentV3.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: AppDeploymentV3): boolean {
        Object.setPrototypeOf(obj, AppDeploymentV3.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (this.manifest) {
            // Lazy import - only evaluated when called
            const { AppManifestV1 } = require("./AppManifestV1");
            AppManifestV1.fixupPrototype(this.manifest);
        }
        if (this.secrets_providers) {
            // Lazy import - only evaluated when called
            const { AppSecretsProvidersV1 } = require("./AppSecretsProvidersV1");
            AppSecretsProvidersV1.fixupPrototype(this.secrets_providers);
        }
        if (this.resources) {
            for (let i0 = 0; i0 < this.resources.length; i0++) {
                let val0 = this.resources[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { AppResourceV3 } = require("./AppResourceV3");
                    AppResourceV3.fixupPrototype(val0);
                }
            }
        }
        if (this.pending_operations) {
            for (let i0 = 0; i0 < this.pending_operations.length; i0++) {
                let val0 = this.pending_operations[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { AppOperationV2 } = require("./AppOperationV2");
                    AppOperationV2.fixupPrototype(val0);
                }
            }
        }
        if (this.metadata) {
            // Lazy import - only evaluated when called
            const { AppSnapshotMetadataV1 } = require("./AppSnapshotMetadataV1");
            AppSnapshotMetadataV1.fixupPrototype(this.metadata);
        }
        if (this.snippets) {
            for (let i0 = 0; i0 < this.snippets.length; i0++) {
                let val0 = this.snippets[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { AppSnippetV1 } = require("./AppSnippetV1");
                    AppSnippetV1.fixupPrototype(val0);
                }
            }
        }
        if (this.extensions) {
            for (let key0 in this.extensions) {
                let val0 = this.extensions[key0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { AppExtension } = require("./AppExtension");
                    AppExtension.fixupPrototype(val0);
                }
            }
        }
    }
}
