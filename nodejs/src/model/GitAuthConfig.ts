// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { BasicAuth } from "./BasicAuth";
import type { SSHAuth } from "./SSHAuth";
import type { SecretValue } from "./SecretValue";


export class GitAuthConfig {
    accessToken?: SecretValue;
    sshAuth?: SSHAuth;
    basicAuth?: BasicAuth;

    getFixupPrototypeFunction() {
        return GitAuthConfig.fixupPrototype;
    }

    static newInstance(model: Partial<GitAuthConfig>): GitAuthConfig {
        let obj = Object.assign(new GitAuthConfig(), model);
        GitAuthConfig.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<GitAuthConfig>): GitAuthConfig | null {
        if (!model) return null;
        return GitAuthConfig.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: GitAuthConfig) {
        if (!obj) return;

        GitAuthConfig.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: GitAuthConfig): boolean {
        Object.setPrototypeOf(obj, GitAuthConfig.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (this.accessToken !== undefined && this.accessToken !== null) {
            // Lazy import - only evaluated when called
            const { SecretValue } = require("./SecretValue");
            if (typeof this.accessToken === "string") {
                this.accessToken = SecretValue.newInstance({plaintext: this.accessToken});
            }
            SecretValue.fixupPrototype(this.accessToken);
        }
        if (this.sshAuth) {
            // Lazy import - only evaluated when called
            const { SSHAuth } = require("./SSHAuth");
            SSHAuth.fixupPrototype(this.sshAuth);
        }
        if (this.basicAuth) {
            // Lazy import - only evaluated when called
            const { BasicAuth } = require("./BasicAuth");
            BasicAuth.fixupPrototype(this.basicAuth);
        }
    }
}
