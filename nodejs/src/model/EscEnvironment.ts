// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { EscEvaluatedExecutionContext } from "./EscEvaluatedExecutionContext";
import type { EscExpr } from "./EscExpr";
import type { EscSchemaSchema } from "./EscSchemaSchema";
import type { EscValue } from "./EscValue";


export class EscEnvironment {
    exprs?: { [key: string]: EscExpr };
    properties?: { [key: string]: EscValue };
    schema?: EscSchemaSchema;
    executionContext?: EscEvaluatedExecutionContext;

    getFixupPrototypeFunction() {
        return EscEnvironment.fixupPrototype;
    }

    static newInstance(model: Partial<EscEnvironment>): EscEnvironment {
        let obj = Object.assign(new EscEnvironment(), model);
        EscEnvironment.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<EscEnvironment>): EscEnvironment | null {
        if (!model) return null;
        return EscEnvironment.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: EscEnvironment) {
        if (!obj) return;

        EscEnvironment.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: EscEnvironment): boolean {
        Object.setPrototypeOf(obj, EscEnvironment.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    static getTypeHierarchy(): (typeof EscEnvironment)[] {
        // Lazy import - only evaluated when called
        const { CheckEnvironmentResponse } = require("./CheckEnvironmentResponse");
        const { EnvironmentResponse } = require("./EnvironmentResponse");

        return [
            CheckEnvironmentResponse,
            EnvironmentResponse,
        ];
    }

    fixupFields() {
        if (this.exprs) {
            for (let key0 in this.exprs) {
                let val0 = this.exprs[key0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { EscExpr } = require("./EscExpr");
                    EscExpr.fixupPrototype(val0);
                }
            }
        }
        if (this.properties) {
            for (let key0 in this.properties) {
                let val0 = this.properties[key0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { EscValue } = require("./EscValue");
                    EscValue.fixupPrototype(val0);
                }
            }
        }
        if (this.schema) {
            // Lazy import - only evaluated when called
            const { EscSchemaSchema } = require("./EscSchemaSchema");
            EscSchemaSchema.fixupPrototype(this.schema);
        }
        if (this.executionContext) {
            // Lazy import - only evaluated when called
            const { EscEvaluatedExecutionContext } = require("./EscEvaluatedExecutionContext");
            EscEvaluatedExecutionContext.fixupPrototype(this.executionContext);
        }
    }
}
