// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { AgentAccountSummary } from "./AgentAccountSummary";
import type { TransferEntity } from "./TransferEntity";
import type { TransferEntityFailure } from "./TransferEntityFailure";


export class AgentClaimResponse {
    agent: AgentAccountSummary;
    entities: TransferEntity[];
    conflicts: TransferEntity[];
    failures: TransferEntityFailure[];
    claimExpiresAt: Date;
    transferToken?: string;

    getFixupPrototypeFunction() {
        return AgentClaimResponse.fixupPrototype;
    }

    static newInstance(model: Partial<AgentClaimResponse>): AgentClaimResponse {
        let obj = Object.assign(new AgentClaimResponse(), model);
        AgentClaimResponse.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<AgentClaimResponse>): AgentClaimResponse | null {
        if (!model) return null;
        return AgentClaimResponse.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: AgentClaimResponse) {
        if (!obj) return;

        AgentClaimResponse.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: AgentClaimResponse): boolean {
        Object.setPrototypeOf(obj, AgentClaimResponse.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (this.agent) {
            // Lazy import - only evaluated when called
            const { AgentAccountSummary } = require("./AgentAccountSummary");
            AgentAccountSummary.fixupPrototype(this.agent);
        }
        if (this.entities) {
            for (let i0 = 0; i0 < this.entities.length; i0++) {
                let val0 = this.entities[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { TransferEntity } = require("./TransferEntity");
                    TransferEntity.fixupPrototype(val0);
                }
            }
        }
        if (this.conflicts) {
            for (let i0 = 0; i0 < this.conflicts.length; i0++) {
                let val0 = this.conflicts[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { TransferEntity } = require("./TransferEntity");
                    TransferEntity.fixupPrototype(val0);
                }
            }
        }
        if (this.failures) {
            for (let i0 = 0; i0 < this.failures.length; i0++) {
                let val0 = this.failures[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { TransferEntityFailure } = require("./TransferEntityFailure");
                    TransferEntityFailure.fixupPrototype(val0);
                }
            }
        }
        if (typeof this.claimExpiresAt === "string") {
            this.claimExpiresAt = new Date(this.claimExpiresAt);
        }
    }
}
