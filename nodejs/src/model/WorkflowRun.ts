// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { JobRun } from "./JobRun";
import type { WorkflowRunStatus } from "./WorkflowRunStatus";


export class WorkflowRun {
    id: string;
    orgId: string;
    userId: string;
    status: WorkflowRunStatus;
    startedAt: Date;
    finishedAt: Date;
    lastUpdatedAt: Date;
    jobTimeout: Date;
    jobs?: JobRun[];

    getFixupPrototypeFunction() {
        return WorkflowRun.fixupPrototype;
    }

    static newInstance(model: Partial<WorkflowRun>): WorkflowRun {
        let obj = Object.assign(new WorkflowRun(), model);
        WorkflowRun.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<WorkflowRun>): WorkflowRun | null {
        if (!model) return null;
        return WorkflowRun.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: WorkflowRun) {
        if (!obj) return;

        WorkflowRun.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: WorkflowRun): boolean {
        Object.setPrototypeOf(obj, WorkflowRun.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    static getTypeHierarchy(): (typeof WorkflowRun)[] {
        // Lazy import - only evaluated when called
        const { ScanStatus } = require("./ScanStatus");
        const { ScanStatusResponse } = require("./ScanStatusResponse");

        return [
            ScanStatus,
            ScanStatusResponse,
        ];
    }

    fixupFields() {
        if (typeof this.startedAt === "string") {
            this.startedAt = new Date(this.startedAt);
        }
        if (typeof this.finishedAt === "string") {
            this.finishedAt = new Date(this.finishedAt);
        }
        if (typeof this.lastUpdatedAt === "string") {
            this.lastUpdatedAt = new Date(this.lastUpdatedAt);
        }
        if (typeof this.jobTimeout === "string") {
            this.jobTimeout = new Date(this.jobTimeout);
        }
        if (this.jobs) {
            for (let i0 = 0; i0 < this.jobs.length; i0++) {
                let val0 = this.jobs[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { JobRun } = require("./JobRun");
                    JobRun.fixupPrototype(val0);
                }
            }
        }
    }
}
