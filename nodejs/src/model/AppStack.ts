// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { AppOperationStatus } from "./AppOperationStatus";
import type { AppStackConfig } from "./AppStackConfig";


export class AppStack {
    id: string;
    orgName: string;
    projectName: string;
    stackName: string;
    currentOperation?: AppOperationStatus;
    activeUpdate: string;
    tags?: { [key: string]: string };
    config?: AppStackConfig;
    version: number;

    getFixupPrototypeFunction() {
        return AppStack.fixupPrototype;
    }

    static newInstance(model: Partial<AppStack>): AppStack {
        let obj = Object.assign(new AppStack(), model);
        AppStack.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<AppStack>): AppStack | null {
        if (!model) return null;
        return AppStack.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: AppStack) {
        if (!obj) return;

        AppStack.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: AppStack): boolean {
        Object.setPrototypeOf(obj, AppStack.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (this.currentOperation) {
            // Lazy import - only evaluated when called
            const { AppOperationStatus } = require("./AppOperationStatus");
            AppOperationStatus.fixupPrototype(this.currentOperation);
        }
        if (this.config) {
            // Lazy import - only evaluated when called
            const { AppStackConfig } = require("./AppStackConfig");
            AppStackConfig.fixupPrototype(this.config);
        }
        if (this.version === undefined) {
            this.version = 0;
        }
    }
}
