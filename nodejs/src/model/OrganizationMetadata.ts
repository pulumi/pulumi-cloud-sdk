// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { EnvironmentPermission } from "./EnvironmentPermission";
import type { InsightsAccountPermission } from "./InsightsAccountPermission";
import type { NeoApprovalMode } from "./NeoApprovalMode";
import type { NeoTaskSharingMode } from "./NeoTaskSharingMode";
import type { OrganizationFeatures } from "./OrganizationFeatures";
import type { OrganizationKind } from "./OrganizationKind";
import type { OrganizationLockedStatus } from "./OrganizationLockedStatus";
import type { OrganizationRole } from "./OrganizationRole";
import type { PreferredVCS } from "./PreferredVCS";
import type { SaasOffer } from "./SaasOffer";
import type { StackPermission } from "./StackPermission";


export class OrganizationMetadata {
    id: string;
    kind: OrganizationKind;
    created: Date;
    backingOrgLogin?: string;
    product?: SaasOffer;
    subscriptionStatus?: string;
    subscriptionTrialEnd?: number;
    retrialExpiration?: Date;
    subscriptionCancelAtPeriodEnd?: boolean;
    subscriptionPeriodEnd?: Date;
    userRole: OrganizationRole;
    defaultStackPermission: StackPermission;
    defaultEnvironmentPermission: EnvironmentPermission;
    defaultAccountPermission: InsightsAccountPermission;
    membersCanCreateStacks: boolean;
    membersCanDeleteStacks: boolean;
    membersCanTransferStacks: boolean;
    membersCanCreateTeams: boolean;
    membersCanCreateAccounts: boolean;
    neoEnabled: boolean;
    neoApprovalMode: NeoApprovalMode;
    neoTaskSharingMode: NeoTaskSharingMode;
    neoCLIExplanationsDisabled: boolean;
    naturalLanguageSearchDisabled: boolean;
    neoAgentDisabled: boolean;
    insightsTrialEnd?: number;
    insightsBillingAccepted?: boolean;
    insightsTrialUsingPolicy: boolean;
    aiEnablement: string;
    preferredVCS: PreferredVCS;
    auditLogsEnabled: boolean;
    webhooksEnabled: boolean;
    features: OrganizationFeatures;
    accountCount: number;
    environmentCount: number;
    stackCount: number;
    maxStacks?: number;
    memberCount: number;
    maxMembers?: number;
    locked?: OrganizationLockedStatus;
    defaultRoleId?: string;
    defaultDeploymentRoleId?: string;
    pastDueSince?: Date;
    maxAccessTokenExpiryDays?: number;

    getFixupPrototypeFunction() {
        return OrganizationMetadata.fixupPrototype;
    }

    static newInstance(model: Partial<OrganizationMetadata>): OrganizationMetadata {
        let obj = Object.assign(new OrganizationMetadata(), model);
        OrganizationMetadata.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<OrganizationMetadata>): OrganizationMetadata | null {
        if (!model) return null;
        return OrganizationMetadata.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: OrganizationMetadata) {
        if (!obj) return;

        OrganizationMetadata.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: OrganizationMetadata): boolean {
        Object.setPrototypeOf(obj, OrganizationMetadata.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (typeof this.created === "string") {
            this.created = new Date(this.created);
        }
        if (typeof this.retrialExpiration === "string") {
            this.retrialExpiration = new Date(this.retrialExpiration);
        }
        if (typeof this.subscriptionPeriodEnd === "string") {
            this.subscriptionPeriodEnd = new Date(this.subscriptionPeriodEnd);
        }
        if (this.features) {
            // Lazy import - only evaluated when called
            const { OrganizationFeatures } = require("./OrganizationFeatures");
            OrganizationFeatures.fixupPrototype(this.features);
        }
        if (this.accountCount === undefined) {
            this.accountCount = 0;
        }
        if (this.environmentCount === undefined) {
            this.environmentCount = 0;
        }
        if (this.stackCount === undefined) {
            this.stackCount = 0;
        }
        if (this.memberCount === undefined) {
            this.memberCount = 0;
        }
        if (typeof this.pastDueSince === "string") {
            this.pastDueSince = new Date(this.pastDueSince);
        }
    }
}
