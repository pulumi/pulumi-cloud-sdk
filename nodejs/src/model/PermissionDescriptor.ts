// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

export class PermissionDescriptor {
    private __type: string;

    protected setDiscriminator(type: string) {
        this.__type = type;
    }

    public getDiscriminator(): string {
        return this.__type;
    }

    getFixupPrototypeFunction() {
        return PermissionDescriptor.fixupPrototype;
    }

    static newInstance(model: Partial<PermissionDescriptor>): PermissionDescriptor {
        let obj = Object.assign(new PermissionDescriptor(), model);
        PermissionDescriptor.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<PermissionDescriptor>): PermissionDescriptor | null {
        if (!model) return null;
        return PermissionDescriptor.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: PermissionDescriptor) {
        if (!obj) return;

        PermissionDescriptor.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: PermissionDescriptor): boolean {
        // Lazy import - only evaluated when called
        const { PermissionDescriptorAllow } = require("./PermissionDescriptorAllow");
        const { PermissionDescriptorCompose } = require("./PermissionDescriptorCompose");
        const { PermissionDescriptorCondition } = require("./PermissionDescriptorCondition");
        const { PermissionDescriptorGroup } = require("./PermissionDescriptorGroup");
        const { PermissionDescriptorIfThenElse } = require("./PermissionDescriptorIfThenElse");
        const { PermissionDescriptorSelect } = require("./PermissionDescriptorSelect");

        switch (obj.__type) {
            case "PermissionDescriptorAllow":
                Object.setPrototypeOf(obj, PermissionDescriptorAllow.prototype);
                break;
            case "PermissionDescriptorCompose":
                Object.setPrototypeOf(obj, PermissionDescriptorCompose.prototype);
                break;
            case "PermissionDescriptorCondition":
                Object.setPrototypeOf(obj, PermissionDescriptorCondition.prototype);
                break;
            case "PermissionDescriptorGroup":
                Object.setPrototypeOf(obj, PermissionDescriptorGroup.prototype);
                break;
            case "PermissionDescriptorIfThenElse":
                Object.setPrototypeOf(obj, PermissionDescriptorIfThenElse.prototype);
                break;
            case "PermissionDescriptorSelect":
                Object.setPrototypeOf(obj, PermissionDescriptorSelect.prototype);
                break;
        }

        return true; // Tell the caller this is a polymorphic type, it should not overwrite the prototype.
    }

    static getTypeHierarchy(): (typeof PermissionDescriptor)[] {
        // Lazy import - only evaluated when called
        const { PermissionDescriptorAllow } = require("./PermissionDescriptorAllow");
        const { PermissionDescriptorCompose } = require("./PermissionDescriptorCompose");
        const { PermissionDescriptorCondition } = require("./PermissionDescriptorCondition");
        const { PermissionDescriptorGroup } = require("./PermissionDescriptorGroup");
        const { PermissionDescriptorIfThenElse } = require("./PermissionDescriptorIfThenElse");
        const { PermissionDescriptorSelect } = require("./PermissionDescriptorSelect");

        return [
            PermissionDescriptorAllow,
            PermissionDescriptorCompose,
            PermissionDescriptorCondition,
            PermissionDescriptorGroup,
            PermissionDescriptorIfThenElse,
            PermissionDescriptorSelect,
        ];
    }

    fixupFields() {
        // Nothing to do.
    }
}
