// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { EnvironmentLinks } from "./EnvironmentLinks";
import type { EnvironmentReferrerMetadata } from "./EnvironmentReferrerMetadata";
import type { EnvironmentSettings } from "./EnvironmentSettings";
import type { UserInfo } from "./UserInfo";


export class OrgEnvironment {
    id: string;
    organization: string;
    project?: string;
    name: string;
    created: string;
    modified: string;
    ownedBy: UserInfo;
    tags: { [key: string]: string };
    deletedAt?: string;
    links?: EnvironmentLinks;
    referrerMetadata: EnvironmentReferrerMetadata;
    settings: EnvironmentSettings;

    getFixupPrototypeFunction() {
        return OrgEnvironment.fixupPrototype;
    }

    static newInstance(model: Partial<OrgEnvironment>): OrgEnvironment {
        let obj = Object.assign(new OrgEnvironment(), model);
        OrgEnvironment.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<OrgEnvironment>): OrgEnvironment | null {
        if (!model) return null;
        return OrgEnvironment.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: OrgEnvironment) {
        if (!obj) return;

        OrgEnvironment.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: OrgEnvironment): boolean {
        Object.setPrototypeOf(obj, OrgEnvironment.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (this.ownedBy) {
            // Lazy import - only evaluated when called
            const { UserInfo } = require("./UserInfo");
            UserInfo.fixupPrototype(this.ownedBy);
        }
        if (this.links) {
            // Lazy import - only evaluated when called
            const { EnvironmentLinks } = require("./EnvironmentLinks");
            EnvironmentLinks.fixupPrototype(this.links);
        }
        if (this.referrerMetadata) {
            // Lazy import - only evaluated when called
            const { EnvironmentReferrerMetadata } = require("./EnvironmentReferrerMetadata");
            EnvironmentReferrerMetadata.fixupPrototype(this.referrerMetadata);
        }
        if (this.settings) {
            // Lazy import - only evaluated when called
            const { EnvironmentSettings } = require("./EnvironmentSettings");
            EnvironmentSettings.fixupPrototype(this.settings);
        }
    }
}
