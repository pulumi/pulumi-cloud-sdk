// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { ServiceMember } from "./ServiceMember";
import type { ServiceProperty } from "./ServiceProperty";


export class Service {
    organizationName: string;
    owner: ServiceMember;
    name: string;
    description: string;
    created?: Date;
    members: ServiceMember[];
    itemCountSummary: { [key: string]: number };
    properties: ServiceProperty[];

    getFixupPrototypeFunction() {
        return Service.fixupPrototype;
    }

    static newInstance(model: Partial<Service>): Service {
        let obj = Object.assign(new Service(), model);
        Service.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<Service>): Service | null {
        if (!model) return null;
        return Service.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: Service) {
        if (!obj) return;

        Service.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: Service): boolean {
        Object.setPrototypeOf(obj, Service.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (this.owner) {
            // Lazy import - only evaluated when called
            const { ServiceMember } = require("./ServiceMember");
            ServiceMember.fixupPrototype(this.owner);
        }
        if (typeof this.created === "string") {
            this.created = new Date(this.created);
        }
        if (this.members) {
            for (let i0 = 0; i0 < this.members.length; i0++) {
                let val0 = this.members[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { ServiceMember } = require("./ServiceMember");
                    ServiceMember.fixupPrototype(val0);
                }
            }
        }
        if (this.properties) {
            for (let i0 = 0; i0 < this.properties.length; i0++) {
                let val0 = this.properties[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { ServiceProperty } = require("./ServiceProperty");
                    ServiceProperty.fixupPrototype(val0);
                }
            }
        }
    }
}
