// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { DeploymentJob } from "./DeploymentJob";
import type { DeploymentNestedUpdate } from "./DeploymentNestedUpdate";
import { ListDeploymentResponse } from "./ListDeploymentResponse";
import type { ListDeploymentSnapshotAgentPool } from "./ListDeploymentSnapshotAgentPool";
import type { PulumiOperation } from "./PulumiOperation";


export class ListDeploymentSnapshot extends ListDeploymentResponse {
    projectName?: string;
    stackName?: string;
    paused?: boolean;
    pulumiOperation: PulumiOperation;
    updates: DeploymentNestedUpdate[];
    jobs: DeploymentJob[];
    initiator?: string;
    agentPool?: ListDeploymentSnapshotAgentPool;

    getFixupPrototypeFunction() {
        return ListDeploymentSnapshot.fixupPrototype;
    }

    static newInstance(model: Partial<ListDeploymentSnapshot>): ListDeploymentSnapshot {
        let obj = Object.assign(new ListDeploymentSnapshot(), model);
        ListDeploymentSnapshot.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<ListDeploymentSnapshot>): ListDeploymentSnapshot | null {
        if (!model) return null;
        return ListDeploymentSnapshot.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: ListDeploymentSnapshot) {
        if (!obj) return;

        ListDeploymentSnapshot.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: ListDeploymentSnapshot): boolean {
        if (ListDeploymentResponse.setPrototype(obj)) {
            return true; // A polymorphic type, skip overwriting the prototype.
        }
        Object.setPrototypeOf(obj, ListDeploymentSnapshot.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        super.fixupFields();
        if (this.updates) {
            for (let i0 = 0; i0 < this.updates.length; i0++) {
                let val0 = this.updates[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { DeploymentNestedUpdate } = require("./DeploymentNestedUpdate");
                    DeploymentNestedUpdate.fixupPrototype(val0);
                }
            }
        }
        if (this.jobs) {
            for (let i0 = 0; i0 < this.jobs.length; i0++) {
                let val0 = this.jobs[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { DeploymentJob } = require("./DeploymentJob");
                    DeploymentJob.fixupPrototype(val0);
                }
            }
        }
        if (this.agentPool) {
            // Lazy import - only evaluated when called
            const { ListDeploymentSnapshotAgentPool } = require("./ListDeploymentSnapshotAgentPool");
            ListDeploymentSnapshotAgentPool.fixupPrototype(this.agentPool);
        }
    }
}
