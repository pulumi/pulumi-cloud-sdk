// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { EscAccessExpr } from "./EscAccessExpr";
import type { EscBuiltinExpr } from "./EscBuiltinExpr";
import type { EscInterpolation } from "./EscInterpolation";
import type { EscPropertyAccessor } from "./EscPropertyAccessor";
import type { EscRange } from "./EscRange";
import type { EscSchemaSchema } from "./EscSchemaSchema";


export class EscExpr {
    range: EscRange;
    schema?: EscSchemaSchema;
    base?: EscExpr;
    keyRanges?: { [key: string]: EscRange };
    literal?: any;
    interpolate?: EscInterpolation[];
    symbol?: EscPropertyAccessor[];
    access?: EscAccessExpr;
    list?: EscExpr[];
    object?: { [key: string]: EscExpr };
    builtin?: EscBuiltinExpr;

    getFixupPrototypeFunction() {
        return EscExpr.fixupPrototype;
    }

    static newInstance(model: Partial<EscExpr>): EscExpr {
        let obj = Object.assign(new EscExpr(), model);
        EscExpr.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<EscExpr>): EscExpr | null {
        if (!model) return null;
        return EscExpr.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: EscExpr) {
        if (!obj) return;

        EscExpr.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: EscExpr): boolean {
        Object.setPrototypeOf(obj, EscExpr.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (this.range) {
            // Lazy import - only evaluated when called
            const { EscRange } = require("./EscRange");
            EscRange.fixupPrototype(this.range);
        }
        if (this.schema) {
            // Lazy import - only evaluated when called
            const { EscSchemaSchema } = require("./EscSchemaSchema");
            EscSchemaSchema.fixupPrototype(this.schema);
        }
        if (this.base) {
            EscExpr.fixupPrototype(this.base);
        }
        if (this.keyRanges) {
            for (let key0 in this.keyRanges) {
                let val0 = this.keyRanges[key0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { EscRange } = require("./EscRange");
                    EscRange.fixupPrototype(val0);
                }
            }
        }
        if (this.interpolate) {
            for (let i0 = 0; i0 < this.interpolate.length; i0++) {
                let val0 = this.interpolate[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { EscInterpolation } = require("./EscInterpolation");
                    EscInterpolation.fixupPrototype(val0);
                }
            }
        }
        if (this.symbol) {
            for (let i0 = 0; i0 < this.symbol.length; i0++) {
                let val0 = this.symbol[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { EscPropertyAccessor } = require("./EscPropertyAccessor");
                    EscPropertyAccessor.fixupPrototype(val0);
                }
            }
        }
        if (this.access) {
            // Lazy import - only evaluated when called
            const { EscAccessExpr } = require("./EscAccessExpr");
            EscAccessExpr.fixupPrototype(this.access);
        }
        if (this.list) {
            for (let i0 = 0; i0 < this.list.length; i0++) {
                let val0 = this.list[i0];
                if (val0) {
                    EscExpr.fixupPrototype(val0);
                }
            }
        }
        if (this.object) {
            for (let key0 in this.object) {
                let val0 = this.object[key0];
                if (val0) {
                    EscExpr.fixupPrototype(val0);
                }
            }
        }
        if (this.builtin) {
            // Lazy import - only evaluated when called
            const { EscBuiltinExpr } = require("./EscBuiltinExpr");
            EscBuiltinExpr.fixupPrototype(this.builtin);
        }
    }
}
