// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { AppInsightsAccountReference } from "./AppInsightsAccountReference";
import type { AppPolicyPackMetadata } from "./AppPolicyPackMetadata";
import type { AppPulumiStackReference } from "./AppPulumiStackReference";


export class AppUpdatePolicyGroupRequest {
    newName?: string;
    addStack?: AppPulumiStackReference;
    removeStack?: AppPulumiStackReference;
    addPolicyPack?: AppPolicyPackMetadata;
    removePolicyPack?: AppPolicyPackMetadata;
    addInsightsAccount?: AppInsightsAccountReference;
    removeInsightsAccount?: AppInsightsAccountReference;
    stacks?: AppPulumiStackReference[];
    policyPacks?: AppPolicyPackMetadata[];
    insightsAccounts?: string[];

    getFixupPrototypeFunction() {
        return AppUpdatePolicyGroupRequest.fixupPrototype;
    }

    static newInstance(model: Partial<AppUpdatePolicyGroupRequest>): AppUpdatePolicyGroupRequest {
        let obj = Object.assign(new AppUpdatePolicyGroupRequest(), model);
        AppUpdatePolicyGroupRequest.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<AppUpdatePolicyGroupRequest>): AppUpdatePolicyGroupRequest | null {
        if (!model) return null;
        return AppUpdatePolicyGroupRequest.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: AppUpdatePolicyGroupRequest) {
        if (!obj) return;

        AppUpdatePolicyGroupRequest.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: AppUpdatePolicyGroupRequest): boolean {
        Object.setPrototypeOf(obj, AppUpdatePolicyGroupRequest.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    static getTypeHierarchy(): (typeof AppUpdatePolicyGroupRequest)[] {
        // Lazy import - only evaluated when called
        const { UpdatePolicyGroupRequest } = require("./UpdatePolicyGroupRequest");

        return [
            UpdatePolicyGroupRequest,
        ];
    }

    fixupFields() {
        if (this.addStack) {
            // Lazy import - only evaluated when called
            const { AppPulumiStackReference } = require("./AppPulumiStackReference");
            AppPulumiStackReference.fixupPrototype(this.addStack);
        }
        if (this.removeStack) {
            // Lazy import - only evaluated when called
            const { AppPulumiStackReference } = require("./AppPulumiStackReference");
            AppPulumiStackReference.fixupPrototype(this.removeStack);
        }
        if (this.addPolicyPack) {
            // Lazy import - only evaluated when called
            const { AppPolicyPackMetadata } = require("./AppPolicyPackMetadata");
            AppPolicyPackMetadata.fixupPrototype(this.addPolicyPack);
        }
        if (this.removePolicyPack) {
            // Lazy import - only evaluated when called
            const { AppPolicyPackMetadata } = require("./AppPolicyPackMetadata");
            AppPolicyPackMetadata.fixupPrototype(this.removePolicyPack);
        }
        if (this.addInsightsAccount) {
            // Lazy import - only evaluated when called
            const { AppInsightsAccountReference } = require("./AppInsightsAccountReference");
            AppInsightsAccountReference.fixupPrototype(this.addInsightsAccount);
        }
        if (this.removeInsightsAccount) {
            // Lazy import - only evaluated when called
            const { AppInsightsAccountReference } = require("./AppInsightsAccountReference");
            AppInsightsAccountReference.fixupPrototype(this.removeInsightsAccount);
        }
        if (this.stacks) {
            for (let i0 = 0; i0 < this.stacks.length; i0++) {
                let val0 = this.stacks[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { AppPulumiStackReference } = require("./AppPulumiStackReference");
                    AppPulumiStackReference.fixupPrototype(val0);
                }
            }
        }
        if (this.policyPacks) {
            for (let i0 = 0; i0 < this.policyPacks.length; i0++) {
                let val0 = this.policyPacks[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { AppPolicyPackMetadata } = require("./AppPolicyPackMetadata");
                    AppPolicyPackMetadata.fixupPrototype(val0);
                }
            }
        }
    }
}
