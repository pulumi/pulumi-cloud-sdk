// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { AppDeploymentV3 } from "./AppDeploymentV3";
import type { AppExtension } from "./AppExtension";
import type { AppJournalEntryKind } from "./AppJournalEntryKind";
import type { AppOperationV2 } from "./AppOperationV2";
import type { AppResourceV3 } from "./AppResourceV3";
import type { AppSecretsProvidersV1 } from "./AppSecretsProvidersV1";
import type { AppSnippetV1 } from "./AppSnippetV1";


export class AppJournalEntry {
    version: number;
    kind: AppJournalEntryKind;
    sequenceID: number;
    operationID: number;
    removeOld: number;
    removeNew: number;
    pendingReplacementOld?: number;
    pendingReplacementNew?: number;
    deleteOld?: number;
    deleteNew?: number;
    state?: AppResourceV3;
    operation?: AppOperationV2;
    isRefresh?: boolean;
    secretsProvider?: AppSecretsProvidersV1;
    newSnapshot?: AppDeploymentV3;
    snippets?: AppSnippetV1[];
    extensionRef?: string;
    extension?: AppExtension;
    requiresByteString?: boolean;

    getFixupPrototypeFunction() {
        return AppJournalEntry.fixupPrototype;
    }

    static newInstance(model: Partial<AppJournalEntry>): AppJournalEntry {
        let obj = Object.assign(new AppJournalEntry(), model);
        AppJournalEntry.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<AppJournalEntry>): AppJournalEntry | null {
        if (!model) return null;
        return AppJournalEntry.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: AppJournalEntry) {
        if (!obj) return;

        AppJournalEntry.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: AppJournalEntry): boolean {
        Object.setPrototypeOf(obj, AppJournalEntry.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (this.version === undefined) {
            this.version = 0;
        }
        if (this.sequenceID === undefined) {
            this.sequenceID = 0;
        }
        if (this.operationID === undefined) {
            this.operationID = 0;
        }
        if (this.removeOld === undefined) {
            this.removeOld = 0;
        }
        if (this.removeNew === undefined) {
            this.removeNew = 0;
        }
        if (this.state) {
            // Lazy import - only evaluated when called
            const { AppResourceV3 } = require("./AppResourceV3");
            AppResourceV3.fixupPrototype(this.state);
        }
        if (this.operation) {
            // Lazy import - only evaluated when called
            const { AppOperationV2 } = require("./AppOperationV2");
            AppOperationV2.fixupPrototype(this.operation);
        }
        if (this.secretsProvider) {
            // Lazy import - only evaluated when called
            const { AppSecretsProvidersV1 } = require("./AppSecretsProvidersV1");
            AppSecretsProvidersV1.fixupPrototype(this.secretsProvider);
        }
        if (this.newSnapshot) {
            // Lazy import - only evaluated when called
            const { AppDeploymentV3 } = require("./AppDeploymentV3");
            AppDeploymentV3.fixupPrototype(this.newSnapshot);
        }
        if (this.snippets) {
            for (let i0 = 0; i0 < this.snippets.length; i0++) {
                let val0 = this.snippets[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { AppSnippetV1 } = require("./AppSnippetV1");
                    AppSnippetV1.fixupPrototype(val0);
                }
            }
        }
        if (this.extension) {
            // Lazy import - only evaluated when called
            const { AppExtension } = require("./AppExtension");
            AppExtension.fixupPrototype(this.extension);
        }
    }
}
