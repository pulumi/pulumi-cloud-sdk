// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { TeamAccountPermission } from "./TeamAccountPermission";
import type { TeamEnvironmentSettings } from "./TeamEnvironmentSettings";
import type { TeamKind } from "./TeamKind";
import type { TeamMemberInfo } from "./TeamMemberInfo";
import type { TeamRole } from "./TeamRole";
import type { TeamStackPermission } from "./TeamStackPermission";


export class Team {
    kind: TeamKind;
    name: string;
    displayName: string;
    description: string;
    members?: TeamMemberInfo[];
    stacks?: TeamStackPermission[];
    environments?: TeamEnvironmentSettings[];
    accounts?: TeamAccountPermission[];
    listMembersError?: string;
    userRole?: TeamRole;
    roleIds?: string[];

    getFixupPrototypeFunction() {
        return Team.fixupPrototype;
    }

    static newInstance(model: Partial<Team>): Team {
        let obj = Object.assign(new Team(), model);
        Team.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<Team>): Team | null {
        if (!model) return null;
        return Team.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: Team) {
        if (!obj) return;

        Team.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: Team): boolean {
        Object.setPrototypeOf(obj, Team.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (this.members) {
            for (let i0 = 0; i0 < this.members.length; i0++) {
                let val0 = this.members[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { TeamMemberInfo } = require("./TeamMemberInfo");
                    TeamMemberInfo.fixupPrototype(val0);
                }
            }
        }
        if (this.stacks) {
            for (let i0 = 0; i0 < this.stacks.length; i0++) {
                let val0 = this.stacks[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { TeamStackPermission } = require("./TeamStackPermission");
                    TeamStackPermission.fixupPrototype(val0);
                }
            }
        }
        if (this.environments) {
            for (let i0 = 0; i0 < this.environments.length; i0++) {
                let val0 = this.environments[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { TeamEnvironmentSettings } = require("./TeamEnvironmentSettings");
                    TeamEnvironmentSettings.fixupPrototype(val0);
                }
            }
        }
        if (this.accounts) {
            for (let i0 = 0; i0 < this.accounts.length; i0++) {
                let val0 = this.accounts[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { TeamAccountPermission } = require("./TeamAccountPermission");
                    TeamAccountPermission.fixupPrototype(val0);
                }
            }
        }
    }
}
