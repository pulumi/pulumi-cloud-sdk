// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { CacheOptionsRequest } from "./CacheOptionsRequest";
import type { DeploymentSettingsGitHubRequest } from "./DeploymentSettingsGitHubRequest";
import type { DeploymentSettingsVCS } from "./DeploymentSettingsVCS";
import type { ExecutorSettingsRequest } from "./ExecutorSettingsRequest";
import type { OperationContextRequest } from "./OperationContextRequest";
import type { SourceContextRequest } from "./SourceContextRequest";


export class DeploymentSettingsRequest {
    tag?: string;
    executorContext?: ExecutorSettingsRequest;
    sourceContext?: SourceContextRequest;
    gitHub?: DeploymentSettingsGitHubRequest;
    vcs?: DeploymentSettingsVCS;
    operationContext?: OperationContextRequest;
    agentPoolID?: string;
    cacheOptions?: CacheOptionsRequest;

    getFixupPrototypeFunction() {
        return DeploymentSettingsRequest.fixupPrototype;
    }

    static newInstance(model: Partial<DeploymentSettingsRequest>): DeploymentSettingsRequest {
        let obj = Object.assign(new DeploymentSettingsRequest(), model);
        DeploymentSettingsRequest.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<DeploymentSettingsRequest>): DeploymentSettingsRequest | null {
        if (!model) return null;
        return DeploymentSettingsRequest.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: DeploymentSettingsRequest) {
        if (!obj) return;

        DeploymentSettingsRequest.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: DeploymentSettingsRequest): boolean {
        Object.setPrototypeOf(obj, DeploymentSettingsRequest.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    static getTypeHierarchy(): (typeof DeploymentSettingsRequest)[] {
        // Lazy import - only evaluated when called
        const { CreateDeploymentRequest } = require("./CreateDeploymentRequest");

        return [
            CreateDeploymentRequest,
        ];
    }

    fixupFields() {
        if (this.executorContext) {
            // Lazy import - only evaluated when called
            const { ExecutorSettingsRequest } = require("./ExecutorSettingsRequest");
            ExecutorSettingsRequest.fixupPrototype(this.executorContext);
        }
        if (this.sourceContext) {
            // Lazy import - only evaluated when called
            const { SourceContextRequest } = require("./SourceContextRequest");
            SourceContextRequest.fixupPrototype(this.sourceContext);
        }
        if (this.gitHub) {
            // Lazy import - only evaluated when called
            const { DeploymentSettingsGitHubRequest } = require("./DeploymentSettingsGitHubRequest");
            DeploymentSettingsGitHubRequest.fixupPrototype(this.gitHub);
        }
        if (this.vcs) {
            // Lazy import - only evaluated when called
            const { DeploymentSettingsVCS } = require("./DeploymentSettingsVCS");
            DeploymentSettingsVCS.fixupPrototype(this.vcs);
        }
        if (this.operationContext) {
            // Lazy import - only evaluated when called
            const { OperationContextRequest } = require("./OperationContextRequest");
            OperationContextRequest.fixupPrototype(this.operationContext);
        }
        if (this.cacheOptions) {
            // Lazy import - only evaluated when called
            const { CacheOptionsRequest } = require("./CacheOptionsRequest");
            CacheOptionsRequest.fixupPrototype(this.cacheOptions);
        }
    }
}
