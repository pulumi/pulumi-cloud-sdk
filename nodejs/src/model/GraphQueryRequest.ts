// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { GraphQueryAggregate } from "./GraphQueryAggregate";
import type { GraphQueryAnchor } from "./GraphQueryAnchor";
import type { GraphQueryPage } from "./GraphQueryPage";
import type { GraphQueryReturn } from "./GraphQueryReturn";
import type { GraphQueryScope } from "./GraphQueryScope";
import type { GraphTraverseStep } from "./GraphTraverseStep";


export class GraphQueryRequest {
    scope?: GraphQueryScope;
    anchor: GraphQueryAnchor;
    traverse?: GraphTraverseStep[];
    aggregate?: GraphQueryAggregate;
    return?: GraphQueryReturn;
    page?: GraphQueryPage;

    getFixupPrototypeFunction() {
        return GraphQueryRequest.fixupPrototype;
    }

    static newInstance(model: Partial<GraphQueryRequest>): GraphQueryRequest {
        let obj = Object.assign(new GraphQueryRequest(), model);
        GraphQueryRequest.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<GraphQueryRequest>): GraphQueryRequest | null {
        if (!model) return null;
        return GraphQueryRequest.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: GraphQueryRequest) {
        if (!obj) return;

        GraphQueryRequest.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: GraphQueryRequest): boolean {
        Object.setPrototypeOf(obj, GraphQueryRequest.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (this.scope) {
            // Lazy import - only evaluated when called
            const { GraphQueryScope } = require("./GraphQueryScope");
            GraphQueryScope.fixupPrototype(this.scope);
        }
        if (this.anchor) {
            // Lazy import - only evaluated when called
            const { GraphQueryAnchor } = require("./GraphQueryAnchor");
            GraphQueryAnchor.fixupPrototype(this.anchor);
        }
        if (this.traverse) {
            for (let i0 = 0; i0 < this.traverse.length; i0++) {
                let val0 = this.traverse[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { GraphTraverseStep } = require("./GraphTraverseStep");
                    GraphTraverseStep.fixupPrototype(val0);
                }
            }
        }
        if (this.aggregate) {
            // Lazy import - only evaluated when called
            const { GraphQueryAggregate } = require("./GraphQueryAggregate");
            GraphQueryAggregate.fixupPrototype(this.aggregate);
        }
        if (this.return) {
            // Lazy import - only evaluated when called
            const { GraphQueryReturn } = require("./GraphQueryReturn");
            GraphQueryReturn.fixupPrototype(this.return);
        }
        if (this.page) {
            // Lazy import - only evaluated when called
            const { GraphQueryPage } = require("./GraphQueryPage");
            GraphQueryPage.fixupPrototype(this.page);
        }
    }
}
