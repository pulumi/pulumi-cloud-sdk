// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { RegistryContentNode } from "./RegistryContentNode";
import type { RegistryDocsProperty } from "./RegistryDocsProperty";
import type { RegistryDocsSupportingType } from "./RegistryDocsSupportingType";


export class GetPackageDocsResponse {
    version: string;
    navUrl: string;
    typeToken: string;
    kind: string;
    language: string;
    name: { [key: string]: string };
    description: RegistryContentNode[];
    constructorSyntax?: RegistryContentNode[];
    inputs?: RegistryDocsProperty[];
    outputs?: RegistryDocsProperty[];
    supportingTypes?: RegistryDocsSupportingType[];

    getFixupPrototypeFunction() {
        return GetPackageDocsResponse.fixupPrototype;
    }

    static newInstance(model: Partial<GetPackageDocsResponse>): GetPackageDocsResponse {
        let obj = Object.assign(new GetPackageDocsResponse(), model);
        GetPackageDocsResponse.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<GetPackageDocsResponse>): GetPackageDocsResponse | null {
        if (!model) return null;
        return GetPackageDocsResponse.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: GetPackageDocsResponse) {
        if (!obj) return;

        GetPackageDocsResponse.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: GetPackageDocsResponse): boolean {
        Object.setPrototypeOf(obj, GetPackageDocsResponse.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (this.description) {
            for (let i0 = 0; i0 < this.description.length; i0++) {
                let val0 = this.description[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { RegistryContentNode } = require("./RegistryContentNode");
                    RegistryContentNode.fixupPrototype(val0);
                }
            }
        }
        if (this.constructorSyntax) {
            for (let i0 = 0; i0 < this.constructorSyntax.length; i0++) {
                let val0 = this.constructorSyntax[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { RegistryContentNode } = require("./RegistryContentNode");
                    RegistryContentNode.fixupPrototype(val0);
                }
            }
        }
        if (this.inputs) {
            for (let i0 = 0; i0 < this.inputs.length; i0++) {
                let val0 = this.inputs[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { RegistryDocsProperty } = require("./RegistryDocsProperty");
                    RegistryDocsProperty.fixupPrototype(val0);
                }
            }
        }
        if (this.outputs) {
            for (let i0 = 0; i0 < this.outputs.length; i0++) {
                let val0 = this.outputs[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { RegistryDocsProperty } = require("./RegistryDocsProperty");
                    RegistryDocsProperty.fixupPrototype(val0);
                }
            }
        }
        if (this.supportingTypes) {
            for (let i0 = 0; i0 < this.supportingTypes.length; i0++) {
                let val0 = this.supportingTypes[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { RegistryDocsSupportingType } = require("./RegistryDocsSupportingType");
                    RegistryDocsSupportingType.fixupPrototype(val0);
                }
            }
        }
    }
}
