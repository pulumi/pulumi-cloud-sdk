// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

export class AgentEntity {
    private type: string;

    protected setDiscriminator(type: string) {
        this.type = type;
    }

    public getDiscriminator(): string {
        return this.type;
    }

    getFixupPrototypeFunction() {
        return AgentEntity.fixupPrototype;
    }

    static newInstance(model: Partial<AgentEntity>): AgentEntity {
        let obj = Object.assign(new AgentEntity(), model);
        AgentEntity.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<AgentEntity>): AgentEntity | null {
        if (!model) return null;
        return AgentEntity.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: AgentEntity) {
        if (!obj) return;

        AgentEntity.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: AgentEntity): boolean {
        // Lazy import - only evaluated when called
        const { AgentEntityPolicyIssue } = require("./AgentEntityPolicyIssue");
        const { AgentEntityPR } = require("./AgentEntityPR");
        const { AgentEntityRepository } = require("./AgentEntityRepository");
        const { AgentEntityStack } = require("./AgentEntityStack");

        switch (obj.type) {
            case "policy_issue":
                Object.setPrototypeOf(obj, AgentEntityPolicyIssue.prototype);
                break;
            case "pull_request":
                Object.setPrototypeOf(obj, AgentEntityPR.prototype);
                break;
            case "repository":
                Object.setPrototypeOf(obj, AgentEntityRepository.prototype);
                break;
            case "stack":
                Object.setPrototypeOf(obj, AgentEntityStack.prototype);
                break;
        }

        return true; // Tell the caller this is a polymorphic type, it should not overwrite the prototype.
    }

    static getTypeHierarchy(): (typeof AgentEntity)[] {
        // Lazy import - only evaluated when called
        const { AgentEntityPolicyIssue } = require("./AgentEntityPolicyIssue");
        const { AgentEntityPR } = require("./AgentEntityPR");
        const { AgentEntityRepository } = require("./AgentEntityRepository");
        const { AgentEntityStack } = require("./AgentEntityStack");

        return [
            AgentEntityPolicyIssue,
            AgentEntityPR,
            AgentEntityRepository,
            AgentEntityStack,
        ];
    }

    fixupFields() {
        // Nothing to do.
    }
}
