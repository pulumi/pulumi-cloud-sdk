// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { AppProjectTemplateConfigValue } from "./AppProjectTemplateConfigValue";
import type { RuntimeLanguage } from "./RuntimeLanguage";
import type { TemplateRuntimeInfo } from "./TemplateRuntimeInfo";
import type { Visibility } from "./Visibility";


export class Template {
    name: string;
    publisher: string;
    source: string;
    displayName: string;
    description?: string;
    runtime?: TemplateRuntimeInfo;
    language: RuntimeLanguage;
    readmeURL?: string;
    downloadURL: string;
    repoSlug?: string;
    visibility: Visibility;
    updatedAt: Date;
    metadata?: { [key: string]: string };
    config?: { [key: string]: AppProjectTemplateConfigValue };
    url: string;

    getFixupPrototypeFunction() {
        return Template.fixupPrototype;
    }

    static newInstance(model: Partial<Template>): Template {
        let obj = Object.assign(new Template(), model);
        Template.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<Template>): Template | null {
        if (!model) return null;
        return Template.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: Template) {
        if (!obj) return;

        Template.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: Template): boolean {
        Object.setPrototypeOf(obj, Template.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    static getTypeHierarchy(): (typeof Template)[] {
        // Lazy import - only evaluated when called
        const { GetTemplateResponse } = require("./GetTemplateResponse");
        const { TemplateVersion } = require("./TemplateVersion");

        return [
            GetTemplateResponse,
            TemplateVersion,
        ];
    }

    fixupFields() {
        if (this.runtime) {
            // Lazy import - only evaluated when called
            const { TemplateRuntimeInfo } = require("./TemplateRuntimeInfo");
            TemplateRuntimeInfo.fixupPrototype(this.runtime);
        }
        if (typeof this.updatedAt === "string") {
            this.updatedAt = new Date(this.updatedAt);
        }
        if (this.config) {
            for (let key0 in this.config) {
                let val0 = this.config[key0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { AppProjectTemplateConfigValue } = require("./AppProjectTemplateConfigValue");
                    AppProjectTemplateConfigValue.fixupPrototype(val0);
                }
            }
        }
    }
}
