// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { AgentAsyncTriggerType } from "./AgentAsyncTriggerType";
import type { AgentEntity } from "./AgentEntity";
import type { AgentTaskRuntimePhase } from "./AgentTaskRuntimePhase";
import type { AgentTaskSource } from "./AgentTaskSource";
import type { AgentTaskStatus } from "./AgentTaskStatus";
import type { AgentTaskType } from "./AgentTaskType";
import type { NeoApprovalMode } from "./NeoApprovalMode";
import type { NeoPermissionMode } from "./NeoPermissionMode";
import type { ToolExecutionMode } from "./ToolExecutionMode";
import type { UserInfo } from "./UserInfo";
import type { VCSProvider } from "./VCSProvider";


export class AgentTask {
    id: string;
    name: string;
    taskType: AgentTaskType;
    asyncTriggerType?: AgentAsyncTriggerType;
    status: AgentTaskStatus;
    createdAt: Date;
    approvalMode: NeoApprovalMode;
    entities: AgentEntity[];
    planMode: boolean;
    isShared: boolean;
    sharedAt?: Date;
    createdBy: UserInfo;
    permissionMode?: NeoPermissionMode;
    role?: string;
    roleName?: string;
    runtimePhase?: AgentTaskRuntimePhase;
    lastHeartbeat?: Date;
    contextUsedTokens?: number;
    contextWindowTokens?: number;
    contextCompactionThresholdPercent?: number;
    tokensUsed: number;
    toolExecutionMode?: ToolExecutionMode;
    sourceAutomationID?: string;
    agentDefinitionId?: string;
    source?: AgentTaskSource;
    vcsProvider?: VCSProvider;

    getFixupPrototypeFunction() {
        return AgentTask.fixupPrototype;
    }

    static newInstance(model: Partial<AgentTask>): AgentTask {
        let obj = Object.assign(new AgentTask(), model);
        AgentTask.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<AgentTask>): AgentTask | null {
        if (!model) return null;
        return AgentTask.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: AgentTask) {
        if (!obj) return;

        AgentTask.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: AgentTask): boolean {
        Object.setPrototypeOf(obj, AgentTask.prototype);
        return false; // Not a polymorphic type, okay to overwrite the prototype.
    }

    fixupFields() {
        if (typeof this.createdAt === "string") {
            this.createdAt = new Date(this.createdAt);
        }
        if (this.entities) {
            for (let i0 = 0; i0 < this.entities.length; i0++) {
                let val0 = this.entities[i0];
                if (val0) {
                    // Lazy import - only evaluated when called
                    const { AgentEntity } = require("./AgentEntity");
                    AgentEntity.fixupPrototype(val0);
                }
            }
        }
        if (typeof this.sharedAt === "string") {
            this.sharedAt = new Date(this.sharedAt);
        }
        if (this.createdBy) {
            // Lazy import - only evaluated when called
            const { UserInfo } = require("./UserInfo");
            UserInfo.fixupPrototype(this.createdBy);
        }
        if (typeof this.lastHeartbeat === "string") {
            this.lastHeartbeat = new Date(this.lastHeartbeat);
        }
        if (this.tokensUsed === undefined) {
            this.tokensUsed = 0;
        }
    }
}
