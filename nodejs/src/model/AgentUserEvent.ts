// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { AgentEntityDiff } from "./AgentEntityDiff";
import type { PageContext } from "./PageContext";


export class AgentUserEvent {
    private type: string;
    timestamp: Date;
    entity_diff?: AgentEntityDiff;
    pageContext?: PageContext;

    protected setDiscriminator(type: string) {
        this.type = type;
    }

    public getDiscriminator(): string {
        return this.type;
    }

    getFixupPrototypeFunction() {
        return AgentUserEvent.fixupPrototype;
    }

    static newInstance(model: Partial<AgentUserEvent>): AgentUserEvent {
        let obj = Object.assign(new AgentUserEvent(), model);
        AgentUserEvent.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<AgentUserEvent>): AgentUserEvent | null {
        if (!model) return null;
        return AgentUserEvent.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: AgentUserEvent) {
        if (!obj) return;

        AgentUserEvent.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: AgentUserEvent): boolean {
        // Lazy import - only evaluated when called
        const { AgentUserEventExecToolCall } = require("./AgentUserEventExecToolCall");
        const { AgentUserEventToolResult } = require("./AgentUserEventToolResult");
        const { AgentUserEventCancel } = require("./AgentUserEventCancel");
        const { AgentUserEventConfirmation } = require("./AgentUserEventConfirmation");
        const { AgentUserEventMessage } = require("./AgentUserEventMessage");

        switch (obj.type) {
            case "exec_tool_call":
                Object.setPrototypeOf(obj, AgentUserEventExecToolCall.prototype);
                break;
            case "tool_result":
                Object.setPrototypeOf(obj, AgentUserEventToolResult.prototype);
                break;
            case "user_cancel":
                Object.setPrototypeOf(obj, AgentUserEventCancel.prototype);
                break;
            case "user_confirmation":
                Object.setPrototypeOf(obj, AgentUserEventConfirmation.prototype);
                break;
            case "user_message":
                Object.setPrototypeOf(obj, AgentUserEventMessage.prototype);
                break;
        }

        return true; // Tell the caller this is a polymorphic type, it should not overwrite the prototype.
    }

    static getTypeHierarchy(): (typeof AgentUserEvent)[] {
        // Lazy import - only evaluated when called
        const { AgentUserEventExecToolCall } = require("./AgentUserEventExecToolCall");
        const { AgentUserEventToolResult } = require("./AgentUserEventToolResult");
        const { AgentUserEventCancel } = require("./AgentUserEventCancel");
        const { AgentUserEventConfirmation } = require("./AgentUserEventConfirmation");
        const { AgentUserEventMessage } = require("./AgentUserEventMessage");

        return [
            AgentUserEventExecToolCall,
            AgentUserEventToolResult,
            AgentUserEventCancel,
            AgentUserEventConfirmation,
            AgentUserEventMessage,
        ];
    }

    fixupFields() {
        if (typeof this.timestamp === "string") {
            this.timestamp = new Date(this.timestamp);
        }
        if (this.entity_diff) {
            // Lazy import - only evaluated when called
            const { AgentEntityDiff } = require("./AgentEntityDiff");
            AgentEntityDiff.fixupPrototype(this.entity_diff);
        }
        if (this.pageContext) {
            // Lazy import - only evaluated when called
            const { PageContext } = require("./PageContext");
            PageContext.fixupPrototype(this.pageContext);
        }
    }
}
