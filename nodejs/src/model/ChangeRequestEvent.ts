// Copyright 2016-2026, Pulumi Corporation. All rights reserved.

// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_all' TO REFRESH.

/* eslint-disable @typescript-eslint/consistent-type-assertions */
/* eslint-disable @typescript-eslint/member-ordering */
/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable max-len */
/* eslint-disable no-underscore-dangle */
/* eslint-disable prettier/prettier */
/* eslint-disable simple-import-sort/exports */
/* eslint-disable simple-import-sort/imports */
/* eslint-disable sonarjs/prefer-single-boolean-return */
/* eslint-disable unused-imports/no-unused-imports */

import type { UserInfo } from "./UserInfo";


export class ChangeRequestEvent {
    private eventType: string;
    id: string;
    replacedBy?: string;
    changeRequestId: string;
    revisionNumber: number;
    createdBy: UserInfo;
    createdAt: Date;
    comment?: string;

    protected setDiscriminator(type: string) {
        this.eventType = type;
    }

    public getDiscriminator(): string {
        return this.eventType;
    }

    getFixupPrototypeFunction() {
        return ChangeRequestEvent.fixupPrototype;
    }

    static newInstance(model: Partial<ChangeRequestEvent>): ChangeRequestEvent {
        let obj = Object.assign(new ChangeRequestEvent(), model);
        ChangeRequestEvent.fixupPrototype(obj);
        return obj;
    }

    static deepClone(model: Partial<ChangeRequestEvent>): ChangeRequestEvent | null {
        if (!model) return null;
        return ChangeRequestEvent.newInstance(JSON.parse(JSON.stringify(model)));
    }

    static fixupPrototype(obj: ChangeRequestEvent) {
        if (!obj) return;

        ChangeRequestEvent.setPrototype(obj);

        obj.fixupFields();
    }

    protected static setPrototype(obj: ChangeRequestEvent): boolean {
        // Lazy import - only evaluated when called
        const { ChangeRequestApprovedEvent } = require("./ChangeRequestApprovedEvent");
        const { ChangeRequestCommentedEvent } = require("./ChangeRequestCommentedEvent");
        const { ChangeRequestDescriptionUpdatedEvent } = require("./ChangeRequestDescriptionUpdatedEvent");
        const { ChangeRequestRevisionAddedEvent } = require("./ChangeRequestRevisionAddedEvent");
        const { ChangeRequestStatusChangedEvent } = require("./ChangeRequestStatusChangedEvent");
        const { ChangeRequestUnapprovedEvent } = require("./ChangeRequestUnapprovedEvent");

        switch (obj.eventType) {
            case "approved_by_user":
                Object.setPrototypeOf(obj, ChangeRequestApprovedEvent.prototype);
                break;
            case "commented":
                Object.setPrototypeOf(obj, ChangeRequestCommentedEvent.prototype);
                break;
            case "description_updated":
                Object.setPrototypeOf(obj, ChangeRequestDescriptionUpdatedEvent.prototype);
                break;
            case "revision_added":
                Object.setPrototypeOf(obj, ChangeRequestRevisionAddedEvent.prototype);
                break;
            case "status_changed":
                Object.setPrototypeOf(obj, ChangeRequestStatusChangedEvent.prototype);
                break;
            case "unapproved_by_user":
                Object.setPrototypeOf(obj, ChangeRequestUnapprovedEvent.prototype);
                break;
        }

        return true; // Tell the caller this is a polymorphic type, it should not overwrite the prototype.
    }

    static getTypeHierarchy(): (typeof ChangeRequestEvent)[] {
        // Lazy import - only evaluated when called
        const { ChangeRequestApprovedEvent } = require("./ChangeRequestApprovedEvent");
        const { ChangeRequestCommentedEvent } = require("./ChangeRequestCommentedEvent");
        const { ChangeRequestDescriptionUpdatedEvent } = require("./ChangeRequestDescriptionUpdatedEvent");
        const { ChangeRequestRevisionAddedEvent } = require("./ChangeRequestRevisionAddedEvent");
        const { ChangeRequestStatusChangedEvent } = require("./ChangeRequestStatusChangedEvent");
        const { ChangeRequestUnapprovedEvent } = require("./ChangeRequestUnapprovedEvent");

        return [
            ChangeRequestApprovedEvent,
            ChangeRequestCommentedEvent,
            ChangeRequestDescriptionUpdatedEvent,
            ChangeRequestRevisionAddedEvent,
            ChangeRequestStatusChangedEvent,
            ChangeRequestUnapprovedEvent,
        ];
    }

    fixupFields() {
        if (this.revisionNumber === undefined) {
            this.revisionNumber = 0;
        }
        if (this.createdBy) {
            // Lazy import - only evaluated when called
            const { UserInfo } = require("./UserInfo");
            UserInfo.fixupPrototype(this.createdBy);
        }
        if (typeof this.createdAt === "string") {
            this.createdAt = new Date(this.createdAt);
        }
    }
}
