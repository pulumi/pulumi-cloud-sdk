// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.time.ZonedDateTime;
import java.util.List;
import java.util.Map;
import com.fasterxml.jackson.annotation.*;
import com.fasterxml.jackson.databind.JsonNode;

@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "kind")
@JsonSubTypes({
    @JsonSubTypes.Type(value = TransferEntity.Environment.class),
    @JsonSubTypes.Type(value = TransferEntity.InsightsAccount.class),
    @JsonSubTypes.Type(value = TransferEntity.RegistryPackage.class),
    @JsonSubTypes.Type(value = TransferEntity.Stack.class)
})
public abstract class TransferEntity {

    @JsonSubTypes({
        @JsonSubTypes.Type(value = TransferEntity.Environment.Rename.class)
    })
    @JsonTypeName("TransferEntityEnvironment")
    public static class Environment extends TransferEntity {
        // The project the environment belongs to.
        public String projectName;

        // The environment name.
        public String environmentName;

        @JsonTypeName("TransferEntityEnvironmentRename")
        public static class Rename extends TransferEntity.Environment {
            // Rename information for ESC environment.
            public TransferEntity.Environment renameAs;
        }
    }

    @JsonSubTypes({
        @JsonSubTypes.Type(value = TransferEntity.InsightsAccount.Rename.class)
    })
    @JsonTypeName("TransferEntityInsightsAccount")
    public static class InsightsAccount extends TransferEntity {
        // The Insights account name.
        public String name;

        @JsonTypeName("TransferEntityInsightsAccountRename")
        public static class Rename extends TransferEntity.InsightsAccount {
            // Renamed information for Insights account.
            public TransferEntity.InsightsAccount renameAs;
        }
    }

    @JsonSubTypes({
        @JsonSubTypes.Type(value = TransferEntity.RegistryPackage.Rename.class)
    })
    @JsonTypeName("TransferEntityRegistryPackage")
    public static class RegistryPackage extends TransferEntity {
        // The package source (e.g. pulumi, opentofu).
        public String source;

        // The package publisher.
        public String publisher;

        // The package name.
        public String name;

        @JsonTypeName("TransferEntityRegistryPackageRename")
        public static class Rename extends TransferEntity.RegistryPackage {
            // Rename information for registry package.
            public TransferEntity.RegistryPackage renameAs;
        }
    }

    @JsonSubTypes({
        @JsonSubTypes.Type(value = TransferEntity.Stack.Rename.class)
    })
    @JsonTypeName("TransferEntityStack")
    public static class Stack extends TransferEntity {
        // The project the colliding stack belongs to.
        public String projectName;

        // The colliding stack name.
        public String stackName;

        // Number of resources currently managed by the stack, as reported by the stack's last activated update. Omitted when the stack has no activated update yet.
        public Long resourceCount;

        // Timestamp of the stack's last activated update start time. Omitted when the stack has no activated update yet.
        public ZonedDateTime lastUpdate;

        @JsonTypeName("TransferEntityStackRename")
        public static class Rename extends TransferEntity.Stack {
            // Rename information for stack.
            public TransferEntity.Stack renameAs;
        }
    }
}
