// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.util.Arrays;
import com.fasterxml.jackson.annotation.*;

public enum GraphVisibility {
    // Every node and edge the traversal touched was within the caller's permissions.
    Complete("complete"),
    // At least one traversal step encountered infrastructure outside the caller's permissions, or the engine could not verify completeness for this run, so the answer may be missing dependencies, dependents, or stacks. No detail about what is hidden is included. Treat a trimmed result as disqualifying for any question that depends on completeness, such as impact analysis or an absence check; a caller with wider permissions can run the same query for a complete answer.
    Trimmed("trimmed");

    private final String value;

    GraphVisibility(String v) {
        value = v;
    }

    @JsonValue
    public String value() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    @JsonCreator
    public static GraphVisibility fromValue(String v) {
        for (var b : GraphVisibility.values()) {
            if (String.valueOf(b.value).equals(v)) {
                return b;
            }
        }

        throw new IllegalArgumentException(String.format("'%s' is not one of the values accepted for %s class: %s", v, GraphVisibility.class.getSimpleName(), Arrays.toString(GraphVisibility.values())));
    }
}
