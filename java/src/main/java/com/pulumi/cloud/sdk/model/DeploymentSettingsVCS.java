// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.time.ZonedDateTime;
import java.util.List;
import java.util.Map;
import com.fasterxml.jackson.annotation.*;
import com.fasterxml.jackson.databind.JsonNode;

@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "provider")
@JsonSubTypes({
    @JsonSubTypes.Type(value = DeploymentSettingsVCS.AzureDevOps.class),
    @JsonSubTypes.Type(value = DeploymentSettingsVCS.Bitbucket.class),
    @JsonSubTypes.Type(value = DeploymentSettingsVCS.Custom.class),
    @JsonSubTypes.Type(value = DeploymentSettingsVCS.GitHub.class),
    @JsonSubTypes.Type(value = DeploymentSettingsVCS.GitLab.class)
})
public abstract class DeploymentSettingsVCS {
    // The VCS repository reference (format varies by provider)
    public String repository;

    // Whether to deploy all commits to the default branch
    public boolean deployCommits;

    // Whether to deploy when a matching tag is pushed.
    public boolean deployTags;

    // Paths within the repository that trigger deployments when changed
    public List<String> paths;

    // Glob patterns matching tag names that trigger a deployment when `deployTags` is true. Supports `!` prefix for negation (e.g. `!*-rc*` excludes release candidates). An empty list with `deployTags=true` matches all tags.
    public List<String> tagFilters;

    // VCS installation/integration ID linking to the VCS provider
    public String installationId;

    // Whether to use pull request templates for deployment PRs
    public boolean pullRequestTemplate;

    // Whether to create preview deployments for pull requests
    public boolean previewPullRequests;

    // Specific pull request number to deploy (overrides automatic deployment)
    public Long deployPullRequest;

    @JsonTypeName("azure_devops")
    public static class AzureDevOps extends DeploymentSettingsVCS {
    }

    @JsonTypeName("bitbucket")
    public static class Bitbucket extends DeploymentSettingsVCS {
    }

    @JsonTypeName("custom")
    public static class Custom extends DeploymentSettingsVCS {
    }

    @JsonTypeName("github")
    public static class GitHub extends DeploymentSettingsVCS {
        // Gates review stack creation. When set, only pull requests carrying a matching label (exact, case-sensitive) create a review stack.
        public List<String> reviewStackLabels;
    }

    @JsonTypeName("gitlab")
    public static class GitLab extends DeploymentSettingsVCS {
    }
}
