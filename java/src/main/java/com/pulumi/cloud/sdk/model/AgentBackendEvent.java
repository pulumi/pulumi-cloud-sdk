// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.time.ZonedDateTime;
import java.util.List;
import java.util.Map;
import com.fasterxml.jackson.annotation.*;
import com.fasterxml.jackson.databind.JsonNode;

@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type")
@JsonSubTypes({
    @JsonSubTypes.Type(value = AgentBackendEvent.AssistantMessage.class),
    @JsonSubTypes.Type(value = AgentBackendEvent.AwaitingApprovalsMessage.class),
    @JsonSubTypes.Type(value = AgentBackendEvent.CancelledMessage.class),
    @JsonSubTypes.Type(value = AgentBackendEvent.ChangeEntities.class),
    @JsonSubTypes.Type(value = AgentBackendEvent.ContextCompressionEvent.class),
    @JsonSubTypes.Type(value = AgentBackendEvent.Error.class),
    @JsonSubTypes.Type(value = AgentBackendEvent.ExecToolCall.class),
    @JsonSubTypes.Type(value = AgentBackendEvent.ExecToolCallProgress.class),
    @JsonSubTypes.Type(value = AgentBackendEvent.ProvidePulumiAccessToken.class),
    @JsonSubTypes.Type(value = AgentBackendEvent.SetTaskName.class),
    @JsonSubTypes.Type(value = AgentBackendEvent.ToolResponse.class),
    @JsonSubTypes.Type(value = AgentBackendEvent.UserApprovalRequest.class),
    @JsonSubTypes.Type(value = AgentBackendEvent.Warning.class)
})
public abstract class AgentBackendEvent {
    // When the event occurred.
    public ZonedDateTime timestamp;

    @JsonTypeName("assistant_message")
    public static class AssistantMessage extends AgentBackendEvent {
        // Message content from the assistant.
        public String content;

        // Optional tool calls within the message.
        public List<AgentBackendEventToolCall> tool_calls;

        // Whether this is the final message in the response.
        public boolean is_final;

        // Token usage metadata grouped by model name.
        public Map<String, AgentBackendEventTokenUsage> token_usage;

        // Opaque thinking-block signatures from the model's response, in order. Must be echoed back unchanged on subsequent API calls to preserve reasoning across tool-use turns when extended thinking is enabled. Empty/null for models or turns without extended thinking. With display:"omitted" (Opus 4.7 default) the thinking text itself is empty; only the signature carries load.
        public List<String> thinking_signatures;
    }

    @JsonTypeName("awaiting_approvals")
    public static class AwaitingApprovalsMessage extends AgentBackendEvent {
    }

    @JsonTypeName("cancelled")
    public static class CancelledMessage extends AgentBackendEvent {
        // Whether the operation was running when cancelled.
        public boolean operation_was_running;
    }

    @JsonTypeName("change_entities")
    public static class ChangeEntities extends AgentBackendEvent {
        // Changes the agent wants to make to the Task's entity list.
        public AgentEntityDiff changes;
    }

    @JsonTypeName("context_compression_event")
    public static class ContextCompressionEvent extends AgentBackendEvent {
        // Optional short human-readable message
        public String message;

        // Status of the compression process
        public String status;

        // Optional context summary generated by the LLM on successful compression
        public String context_summary;

        // Optional length of the tail preserved after compression
        public int tail_length;
    }

    @JsonTypeName("error")
    public static class Error extends AgentBackendEvent {
        // A user facing message describing the problem.
        public String message;
    }

    @JsonTypeName("exec_tool_call")
    public static class ExecToolCall extends AgentBackendEvent {
        // Tool call identifier.
        public String tool_call_id;

        // Tool name.
        public String name;
    }

    @JsonTypeName("exec_tool_call_progress")
    public static class ExecToolCallProgress extends AgentBackendEvent {
        // Tool call identifier.
        public String tool_call_id;

        // Progress status.
        public String status;

        // Progress metadata.
        public Map<String, JsonNode> metadata;
    }

    @JsonTypeName("provide_pulumi_access_token")
    public static class ProvidePulumiAccessToken extends AgentBackendEvent {
        // Encrypted Pulumi access token for authorization.
        public String encrypted_pulumi_access_token;
    }

    @JsonTypeName("set_task_name")
    public static class SetTaskName extends AgentBackendEvent {
        // The new name of the task
        public String name;
    }

    @JsonTypeName("tool_response")
    public static class ToolResponse extends AgentBackendEvent {
        // Tool call identifier.
        public String tool_call_id;

        // Tool name.
        public String name;

        // Tool execution result content.
        public JsonNode content;

        // Whether this tool response represents an error.
        public boolean is_error;
    }

    @JsonTypeName("user_approval_request")
    public static class UserApprovalRequest extends AgentBackendEvent {
        // Unique identifier for the approval request.
        public String id;

        // Message to show to the user requesting approval.
        public String message;

        // Optional buttons for user interaction.
        public List<String> buttons;

        // Source of the approval request.
        public String source;

        // Type of approval being requested.
        public String approval_type;

        // Additional context for the approval request.
        public AgentBackendEventUserApprovalRequestContext context;

        // Distinguishes between moderate-sensitivity and high-sensitivity operations for auto-approval. Regular indicates moderate-sensitivity operations that will be auto-approved in both Balanced and Auto approval modes. Sensitive indicates very sensitive operations (e.g., pulumi up) that still require manual approval in Balanced mode and will only auto-approve in Auto mode. Defaults to Regular when not specified.
        public Sensitivity sensitivity;
    }

    @JsonTypeName("warning")
    public static class Warning extends AgentBackendEvent {
        // A user facing message describing the problem.
        public String message;
    }
}
