// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.util.Arrays;
import com.fasterxml.jackson.annotation.*;

public enum AccessTokenType {
    // A personal access token created by a user.
    Personal("personal"),
    // A refresh token, exchanged for short-lived OBO access tokens at /api/oauth/token. Not authorized for any other endpoint.
    Refresh("refresh"),
    // A machine token bound to an organization's robot user.
    Organization("organization"),
    // A machine token bound to a team's robot user within an organization.
    Team("team");

    private final String value;

    AccessTokenType(String v) {
        value = v;
    }

    @JsonValue
    public String value() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    @JsonCreator
    public static AccessTokenType fromValue(String v) {
        for (var b : AccessTokenType.values()) {
            if (String.valueOf(b.value).equals(v)) {
                return b;
            }
        }

        throw new IllegalArgumentException(String.format("'%s' is not one of the values accepted for %s class: %s", v, AccessTokenType.class.getSimpleName(), Arrays.toString(AccessTokenType.values())));
    }
}
