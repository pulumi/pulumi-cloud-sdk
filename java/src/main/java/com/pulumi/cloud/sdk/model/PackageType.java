// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.util.Arrays;
import com.fasterxml.jackson.annotation.*;

public enum PackageType {
    // A package that offers native resources.
    Native("native"),
    // A package that offers component resources.
    Component("component"),
    // A package that is bridged from a different ecosystem (e.g. OpenTofu).
    Bridged("bridged");

    private final String value;

    PackageType(String v) {
        value = v;
    }

    @JsonValue
    public String value() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    @JsonCreator
    public static PackageType fromValue(String v) {
        for (var b : PackageType.values()) {
            if (String.valueOf(b.value).equals(v)) {
                return b;
            }
        }

        throw new IllegalArgumentException(String.format("'%s' is not one of the values accepted for %s class: %s", v, PackageType.class.getSimpleName(), Arrays.toString(PackageType.values())));
    }
}
