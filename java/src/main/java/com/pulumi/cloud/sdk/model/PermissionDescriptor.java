// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.time.ZonedDateTime;
import java.util.List;
import java.util.Map;
import com.fasterxml.jackson.annotation.*;
import com.fasterxml.jackson.databind.JsonNode;

@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "__type")
@JsonSubTypes({
    @JsonSubTypes.Type(value = PermissionDescriptor.Allow.class),
    @JsonSubTypes.Type(value = PermissionDescriptor.Compose.class),
    @JsonSubTypes.Type(value = PermissionDescriptor.Condition.class),
    @JsonSubTypes.Type(value = PermissionDescriptor.Group.class),
    @JsonSubTypes.Type(value = PermissionDescriptor.IfThenElse.class),
    @JsonSubTypes.Type(value = PermissionDescriptor.Select.class)
})
public abstract class PermissionDescriptor {

    @JsonTypeName("PermissionDescriptorAllow")
    public static class Allow extends PermissionDescriptor {
        // List of permissions to allow
        public List<RbacPermission> permissions;

        // Optional contextual constraints for the permissions
        public RbacPermissionConstraints constraints;
    }

    @JsonTypeName("PermissionDescriptorCompose")
    public static class Compose extends PermissionDescriptor {
        // References to other descriptors to include in the tree
        public List<String> permissionDescriptors;
    }

    @JsonTypeName("PermissionDescriptorCondition")
    public static class Condition extends PermissionDescriptor {
        // The boolean condition to evaluate.
        public PermissionBooleanExpression condition;

        // The permission descriptor to apply when the condition is true.
        public PermissionDescriptor subNode;
    }

    @JsonTypeName("PermissionDescriptorGroup")
    public static class Group extends PermissionDescriptor {
        // The list of permission descriptor entries in this group.
        public List<PermissionDescriptor> entries;
    }

    @JsonTypeName("PermissionDescriptorIfThenElse")
    public static class IfThenElse extends PermissionDescriptor {
        // The boolean condition to evaluate.
        public PermissionBooleanExpression condition;

        // The permission descriptor to apply when the condition is true.
        public PermissionDescriptor subNodeForTrue;

        // The permission descriptor to apply when the condition is false.
        public PermissionDescriptor subNodeForFalse;
    }

    @JsonTypeName("PermissionDescriptorSelect")
    public static class Select extends PermissionDescriptor {
        // The expression used to select which option to apply.
        public PermissionExpression selector;

        // The available options to select from based on the selector expression.
        public List<PermissionSelectvalue> options;
    }
}
