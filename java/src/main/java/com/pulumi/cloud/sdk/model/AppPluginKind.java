// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.util.Arrays;
import com.fasterxml.jackson.annotation.*;

public enum AppPluginKind {
    // AnalyzerPlugin is a plugin that can be used as a resource analyzer.
    AnalyzerPlugin("analyzer"),
    // LanguagePlugin is a plugin that can be used as a language host.
    LanguagePlugin("language"),
    // ResourcePlugin is a plugin that can be used as a resource provider for custom CRUD operations.
    ResourcePlugin("resource"),
    // ConverterPlugin is a plugin that can be used to convert from other ecosystems to Pulumi.
    ConverterPlugin("converter"),
    // ToolPlugin is an arbitrary plugin that can be run as a tool.
    ToolPlugin("tool");

    private final String value;

    AppPluginKind(String v) {
        value = v;
    }

    @JsonValue
    public String value() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    @JsonCreator
    public static AppPluginKind fromValue(String v) {
        for (var b : AppPluginKind.values()) {
            if (String.valueOf(b.value).equals(v)) {
                return b;
            }
        }

        throw new IllegalArgumentException(String.format("'%s' is not one of the values accepted for %s class: %s", v, AppPluginKind.class.getSimpleName(), Arrays.toString(AppPluginKind.values())));
    }
}
