// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.time.ZonedDateTime;
import java.util.List;
import java.util.Map;
import com.fasterxml.jackson.annotation.*;
import com.fasterxml.jackson.databind.JsonNode;

public class EngineEvent {
    // Timestamp of the event (seconds).
    public Long timestamp;

    // Type describes which payload object is associated with the event, such that in JavaScript `event[event.type]` is never undefined/null.
    @JsonProperty("type")
    public String type_;

    // Present when `type` is `cancelEvent`.
    public AppCancelEvent cancelEvent;

    // Present when `type` is `stdoutEvent`.
    public AppStdoutEngineEvent stdoutEvent;

    // Present when `type` is `diagnosticEvent`.
    public AppDiagnosticEvent diagnosticEvent;

    // Present when `type` is `preludeEvent`.
    public AppPreludeEvent preludeEvent;

    // Present when `type` is `summaryEvent`.
    public AppSummaryEvent summaryEvent;

    // Present when `type` is `resourcePreEvent`.
    public AppResourcePreEvent resourcePreEvent;

    // Present when `type` is `resOutputsEvent`.
    public AppResOutputsEvent resOutputsEvent;

    // Present when `type` is `resOpFailedEvent`.
    public AppResOpFailedEvent resOpFailedEvent;

    // Present when `type` is `policyEvent`.
    public AppPolicyEvent policyEvent;

    // Present when `type` is `policyRemediationEvent`.
    public AppPolicyRemediationEvent policyRemediationEvent;

    // Present when `type` is `policyLoadEvent`.
    public AppPolicyLoadEvent policyLoadEvent;

    // Present when `type` is `policyAnalyzeSummaryEvent`.
    public AppPolicyAnalyzeSummaryEvent policyAnalyzeSummaryEvent;

    // Present when `type` is `policyRemediateSummaryEvent`.
    public AppPolicyRemediateSummaryEvent policyRemediateSummaryEvent;

    // Present when `type` is `policyAnalyzeStackSummaryEvent`.
    public AppPolicyAnalyzeStackSummaryEvent policyAnalyzeStackSummaryEvent;

    // Present when `type` is `startDebuggingEvent`.
    public AppStartDebuggingEvent startDebuggingEvent;

    // Present when `type` is `progressEvent`.
    public AppProgressEvent progressEvent;

    // Present when `type` is `errorEvent`.
    public AppErrorEvent errorEvent;
}
