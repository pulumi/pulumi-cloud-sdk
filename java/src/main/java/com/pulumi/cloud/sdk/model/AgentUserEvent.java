// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.time.ZonedDateTime;
import java.util.List;
import java.util.Map;
import com.fasterxml.jackson.annotation.*;
import com.fasterxml.jackson.databind.JsonNode;

@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type")
@JsonSubTypes({
    @JsonSubTypes.Type(value = AgentUserEvent.Cancel.class),
    @JsonSubTypes.Type(value = AgentUserEvent.Confirmation.class),
    @JsonSubTypes.Type(value = AgentUserEvent.ExecToolCall.class),
    @JsonSubTypes.Type(value = AgentUserEvent.Message.class),
    @JsonSubTypes.Type(value = AgentUserEvent.ToolResult.class)
})
public abstract class AgentUserEvent {
    // When the event occurred.
    public ZonedDateTime timestamp;

    // Entities to add or remove from the agent.
    public AgentEntityDiff entity_diff;

    // Optional Pulumi Cloud page the user has the Neo pane open on at the moment they sent this event. Persisted with the event so cold-start replay can reproduce per-turn page context exactly. Omitted by clients with no concept of a current page (CLI, scheduled tasks, Slack, GitHub triggers, API consumers).
    public PageContext pageContext;

    @JsonTypeName("user_cancel")
    public static class Cancel extends AgentUserEvent {
    }

    @JsonTypeName("user_confirmation")
    public static class Confirmation extends AgentUserEvent {
        // ID to correlate the question the user is responding to.
        public String approval_request_id;

        // Boolean confirmation response.
        public boolean ok;

        // If the request was rejected, this is field carries instructions to the agent on what to do instead.
        public String instructions;
    }

    @JsonTypeName("exec_tool_call")
    public static class ExecToolCall extends AgentUserEvent {
        // Tool call identifier, correlating to a tool call from a prior AssistantMessage with execution_mode=cli.
        public String tool_call_id;

        // Tool name.
        public String name;
    }

    @JsonTypeName("user_message")
    public static class Message extends AgentUserEvent {
        // The exact natural language instruction from the user.
        public String content;

        // Slash Commands the user selected when creating their message.
        public Map<String, AgentSlashCommand> commands;
    }

    @JsonTypeName("tool_result")
    public static class ToolResult extends AgentUserEvent {
        // Batch of tool results from CLI-side execution.
        public List<AgentUserEventToolResultItem> tool_results;
    }
}
