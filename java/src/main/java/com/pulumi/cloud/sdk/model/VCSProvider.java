// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.util.Arrays;
import com.fasterxml.jackson.annotation.*;

public enum VCSProvider {
    // GitHub version control system
    GitHub("github"),
    // GitLab version control system
    GitLab("gitlab"),
    // Bitbucket version control system
    Bitbucket("bitbucket"),
    // Azure DevOps version control system
    AzureDevOps("azure_devops"),
    // Custom version control system
    Custom("custom"),
    // GitHub Enterprise Server version control system
    GitHubEnterprise("github_enterprise");

    private final String value;

    VCSProvider(String v) {
        value = v;
    }

    @JsonValue
    public String value() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    @JsonCreator
    public static VCSProvider fromValue(String v) {
        for (var b : VCSProvider.values()) {
            if (String.valueOf(b.value).equals(v)) {
                return b;
            }
        }

        throw new IllegalArgumentException(String.format("'%s' is not one of the values accepted for %s class: %s", v, VCSProvider.class.getSimpleName(), Arrays.toString(VCSProvider.values())));
    }
}
