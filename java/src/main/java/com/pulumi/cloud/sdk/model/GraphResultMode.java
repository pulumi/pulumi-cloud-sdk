// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.util.Arrays;
import com.fasterxml.jackson.annotation.*;

public enum GraphResultMode {
    // No engine limit clipped the underlying result and the search backend reported a complete answer.
    Exact("exact"),
    // An engine limit clipped the underlying result, or the search backend reported a partial answer (failed shards or a search timeout), so matching data may exist that no cursor will reach.
    Truncated("truncated");

    private final String value;

    GraphResultMode(String v) {
        value = v;
    }

    @JsonValue
    public String value() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    @JsonCreator
    public static GraphResultMode fromValue(String v) {
        for (var b : GraphResultMode.values()) {
            if (String.valueOf(b.value).equals(v)) {
                return b;
            }
        }

        throw new IllegalArgumentException(String.format("'%s' is not one of the values accepted for %s class: %s", v, GraphResultMode.class.getSimpleName(), Arrays.toString(GraphResultMode.values())));
    }
}
