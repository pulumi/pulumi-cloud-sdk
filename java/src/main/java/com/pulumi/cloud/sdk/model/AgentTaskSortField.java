// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.util.Arrays;
import com.fasterxml.jackson.annotation.*;

public enum AgentTaskSortField {
    // Sort by the time of the most recent event on the task.
    LastEvent("lastEvent"),
    // Sort by when the task was created.
    Created("created"),
    // Sort by task status, with running ordered before idle when ascending.
    Status("status"),
    // Sort by task name.
    Name("name"),
    // Sort by total Neo tokens consumed by the task. Tasks with no reported usage are treated as zero.
    TokensUsed("tokensUsed");

    private final String value;

    AgentTaskSortField(String v) {
        value = v;
    }

    @JsonValue
    public String value() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    @JsonCreator
    public static AgentTaskSortField fromValue(String v) {
        for (var b : AgentTaskSortField.values()) {
            if (String.valueOf(b.value).equals(v)) {
                return b;
            }
        }

        throw new IllegalArgumentException(String.format("'%s' is not one of the values accepted for %s class: %s", v, AgentTaskSortField.class.getSimpleName(), Arrays.toString(AgentTaskSortField.values())));
    }
}
