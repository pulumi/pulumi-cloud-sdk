// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.util.Arrays;
import com.fasterxml.jackson.annotation.*;

public enum AppEnforcementLevel {
    // Advisory is an enforcement level where the resource is still created, but a message is displayed to the user for informational / warning purposes.
    Advisory("advisory"),
    // Mandatory is an enforcement level that prevents a resource from being created.
    Mandatory("mandatory"),
    // Remediate is an enforcement level that fixes policy issues instead of issuing diagnostics.
    Remediate("remediate"),
    // Disabled is an enforcement level that disables the policy from being enforced.
    Disabled("disabled");

    private final String value;

    AppEnforcementLevel(String v) {
        value = v;
    }

    @JsonValue
    public String value() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    @JsonCreator
    public static AppEnforcementLevel fromValue(String v) {
        for (var b : AppEnforcementLevel.values()) {
            if (String.valueOf(b.value).equals(v)) {
                return b;
            }
        }

        throw new IllegalArgumentException(String.format("'%s' is not one of the values accepted for %s class: %s", v, AppEnforcementLevel.class.getSimpleName(), Arrays.toString(AppEnforcementLevel.values())));
    }
}
