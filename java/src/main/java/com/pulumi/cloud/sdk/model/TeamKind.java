// Copyright 2016-2026, Pulumi Corporation.  All rights reserved.
// THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.
// RUN 'make openapi_java' TO REFRESH.

package com.pulumi.cloud.sdk.model;

import java.util.Arrays;
import com.fasterxml.jackson.annotation.*;

public enum TeamKind {
    // TeamKindGitHub is for teams where membership is managed by GitHub.
    GitHub("github"),
    // TeamKindPulumi is for teams where membership is managed by Pulumi.
    Pulumi("pulumi"),
    // TeamKindSCIM is for SCIM provisioned teams. Membership is managed by its IdP.
    SCIM("scim");

    private final String value;

    TeamKind(String v) {
        value = v;
    }

    @JsonValue
    public String value() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    @JsonCreator
    public static TeamKind fromValue(String v) {
        for (var b : TeamKind.values()) {
            if (String.valueOf(b.value).equals(v)) {
                return b;
            }
        }

        throw new IllegalArgumentException(String.format("'%s' is not one of the values accepted for %s class: %s", v, TeamKind.class.getSimpleName(), Arrays.toString(TeamKind.values())));
    }
}
